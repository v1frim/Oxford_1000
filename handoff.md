# Oxford 1000 — Handoff Document

> Документ для передачі контексту між сесіями Claude Code.
> Якщо нова сесія — спочатку прочитай цей файл, потім `CLAUDE.md`.

---

## Проект

Українсько-англійський словниковий тренажер: сторінка + два файли даних, без build-step.

- **Файли (сесія 48 — ДАНІ ВИНЕСЕНО з index.html):**
  - `index.html` — **~0.53 МБ**, розмітка + уся логіка гри;
  - **`dict.js` — ~3.25 МБ, УВЕСЬ СЛОВНИК** (`WORDS`, `TRANS`, `HINTS`, `EXAMPLES`,
    `EXAMPLES_UA`, `EXPRESSIONS`, `CEFR_DATA`);
  - `songs.js` — дані пісень, ~515 КБ.
  - Порядок підключення: `songs.js` → `dict.js` → головний inline-скрипт.
  ⚠️ **Інструменти репо читають і ПИШУТЬ у `dict.js`**, не в `index.html`:
  `add-batch.js`, `cefr-add.js`, `hints-add.js`, `expr-add.js`, `gloss-check.js`,
  `check-coverage.js`, `check-cefr.js`, `oxford-gap.js`, `check-song-words.js`.
  Новий блок даних — теж у `dict.js`. Обидва файли великі: шукай `grep -n`, не читай цілком.
- **Обсяг даних (сесія 57):** `WORDS` **13766**, `EXPRESSIONS` **1293**, `HINTS` **1481**,
  `EXAMPLES` **13790** (з них 66 — ключі «en:ua» під конкретний глос),
  банк «Вирази і сленг» ~1840 карток, пісень 41.
  Рівні: A1 935 · A2 1389 · B1 3319 · B2 3973 · C1 3165 · C2 979 · без рівня 5 (власні назви).
- **Розгортання:** GitHub Pages → https://v1frim.github.io/Oxford_1000/
- **Git репо:** v1frim/Oxford_1000 (публічний)
- **Технології:** HTML/CSS/JS без фреймворків і без build-step. ⚠️ **«Single-file» більше НЕ актуально** — у сесії 48 дані винесені в `dict.js` заради швидкості (браузер не кешує байткод inline-скриптів). Логіка й далі вся в одному inline-скрипті `index.html`.
- **Гілка для розробки:** `main` (push в `main` оновлює сайт автоматично — див. CLAUDE.md)
- **⚠️ Stop-хук про «Unverified» підписи — ВІДОМИЙ ХИБНОПОЗИТИВ (сесія 20):** коміти CCR
  підписані й на GitHub **Verified**. Локально хук їх не перевіряє (в ефемерному контейнері
  немає `allowedSignersFile`, `commit_signing_key.pub` порожній) → `git %G?`=`N`; ще й
  порівнює з фантомною сесійною `claude/...`-гілкою (на сервері її нема — пушимо в `main`).
  **НЕ** переписувати історію / **НЕ** force-push. Якщо нагадування заважає — глуши локально:
  `git update-ref refs/remotes/origin/$(git branch --show-current) HEAD` (вирівнює базу
  порівняння хука на HEAD; нічого не пушить і не переписує).
- **⚠️ GitHub Pages: м'який ліміт ~10 білдів/год (інцидент сесії 32).** При активній сесії
  (багато пушів) деплої починають падати з «Deployment failed, try again later» (артефакт
  збирається, падає крок публікації). Що робити: НЕ панікувати і НЕ шукати баг у коді —
  зачекати 15-20 хв і пушнути порожній коміт (`git commit --allow-empty`). Перевірка статусу
  білдів — GitHub MCP `actions_list` (workflow «pages build and deployment»); rerun через API
  недоступний (403), прямий curl до v1frim.github.io/api.github.com зарізаний egress-проксі.
  Профілактика: групувати дрібні правки в один коміт.
- **⚠️ ДРУГИЙ ІНЦИДЕНТ — ЧЕРГА ПУБЛІКАЦІЇ (сесія 45, 06.08).** Виглядає так само («на сайті
  нічого не помінялось»), але причина ІНША, і лікується інакше. Симптом у логах:
  ```
  Getting Pages deployment status...
  Current status: deployment_queued        ← кожні 5 сек, 10 хвилин поспіль
  ##[error]Timeout reached, aborting!
  Canceled deployment with ID <sha>
  ```
  Тобто job `build` = success за ~25 сек, артефакт залито, GitHub ПРИЙНЯВ запит на
  публікацію — і той просто не рушає з черги, поки крок не здається за таймаутом.
  Впало двічі поспіль: `538f6af` (12:22→12:32) і `a3f3368` (12:55→13:05), між ними було
  22 хв паузи. **Що це означає:** порожній коміт «щоб перезапустити» тут НЕ допомагає —
  новий деплой стає в ту саму чергу. Правильна реакція: перестати пушити, зачекати
  30-60 хв і спробувати ОДИН раз; наступний пуш суміщати з корисною правкою, а не робити
  порожнім. ⚠️ `githubstatus.com` теж зарізаний egress-проксі (`CONNECT tunnel failed,
  403`), тож підтвердити інцидент на боці GitHub зсередини контейнера НЕМОЖЛИВО — просить
  користувача глянути статус у своєму браузері.
  **Як діагностувати за 1 виклик:** `mcp__github__get_job_logs` (job_id кроку deploy,
  `return_content:true`, `tail_lines:40`). Відповідь `actions_list` на 370 КБ не влазить
  у контекст — вона падає у файл, парсити звідти `python3 -c "json.load(...)"`, а не Read.
  **⚠️ ГОЛОВНЕ, ЧИМ ЗАКІНЧИЛОСЬ (перевірено користувачем):** попри ТРИ `failure` поспіль,
  зміни на сайт **таки виїхали** — черга доповзла вже після того, як крок здався за
  таймаутом. Тобто **`failure` у Actions ≠ «не опублікувалось»**: таймаут стосується лише
  очікування в екшені, сама публікація може завершитись пізніше. Тому перш ніж пушити
  повторно — СПОЧАТКУ попросити користувача оновити сторінку (Ctrl+F5) і глянути, чи
  зміни вже там. Інакше легко нагнати чергу зайвими деплоями, ганяючись за привидом.

### 🧰 Інструменти в репо (індекс)

Усі мають коментар-інструкцію в шапці файлу — читати там, а не гадати.

| файл | що робить |
|---|---|
| `verify.sh` | **передкомітна перевірка**, ШІСТЬ кроків: покриття + синтаксис (`index.html` **і `dict.js`**) + стенд + рівні CEFR нових слів + IPA + **написання (`check-brit.js`, сесія 51)**. ⚠️ Кликати ГОЛИМ рядком, не через `\| tail` — пайп з'їдає код виходу |
| `check-brit.js` | **вартовий американської норми в НАПИСАННІ (сесія 51):** британський ключ картки і британізм у тексті прикладу. Два детектори — структурний (ключ → власний `enAlt`) і лексемний; `--stats` — повні списки |
| `check-coverage.js` | слова в прикладах без hover-перекладу (ядро `verify.sh`) |
| `check-ipa.js` | **вартовий американської норми в транскрипціях (сесія 50):** ловить брит. `ɒ`/`əʊ`/`eə`/`ɐ`, неротичність і BATH-лексеми; `--stats` — розклад + повний список |
| `check-cefr.js` | **вартовий рівнів (сесія 46):** нові слова (порівняння з `git HEAD`) мусять мати рівень у `CEFR_DATA`; `--stats` — розклад по рівнях + список без рівня |
| `loadtest.js` | виконує `songs.js` + `dict.js` + головний inline-скрипт під мок-DOM — ловить TDZ/ReferenceError |
| `gloss-check.js` | чи вільний укр. глос для кандидата (`⛔осн` / `~alt`) |
| `add-batch.js` | вставка партії слів у WORDS+TRANS+EXAMPLES+EXAMPLES_UA + enAlt (**пише в `dict.js`**) |
| `expr-add.js` | **вставка партії у `EXPRESSIONS`** (сесія 48; дублі за `en` пропускає) |
| `uaalt-add.js` | **дописування `uaAlt` партією** (сесія 52). Править ЛИШЕ в межах блоку WORDS, рахує картки ДО/ПІСЛЯ, відхиляє тавтологію з основним глосом і дужки |
| `cefr-add.js` | дописує ключі партії в `CEFR_DATA` (рівень для гри 🎓) |
| `hints-add.js` | дописує уточнення в `HINTS` (звіряє ключі з WORDS; голий ключ слова-пари відхиляє — потрібен «en:ua») |
| `test-cefr-solo.js` | 33 перевірки 📖-ряду чипсів (сесія 46) |
| `test-cefr.js` | 28 перевірок вибору рівнів (у модалці «Тренування») + правил лідерборду. ⚠️ дата: пін 05.08 лише для міграції, «сьогоднішні» перевірки беруть `todayKey()` зі сторінки (сесія 45) |
| `test-hints.js` | **49** перевірок підказок HINTS (ключі-картки «en:ua» — сесія 45; заборона дужок у тексті підказки — сесія 50; ключ-варіант з `enAlt` і автопозначка амер./брит. — сесія 51; **словниковий шар мітки (`LEX_PAIRS`) — сесія 53**) |
| `test-lb-period.js` | **55** перевірок вкладки 🕒 «Рекорди за період» (сесія 51): вікна, сортування, кап топ-10, рядок «ост», персист, Shift-гортання в меню й на фіналі, підписи пулів, верстка чипсів |
| `test-expr.js` | 50 перевірок розділу «Вирази і сленг» (сесія 47): XP за картки й лог по днях, пункти «Плани» (12 карток = 1, перенесення залишку), два напрямки з окремими сховищами статусів, виключення «Вивчено» з колоди, лічильники статусів, значок 💬 у прогресі, `formula_v17` без TDZ |
| `test-phrase-hover.js` | 18 перевірок фразового ховера (сесія 45): склейки, список-виняток, збереження тексту в усіх 8647 прикладах |
| `test-homograph.js` | **75** перевірок правил зарахування (сесії 45, 50, 56): пари з однаковим en, «X»/«to X», неправильні форми, дужка в глосі (`uaBare`), взаємні синоніми через enAlt, контроль витоку сесії 30, **дефіс ≡ пробіл в укр. відповіді + строгий англійський бік**, **панель `machine` без `car`**, **сполучник й/і/та необов'язковий (сесія 56)** |
| `test-panel.js` | **68** перевірок центральної панелі, її верстки, кольорів ряду «Щодня», модалки «Тренування» **і геометрії лейауту — центр картки == центр екрана (сесія 50)** |
| `test-due.js` | **40** перевірок боргу: підмішування, розкид, засів `oxford_due_seed_v1`, відсутність кнопки «📒 Помилки» (сесія 45) |
| `test-review.js` | **11** перевірок напрямків у повторенні (сесія 53): план 2+1, чергування основного напрямку, живий прогін, повага до явно обраного напрямку |
| `example-add.js` | **(сесія 53)** вставка прикладів під конкретний глос — ключі «en:ua» одразу в `EXAMPLES` і `EXAMPLES_UA`, зі звіркою кількості |
| `test-wrong.js` | 14 перевірок архіву «не дались» і клікабельної комірки 🔁 |
| `oxford-gap.js` | покриття Oxford 3000/5000 + списки кандидатів |
| `cefr-batch.js` | (сесія 49) розкидає рівні CEFR прямо з партії: кожна картка має поле `cefr`. Прибирає ручне складання чотирьох списків і робить пастку «порожній B2» неможливою — лається, якщо B2 порожній |
| `final-audit.js` | (сесія 49) фінальний аудит покриття тематичного xlsx: кожна позиція має бути в WORDS/enAlt, EXPRESSIONS або у `SKIP` |
| `transparent.js` | (сесія 49) шукає «прозорі композити» — картки, чий глос = склейка глосів складників. ⚠️ Більшість знахідок ЛЕГІТИМНІ (solar system, blood pressure) — див. правило нижче, чистити лише точково |
| `remove-card.js` | (сесія 49) акуратне видалення картки з усіх блоків `dict.js` одразу: WORDS + TRANS + EXAMPLES + EXAMPLES_UA + HINTS + рівень у `CEFR_DATA` |
| `gen-cefr.js` | разова генерація блоку `CEFR_DATA` (сесія 42) |
| `build-song.js`, `check-song-words.js` | розділ «Пісні» |
| `fetch-definitions.js` | розширення EN→EN даних (локально в користувача) |
| `oxford-cefr.json`, `c2-candidates.json` | вихідні списки рівнів (обидва відпрацьовані) |

⚠️ Браузерні тести потребують драйвера: `npm i playwright-core` (Chromium уже лежить
у `/opt/pw-browsers`). Якщо драйвер в іншій теці — `PW_CORE=<шлях> node test-*.js`.

---

## Реалізовані фічі

### 🎛 СТАРТОВИЙ ЕКРАН ПЕРЕБУДОВАНО (сесія 44) — читати ПЕРШИМ

**Привід:** «у мене виходить досить багато режимів і всі досить розбиті». Розбір показав
причину: на старті були змішані **дві незалежні осі** — *формат питання* (UA→US, US→UA,
Def/Syn/Ant/Cloze) і *набір слів* (Незасвоєні, Рівні CEFR, категорії, помилки дня), плюс
окремі розділи-матеріали. Через це «Рівні CEFR» і «UA→US» стояли поруч, ніби це однорідні
речі. Користувач відкинув три запропоновані макети і продиктував свій — його й зроблено.

**Центральна панель тепер (бічні панелі та верх меню НЕ чіпані):**
1. велика кнопка **`#start-btn` «▶ Тренування · 60 сек»** + `#start-sub` — підпис останнього
   вибору («🎓 C1 · UA → US»). Клік/Enter/Shift → модалка «Тренування»;
2. **`#due-banner`** — інформаційна плашка «🔁 На повторенні N слів» (див. борг нижче).
   ⚠️ Вона НЕ клікабельна (сесія 45 прибрала й `:hover`, і застарілий `title` про клік);
   між нею і рядом «Щодня» до сесії 45 стояла кнопка «📒 Помилки (N)» — **її більше нема**;
3. ряд **«Щодня»** `#daily-row` (⚠️ вигляд ряду дошліфовано в сесії 45 — див. нижче): 🦉 Duolingo (лічильник + «−»), 🎵 Пісні (+🎧, «−»),
   📚 LingoHut, 💬 Вирази, 🎬 Кіно. ⚠️ **Duolingo і Кіно ПЕРЕНЕСЕНО з `#bottom-left-btns`
   у центр** — решта лівої панелі (Прогрес, Копія, Відновити, GPT) на місці
   (кнопку «📚 Повторив» прибрано в сесії 47 — див. `#bottom-left-btns`).
   ⚠️ **Верстка ряду — grid на 6 колонок зі спанами, НЕ flex-wrap:** пари з лічильником
   (`#duo-wrap`, `#songsess-wrap`) займають по 3 колонки, одиночні (`#lh-btn`, `#expr-btn`,
   `#movie-wrap`) — по 2, і **порядок у DOM саме такий** (спершу обидві пари) — інакше
   ряд розсипається на 3-4 поверхи, а підписи ріжуться в «Duoling…»/«♫…». Перевіряє
   `test-panel.js` (рівно 2 ряди + жодного `scrollWidth > clientWidth`).
   ⚠️ **ЄДИНА ВИСОТА Й КОЛЬОРИ (сесія 45, за запитом «зроби висоту однаковою і додай
   кольорів»).** Дві незалежні причини, обидві — хвіст переїзду кнопок із лівої панелі:
   • **висота 44 замість 34 px** — у `#expr-btn` лишався легасі-`margin: 0 0 10px`, а grid
     тягне ВСІ комірки ряду під найвищу, тож «завищеними» виглядали сусіди (LingoHut,
     Кіно), хоч винна була інша кнопка. Лік: `margin: 0` у правилі `#daily-row button`;
   • **колір «зникав»** — правило ряду `#daily-row button` має специфічність (0,1,0,1) і
     б'є голий `#songs-btn`/`#expr-btn`/`#lh-btn` (0,1,0,0). Вижили лише `#duo-btn` і
     `#movie-btn`, бо в них `!important`. Лік: кольори пишемо В КОНТЕКСТІ ряду —
     `#daily-row #lh-btn { … }`. Теми: 🦉 зелений `#7fdd40`, 🎵/💬 фіолетовий `#c89bff`,
     📚 синій `#87b4ff`, 🎬 оранжевий `#ffa94d`; лок кіно тепер притлумлено-оранжевий
     (не сірий), але підкладка лишилась СОЛІДНОЮ — вимога світлих фаз доби (сесія 33).
   Мертві легасі-правила (`#extra-btns`, геометрія `#lh-btn`/`#expr-btn`/`#songs-btn`/
   `#songsess-*`) ВИДАЛЕНІ — тепер вигляд ряду задається в одному місці. Закріплено
   8 перевірками в `test-panel.js` (однакова висота, відсутність margin, кожен колір);
4. ряд **English only** `#eng-row` (Def/Syn/Ant/Cloze) — дрібний, **клік одразу стартує гру**
   (окремої «Почати» для них більше нема).

**Модалка `#train-modal` — два ряди замість плаского списку:**
- «Які слова» (`.tm-set`): 🎓 Рівні CEFR (**першою — грається найчастіше**) · 📚 Весь словник ·
  📖 Незасвоєні. Під CEFR з'являються чипси рівнів `#tm-levels` (той самий `cefrSel`);
- «Напрямок» (`.tm-dir`): UA→US / US→UA. **«Випадковий» ПРИБРАНО** (за запитом: не смикати
  розкладку посеред гри) — з `RM_DIRS` теж;
- клавіатура: **Tab / Shift+Tab — набір**, стрілки — те саме, **Shift — напрямок**,
  Enter — почати, Esc — назад. Підказка по клавішах є в самій модалці;
- стан: `trainSet` / `trainDir` у `oxford_train_sel_v1`; `startTraining()` збирає пул
  (`trainPool()`) і кличе `startGame(pool,{timed:true,tag:"cefr:…"})` або `startGame()`.

**⚠️ ЩО ВИДАЛЕНО НАЗАВЖДИ (не відновлювати «на автопілоті»):**
кнопки `#btn-ua-en` / `#btn-en-ua` / `#btn-tolearn` / `#cefr-btn`, **уся модалка `#cefr-modal`**
разом із `renderCefrModal` / `openCefrModal` / `startCefrGame`, Shift-цикл `ALL_MODES`,
варіант `random` у виборі напрямку. Живими лишились дані й хелпери CEFR (`CEFR_DATA`,
`cefrSel`, `cefrIndices`, `cefrCounts`, `cefrTagLabel` — останній потрібен ще й лідерборду).

**⚠️ ПАСТКА, НА ЯКІЙ Я ВЖЕ ОБПІКСЯ:** блок «Тренування» я вставив між `renderCefrModal` і
`openCefrModal`, а потім чистка старого CEFR-коду вирізала діапазон «від renderCefrModal до
openCefrModal» — і знесла новий блок разом зі старим. Зловив `loadtest.js`. Мораль: чистити
код зрізом «від функції до функції» можна ЛИШЕ переконавшись, що між ними нічого нового.

**Дизайн модалки (та ж сесія, за скаргою «допотопно виглядає»):** стара синя картка
`.rm-card` (rgba(30,60,114)) замінена на темно-фіолетове скло в тон «Космосу» — градієнт,
золота рамка, глибока тінь, поява fade+підйом; набори як картки з іконкою та ✓ в кутку,
рівні як пігулки з тьмяним числом (`.lvn`), «Почати» з підсвіткою. ⚠️ Прапорці-емодзі в
системі користувача рендеряться як дрібні «ua/us», тому в підписах — текст «UA → US».

**Дошліфовано за фідбеком (та ж сесія, кілька ітерацій — не «спрощувати» назад):**
- секція називається **«Вибір режиму»** (не «Які слова»);
- у картці режиму вміст **по центру**, емодзі 26 px, а **лічильник слів — окремим рядком
  унизу** (`.tm-cnt`, `margin-top:auto`), щоб у трьох картках числа стояли на одній лінії;
  над ним примітка (`обрано A1` / `усі рівні разом` / `вивчаю + нові`), крапки-роздільника
  між ними НЕМА;
- **`plWords(n)`** відмінює: `1 слово · 904 слова · 1567 слів · 8562 слова · 11 слів`.
  Раніше скрізь писалось «слів». Хелпер уживається ще й у плашці боргу та в заголовку
  модалки слів дня; таблиця відмінків зафіксована перевіркою в `test-panel.js`;
- у «Напрямку» лишились **самі назви** `UA → US` / `US → UA` крупним шрифтом — без
  прапорців і без пояснень дрібним шрифтом (їх прибрано за запитом як шум).

---

### 🔁 ІНТЕРВАЛЬНЕ ПОВТОРЕННЯ ПОМИЛОК — «борг» (сесія 44, за запитом)

Помилкові слова більше не чекають, поки натиснеш «📒 Повторити помилки» — вони самі
підмішуються в наступні ігри.

- **помилка сьогодні → борг на ЗАВТРА, рівно ОДНЕ підмішування.** Користувач: «якщо
  відповім правильно — значить уже знаю; а три повтори й так дає повторення після гри»;
- **⚠️ БОРГ СТАВИТЬ ЛИШЕ ЗВИЧАЙНА ГРА (фікс сесії 45 — читати перед будь-якою правкою
  боргу).** `scheduleDue` стояв у гілці помилки `recordAnswer` БЕЗ умови, тож борг
  наповнювався ще й із сесій ПОВТОРЕННЯ. А там слово ганяється, доки не даси 3 правильні
  поспіль — 3-4 затинки на одному слові норма, і черга роздувалась дрилом, а не помилками
  з ігор (у користувача набігло 35 слів при жменьці відкритих помилок; його формулювання:
  «замішуватись повинні саме помилкові слова, а не ті, що були у повтореннях»). Тепер
  `if (!reviewRemaining) scheduleDue(...)` — той самий захист, що вже стояв рядком нижче
  на `updateMastery`. НЕ прибирати. Перевіряють 2 тести в `test-due.js`.
- **Ремонт уже засміченої черги** — `fixDuePollutedByReview()`, флаг `oxford_due_fix_v1`:
  лишає в борзі ЛИШЕ те, що досі є відкритою помилкою (`oxford_day_mistakes_v1`).
  ⚠️ Відрізнити «сміття з дрилу» від справжніх помилок заднім числом НЕМА ЯК — обидва
  архіви пишуться в обох режимах, тому ремонт зводить борг до інваріанта «борг ⊆ відкриті
  помилки». Побічний ефект: знімає й слова, закриті в post-game повторенні.
- **розкид:** у гру береться максимум `DUE_MAX_PER_GAME` = 3 боргових слова, і кожне лише
  з шансом `DUE_CHANCE` = 0.35 → 4-6 боргів природно розтягуються на кілька ігор («одне в
  першій, дві в четвертій»), а не валяться всі одразу. Після `DUE_FORCE_AFTER` = 4
  пропущених ігор слово береться **примусово**, щоб не загубилось;
- **⚠️ ГАРАНТІЯ «У 5-Й ГРІ ТОЧНО» (фікс сесії 57, скарга «зіграно 6 ігор, а слово все
  висить»).** Примусове підмішування було деклароване з сесії 44, але не працювало:
  1. `pickDueIndices` скидав `rec.waited = 0` на сам ФАКТ вибору слова. А вибір ≠ показ:
     слово лягало в чергу на позицію 1-12, 60 секунд не завжди докручують до 12-го
     питання — і слово, якого користувач так і не побачив, починало відлік заново.
     Замкнене коло: черга ніколи не доходила до `waited >= 4`. Тепер лічильник росте
     **в КОЖНІЙ грі**, незалежно від вибору. Спрацьоване слово йде з черги само
     (вгадав → `clearDue`; схибив/пропустив → `scheduleDue`, waited=0, due=завтра),
     тож рахувати треба **ігри, а не вибори**. НЕ повертати скидання на вибір.
  2. Примусове слово тепер стає в **перші питання** черги (`span = 4` замість 12
     у `startGame`) — інакше «примусово» нічого не гарантує, воно знову могло не
     дочекатись свого показу.
  3. Ліміт трьох слів на гру віддається **спершу прострочeним** (два проходи в
     `pickDueIndices`): одним проходом слово, що відчекало свої 4 гри, могло не влізти
     в трійку через випадково взяте слово, що стоїть раніше за ключем.
  Перевіряють 4 нові тести в `test-due.js` (усього 44);
- **якщо день пропущено — борг НЕ згорає й НЕ подвоюється:** `due` лишається ≤ сьогодні,
  `waited` рухається лише при СТАРТІ гри. Не грав тиждень → те саме слово чекає далі
  й підмішається в найближчі ігри (а відчекане з минулих днів — примусово вже в першій);
- **⚠️ БОРГ = ПОМИЛКИ, НЕВІДПРАЦЬОВАНІ ПІСЛЯ POST-GAME ПОВТОРЕННЯ (правило сесії 45 —
  воно СКАСОВУЄ формулювання сесії 44 «борг працює зверху повторення»).** Дослівно від
  користувача: зіграв «12 (3/15)» → 3 помилки; після раунду пропонується пройти їх ще раз
  або вийти в меню. Пройшов і на 2 з 3 відповів чисто, а на одному знову спіткнувся →
  **на завтра їде лише той один**. Не пішов у повторення взагалі → їдуть усі три.
  А назавтра вони підмішуються в ігри «наче звичайні рандомні слова».
  Механіка: `clearDue` стоїть поруч із `removeDayMistake` у гілці закриття слова
  (3 правильні поспіль) — але **лише для НЕ-tainted** слова, тобто такого, на якому в
  повторенні не було жодної затинки. Спіткнувся → tainted → лишається і в помилках дня,
  і в борзі. НЕ повертати «борг зверху»; перевіряють 4 сценарні тести в `test-due.js`.
- вгадав → борг закрито (`clearDue`); помилився знову → борг знову на завтра;
- ключ **`oxford_due_v1`** `{wordKey:{due,waited}}`, у `BACKUP_KEYS`. Функції: `scheduleDue`,
  `clearDue`, `pickDueIndices`, `dueNowKeys`. Підмішування — у `startGame` у перші ~12 питань
  і **ЛИШЕ у звичайні timed-ігри** (в `activePool`-повтореннях це було б масло масляне);
- ~~post-game повторення помилок лишилось БЕЗ ЗМІН — борг працює зверху~~ — ⚠️ **ЗМІНЕНО
  В СЕСІЇ 45:** чисте закриття слова в post-game повторенні тепер СКАСОВУЄ борг (див.
  правило вище). «Зверху» борг більше не працює.

**🗑 СЕСІЯ 45: КНОПКА «📒 ПОМИЛКИ (N)» І ПУНКТИ ЗА НЕЇ — ВИДАЛЕНІ НАЗАВЖДИ (за запитом).**
Помилки повертає в ігри ВИКЛЮЧНО борг; окремого входу/лічильника/вітання під це бути не
повинно (та сама лінія: сесія 47 прибрала «📚 Повторив», сесія 50 — вітання «✨ Усі помилки
закриті»). Вирізано: `#review-today-btn`, Пробіл на старт-екрані, `updateReviewTodayBtn`,
`pickReviewDay`, прапор `reviewFromMenu`, нарахування пунктів «15 закритих = 1 пункт + 30 XP».
`addXp(2)` за закрите слово повторення став БЕЗУМОВНИМ.
- **⚠️ ДАНІ ЗАМОРОЖЕНІ, НЕ СТЕРТІ:** `oxford_mistake_reviews_v1` не поповнюється, але
  `loadMistakeReviews` / `mistakeReviewPointsByDate` і читачі (термометр, місячна ачівка,
  `computeDayStatsXp` з `mrPts`/`mrWords`) ЛИШИЛИСЬ — прибрати їх = обнулити пункти й XP
  минулих днів заднім числом. Ключ лишається в `BACKUP_KEYS`.
- **`oxford_day_mistakes_v1` живий, але без UI** — його читають лише ДВІ одноразові
  міграції боргу (`seedDueFromMistakes` під `oxford_due_seed_v1`, `fixDuePollutedByReview`
  під `oxford_due_fix_v1`), обидві кличуться в init ПІСЛЯ `sanitizeDayMistakes()`. НЕ чистити
  як мертвий код: на новому пристрої прапорців немає і міграції відпрацюють знову.
  Вгадане боргове слово закриває і його (`removeDayMistake` поруч із `clearDue`).

**🔁 Комірка «повтори» в «Прогресі по днях» стала клікабельною (та ж сесія, за запитом):**
- ✓/↗/● вели до слів дня, а 🔁 — ні, хоча саме там «те, де я облажався»;
- ⚠️ **чому це не робилось раніше:** `oxford_day_mistakes_v1` — це ВІДКРИТИЙ БОРГ, слово
  зникає звідти, щойно його закрили повторенням, тож історію відновити нізвідки. Тому
  заведено **окремий архів `oxford_day_wrong_v1`** `{date:[wordKey]}` — пишеться поряд
  (`addDayWrong` біля `addDayMistake`), **не чиститься ніколи**, ключі в межах дня
  унікальні. Одноразовий засів зі старого боргу: `oxford_daywrong_seed_v1`;
- число в комірці лишилось = кількість ПОВТОРЕНЬ (як було), а модалка показує **різні
  слова** (запит: «47 повторень, а реальних слів менше»); title пояснює обидва числа.
  Якщо повторень 0, а помилки були — комірка показує кількість слів;
- модалка — той самий `openDayTransModal(date,"r")`, `CAT_META.wrong`; гра з неї має тег
  **`wrong`** → поза топ-10 (пул із кількох слів не порівнюваний зі звичайною грою),
  але лишається в лозі з підписом «🔁 Не дались» — те саме правило, що для CEFR-ігор.

**🎬 Поріг кіно: 500 → 1500** слів у «Знаю» (`MOVIE_UNLOCK_KNOWN`). Обґрунтування: 500 —
половина рівня A1, з такою базою фільм із подвійними субтитрами = суцільний словник;
1500 ≈ A1+A2 разом (898+792 за Oxford) і досяжно за пару місяців при темпі ~20 слів/день.

---

### Режими гри (6 форматів; ⚠️ кнопки перебудовано в сесії 44)

⚠️ Нижче описані САМІ РЕЖИМИ (що показують, що приймають) — вони живі. А от
**кнопки й лаунчери** з цього розділу зникли: набір і напрямок тепер обираються
в модалці «Тренування» (розділ вище). Читати з поправкою на це.

**Переклад:**
- `🇺🇦 → 🇺🇸` (ua-en) — показ UA → ввод EN
- `🇺🇸 → 🇺🇦` (en-ua) — показ EN → ввод UA
- `📖 Незасвоєні` (сесія 34, ЗАМІНИЛА кнопку «🔀 Мікс») — набір **Вивчаю ∪ Нові** = усе,
  КРІМ «Знаю» (streak≥3): `toLearnIndices()` (`!(s>=3)`), `startToLearnGame()`. Timed-гра з
  оновленням mastery; напрямок обирається в модалці «Тренування» (сесія 44). ⚠️ Спершу було
  Вивчаю ∪ Знаю під назвою «⭐ Знайомі» — ЗМІНЕНО за запитом, назву відкинуто.
- **⚠️ «mix» лишається валідним ВНУТРІШНІМ значенням** (`selectedMode`/`mode`, мітка
  лідерборду «🔀 Мікс»), але кнопки й пункту в `ALL_MODES`/`MODE_BTNS` більше немає (сесія 44
  прибрала й «Випадковий» із модалки «Тренування»). Його вмикає лише «Випадковий» у модалці
  напрямку повторення/категорії. `selectedMode` = ОБРАНИЙ режим (вкл. "mix"), `mode` =
  КОНКРЕТНИЙ напрям; у міксі `nextWord` перевипадковлює `mode` щослова.

**English only:**
- `📖 Def` (en-def) — показ визначення → ввод слова
- `🔁 Syn` (en-syn) — показ слова → ввод синоніма
- `↔ Ant` (en-ant) — показ слова → ввод антоніма
- `📝 Cloze` (en-cloze) — речення з `____` → заповнити слово

### Гра
- 60 секунд на раунд
- При помилці — correction-панель 3.5 сек з правильною відповіддю, прикладом, TTS
- "Останнє слово" — після таймера ще одна спроба
- TTS через Web Speech API (preferred female US English voice)
- Звукові ефекти: Web Audio API — E5→G5 старт, G5→E5→C5 фініш
- **Лічильник «✗N» біля рахунку (сесія 29):** показує **неправильні + пропущені**
  (`status !== "correct"`), живий у ВСІХ режимах. Єдина `updateWrongBadge()` викликається
  з `recordAnswer` після кожної відповіді + скидається на старті будь-якої гри. ⚠️ id елемента
  — `review-wrong-badge` (історична назва), але вживається скрізь, не лише в повтореннях.
  НЕ повертати на «лише wrong» і НЕ робити його review-only (раніше так і було → у таймерній
  грі висів застряглий лічильник зі старої review-сесії, що не враховував пропуски).

### Milestone-вітання (overlay `#milestone-overlay`)
Функція `showMilestone(icon, text, sub, type)` + **черга** `enqueueMilestone({icon,text,sub,type,sfx})`.
Черга показує вітання послідовно (2.7с кожне), щоб вони не перезаписували одне одного.
Типи (кожен свій колір + звук): milestone / nudge / rank / achievement / levelup.
⚠️ Тип **`cleared`** («✨ Усі помилки закриті») **ВИДАЛЕНО в сесії 50** — див. нижче.
- **⚠️ Вітання лише в ГОЛОВНОМУ МЕНЮ (сесія 34, за запитом):** поки триває ігрова сесія —
  від старту гри, через екран результатів/повторень, аж до повернення в меню — ВСІ вітання
  (термометр 🎯/💪, рекорди 🏆, ачівки 🏅, level-up 🎉) НЕ вискакують, а
  відкладаються у буфер `_msDeferred`. Флаг `_gameSessionActive` виставляється у `startGame`,
  скидається у `flushDeferredMilestones()` (кличеться при поверненні в меню — кнопка «🏠 На
  головну», Esc з фіналу делегує клік, Enter з фіналу). Флаг НАДІЙНІШЕ за перевірку видимості
  екранів: у `endGame` є мить, коли і гра, і екран результатів приховані. Вітання ПОЗА грою
  (Duolingo/LingoHut/GPT «+», статус пісні «вивчено») — флаг false → показуються одразу.
- **milestone** (🎯 жовтий, `sfxMilestone`) — **пункти «термометра» кратні 4** (план виконано:
  4/8/12/16…). ⚠️ Сесія 24: було «серія ігор кратна 4» — переведено на пункти термометра.
- **nudge** (💪 синій, `sfxNudge`) — **пункти «термометра» = 2 (mod 4)** (півплану: 2/6/10/14…).
- ~~**cleared** (✨ зелений, `sfxCleared`) — усі помилки дня закриті (review)~~ —
  **⚠️ ВИДАЛЕНО В СЕСІЇ 50 за прямим запитом користувача. НЕ повертати.** Дослівно:
  «плашка з похвалою… вже можна спокійно забирати, оскільки вона зараз ніяк не
  використовується і усі помилкові слова замішуються автоматично на наступний день».
  Тобто відколи борг (`oxford_due_v1`) сам повертає помилки в ігри, «закрити всі
  помилки» перестало бути подією — список порожніє й без участі користувача.
  Прибрано: гілку `if (allDayMistakeKeys().length === 0)` в `endGame`, функцію
  `sfxCleared`, гілку `"cleared"` у `showMilestone`, CSS `.ms-box.cleared` і
  `.ms-box.cleared .ms-text`. `allDayMistakeKeys()` ЛИШИЛАСЬ — її кличуть одноразові
  міграції боргу (див. попередження біля `oxford_day_mistakes_v1`).
- **rank** (🏆/🥇 фіолетовий `#c89bff`, `sfxRank` фанфара) — гра потрапила в топ-10 leaderboard.
  Показується ТІЛЬКИ якщо список був уже повний (`scoresWereFull`, ≥10 ігор) — щоб не спамити на старті.
  rank===1 → 🥇 "Новий рекорд!"; rank 2-3 → "топ-3"; 4-10 → "топ-10". Анімація іконки `ms-trophy`.

**⚠️ Milestone'и за пунктами термометра (сесія 24):** `checkThermoMilestones()` рахує від
ПОКАЗАНИХ пунктів (`getThermoPointsToday()`, з carry-in) і ловить ПЕРЕТИН порогу (пункт може
стрибнути на +2 від занять піснями/LingoHut №36+). Викликається з УСІХ джерел пунктів (кінець
гри, Duolingo +, LingoHut, заняття піснями +) і на старті (виставляє базу). Дедуп через сесійну
«верхню планку» `thermoMsHighWater`/`thermoMsDate` — не спамить на reload і на дай-бери −/+.

### Review-режим
- 3 правильні підряд = освоєно
- **⚠️ НАПРЯМОК: ДВІ МОНЕТКИ + ГАРАНТІЙНИЙ ТРЕТІЙ КРОК (сесія 53, за запитом).** Було
  `Math.random() < 0.5` щопитання — і слово легко приходило всі три рази одним боком
  (у користувача: `dictionary` тричі UA→US, `traveler` тричі US→UA; ймовірність 25% на
  слово, тож на пулі це траплялось постійно). `buildReviewDirPlan(pool)` роздає кожному
  слову трійку: перші два кроки — ЧЕСНА монетка, і ЛИШЕ якщо вони збіглися, третій
  примусово дає напрямок, якого ще не було.
  - ⚠️ **ПЕРША РЕДАКЦІЯ БУЛА ЖОРСТКИМ ЧЕРГУВАННЯМ 2+1 між словами — користувач її
    ВІДХИЛИВ:** «щоби вона не завжди повторювалася в такому руслі». Не «повертати
    як було»: сенс саме в тому, що розкладка лишається живою, а гарантія — тільки на
    третьому кроці.
  - ⚠️ Наслідок, а не окреме правило: у трійці все одно завжди виходить 2+1 (три слоти,
    обидва напрямки), але ЯКИЙ напрямок подвоїться — щоразу як випаде.
  - ⚠️ Крок береться з `reviewCorrectCounts`, тобто з ПРАВИЛЬНИХ відповідей: спіткнувся —
    те саме питання тим самим боком повториться. Це навмисно.
  - ⚠️ План діє ЛИШЕ при `reviewDir === "random"`. Явно обраний у модалці напрямок
    (EN→UA / UA→EN) він не перебиває; у звичайній грі `reviewDirPlan = null`.
- **TAINTED-логіка:** бодай 1 помилка/пропуск → слово НЕ видаляється з "помилок дня" навіть після 3 правильних
- Таймер рахує скільки пройшло (не зворотний відлік)
- Review direction modal: EN→UA / UA→EN / Випадковий

### Mastery tracking
Категорія визначається **наявністю в `m` (mastery-об'єкті) + значенням streak**, а НЕ лише streak:
- **Знаю** (streak ≥ 3): зелений ✓
- **Вивчаю** — слово вже траплялось у грі хоча б раз (є запис у `m`, streak 0-2): синій ↗
- **Нові** — слово ще НЕ траплялось (немає запису в `m`): оранжевий ●
- ⚠️ Слово зі streak 0 в `m` (відповів НЕправильно/пропустив) = **Вивчаю**, а НЕ Нові.
  "Нові" = слова, яких немає в `m` взагалі; streak 0 свідомо в learning.
- **⚠️ Лічильники бейджів — по УНІКАЛЬНИХ словах-ключах (сесія 39, фікс розбіжності):**
  `renderMasteryStats` ітерує ключі WORDS (`wordKey`, дедуп через Set) — НЕ `Object.values(m)`
  і НЕ записи. Причина: 20 same-en пар (run/fair/bank/…) ділять mastery-ключ, і старий код
  давав бейджі≠модалки (бейдж рахував ключі, модалка — записи, «Нові» = гібрид записи−ключі;
  у користувача було 57/1448/1737 проти 58/1455/1729). Тепер бейджі = модалки = дельти
  прогресу структурно; **тотал-бейдж = унікальні слова (3222), НЕ записи (3242)**, з
  title-тултіпом поясненням. Ітерація по ключах WORDS дає імунітет до осиротілих ключів
  mastery (перейменоване слово лишає streak — mastery НІХТО не санує, на відміну від
  sanitizeDayMistakes). ⚠️ СВІДОМО НЕ уніфіковано (не «баг», НЕ чіпати): snapshot
  `t=WORDS.length` і дельта «+N ●» — нетто ЗАПИСІВ бази; `wordCount` ігор у лідерборді —
  записи на момент гри; `countKnownWords()` (ачівка + кіно-лок) — ключі З mastery (сьогодні
  = бейджу «Знаю», розійтись може лише на осиротілі ключі — прийнятно, пороги грубі).
  Захисні коментарі стоять у коді біля всіх трьох.
- **⚠️ ua-en: прогрес отримує ВВЕДЕНЕ слово (сесія 37, за вибором користувача):**
  `masteryTargetIndex(userVal)` у correct-гілці recordAnswer — написав real на промпт
  «справжній» (картка authentic) → +streak у real, НЕ в authentic (кейс: authentic «перейшов
  у Знаю», хоча користувач його жодного разу не писав). Перевага запису, чий ua/uaAlt містить
  показаний промпт (омографи record/to record); введене без власного запису (enAlt-відповіді
  типу suggestion) → fallback показана картка. Помилки/регресії — ЗА ПОКАЗАНОЮ карткою.
  Інші режими не зачеплені.
- localStorage: `oxford_word_mastery_v1` — **ключ = англійське слово
  (`wordKey(w)=getEn(w)[0]`), НЕ позиція в WORDS** (виправлено в сесії 18 — див. changelog)
- Review-сесії **не** оновлюють mastery (тільки звичайна гра)
- **⚠️ ФОРМАТ ЗАПИСУ = число АБО `{s,c,w}` (сесія 40, за запитом):** `s` = streak (0-3, як раніше),
  `c`/`w` = КУМУЛЯТИВНІ лічильники правильних / неправильних+пропущених відповідей на це слово.
  Причина: streak має стелю 3 і обнуляється помилкою, тож історія слова стиралась — у «Вивчаю»
  (1400+ слів) неможливо було відрізнити «майже знаю» від «хронічно збиває».
  - **МІГРАЦІЇ НЕМАЄ і не треба:** старе голе число читається як streak із нульовими лічильниками
    (так само старі бекапи), а об'єктом запис стає при першій же відповіді на слово. Лічильники
    ведуться З МОМЕНТУ фічі — стару історію не відновити (лог ігор слів не зберігає).
  - **⚠️ Читати ЛИШЕ через `mStreak(v)`** (`v && typeof v==="object" ? v.s : v`), лічильники —
    через `mTally(v)` → `{c,w}`. Обидва оголошені біля `saveMastery`. Усі читачі вже переведені:
    `renderMasteryStats`, `saveMasterySnapshot`, `categoryIndices`, `toLearnIndices`,
    `countKnownWords`, `computeRetroXp`, `updateMastery`, `openCategoryModal`. Нове читання
    голого значення = баг (об'єкт у числовому порівнянні дасть false).
  - **⚠️ КАТЕГОРІЇ рахуються ВИКЛЮЧНО по `s`** — c/w це довідковий шар. Від них НЕ має залежати
    жодна категоризація, XP, дельта прогресу чи снапшот. НЕ «покращувати» правило «Знаю» на
    щось на кшталт `c>=5` без узгодження.
  - **⚠️ Рухає лічильники ЛИШЕ `updateMastery`, тобто ЛИШЕ звичайна гра** (за прямим запитом:
    повторення / «📒 Помилки» — це засвоєння вже проваленого слова, «правильно» там не рахується).
    НЕ підключати до review-гілки `recordAnswer`. Пропуск приходить як `correct=false` → в `w`.
  - Ігри по категоріях і «📖 Незасвоєні» — звичайна timed-гра, лічильники в них працюють.

### Категорійні ігри в лідерборді: теги + «Знаю» ПОЗА рейтингом (сесія 37, за запитом)
- **Тег пулу** тягнеться крізь флоу: `catModalPoolTag` (openCategoryModal="known/learning/new";
  openDayTransModal мапить k/l/t) → `pendingCatTag` (startCategoryGame, чиститься при скасуванні
  модалки напрямку) → `startGame(pool,{timed,tag})` → `timedPoolTag` (модульна змінна).
- **«Знаю»-ігри (tag="known") НЕ входять у «Найкращі ігри» ВЗАГАЛІ** (за запитом — easy-режим):
  у endGame гілка `timedPoolTag==="known"` робить ЛИШЕ recordDayStats+checkThermoMilestones;
  БЕЗ saveScore / rank-milestone / updateTodayBest / updateLatest / appendGamesLog. Статистика
  дня (ігри/відповіді/XP/пункти) рахується як звичайно. Mastery оновлюється (гра timed).
- **Інші теги (learning/new) зберігаються З tag** (усі 4 сховища переносять поле tag) і в
  лідерборді підписуються «↗ Вивчаю» / «● Нові» замість напрямку/«Мікс» (LB_TAG_LABELS у
  renderGameRow). Ігри «Незасвоєні» (tolearn) тегів НЕ мають — як і раніше «Мікс»/напрямок.
- **Одноразова міграція `oxford_fix_known_lb_v1`:** точково прибрала «Знаю»-гру 20.07.2026
  (mix, 8, 0/0), зіграну ДО фічі (займала 14-те місце): scores+games-log+latest; todayBest
  перераховано з логу. Прапор ставиться лише при успіху.

### Гра по категоріях (Знаю / Вивчаю / Нові) — додано сесія 10
Три бейджі mastery (`✓ Знаю`, `↗ Вивчаю`, `● Нові`) на стартовому екрані **клікабельні**:
клік → модалка `#cat-modal` зі списком усіх слів цієї категорії (відсортовані за EN,
формат `EN — переклад`, клік по рядку = TTS) + кнопка **«▶ Грати ці слова (N)»**.
- **⚠️ Рядки ГРУПУЮТЬСЯ за wordKey у ВСІХ трьох категоріях (сесія 39):** same-en пари —
  один рядок «run — бігти, бігати / керувати» (патерн openDayTransModal сесії 38);
  лічильник заголовка і кнопки = СЛОВА (групи), пул гри = УСІ індекси (обидва сенси
  тренуються; гра може мати на 1-2 питання більше, ніж число на кнопці — свідомо).
  data-i рядка = перший індекс групи (TTS). НЕ повертати на «рядок = запис» — інакше
  модалка знову розійдеться з бейджами меню (і в «Нових» висітимуть дублі пар).

- **⚠️ Лічильники ✓/✗ і сортування «Вивчаю» (сесія 40, за запитом):** праворуч у рядку —
  бейдж `.cat-tally` (`✓N` зелений / `✗N` червоний) із `mTally`; показується лише коли є
  бодай одна відповідь (легасі-записи 0/0 — без бейджа). **Категорія «Вивчаю» сортується
  за проблемністю** (помилок ↓, потім правильних ↑, потім алфавіт), у заголовку підпис
  «спершу проблемні»; «Знаю»/«Нові» лишились алфавітними (бейдж там теж видно).
  ⚠️ Наслідок правила «менше правильних → вище»: серед БЕЗпомилкових слів легасі-запис (0/0)
  стоїть НАД словом з однією правильною — тобто правильні відповіді поступово опускають слово
  вниз списку. Це СВІДОМО (перевірено стендом), не «виправляти» на «слова без даних — вниз»
  без узгодження. Модалка «слова дня» (`openDayTransModal`) лічильників НЕ показує.
- Кнопка запускає **звичайну гру 60 сек, обмежену цими словами** (mastery оновлюється:
  Нові→Вивчаю→Знаю). Це НЕ режим повторень.
- **⚠️ Вибір напрямку (сесія 32, за запитом):** «▶ Грати ці слова» тепер відкриває ту саму
  модалку «Напрямок перекладу» (`#review-mode-modal`), що й повторення: US→UA / UA→US /
  Випадковий. Стан `pendingCatPool` (поряд із `pendingReviewPool`; гілка в `confirmRmSelection`);
  «Випадковий» → `setMode("mix")` (існуюча мікс-механіка, лідерборд «🔀 Мікс»). Скасування
  (Esc/фон/Скасувати) чистить ОБИДВА pending-пули. Через це перетин із `modeValidPool()`
  ПРИБРАНО з openCategoryModal/startCategoryGame — категорійна гра завжди переклад
  (гра словами категорії в Def/Syn/Ant/Cloze більше недоступна; раніше брався активний режим).
- **`startGame(pool, opts)`**: новий другий аргумент. `opts.timed === true` → грати timed-гру
  по кастомному пулу (mastery оновлюється). Без `opts` або з `pool` без `timed` →
  стара поведінка (review-режим: 3× правильно щоб закрити, mastery НЕ оновлюється).
  Розв'язано через дві змінні: `timedPool` vs `activePool`. **Review-флоу не зачеплено.**
- ⚠️ ЗАСТАРІЛЕ З СЕСІЇ 32: перетину з `modeValidPool()` БІЛЬШЕ НЕМАЄ — категорійна гра
  завжди переклад (вибір напрямку в модалці, див. пункт вище); `modeValidPool()` лишилась
  (використовується деінде для режимних пулів).
- Хелпери: `categoryIndices(cat)` ("known"/"learning"/"new"), `openCategoryModal(cat)`,
  `closeCategoryModal()`, `startCategoryGame()`, конст `CAT_META`. Делегований клік на
  `#mastery-stats` (data-cat). Клавіші модалки: Esc — закрити, Enter — грати.

**⚠️ СЕМАНТИКА ДЕЛЬТ ✓/↗/● у «Прогресі по днях» (сесія 39, після 2-ї скарги «числа
не сходяться»):** число комірки = **НЕТТО-зміна категорії** за день, а модалка по кліку =
**БРУТТО-лог входів**. Інваріант: `Δ↗ + Δ✓ = кількість перших зустрічей слів за день`.
Кейс користувача: 7 слів увійшло у «Вивчаю», 2 інших того ж дня пішли далі в «Знаю» →
комірка «+5 ↗», модалка «7 слів». ЦЕ НЕ БАГ (перевірено симуляцією і фаззом 400 днів).
Що зроблено, щоб більше не плутало: (а) title на комірках ✓/↗ з ПОВНИМ розкладом потоків,
відновленим з лога+дельт (інваріанти: F=dl+dk перших зустрічей; R=lLog−F регресій;
P=dk+R переходів у «Знаю») — «У «Вивчаю» ввійшло 7 (нових 3, випали зі «Знаю» 4),
пішло далі в «Знаю» 6 · нетто +1»; ✓ — «У «Знаю» пішло 6, випало назад у «Вивчаю» 4 ·
нетто +2». ⚠️ Кейс 31.07 (2-е запитання користувача): «пішло далі 6» при ✓ «+2» — ПРАВДА,
бо 4 давні слова того ж дня випали зі «Знаю» (помилки в грі), 6−4=+2;
(б) заголовок модалки дописує «(у прогресі +5)», коли
брутто ≠ нетто; (в) комірка рендериться і при нетто 0 чи від'ємному, якщо лог дня
непорожній (інакше слова були б недосяжні; при dl<0 клікабельна «−N» — список все одно
показує входи). НЕ «виправляти» дельту на брутто: лог ведеться лише з сесії 37 (старі
рядки обнулились би), а сума нетто-дельт = поточні розміри категорій.

**Бонус +3 XP за Нові → Вивчаю (оновлено в сесії 9):** нараховується при **першій в
історії** зустрічі слова в грі — `prev === undefined`, **незалежно** від того, чи
відповідь правильна, неправильна, чи пропуск. Логіка: коли з'являється нове слово,
користувач витрачає час на його вивчення навіть якщо одразу відповів неправильно —
це теж прогрес. `prev === undefined` спрацьовує рівно один раз на слово, тож подвійного
нарахування немає. Код: у `updateMastery` бонус `addXp(3)` стоїть **в обох гілках** —
у `correct` (`prev === undefined && m[wordIndex] === 1`) і в `else` (`prev === undefined`).
Це **узгодило** live-логіку з міграційною формулою `computeDayStatsXp` (тоді
`max(0,ΔВивчаю)×3`; ⚠️ з сесії 39 формула mastery-XP — `3·ΔВивчаю + 8·ΔЗнаю`, точний
брутто-еквівалент live — деталі в чейнджлозі v13).

**Історія:** сесія 8 прибрала повторний +3 (умова строго `prev === undefined`); сесія 9
поширила бонус і на неправильну/пропущену першу зустріч. Захист «раз на слово» збережено.

**Про слово "lead":** в словнику є ДВА окремих записи:
- `{en:"to lead", ua:"вести"}` — дієслово
- `{en:"lead", ua:"свинець"}` — іменник (метал, вимова `/ˈlɛd/`)
Це коректно, не об'єднувати.

**Про слово "ride" (сесія 27):** так само ДВА записи:
- `{en:"to ride", ua:["їздити","кататися"]}` — дієслово
- `{en:"ride", ua:["поїздка","атракціон"]}` — іменник (поїздка; атракціон у парку, амер.)
Приклад іменника лежить під `EXAMPLES["ride"]`, дієслова — під `"to ride"`, тож
`lookupExample` їх НЕ плутає (повний ключ пріоритетніший за голий). IPA `/raɪd/`
спільна. `EN_TO_UA["ride"]` тепер містить усі 4 значення (для hover). Не об'єднувати.

**🩹 Санація ключів помилок (сесія 37, фікс «вічної» помилки):** `sanitizeDayMistakes()`
на старті нормалізує збережені ключі `oxford_day_mistakes_v1`, що більше не резолвляться
у WORDS (спроба парної форми "to X"↔"X"; інакше — дроп). ⚠️ Сесія 45: санація потрібна
й далі — тепер її результат читає засів боргу (`seedDueFromMistakes` іде одразу після неї).
Історична причина появи: бейдж «📒 Помилки (N)»
рахував УСІ збережені ключі, а старт повторення відфільтровував нерезолвні → помилка
«висіла», але її неможливо було пройти. Бейдж тепер теж рахує лише резолвні (та сама
умова, що старт). ⚠️ Друга можлива причина «непрохідної» помилки — TAINTED-правило
(слово з бодай 1 помилкою в сесії повторення НЕ знімається навіть після 3 правильних;
це СВІДОМИЙ дизайн, див. «Review-режим») — при 1 слові в пулі юзеру треба 3 поспіль
БЕЗ жодного слипу за сесію.

**⚠️ Аудит «глос ↔ приклад» (сесія 37):** проскановано всі ~2904 приклади на розбіжність
показаного глоса й сенсу в укр. прикладі. Дубль-омоніми (fan/hot/spring/nail/board/right/
square/tip/patient/fair/orange/run/light/cool/kind/bank/fine/match/present/content) — усі
ВЖЕ мали правильні дизамбіг-приклади `en:ua` (напр. `fan:вентилятор`=«Turn on the fan»),
гра бере коректний приклад для кожного сенсу — НЕ чіпати. Виправлено ОДИНИЧНІ записи, чий
базовий приклад ілюстрував ДІЄСЛОВО замість іменникового сенсу: додано дизамбіг-приклади
`watch:годинник`, `play:п'єса`, `like:подібний`, `lead:свинець` (дієслова `to watch`/`to
play`/`to lead` далі беруть базовий ключ через bare-fallback); `number` базовий приклад
переведено з «номер телефону» на сенс «число»; прибрано ДУБЛЬ-запис `narrow` (був двічі).
М'якші near-synonym випадки (purpose ціль/мета, story розповідь/історія, point точка/сенс)
лишені — прийнятні синоніми, не плутають. Скан: scratchpad/flagged2.json.

**Про слово "handle" (сесія 30):** ДВА записи (розділено з колишнього `ua:["ручка","справлятися"]`):
- `{en:"to handle", ua:"справлятися"}` — дієслово (приклад «She can handle any problem»)
- `{en:"handle", ua:"ручка"}` — іменник (приклад «Turn the handle to open the door»)
Причина розділення: одним записом крос-логіка ua-en брала ВЕСЬ масив getUa, тож на промпт
«справлятися» хибно зараховувався `pen` (бо handle ще й «ручка», а pen=«ручка»). Не об'єднувати.

**🧹 ВЕЛИКА ЧИСТКА «розлитих сенсів» (сесія 36, за запитом після кейсу civil):** аудит усіх 338
багатозначних записів (LLM-судді + скептичні верифікатори) знайшов записи, що змішували
НЕПОВʼЯЗАНІ значення (як civil): промпт ua-en показував випадковий сенс, а приклад/відповіді
пасували іншому. Виправлено **43 HIGH-слова** (2861→2877 записів):
- **Патерн trim:** сирота-сенс → `uaAlt` (приймається в en-ua, НЕ промптиться в ua-en):
  admit, commit, express, graze, log, pursue(→прагнути), rate(→темп), relative(→родич), relay,
  reserved, row(→ряд), save(→зберігати), sensation, sense, speaker(→динамік), stall, strike(→страйкувати),
  swear, tire(→шина), chat; tear(сльоза)/to tear(рвати) розведено взаємно.
- **Патерн trim + новий приклад:** outline(контур), treasure(скарб), vault(сховище), ride(поїздка —
  атракціон тепер uaAlt; приклад «The ride home took two hours»).
- **Патерн split «to X» (прецедент handle/transport):** approach/to approach, charge(плата)/to charge(заряджати),
  dash(тире)/to dash, drop(краплина)/to drop, guide(гід)/to guide, land(земля)/to land,
  order(порядок)/to order, record(рекорд)/to record, sign(знак)/to sign, tie(краватка)/to tie.
- **Патерн split same-en (2 записи з однаковим en, дизамбіг-ключі прикладів "en:ua"):**
  fan(фанат)/fan(вентилятор), nail(ніготь)/nail(цвях), patient(терплячий)/patient(пацієнт),
  right(право)/right(правий; «правильний» пішло до correct/proper), square(площа)/square(квадрат),
  tip(чайові)/tip(порада), board(дошка)/board(правління) — існуючі 2 записи розведено.
  ⚠️ mastery-ключ same-en пар СПІЛЬНИЙ (wordKey=getEn[0]) — як у board раніше, свідомо.
- **enAlt-компенсація:** correct/proper приймають "right", rescue приймає "save", hit приймає "strike"
  (сенси, що зникли з промптів, лишились законними відповідями у власників).
- **⚠️ ПІЗНІШІ ТОЧКОВІ TRIM'И (той самий патерн, коли користувач натрапляє в грі):**
  **`machine` (сесія 50)** — було `ua:["механізм","машина","пристрій"]`, стало
  `ua:["механізм","пристрій"] + uaAlt:["машина"]`. Привід: приклад «Машина знову
  зламалася» не пасував промпту «механізм». Але корінь був глибший — глос «машина»
  затягував **`car` у список правильних відповідей на «механізм»** (панель збирає всі
  слова, що ділять БУДЬ-ЯКИЙ глос картки, а `car.ua` = «машина»).
  ⚠️ **ЯК ЦЕ ДІАГНОСТУВАТИ ВЗАГАЛІ:** промпт у UA→US береться **ВИПАДКОВО** з усіх
  глосів (`currentShown = vals[Math.floor(Math.random()*vals.length)]`), а приклад
  один — `lookupExample` бере `uaWord = getUa(currentWord)[0]`, тобто ПЕРШИЙ глос,
  незалежно від показаного. Тому картка з N глосів різних сенсів приречена показувати
  «не той» приклад (N−1)/N разів, і дизамбіг-ключ `"en:ua"` тут НЕ рятує — він теж
  прив'язаний до першого глоса. Лік один: тримати в `ua` лише глоси ОДНОГО сенсу.
  ⚠️ Побічний ефект, прийнятий свідомо: на промпт «машина» (картка `car`) відповідь
  `machine` більше не зараховується. Закріплено 6 перевірками в `test-homograph.js`.
- ✅ **MEDIUM-СПИСОК ЗАКРИТО В СЕСІЇ 51** (34 слова, за запитом «пройдись рештою MEDIUM-списку
  тим самим патерном»). Привід — `rather`: у `ua` лежали «досить» і «скоріше», промпт береться
  ВИПАДКОВО, а приклад прив'язаний до ПЕРШОГО глоса, тож половина показів не пасувала.
  Патерн той самий, що для `machine`: у `ua` лишається ОДИН сенс, решта → `uaAlt`.
  - **Приклад мусить пасувати тому глосу, що ЛИШИВСЯ.** Перероблено 4 англійські речення
    (`crisp`, `hall`, `rather`, `waste`) і 9 українських перекладів — там, де переклад уживав
    слово, яке щойно поїхало в `uaAlt` («Не розбий вазу» при глосі «ламати»).
  - **`waste` / `to waste` РОЗВЕДЕНО ПО-СПРАВЖНЬОМУ:** іменник = «відходи», дієслово =
    «марнувати», і дієслівна картка нарешті дістала ВЛАСНИЙ приклад (доти вона тягла
    іменниковий через bare-fallback `lookupExample`).
  - **`chip` → «щербина»** (було «відколотий шматочок»): природніше українською і саме це
    слово вживає приклад.
  - **⚠️ ГОЛОВНИЙ ХВІСТ TRIM'У — ПІДКАЗКИ.** `HINTS` писались під СТАРІ багатозначні глоси,
    тож після trim'у 14 із них або стали тавтологією, або описували сенс, що поїхав у `uaAlt`
    («break → пауза, перепочинок» при глосі «ламати»). Усі 14 ВИДАЛЕНО за правилом «підказка
    лише коли за глос конкурує інше англ. слово», а 6 переписано під нові групи
    (`rather`/`quite` на «досить», `to serve`/`to wait on` на «обслуговувати», `web`, `entry`).
    **Робити цей крок ОБОВ'ЯЗКОВО разом із trim'ом** — інакше гра пояснює не той сенс.
    Перевірка — скрипт-аудит у два боки: група без підказки / підказка без конкурента.
  - **✅ ПАРИ «ІМЕННИК / ДІЄСЛОВО» РОЗДІЛЕНО НА ДВІ КАРТКИ (та ж сесія, за запитом).**
    Trim прибирає другий сенс із промптів UA→US — «перерва» перестала б питати `break`.
    Тому 8 пар доведено до кінця патерном сесії 36 (`handle`/`to handle`):
    `break`/`to break` · `contact`/`to contact` · `court`/`to court` · `damage`/`to damage` ·
    `graduate`/`to graduate` · `matter`/`to matter` · `stress`/`to stress` · `trigger`/`to trigger`.
    ⚠️ Іменникова картка отримує НОВИЙ приклад — старий їде до дієслівної разом із сенсом.
    ⚠️ Розділення САМЕ СТВОРЮЄ нові групи спільних глосів: `break`+`interruption` на «перерва»,
    `damage`+`harm` на «шкода», `matter`+`affair` на «справа», `to stress`+`to emphasize` на
    «наголошувати» — усім чотирьом парам дописано підказки ТОГО Ж заходу.
    ⚠️ У US→UA нічого не втрачено: правило сесії 45 віддає голому промпту `break` глоси
    ОБОХ карток (`sameKeyWords(k).concat(sameKeyWords("to "+k))`), а UA→US лишається розведеним.
    ⚠️ Mastery в них РІЗНИЙ (`wordKey` = «break» проти «to break») — прогрес на старій картці
    лишається в іменника, дієслово починає з нуля. Прийнято свідомо.

**Про слово "transport" (сесія 30):** так само ДВА записи (та сама причина — приклад був про іменник):
- `{en:"transport", ua:"транспорт"}` — іменник (приклад «Public transport in the city is excellent»)
- `{en:"to transport", ua:"перевозити"}` — дієслово (приклад «They transport food to the city»)
Одним записом на «транспорт» не зараховувалось (показ «перевозити», приклад про іменник). Не об'єднувати.

### XP / Рівні

**Ставки XP:**
| Дія | XP |
|-----|-----|
| Правильна відповідь у звичайній грі | +1 |
| Закрите слово в повтореннях (після 3 правильних) | +2 — **ЛИШЕ post-game** (сесія 13; обмежено в 41) |
| Нові → Вивчаю (перше в історії правильне) | +3 бонус |
| Вивчаю → Знаю (streak = 3) | +5 бонус |
| Знаю → Вивчаю (регресія) | −3 |
| 🎧 Заняття піснями | +30 (сесія 41; було 20) |
| 🎬 Сеанс кіно | +60 (сесія 41; було 40) |
| ~~📒 Пункт «Плани» за помилки (15 закритих слів)~~ | ~~+30~~ — **джерело ЗАКРИТО в сесії 45** (нараховане в історії лишилось) |
| ~~📖 Повторення слів~~ | ~~+30~~ — **джерело ЗАКРИТО в сесії 47** разом із кнопкою «📚 Повторив» (нараховане в історії лишилось) |
| 🦉 Duolingo сесія | +25 (сесія 41; було 30) |
| 🤖 Запит GPT | +15 |
| 💬 Картка «Вирази і сленг» — показ перекладу | +1 (сесія 47) |
| 💬 Картка «Вирази і сленг» — «✓ Знаю» | +3 (сесія 47; лише за перехід у «вивчено», лише з тренажера) |

**⚠️ Важливо:** +3 і +5 — це БОНУСИ на додаток до +1 за правильну відповідь у грі.
Перехід Нові→Вивчаю = +1 + +3 = +4 разом. Перехід Вивчаю→Знаю = +1 + +5 = +6.

**Виправлено в сесії 8 — баг XP за повторення:** раніше `addXp(1)` стояв на КОЖНІЙ
правильній відповіді, у т.ч. у review. Оскільки слово закривається лише після 3
правильних, виходило +3 XP за слово, хоча лічильник 🔁 рахує закриті слова (1/слово).
Тепер у review `addXp` спрацьовує ЛИШЕ коли слово закрите (`reviewCorrectCounts >= 3`).
Звичайна гра без змін (+1 за кожну правильну).

**Сесія 13 — ставку review піднято 1→2 XP** (за запитом: повтор слова забирає ~вдвічі
більше часу). Змінено в чотирьох місцях: live `addXp(2)` (recordAnswer), `computeRetroXp`
(×2), `computeDayStatsXp` (`reviewCorrect×2`), + нова міграція `oxford_xp_formula_v5`
перерахувала історичний total+per-day. Лічильник 🔁 рахує закриті слова (1/слово), XP за них тепер ×2.

**20 рівнів**, трикутна крива `(k-1)*k/2 * 800`, масштабована ×(180000/152000)
і округлена до сотень (Level 20 = 180,000 XP; стелю піднято в сесії 17, була 152,000):
```
1 Новачок 0 | 2 Початківець 900 | 3 Учень 2800 | 4 Практик 5700
5 Знавець 9500 | 6 Фахівець 14200 | 7 Досвідчений 19900 | 8 Майстер 26500
9 Провідний 34100 | 10 Експерт 42600 | 11 Старший Експерт 52100
12 Спеціаліст 62500 | 13 Провідний Спеціаліст 73900 | 14 Лінгвіст 86200
15 Старший Лінгвіст 99500 | 16 Поліглот 113700 | 17 Аналітик 128800
18 Майстер Мов 144900 | 19 Гуру 162000 | 20 Легенда 180000
```
**⚠️ Пороги — абсолютні, без міграції:** XP зберігається як число (`oxford_xp_v1`),
рівень рахується динамічно з масиву `XP_LEVELS` у `getLevelInfo`. Зміна порогів не
чіпає збережений XP — користувач просто опиняється на рівні за новою шкалою.

**localStorage ключі XP:**
- `oxford_xp_v1` — загальний XP (число)
- `oxford_xp_day_v1` — `{date: xpGained}` щоденний приріст
- `oxford_levelup_at_v1` — ts останнього level-up (годинне «свіже» підсвічування `xp-fresh`)

**Функції:** `loadXp`, `saveXp`, `addXp(delta)` (також викликає `trackDayXp`), `trackDayXp`, `getTodayXpGain`, `getDayXpGain(date)`, `getLevelInfo(xp)`, `computeRetroXp()`, `renderXpBar()`

**Сьогоднішній приріст на смужці (сесія 32):** кінець заливки — окремий зелений сегмент
`#xp-today-fill` (#06d6a0, у тон піла «+N XP»; шар між #xp-fill і поділками #xp-seg).
`renderXpBar` рахує `inLevel = xp − max(cur, xp − gain)` — при level-up показується лише
частина В МЕЖАХ поточного рівня. Приріст 0 → display:none.

**UI:** `#xp-section` під mastery-bar: `⚡ Рівень N · Назва` + зелений піл `+N XP` (якщо > 0 сьогодні) + `N / N XP` дрібно праворуч знизу.

**🎉 Свято підняття рівня (сесія 20):** при справжньому level-up у сесії — окремий звук
`sfxLevelUp()` + оверлей `enqueueMilestone({type:"levelup"})` (шеврон із номером рівня;
колір = ліга рівня через `ACH_LEAGUES[xpTierForLevel(level)]`, тобто рівень 5 = срібло) +
годину шеврон і смужка «переливаються» (клас `xp-fresh`). Деталі — чейнджлог сесії 20.

### Duolingo / LingoHut інтеграція

**localStorage ключі:**
- `oxford_duolingo_v1` — `{"YYYY-MM-DD": count}` — кількість сесій Duolingo на день (лічильник скидається щодня)
- `oxford_lingohut_v1` — `{lessonNum: "YYYY-MM-DD"}` — дати проходження уроків LingoHut

**XP за LingoHut (актуально, сесія 12):**
```javascript
function getLhXp(n) { return n <= 35 ? 50 : n <= 70 ? 75 : 100; }
```
(Уроки 1-35: 50 XP, 36-70: 75 XP, 71-125: 100 XP)

⚠️ **Історія ставок:** 100→80 (сесія 5) → 70 (сесія 8) → **50/75/100 (сесія 12)**.
Кожне зниження супроводжувалось міграцією, щоб збережений total не розходився з тим,
що показується ("XP отримано з уроків" рахується динамічно через `getLhXp`).

**Одноразові коригування (історичні, застосовані — НЕ чіпати):**
- `oxford_lh_adj_v1` — урок 1: 100→80, -20 XP.
- `oxford_lh_adj_v2` — урок 1: 80→70, -10 XP.

**Перерахунок XP з формули (актуальний прапор `oxford_xp_formula_v13`):** одноразово
перебудовує `oxford_xp_v1` (total) і `oxford_xp_day_v1` (per-day) детерміновано зі
статистики через `computeDayStatsXp()`: за день = `correct×1 + reviewCorrect×2 +
3·ΔВивчаю + 8·ΔЗнаю + Duolingo×30 + LingoHut(50/75/100) + GPT×15 + 🎧×20 + 🎬×40 +
📒пункти×15` (+ XP пісень окремим блоком міграції). Прибрало
історичне перенарахування і ДОБІЛО XP за ранні дні (back-fill). Міграція
**ідемпотентна** (читає лише статистику), прапор ставиться ЛИШЕ при успіху (throw →
повтор на наступному завантаженні). Далі total і per-day тримаються в синхроні через
`addXp` (saveXp + trackDayXp).
- ⚠️ **Прапори формули v2→…→v12→v13** — кожна нова версія = повний перерахунок з нуля.
  `formula_v4` (сесія 12) застосував ставки LingoHut 50/75/100; `formula_v5` (сесія 13)
  застосував review×2 і до історичного XP; `formula_v6` (сесія 15) подвоїв XP пісень
  (400–1200) + вперше включив songs XP у розрахунок total; `formula_v7` (сесія 15)
  розширив шкалу пісень до 400–1600 (розмах 4×); `formula_v8` (сесія 27) додав GPT×10
  (back-fill історичних GPT-запитів: gpt у `computeDayStatsXp` + новий блок міграції як v7);
  `formula_v10` (сесія 38) перекалібрував шкалу XP ПІСЕНЬ (дедуп повторюваних рядків, див. розділ «Пісні»);
  `formula_v9` (сесія 32) підняв ставки Duolingo 25→30 і GPT 10→15 (константи DUO_XP/GPT_XP —
  live-логіка і computeDayStatsXp читають їх, тож мінялись ЛИШЕ константи + блок міграції).
  `formula_v11/v12/v13` (сесія 39) — XP за 🎧/🎬/📒 і фікс mastery-формули (v11 «+1/слово»
  та v12 прожили в проді по кілька хвилин — деталі в чейнджлозі сесії 39).
  Щоб змінити будь-яку ставку ще раз — зміни константу/computeDayStatsXp + додай `formula_v14`
  (для пісень: також перерахуй songs XP у блоці, як formula_v7). НЕ забудь синхронно
  оновити live-логіку (`addXp` у recordAnswer/updateMastery) і `computeRetroXp`.
- ⚠️ `v1` → `v2`: справжня причина, чому v1 встановився, а XP лишився старий —
  у коміті `eec07b4` випадково **видалено `const XP_DAY_KEY = "oxford_xp_day_v1"`**.
  Через це `XP_DAY_KEY` стала неоголошеною, і `localStorage.setItem(XP_DAY_KEY,...)`
  у міграції кидало `ReferenceError` ДО `saveXp(_total)` → total не оновлювався, прапор
  (у v1 — поза try) встановлювався. Те саме ламало `trackDayXp`/`getDayXpGain` →
  per-day `⚡` зникло. Константу повернуто (рядок ~9085, з коментарем «do NOT remove»).
  ⚠️ **НІКОЛИ не видаляй `const XP_DAY_KEY`** — від неї залежить увесь per-day XP.

**Функції:** `loadDuoSessions()`, `saveDuoSessions()`, `loadLhDone()`, `saveLhDone()`, `getLhXp(n)`

**UI:** Кнопки на стартовому екрані — 🦉 Duolingo (лічильник + +N XP), 📚 LingoHut (урок N, +N XP)

### Запити GPT 🤖 (сесія 25)
Трекер запитів до ChatGPT (розбір різниці між словами тощо). **15 XP за запит** (сесія 32: 10→15; сесія 27: перші 10, раніше БЕЗ XP).

- **localStorage `oxford_gpt_v1`** `{date: count}` (у `BACKUP_KEYS`).
- **Кнопка «🤖 Запити GPT»** у `#bottom-left-btns`: головна кнопка **ЛИШЕ відкриває ChatGPT**
  (`window.open("https://chatgpt.com/")`), **НЕ рахує** — ⚠️ за прямим запитом користувача
  (запити робляться під час гри, тож навігація ≠ підрахунок). Поряд: **«+»** (`gpt-plus`) —
  додає запит будь-коли (без відкриття сайту) **і нараховує +15 XP** (`addXp(GPT_XP)`),
  **«−»** (`gpt-minus`) — скасовує сьогоднішній **і знімає рівно стільки ж** (`addXp(-GPT_XP)`).
  ⚠️ Було твердження, що «−» знімає 10 при «+» 15 — це ПОМИЛКА документації (текст сесії 27
  не оновили після підняття ставки в 32). Код завжди був симетричний; перевірено в сесії 39.
- **Фінал-екран гри:** посилання «🤖 GPT» поряд з «🌐 Перекладач» (`#gpt-link`, клас
  `gt-link gpt`, бірюзове) — **РАХУЄ запит одразу при кліку** (+10 XP) **і** відкриває ChatGPT
  (сесія 30; раніше — лише навігація). Логіка: клік тут = реальний запит під час розбору щойно
  зробленої помилки. Спільна функція `countGptRequest()` (та сама, що й «+»).
- **Термометр «Плани»:** кожні **2 запити = +1 пункт**, і З СЕСІЇ 34 пари рахуються
  **НАСКРІЗЬ через дні** (`gptPointsByDate(gpt)` — кумулятивне спарювання як у 15-ок
  пройдених помилок: непарний хвіст дня чекає наступного запиту; пункт падає в день,
  коли пара замкнулась). Хелпер мемоізований за identity об'єкта gpt (безпечно:
  `loadGptRequests` щоразу парсить свіжий об'єкт). `thermoRawForDate` бере
  `gptPointsByDate(gpt)[date]` — сигнатури/виклики НЕ мінялись (всі так само передають
  сирий gpt). ⚠️ Ретро-ефект: історичні дні з непарними хвостами могли віддати/отримати
  пункт — усе рахується динамічно, без міграції; можливе одноразове «Новий рекорд» від
  перерахунку. (До сесії 34 було `floor(count/2)` ЗА ДЕНЬ — хвости згорали.) Місячна ачівка
  🌡️ «Пунктів за місяць» враховує GPT автоматично.
- **Статистика/прогрес:** рядок «Запити GPT» у `#lb-totals` (`getTotalGptRequests()`, бірюза
  `#2dd4bf`) + значок 🤖N у «Прогрес по днях» (`.prog-gpt`).
- **Функції:** `loadGptRequests`/`saveGptRequests`/`getTotalGptRequests`/`renderGptBtn`,
  `GPT_KEY`. ⚠️ Оголошено ПЕРЕД init-викликами (як songsess) — інакше TDZ у thermo-функціях.
- **⚠️ ГОЛОВНА кнопка меню — НЕ повертати до «відкрити+порахувати»**: вона ЛИШЕ відкриває
  ChatGPT (у меню перехід ≠ запит). Рахують тільки «+» і фінал-екранний `#gpt-link` (сесія 30).

### Панель "Прогрес по днях"
- Рядки: `дата · ігри · 🦉N · 📚N · 🤖N · 🎧N · 🎵N · ⚡+N` ліворуч, `+N ✓ +N ↗ 🔁+N +N •` праворуч.
  Порядок meta-значків і дельт (знаю→вивчаю→🔁повторення→нові) фіналізовано в сесії 26.
- 🦉N Duolingo (зелений `#7fdd40`) · 📚N LingoHut (синій `#87b4ff`) · 🤖N GPT (бірюза `#2dd4bf`,
  сесія 25) · 🎧N занять піснями (фіол. `#c89bff`, сесія 23) · 🎵N пісень вивчено (фіол. `#c89bff`) ·
  ⚡+N XP/день (жовтий). Усі meta — **12px**, `font-weight:600`, `opacity:0.95`; ігри —
  `color:rgba(255,255,255,.72)` (тьмяніші, без emoji).
- **⚠️ Видимість значків (сесія 26):** `.prog-date` БІЛЬШЕ не має `opacity:0.65` — вона множилась
  на opacity дочірніх meta-span'ів (📚🦉🎵🎧🤖⚡ виходили ~0.33–0.55, «бліді»). Тепер дата тьмяніє
  через `color:rgba(255,255,255,0.68)`, а значки яскраві; розмір 11px→12px.
- **⚠️ Без накладання (сесія 26):** `.prog-date { flex:1 1 auto; min-width:0 }` (на насичених рядках
  meta переноситься), `.prog-delta { flex:0 0 auto }` (права колонка тримає розмір). Раніше
  `justify-content:space-between` між двома стискуваними колонками давав колізії значків.
- **Згортка (сесія 27, спрощено; сесія 28 — прибрано «Загалом»):** останні **7 днів** окремими
  рядками → **Місяці** (усі місяці, повні суми, новіші згори; поточний місяць НАКЛАДАЄТЬСЯ на
  денний блок). ⚠️ Блок **«Загалом / За весь час»** (агрегат усієї історії через
  `_progAggregate(allDates)`) **ПРИБРАНО в сесії 28** за запитом — він дублював «Загальну
  статистику» (`#lb-totals`). НЕ повертати без узгодження. `_progAggregate`/`groupRows`
  лишились (їх використовують Місяці).
  ⚠️ Тижні ПРИБРАНО (сесія 27). **Роки ПОВЕРНУТО в сесії 32** (редизайн, за явним запитом):
  місяці показуються лише для ПОТОЧНОГО року (рік — у заголовку секції «Місяці · 2026»,
  підпис = лише назва місяця), а завершені роки згортаються в один рядок «YYYY рік»
  (секція «Роки»). `_progFmtMonth` («Червень 2026») ще визначена, але в рендері
  використовується лише її назва місяця. `_progMondayKey`/`_progFmtRange` не використовуються.
  **Формат рядків з сесії 32 — вертикальні комірки `_pgCell`** (див. розділ «Редизайн v2»).
- **Згруповані = той самий однорядковий формат, що й денні (сесія 26):** тижні/місяці/роки
  рендеряться через `_progMetaHtml`/`_progDeltaHtml` (один щільний рядок, шрифт 12px через
  `.prog-row.prog-group .prog-date/.prog-known/.prog-learning`). ⚠️ У сесії 26 пробували 2-рядкову
  сітку (активності 3×2, дата по центру, «повторення» під «вивчаю») — **ВІДКОЧЕНО** за запитом
  («надто розкинуто», «постав в 1 ряд кучно»). НЕ повертати без узгодження.
- **Панель скролиться (сесія 19):** `#progress-panel` — flex-колонка, `#prog-list`
  має `overflow-y:auto`, висота `max-height: calc(100vh - 40px)` — більше НЕ ріжеться
  краєм екрана на низьких вікнах (була жорстка `height:560px` без внутрішнього скролу).
- **Кастомний скролбар `#prog-list` (сесія 27):** прозорий трек + тонкий (10px/`thin`)
  заокруглений напівпрозорий білий повзунок (rest `rgba(255,255,255,.18)` → hover `.34`),
  замість дефолтної сірої смужки. Задано ОБИДВА набори: стандартні `scrollbar-width/-color`
  (Firefox + новий Chromium, де вони перекривають webkit) і `::-webkit-scrollbar-*`
  (Safari / старий Chromium). Скоупнуто лише на `#prog-list`, інші панелі не зачеплені.
- **⚠️ Ширина панелі: 500px на екранах ≥1900px, інакше 440px (сесія 50).** Розширено
  за запитом («місячним рядкам ледь місця вистачає»); 500 — МІНІМУМ, за якого щільний
  місячний рядок не переноситься (440-490 усі переносять). Компенсація центру
  `.layout.balance-center { margin-right }` мусить дорівнювати 500−420=80. Деталі й
  заміри — у чейнджлозі сесії 50.
- **Ширина панелі: 440px (сесія 32; лідерборд лишився 420)** — правило «прогрес і лідерборд
  однакової ширини» СКАСОВАНО за запитом (насичені місячні рядки не вміщали всі комірки).
  Історія: 420px (сесія 28 — ВІДКОЧЕНО розширення сесії 27 485→420). Central `.card`
  теж повернуто 530→460px, `.layout.shift-right` 254→222px. Причина: на широких екранах (~1880px)
  розширені панелі (485) приводили до того, що правий leaderboard правим нижнім кутом (рядки 9-10)
  ЗАХОДИВ ПІД фіксований `#lb-totals` (Загальна статистика, bottom/right:18px, z-index:200), і
  користувач вважав панелі «занадто розтягнутими». На вужчих екранах спрацьовує `max-width:94vw` +
  `flex-wrap`. ⚠️ НЕ розширювати знову без узгодження. ⚠️ Залишковий нюанс: на екранах <~1814px
  кут leaderboard все одно може заходити під `#lb-totals` (так було й до сесії 27 — це не регресія,
  бо геометрія: фіксований кутовий блок + широкий центрований 3-колонковий layout). Універсальний
  фікс (медіа-правило / репозиція lb-totals) не робили — користувач на широкому екрані.

### Клікабельні дельти ✓/↗ у «Прогресі по днях» → слова дня (сесія 37, за запитом)
Клік по комірці «+N ✓» / «+N ↗» ДЕННОГО рядка відкриває модалку зі словами, які того дня
перейшли в «Знаю»/«Вивчаю» (EN — UA, клік по рядку = TTS, кнопка «▶ Грати ці слова»).
- **Лог переходів `oxford_mastery_trans_v1`** `{date:{k:[enWord],l:[enWord]}}` (у BACKUP_KEYS).
  Пишеться в `updateMastery` через `recordMasteryTrans(cat,key)`: перша зустріч (обидві гілки)
  → "l"; streak 1/2→3 → "k"; регресія (prev>=3, помилка) → "l". Списки дня ДИЗ'ЮНКТНІ:
  вхід в одну категорію прибирає слово з іншої за ЦЕЙ день.
- **⚠️ Лог ведеться З МОМЕНТУ фічі** — старіші дні лишаються некликабельними числами
  (слова не відновити). Комірка клікабельна лише якщо в лозі є слова за date+cat.
- `_progDeltaHtml(dk,dl,rev,dt,date)` — НОВИЙ 5-й параметр date (лише денні рядки; групові
  місяці/роки НЕ передають → некликабельні). Клас `.prog-click` + data-tdate/data-tcat;
  делегований клік на `#prog-list` → `openDayTransModal(date,cat)`.
- **Реюз #cat-modal:** нова `openDayTransModal` + `catModalCustomPool` (кастомний пул
  індексів). `startCategoryGame` бере `catModalCustomPool || categoryIndices(catModalCat)` —
  далі стандартний флоу `pendingCatPool` (модалка напрямку US→UA/UA→US/Випадковий, timed-гра
  з оновленням mastery). `closeCategoryModal`/`openCategoryModal` скидають кастомний пул.
- ⚠️ **Пари з однаковим EN — ОДИН рядок (сесія 38, за скаргою на «дубль»):** такі пари
  (run бігти/керувати, fan фанат/вентилятор, nail, patient…) ділять mastery-ключ, тож `idxs`
  містить ОБИДВА записи, а дельта в «Прогресі» рахує КЛЮЧІ. Раніше список показував обидва
  записи окремими рядками → «+2 ✓», а в модалці 3 рядки. Тепер `openDayTransModal` групує
  рядки за `wordKey` (`groups` Map), сенси через « / » (`run — бігти, бігати / керувати`);
  лічильник у заголовку і кнопка рахують `rows.length` (СЛОВА), а не записи. **Пул для гри
  (`catModalCustomPool`) лишається з УСІХ індексів** — обидва сенси все одно тренуються.
  НЕ повертати на «рядок = запис».
- **Комірка «+N ●» (нові) ТЕЖ клікабельна (сесія 37, за запитом):** показує слова, ДОДАНІ В
  СЛОВНИК того дня. Джерело — окремий **реєстр появи `oxford_words_added_v1`** `{enKey: date|""}`
  (у BACKUP_KEYS), САМОПІДТРИМУВАНИЙ: `updateWordsAddedRegistry()` на старті штампує невідомі
  ключі сьогоднішньою датою (нові слова після деплоя підхоплюються автоматично, без ручних
  правок). Перший запуск = сідінг: партії 18-19.07.2026 (WORDS_ADDED_SEED — стор. 24/26/28 EFE
  + ретро-розподіл) отримали свої дати, стара база — "" (некликабельні числа). Гілка cat==="t"
  в `openDayTransModal` (мета CAT_META.new, оранж); `wordsAddedByDate()` — інверсія реєстру.
  ⚠️ Число модалки може відрізнятись від дельти ● — ЦЕ НЕ БАГ: дельта = НЕТТО-зміна розміру
  словника зі снапшотів t (додані МІНУС видалені за день), модалка = фактично ДОДАНІ слова.
  Розібраний кейс 19.07.2026: +20 додано, −1 видалений дубль narrow → дельта +19, модалка 20.
  Пояснено користувачу, він у курсі. НЕ прибирати сідінг — без нього перший запуск штампує
  ВСЮ базу сьогоднішнім днем.

**Хелпер `_progMetaHtml(games, lh, duo, songs, songSess, gpt, movies, expr)`** — span-и meta в порядку (сесія 26):
ігри · 🦉Duolingo · 📚LingoHut · 🤖GPT · 🎬кіно · **💬вирази** · 🎧занять · 🎵пісень (сесія 23: +songs
+songSess; сесія 25: +gpt; сесія 36: +movies; **сесія 47: +expr**).
⚠️ Параметр `selfRev` (📖) БУВ і ЗНЯТИЙ у сесії 47 — не повертати.
⚠️ **`.prog-expr` рожевий `#f78fb3` СВІДОМО** — 🎧 і 🎵 у тому ж рядку вже `#c89bff`,
три однакові кольори поспіль не розрізняються (та сама логіка, що колись розвела 📚 і 📖).
Викор. і денними, і згрупованими рядками.
**`_progAggregate(dates, history, allDates, days)`** — повертає `{dk, dl, dt, games, rev, duo, lh, songs, songSess}`.

### Leaderboard (СІМ вкладок; ⚡ — сесія 32, 🎓 — сесія 43, 🕒 — сесія 51; стартова вкладка 🕒 — сесія 53)

**⚠️ ПОРЯДОК ВКЛАДОК (сесія 51, за запитом — мінявся ДВІЧІ за сесію):**
`🕒 період · 🎓 CEFR · 🏆 ігри · 📅 дні · ⚡ XP-дні · 🔥 серії · 🎖️ ачівки`.
**⚠️ АКТИВНА ЗА ЗАМОВЧУВАННЯМ — 🕒 «Рекорди за період» (сесія 53, за запитом).** Дослівно:
«в стартовому положенні гра включається на лідерборді в найкращі ігри, а хотілось би, щоб
відкривало воно, показувало рекорди за останній час». Було 🏆 (у сесії 51 користувач просив
переставити кнопки, а не змінити стартову вкладку — у 53 попросив і друге). Стартова вкладка
живе у ДВОХ місцях, які мусять збігатися: клас `active` на `.lb-tab[data-tab="period"]` у
розмітці + `let lbActiveTab = "period"`. Вкладка НЕ персиститься — кожне перезавантаження
знову відкриває 🕒 (обраний ПЕРІОД усередині вкладки персиститься, ключ `oxford_lb_period_v1`).
⚠️ Тест, що читає рядок «остання гра» з `oxford_latest_v1` (`test-cefr.js`), мусить спершу
клікнути 🏆 — після reload лідерборд уже не на «Найкращих іграх».
Tab-цикл тримати СИНХРОННО з розміткою: масив `tabs` у Tab-обробнику + порядок
`<button class="lb-tab">`.

**⚠️ Заголовки вкладок — ЛИШЕ через `setLbTitle(html, tip)` (сесія 45).** Раніше кожна
вкладка робила `lbTitleEl.textContent = "..."`. Коли в «Найкращих іграх» з'явилися підпис
`<span class="lb-title-sub">· весь словник</span>` і tooltip, цього стало мало: `textContent`
у сусідній вкладці знімає текст, але НЕ знімає `title` — і пояснення про CEFR/«Знаю»
«переїжджало» на чужу вкладку. Хелпер чистить `title` через `removeAttribute`. Перевіряють
4 тести в `test-lb-cefr.js`.
**Навіщо підпис:** вкладка показує лише звичайні ігри по всьому словнику, а ігри за рівнями
CEFR, пулом «Знаю» і «не дались» у неї не потрапляють (правила — нижче). Без підпису це
читалось як «усі мої ігри», і було незрозуміло, куди зникла свіжа гра.

1. 🏆 Найкращі ігри — топ-10
1а. 🎓 **Рекорди за рівнями CEFR** (сесія 43) — окремий рейтинг CEFR-ігор (у топ-10 вони
   не входять з сесії 42). Один рядок = один НАБІР рівнів (мітка тегу: «A1», «A1-B2», «—»),
   значення = рекорд саме цього пулу + скільки ігор ним зіграно; порядок — за рівнями
   (комбінації йдуть за своїм першим рівнем). Джерело — `oxford_games_log_v1` (нового
   сховища НЕ заводили). Остання CEFR-гра: якщо вона й є рекордом — синя риска
   `is-latest-game` у списку, якщо ні — рядок «ост» під рискою. Медалі/нумерація вимкнені
   класом `.lb-cefr` на `#lb-list` (рядки з РІЗНИХ пулів — спільного місця не буває),
   зліва «🎓». Кошик-reset прихований (лог годує ще й «Точність» — чистити звідти не можна).
   Tab-цикл — див. порядок вкладок вище.
1б. 🕒 **«Рекорди за останній період» (сесія 51, за запитом)** — привід дослівно: «є лідерборд
   за весь час по всім іграм та за весь час по рівням CEFR, але я через це не бачу результати
   за останній період». 🏆 і 🎓 показують рекорди ЗА ВСЮ ІСТОРІЮ, тож свіжа форма в них тоне.
   • **П'ять вікон** (`LB_PERIODS`, чипси `#lb-period-chips`): 7 днів · 30 днів · Цей місяць ·
     100 ігор · **За весь час**. ⚠️ Користувач попросив «понакидати варіантів», щоб потім прибрати
     зайві — прибирання = видалити рядок із `LB_PERIODS`, більше ніде не згадується.
   • **⚠️ «500 ігор» → «За весь час» (сесія 57, за запитом).** Вікно на 500 ігор при 422
     зіграних не відсікало нічого — чип уже показував увесь журнал, лише під назвою, що
     обіцяла обмеження. Тепер це `{id:"all", kind:"all"}` — `lbPeriodGames` не фільтрує
     взагалі (гілка `kind === "games"` лишилась під «100 ігор»). Стеля de-facto — кап
     журналу `GAMES_LOG_MAX` (у тій же сесії піднятий 5000 → 20000, ≈7 років гри).
     Збережений старий вибір `"g500"` мігрує на `"all"` прямо в ініціалізації `lbPeriod` —
     без цього вкладка мовчки падала б на «30 днів». Перевіряє `test-lb-period.js`.
   • **Без нового сховища** — усе з `oxford_games_log_v1` (там кожна гра з `ts`). Наслідок:
     лог ведеться з сесії 33, а ігри пулом «Знаю» в нього не пишуться взагалі.
   • **⚠️ CEFR-ігри СЮДИ ВХОДЯТЬ**, на відміну від 🏆. Питання тут «як я граю ЗАРАЗ», а грає
     користувач саме рівнями — виключити означало б показати порожню вкладку. Кожен рядок
     підписаний своїм пулом; застереження — у tooltip заголовка.
   • **⚠️ Вікно «N днів» — від ПОЧАТКУ дня N-1 день тому**, не «мінус N×24 год»: інакше вранці
     з вікна випадала б учорашня вечірня гра і список «стрибав» би протягом дня.
   • **Рядок останньої гри під рискою** — патерн 1-в-1 як у 🏆: гра вже в топ-10 → лише синя
     риска `is-latest-game`, дубля НЕ додаємо; поза топом → `lb-sep` + рядок «ост».
     ⚠️ Номер місця — **за період**, а не глобальний, і показується навіть CEFR-рядкам через
     `ctx.rankAnyPool` (на 🏆 такого номера в них немає — правило сесії 42 не чіпали).
   • Підсумок під чипсами: `N ігор · сер. X · точність Y%`. Кошик-reset прихований (лог годує
     ще й «Точність»). Ключ `oxford_lb_period_v1` — UI-налаштування, СВІДОМО не в BACKUP_KEYS.
   • **`renderGameRow` винесено з `renderTabGames`** у модульну область — обидві вкладки малюють
     рядок одним кодом. `ctx` передає вже обчислені `latest`/`today` (без нього рахує сам).
   • Перевіряє `test-lb-period.js` (51).
2. 📅 **Найкращі ІГРОВІ дні** — топ-10 (перейменовано в сесії 32 — «суто про ігри», бо
   активностей стало багато; 🔁 повторення ПРИБРАНО з рядків за запитом — не повертати)
3. ⚡ **Найкращі XP-дні** (сесія 32) — топ-10 днів за XP **БЕЗ вивчених пісень**
   (`xpDayNoSongs()` — спільний хелпер із рекордом «Досвіду (XP) за день»; заняття 🎧 і так
   відбивають процес вивчення, галочка «вивчено» лише підрезюмовує). Рядок: ⚡XP золотом +
   перелік активностей дня (🎮🦉📚🤖🎧🎵 — 🎵 ВИДНО як активність, просто XP без неї) + дата.
   Кошик-reset НА ЦІЙ вкладці ПРИХОВАНИЙ (дані виводяться з oxford_xp_day_v1, не з
   лідерборд-сховища). Tab-цикл включає "xpdays" (games→days→xpdays→streaks→ach).
4. 🔥 Серії днів підряд — топ-10
- **Реальне місце під рискою (сесія 33):** рядки поза топ-10 (★ сьогодні / «ост» остання гра)
  показують СПРАВЖНІЙ номер в історії (`data-rank` на `<li>`, CSS `attr(data-rank)` перебиває ★).
  Дні/XP-дні — точно по ВСІЙ історії (`oxford_days_v1`/`oxford_xp_day_v1` зберігають усі дні).
  Ігри — серед ЗБЕРЕЖЕНИХ: новий лог **`oxford_games_log_v1`** (масив {score,wrong,skipped,mode,
  ts,wordCount}, пишеться в endGame поряд з updateLatest, кап `GAMES_LOG_MAX` 20000 ≈ 2МБ, у BACKUP_KEYS);
  `gameRanks(extras)` = Map ts→місце в (лог ∪ топ-10 ∪ extras, дедуп за ts) ПОЗИЦІЄЮ у
  відсортованому списку (score↓,помилки↑,ts↑) — обидва рядки під рискою ранжуються в ОДНОМУ
  списку, тож номери НЕ дублюються навіть коли лог сьогодні порожній (фікс сесії 33). ⚠️ Лог ведеться З МОМЕНТУ фічі —
  стара історія поза топ-10 втрачена, тож першi тижні місце ігор «серед відомих» і трохи
  занижене (лесше ігор у базі). Хелпери: loadGamesLog/appendGamesLog/gameRank.
5. 🏅 Досягнення (achievements) — `checkAchievements()`, дані `ACHIEVEMENTS`
   (напр. кількість уроків LingoHut: пороги `[10,35,70,100,125]`). Вкладка `ach`.
   - **«Пісень вивчено» розблоковано (сесія 19):** пороги `[5,10,20,30,40]`
     (верхня планка 40 — за запитом: 36 поточних + 4 на перспективу),
     `value = countLearnedSongs()` (лічить статуси "learned" у `oxford_songs_status_v1`).
     `checkAchievements(true)` викликається при перемиканні статусу пісні (live-вітання;
     milestone-overlay z-index 10000 > songs-overlay 150 — видно поверх розділу пісень).
   - **«Періодичні» досягнення (`periodic:true`; сесія 30 — було `monthly` сесії 24):** ДВА —
     «Пунктів за місяць» 🌡️ (`value=thermoPointsThisMonth`, `record=bestMonthPoints`, `isMonth:true`,
     thresholds [60,120,180,240,300]) і «Днів поспіль» 🔥 (`value=currentStreakLength`,
     `record=bestStreakLength`, [7,14,30,60,90]). `value` = ПОТОЧНЕ (цей місяць / поточна серія),
     `record` = РЕКОРД за весь час, + `unit`. Лічильник скидається (місяць — щомісяця; серія — при
     збитті стріку), а здобута ліга лишається НАЗАВЖДИ (`checkAchievements` лише підвищує claimed —
     скидання поточного НЕ знижує лігу). **Рендер `renderAchVariantA` гілка `a.periodic`:** значок =
     найвища здобута ліга (claimed), смужка/лічильник = ПОТОЧНЕ до наступної ліги, підпис =
     «зараз/цей місяць: N unit · рекорд: M unit». ⚠️ Раніше «Днів поспіль» показувала РЕКОРД на
     смужці (застрягала при збитті стріку) — уніфіковано з місячним за запитом користувача (сесія 30).
     ⚠️ `ACH_KEY` (`oxford_achievements_v1`) додано в `BACKUP_KEYS`.
   - ⚠️ **`const SONGS_STATUS_KEY` оголошено біля `ACH_KEY`** (НЕ в розділі пісень):
     top-level `checkAchievements(false)` викликає `value()` → `loadSongStatus()` → цей
     const. Якщо перенести вниз — TDZ ReferenceError на старті. НЕ переносити.
- **Ширина: 420px** (сесія 28; ⚠️ з сесії 32 БІЛЬШЕ НЕ симетрично — панель прогресу 440px, правило однакової ширини скасовано за запитом)

### Фіксовані UI-елементи

**`#bottom-left-btns`** — fixed, bottom: 18px, left: 18px:
- 📈 Прогрес по днях (toggle progress panel)
- 💾 Копія (backup)
- 📂 Відновити (restore)
- 🦉 Duolingo (+ «−»)
- 🤖 GPT (підпис скорочено з «Запити GPT» у сесії 26; головна = відкрити ChatGPT; «+» рахує; «−» скасовує) — сесія 25
- 🎬 Кіно (сесія 36, патерн GPT-кнопки): головна ЛИШЕ відкриває https://2sub.movie/ (кіно з
  подвійними EN+переклад субтитрами); «+» відмічає сеанс перегляду (**+1 пункт «Плани»**, БЕЗ XP —
  за узгодженням), «−» скасовує сьогоднішній. Ключ `oxford_movies_v1` {date:count} у BACKUP_KEYS.
  **🔒 ЗАБЛОКОВАНО до 500 слів у «Знаю»** (`MOVIE_UNLOCK_KNOWN=500`, за запитом — без бази кіно
  фруструє; заблокована кнопка = жива ціль): сіра кнопка «🔒 Кіно N/500» (лічильник оновлюється
  з mastery через renderMovieBtn, яку кличе renderLbTotals — усі точки зміни даних), «+»/«−»
  приховані, клік не відкриває сайт. При перетині порога — одноразове вітання 🎬 «Кіно
  розблоковано!» (прапор `oxford_movie_unlocked_v1`, СВІДОМО не в BACKUP_KEYS — на новому
  пристрої перевітає тихо... ні, покаже ще раз — прийнятно). НЕ знімати лок без узгодження.
  Інтегровано скрізь як GPT: thermoRawForDate (НОВИЙ останній параметр movies — сигнатура
  розширена, всі 9 місць виклику оновлені), carry-in, місячна ачівка, avg/день, рекорди
  (bestPts), 🎬N у «Прогресі по днях» (_progMetaHtml+_progAggregate), рядок «Кіно англійською»
  в #lb-totals (оранж #ffa94d). Функції: loadMovieSessions/saveMovieSessions/
  getTotalMovieSessions/renderMovieBtn, MOVIES_KEY.
- ~~📚 **Повторив (сесія 40)**~~ — **⚠️ ФІЧУ ЗАКРИТО В СЕСІЇ 47, кнопки більше НЕМА** (після
  закриття 📒-джерела лічильник міряв лише власне натискання «+»). Разом прибрано рядок
  «Повторення слів» з `#lb-totals` і значок 📖 у «Прогресі по днях» — НЕ повертати.
  ⚠️ **ДАНІ ЗАМОРОЖЕНІ (той самий патерн, що `oxford_mistake_reviews_v1`):** ключ
  `oxford_self_reviews_v1`, `SELF_REVIEW_KEY`, `SELF_REVIEW_XP`=30 і `loadSelfReviews()`
  лишились — їх читає весь історичний тракт (`thermoRawForDate` параметр `selfRev`,
  carry-in, місячна ачівка, `computeDayStatsXp`, прапор `oxford_xp_formula_v14`). У UI сліду
  нема; прибрати читачів = обнулити пункти й XP минулих днів. Ключ у `BACKUP_KEYS`.
Стиль: матове скло `rgba(12,32,68,0.52)` + blur(18px), flex-column, gap 6px.
**⚠️ Однакова ширина рядків (сесія 25, звужено в 26):** контейнер `width:max-content` (було `210px` —
звужено до найширшого рядка, сесія 26); одиночні кнопки `width:100%`; у парах (`#duo-wrap`/`#gpt-wrap`
`width:100%`) головна кнопка `flex:1 1 auto; min-width:0`, а `+`/`−` фіксовані; кнопки
`box-sizing:border-box; white-space:nowrap`.
**⚠️ Ховається під час гри** (`classList.add("hidden")` при старті гри), відновлюється при поверненні в меню.

**`#lb-totals`** — fixed, bottom: 18px, right: 18px:
- Загальна статистика: Днів гри, Ігор, Відповідей, Правильних, Неправильних,
  **🎯 Точність · весь час + 📈 Точність · останні 300 (сесія 36, 2 рядки)**, Повторень,
  Duolingo, **Уроки LingoHut (N/125)**, **Запити GPT** (перенесено вгору за запитом),
  **Пісень вивчено (N/total)**, **Пісні англійською** (=заняття 🎧, перейм. за запитом),
  **Запити GPT (сесія 25)**, **Кіно англійською**, Досвід (XP), **«Пунктів цього місяця» (`thermoPointsThisMonth`) + «Сер. пунктів/день» + «Сер. за цей місяць» (сесія 30)**.
  ⚠️ Рядок **«📖 Повторення слів» ПРИБРАНО в сесії 47** (разом із кнопкою «📚 Повторив») — не повертати.
- **«Сер./день · всього» / «Сер./день · міс.»** — середні СИРІ пункти «Плани» за АКТИВНИЙ день
  (`avgThermoPerDay(monthOnly)`: сума `thermoRawForDate` по днях із pts>0 / к-сть таких днів; НЕ календарні).
- **⚠️ Блок «🌡️ Плани · поточне» відділено (сесія 32, за запитом):** три ЗМІННІ (не накопичувальні)
  рядки — «Цього місяця» / «Сер./день · всього» / «Сер./день · міс.» — загорнуто в `.lb-variable`
  (оранж-тонована картка з пунктирним `border-top`) з підзаголовком `.lb-subhead`. Значення різних
  відтінків (оранж / жовтий / голубий), щоб не зливались із рештою чисел. Причина: користувач скаржився,
  що всі числа схожі й важко на швидку розібрати; змінні пункти тепер явно окремі від накопичувальних.
- **⚠️ Ховається під час гри (сесія 32, за запитом):** лише в головному меню. Той самий цикл,
  що #bottom-left-btns/#thermo: hide на старті гри, restore у «🏠 На головну» та Esc з фіналу
  (НЕ показується і на фінальному екрані).
- **🎯📈 «Точність» — ДВА рядки (сесія 36, за запитом):** «Точність · весь час» (baseline, уся
  історія `correct/(correct+wrong+skipped)` з oxford_days_v1) і «Точність · останні 300» (свіже
  вікно `recentAccuracy(300)` — логом `oxford_games_log_v1` від найновіших назад до ≥300 відповідей,
  остання гра цілком). **Десяткові з КОМОЮ (51,2%)** — 1% рухається повільно,
  дробова частина показує зміну (`fmtPct=p=>p.toFixed(1).replace(".",",")+"%"`; `recentAccuracy`
  повертає FLOAT pct). ⚠️ Стрілку тренду (↗/↘/→) ПРИБРАНО за запитом одразу після додавання —
  лише число; порівняння видно з сусіднього рядка. НЕ повертати. ⚠️ Пропуски = «не влучив» (як «Неправильних»).
  Лог з сесії 33 → «останні» точні; нема логу → «—» у 2-му рядку. Обчислюється в renderLbTotals
  (`accAllHtml`/`accRecHtml`).
- Функція: `renderLbTotals()` — викликається на старті і після кожної гри/зміни статусу пісні
  (наприкінці — `checkRecordMilestones()`, див. «Вітання Новий рекорд» у розділі редизайну)

**`#thermo` — термометр «Плани» (сесія 14)** — fixed, left: 18px; по вертикалі **`positionThermo()`
центрує його відносно панелі «Прогрес по днях»** (сесія 27; панель притиснута до верху найвищої
колонки, тож її центр вище 50% — звідси й прохання підняти). Панель схована → fallback `top:50%`.
Викликається з `updateLayoutAlign()` (всі тогли/відновлення меню) + `requestAnimationFrame` на старті + на `resize`:
- **Щоденний** лічильник зусиль. 1 пункт = 1 гра / 1 Duolingo / 1 урок LingoHut №1–35;
  2 пункти = 1 урок LingoHut №36–125 / **1 заняття піснями 🎧 (сесія 23)**;
  **кожні 2 запити GPT 🤖 = 1 пункт (сесія 25; з сесії 34 — пари НАСКРІЗЬ через дні,
  `gptPointsByDate`, хвіст переноситься)**; **1 сеанс кіно 🎬 = 1 пункт (сесія 36)**;
  **кожні 12 пройдених карток «Вирази і сленг» 💬 = 1 пункт (сесія 47, за запитом;
  `EXPR_POINT_EVERY`=12, `exprPointsByDate` — залишок переноситься між днями, як у 📒)**;
  ~~**кожні 15 «пройдених» помилок 📒 = 1 пункт**~~ — **⚠️ ДЖЕРЕЛО ЗАКРИТО В СЕСІЇ 45**
  разом із кнопкою «📒 Помилки» (деталі — розділ «🗑 Сесія 45» вище). Нових пунктів звідси
  не буває; `oxford_mistake_reviews_v1` заморожений і далі ЧИТАЄТЬСЯ, щоб історичні пункти
  й XP не зсунулись заднім числом. Живими лишились `loadMistakeReviews` і
  `mistakeReviewPointsByDate(mr)` → `{date: пунктів цього дня}` з кумулятивним перенесенням
  залишку (<15). Інтегровано в `thermoRawForDate` останнім параметром `mrPts` (як gpt); усі
  виклики (`getThermoCarryIn`/`getThermoPointsToday`/`thermoPointsThisMonth`/`getBestDayPoints`)
  обчислюють `mrPts` і передають — НЕ прибирати. Видалені: `saveMistakeReviews`,
  `recordMistakeReviews`, `_totalMistakePoints`, індикатор `.rt-prog` на кнопці.
  ⚠️ const/функції оголошено ПЕРЕД `thermoRawForDate` (до init — інакше TDZ).
- **⚠️ Перенесення залишку (сесія 23):** більше НЕ обнуляється повністю — виконані плани
  (×4) «згоряють», а хвіст 1–3 пункти переходить на наступний день ланцюжком (7→завтра 3,
  13→1). `getThermoCarryIn(today,…) = (сума сирих пунктів минулих днів) % 4`; пункти за день
  рахує `thermoRawForDate(date,…)`. `getThermoPointsToday()` = carryIn + сире сьогодні.
  Лише візуально; порожні дні залишок зберігають; без нового сховища.
- **⚠️ Холодний колір перенесених пунктів (сесія 32, за запитом):** заливка розділена на ДВА
  шари. Тепла (`#thermo-fill`, жовто-помаранч. градієнт) = зароблене СЬОГОДНІ (`thermoRawForDate(today)`),
  знизу. Холодна (`#thermo-carry`, голубий `#4ba3e3→#8fd3ff`) = перенесене з минулих днів
  (`carryIn`, 0–3), сидить НАД теплою (`bottom:warmPct%`) — «у самому верху заливки». Позначки
  в перенесеній зоні теж голубі (клас `.tm.carry.done`, специфічність рівна з `.tm-major.done` →
  виграє порядком, оголошена нижче). `renderThermo` тепер сам вантажить days/duo/lh/songSess/gpt/mrPts
  (щоб порахувати warm vs carry окремо). Якщо carryIn=0 → `#thermo-carry` display:none.
- **План = кожні 4 пункти** (для користувача це ~30 хв англійської). Мітки стоять на
  4, 8, 12, 16… і **підписані значенням пунктів** (НЕ «п1/п2» — це збивало). Знизу
  `план N` (= `floor(pts/4)`), зверху `N п.` (пункти за сьогодні).
- **Шкала ФІКСОВАНА (сесія 18):** `topMark = 16`, `maxPts = 18`. Раніше калібрувалась по
  найкращому дню (`max(16, floor(best/4)*4)`) і доростала до 20/22 — користувач попросив
  стабільні 16/18. `getBestDayPoints()` лишилась визначена, але в renderThermo НЕ
  використовується. Заливка = `min(100, pts/maxPts*100)`.
- **Мітки на КОЖЕН пункт 1–17 (сесія 18):** loop `pAt=1..maxPts-1` (= 1–17; 18 = верх
  трубки без мітки). Клас `tm-major` (кратні 4 = межі планів) vs `tm-minor` (решта).
  **Розмір риски/цифри ФІКСОВАНИЙ за типом** (major 11px/10px, minor 6px/7px) — НЕ
  залежить від статусу (раніше done робив лінію 10px → розбіжність 1-8 vs 9-16).
- **Підсвітка основних за фактом ПРОЙДЕНО (сесія 18, друга ітерація за запитом):**
  пройдені основні (`.tm-major.done`, напр. 4/8) — жовто-оранжеві `#ffd166`; майбутні
  основні (12/16) — нейтрально-білі. Проміжні завжди дрібні-тьмяні. Окреме виділення
  «наступного плану» (`now`) ПРИБРАНО — акцент на досягнутому, а не на цілі.
- **Кулька `.th-bulb` міняє відтінок за рівнем (сесія 18):** у renderThermo її колір =
  `thermoColor(fillPct/100)` (жовта внизу → помаранчева → червона вгорі), inline
  background+boxShadow. Той самий градієнт, що в трубці заливки.
- **Функції:** `getThermoPointsToday()`, `thermoRawForDate(date,…)`, `getThermoCarryIn(today,…)` (сесія 23), `renderThermo()`, `thermoColor(t)`.
- **CSS:** матове скло, тонка трубка `.th-tube` (overflow:hidden), градієнт заливки
  `linear-gradient(to top, #ffd166, #ffa94d 50%, #ff6b6b)`, `background-size:100% 300px`.
  Мітки: `.tm-major` (основні), `.tm-major.done` жовто-оранжеві (пройдені), `.tm-minor` (дрібні).
- **Ховається під час гри** (`#thermo`.classList add/remove `hidden`) разом з `#bottom-left-btns`.
- Елементи: `#thermo-fill`, `#thermo-pts`, `#thermo-marks`, `#thermo-plans`.

### Візуальний дизайн

**Фон:** CSS sky gradient + cloud radial-gradients (без зображень):
```css
background: radial-gradient(cloud1), radial-gradient(cloud2), ...,
  linear-gradient(180deg, #7ab8e0 0%, ..., #eef6fd 100%);
```

**Картки** (.card, .leaderboard): темне матове скло:
```css
background: rgba(12, 32, 68, 0.58);
backdrop-filter: blur(22px) saturate(1.3);
border: 1px solid rgba(255,255,255,0.22);
```

**⚠️ Reveal-on-ready для `.layout` (сесія 28) — `.layout { opacity:0 }` → клас `.ready`
(`opacity:1`).** Причина: на **hard-reload (Ctrl+Shift+R)** `songs.js` (блокуючий `<script>`
перед основним) тягнеться з мережі; поки він вантажиться, Chrome малює body, де видно ЛИШЕ
картку (бічні панелі ще `hidden`). Коли основний скрипт відпрацьовує і поряд із карткою
«спалахує» панель прогресу — Chrome переcomposить `backdrop-filter` картки → **світла смужка
ліворуч на картці** (доля секунди, лише коли прогрес відкритий, лише hard-reload, F5 з кешем —
ні). Фікс: тримаємо `.layout` невидимим, показуємо ОДНИМ кадром уже зібрану розкладку. `.ready`
додається: (1) головний шлях — у фінальному `requestAnimationFrame` ініту (після `updateLayoutAlign`),
(2) **fallback** — крихітний inline-скрипт `addEventListener("load",…)` ПЕРЕД `songs.js` (показує
лейаут навіть якщо основний скрипт кинув помилку до свого reveal-виклику → не лишаємо порожній екран).
⚠️ `#thermo`/`#bottom-left-btns`/`#lb-totals` — fixed, ПОЗА `.layout`, тож не гасяться (видимі одразу).
⚠️ Headless відтворити смужку не вдалось (preload-scanner + GPU-composite); фікс перевірявся
користувачем через Ctrl+Shift+R. НЕ прибирати `opacity:0`/`.ready` без узгодження.

### 🔗 Фразовий ховер у прикладах (сесія 45, за запитом)

**Привід:** «work out разом це тренування, а тут цього не вказано». `wrapSentence`
розбирала речення ПОСЛІВНО, тож у 508 реченнях багатослівний ключ, який словник знає
цілком, показувався частинами: «worked» → «працювати, робота», а «out» узагалі без
підказки (частки лежать у `HW_SKIP`).

- **Як працює:** перед послівним розбором пробуємо збіг фрази 2-4 слова
  (`phraseIndex()` — мапа «перше слово → кандидати», будується лениво з `EN_TO_UA`,
  довші кандидати першими). Перше слово матчиться як є або через `stemEn`
  («worked out» → «work out»), решта — буквально. Між словами фрази має бути **рівно
  один пробіл**, тож кома чи тире розривають збіг. Уся фраза йде одним `.hw`-спаном
  із `data-trans` і `data-phrase="1"`.
- **Що це дає безкоштовно:** клік озвучує фразу цілком (`speak(hw.textContent)`),
  тултіп бере `data-trans` — обидва обробники не міняли.
- **⚠️ `PHRASE_HOVER_SKIP` — 7 фраз, які НЕ склеюємо ніде:** `sleep in`, `wait on`,
  `run out`, `stick to`, `move in`, `go through`, `hold on`. Їхня частка збігається з
  прийменником місця, і в корпусі є реальні буквальні вживання: «The horses **sleep in**
  the barn», «Cars **wait on** the forecourt», «He ran **out** the door», «Nougat
  **sticks to** the teeth», «The rook **moves in** straight lines», «The lace **goes
  through** each eyelet», «He **held on** tenaciously». ⚠️ Автоматично розрізнити ці
  читання НЕ можна: евристика «частка + артикль = буквально» ламається на «put on
  **your** jacket». З'явиться нове хибне спрацювання — дописати сюди, це єдиний важіль.
- **⚠️ Порядок атрибутів `class="hw"` → `data-trans` → решта** тримати як є: регекс-
  інструменти (`_snap`-подібні зрізи, `test-hints.js:73`) очікують `data-trans` одразу
  за класом. Я на цьому спіткнувся під час заміру — склейки виглядали як «без перекладу».
- **Тултіп на перенесеному спані:** позиціювання перейшло на `getClientRects()[0]`.
  Фраза з двох слів може розірватись на два рядки, і `getBoundingClientRect()` дав би
  об'єднаний прямокутник на всю ширину — тултіп зʼїжджав би вбік.
- **Перевіряє `test-phrase-hover.js` (18 перевірок)**, і головна там — прогін УСІХ 8647
  прикладів на те, що рендер не втрачає ані символу (`textContent === речення`).
  Перевірено, що набір ловить обидва режими відмови: без списку-винятку падають
  перевірки буквальних вживань, зі з'їденим пробілом — перевірки меж фрази.
- ⚠️ **Відомий і НЕ виправлений:** `esc()` не екранує подвійні лапки, тож глос із `"`
  порвав би атрибут `data-trans`. Зараз таких глосів у словнику НЕМА (перевірено), і
  ризик однаковий для однослівних підказок — тому не чіпав, щоб не міняти `esc`
  глобально. Якщо колись з'явиться глос із лапкою — екранувати в місці підстановки.

### Hover translations
- `.hw` spans в реченнях → тултіп з перекладом + IPA
- Клік → TTS
- `wrapEnWord(words)` — розбиває "A / B / C" на окремі spans
- **⚠️ ПРИСВІЙНІ ФОРМИ (сесія 41, баг «baby's без перекладу»):** `stemEn` зрізає присвійний
  суфікс (`'s` / `s'`) ПЕРЕД пошуком у EN_TO_UA. Раніше апостроф ламав усі спроби стемінгу
  («baby's» → немає ключа, `s$`-зріз давав «baby'»), і слово висіло в реченні як TTS-only
  без тултіпа — так було для 14 токенів (child's, people's, parents', today's, everyone's…).
  ⚠️ Список винятків ПРЯМО В `stemEn` (не окрема константа — щоб `check-coverage.js`,
  який слайсить саму функцію, автоматично мав ту саму логіку): it/he/she/that/what/there/
  here/who/how/why/let/one/everybody — там `'s` це is/has, і переклад бази («дозволяти»
  для Let's) лише збивав би. НЕ прибирати зріз і НЕ розширювати список на іменники.
- **⚠️ enAlt-СИНОНІМИ В HOVER (сесія 41, баг «nappy без перекладу»):** `EN_TO_UA` будується
  з `WORDS.en` (+bare-форма дієслів), тож 55 англійських синонімів-відповідей із `enAlt`
  (nappy, trousers, pram, pyjamas, spectacles, plaster, drugstore…) показувались у панелі
  правильної відповіді БЕЗ перекладу. Окремий пас `(() => {   // enAlt-hover` ОДРАЗУ після
  IIFE `EN_TO_UA` (не всередині — там TDZ на самій мапі) реєструє їх.
  ⚠️ **Умова `!EN_TO_UA.has(key) && !stemEn(key)` — КРИТИЧНА, не спрощувати:** enAlt-омографи
  `checked` (від checkered) і `spotted` (від polka dot) інакше перебили б дієслівні форми
  check/spot у реченнях («He checked the box» → тултіп «картатий»). Перевірено: після пасу
  checked→перевіряти, spotted→помічати, older→старий; nappy/trousers/pram→свої переклади.

### Auto-backup
- File System Access API (Chrome/Edge) + IndexedDB handle
- Fallback: традиційне завантаження для Safari/Firefox

### Гарячі клавіші
- **Enter** — submit / advance / start
- **Esc** — end / close
- **Пробіл** — speak / start review (з **фінального** екрана. ⚠️ Сесія 45: на СТАРТ-екрані
  Пробіл більше нічого не робить — він відкривав «📒 Помилки», кнопки нема)
- **Tab/Shift+Tab** — cycle leaderboard tabs. ⚠️ Якщо поверх лідерборду відкрите
  меню «Уроки LingoHut» (`#lh-screen`) — перший Tab його ЗАКРИВАЄ (`closeLhScreen`),
  не перемикаючи вкладку (сесія 12, баг «меню стрибало вниз»). Меню ховає вкладки
  лідерборду, тож Tab — єдиний шлях туди; фікс лише в Tab-обробнику.
- **⚠️ LeftShift / RightShift — ГОРТАЮТЬ ПЕРІОД вкладки 🕒 (сесія 51, за запитом).**
  Дослівно: «зроби так, щоб ігри саме включалися за допомогою Enter, а вже коли відкрився
  цей другий слайд з вибором напрямку, то цей самий напрямок переключався би за допомогою
  Shift. А в основному меню вже за допомогою того ж самого Shift переключалися би ці самі
  періоди в лідерборді». Тобто одна клавіша, три ролі, розведені СТАНОМ:
  • меню / екран результатів → період вкладки 🕒;
  • відкрита модалка «Тренування» → напрямок перекладу (як було з сесії 44);
  • під час гри → озвучити слово (як було).
  **Лівий Shift — уперед, правий — назад** (прямий вибір користувача; для цього
  `pendingShiftCode` і зберігає `e.code`). ⚠️ **Shift БІЛЬШЕ НЕ ВІДКРИВАЄ «Тренування»** —
  це роблять Enter і клік. НЕ повертати `openTrainModal()` у гілку Shift.
  ⚠️ **Перший Shift із ЧУЖОЇ вкладки лише переводить на 🕒 і НЕ гортає** — інакше період
  мінявся б наосліп. Патерн «спершу сфокусуй, потім дій», як Tab із меню LingoHut.
  ⚠️ **Умова позитивна: видимий старт-екран АБО видимий екран результатів.** Спокуса
  написати «якщо не гра» — і перетнутися з ігровою роллю Shift (озвучка). Під час гри
  обидва екрани сховані, тож гілка мовчить сама. Закріплено перевіркою.
  ⚠️ Поки відкрита модалка категорій / вибору напрямку — Shift мовчить (додано тоді ж).
- **Alt** — toggle "Прогрес по днях"
- **⚠️ «Чисті» модифікатори (сесія 35):** Shift-цикл і Alt-тогл спрацьовують на **KEYUP** і
  ЛИШЕ якщо модифікатор натиснуто БЕЗ інших клавіш: (а) інший модифікатор уже затиснутий на
  момент keydown (Ctrl+Shift / Alt+Shift = зміна розкладки) — скасовує; (б) БУДЬ-ЯКА інша
  клавіша між keydown і keyup модифікатора (ловимо і keydown, І keyup — PrtSc у Windows шле
  ЛИШЕ keyup, тому Alt+PrtSc не тоглить панель) — скасовує. Стан: `pendingShiftCode` /
  `pendingAltToggle` (спільний трекер ПЕРЕД великим keydown-обробником; старий Alt-блок з
  великого обробника ВИДАЛЕНО). `window.blur` скидає обидва (Alt+Tab). Alt-тогл на keyup
  перевіряє cat-modal/review-mode-modal (як раніше великий обробник). НЕ повертати тогл на
  keydown — Alt+PrtSc знову ховатиме панель. **⚠️ `!e.repeat` при озброєнні ОБОВ'ЯЗКОВИЙ**
  (знайдено адверсарійною ревізією): без нього авто-повтор затиснутого Shift «воскрешає»
  скасовану дію (Ctrl+Shift розкладка: Ctrl відпущено на пів секунди раніше → repeat-keydown
  Shift уже без ctrlKey → цикл таки спрацьовував). Також: repeat-Enter НЕ підтверджує модалку
  напрямку (утримуваний Enter відкрив би і одразу підтвердив), а Shift-цикл ПРОПУСКАЄ
  "tolearn", якщо `toLearnIndices()` порожній (мертвий лаунчер, коли все засвоєно).
- **Shift / Tab** (під час гри) — озвучити англійське слово. Поведінка залежить від стану:
  - **Питання активне** (correction-панель прихована): Tab озвучує лише в en-ua/en-syn/
    en-ant (де англ слово на екрані). У ua-en/en-def/en-cloze Tab МОВЧИТЬ — інакше
    озвучував би приховану ВІДПОВІДЬ (`getEn`), тобто підказував би голосом.
  - **Correction-панель відкрита** (`awaitingAfterWrong`): відповідь УЖЕ на екрані, тож
    Tab озвучує правильне англ слово в БУДЬ-ЯКОМУ режимі — `(mode==="en-ua") ? currentShown
    : getEn(currentWord)[0]` (та сама логіка, що й кнопка 🔊 у панелі). Сесія 13: скарга
    «Tab вже взагалі не озвучує» — попередній фікс (сесія 12) глушив Tab у ua-en і на
    correction-екрані теж, хоча там підказка «Tab — озвучити слово» і відповідь видима.

### Нормалізація відповідей
- "to X" / "X" — обидва прийнятні для дієслів
- `г` → `ґ` нормалізація (для UA відповідей без клавіші ґ)
- **⚠️ ДЕФІС ≡ ПРОБІЛ в українській відповіді (сесія 50, привід «бізнес план»).**
  `бізнес план` = `бізнес-план` = `бізнес  план`. За правописом дефіс обов'язковий
  (§ 36: незмінювана іншомовна перша частина — `бізнес-`, `веб-`, `прес-`), але це
  орфографічна пастка, а не перевірка словника — той самий дух, що й `г → ґ`.
  Зачіпає 152 картки з дефісом у глосі (веб-сайт, штаб-квартира, вагон-ресторан…);
  злиттів укр. глосів рівно **0** (перевірено на всіх 13684 картках).
  - **⚠️ ПРАВИЛО ОДНОСТОРОННЄ — лише український бік.** По англійському таке зведення
    дало б **19 реальних злиттів**, де дефіс і розрізняє сенс: `pick up` (забрати) vs
    `pick-up` (звукознімач), `check-in`, `take-off`, `warm-up`, `sign-up`, `carry-on`.
    `normEn` лишається строгим. НЕ поширювати.
  - **⚠️ ЖИВЕ В ОКРЕМОМУ `normUaSoft`, не в `normUa` — і це не стилістика.** З `normUa`
    будується `inp`, а з `inp` — `inpNorm` для АНГЛІЙСЬКОЇ гілки. Дописане прямо в
    `normUa` правило протікає в ua-en, і `pick-up` починає приймати «pick up».
    Я цю помилку зробив і зловив перевіркою в `test-homograph.js` — не «спрощувати»
    дві функції назад в одну.
  - Обидва боки закриті 7 перевірками в `test-homograph.js` (тест падає і якщо правило
    звузити назад, і якщо поширити на англійську).

---

## Архітектура коду

### Дані
- `WORDS` — масив `{en, ua}` (актуальна кількість — у шапці «Обсяг даних»). Історія
  добору по сесіях (EFE стор.12-402, Oxford 3000/5000, C2, тематичні xlsx, чистки) — у
  `handoff-log.md` і розділах «ВЕЛИКИЙ ВІДБІР», «OXFORD-ДОБІР», «C2-ДОБІР» нижче.
  Опційні поля **`uaAlt:[…]` / `enAlt:[…]`** — ПРИЙНЯТНІ відповіді, які НЕ показуються як
  промпт: `uaAlt` = додаткові укр. переклади (лише en-ua; `getUa` їх НЕ повертає → ua-en не
  зачеплено); `enAlt` = англ. синоніми-відповіді (лише ua-en). Деталі й правила відбору —
  «Що НЕ змінювати → uaAlt/enAlt». ⚠️ Діакритика в ключі ЛАМАЄ токенізатор (`fiancé`→`fiance`,
  `soufflé`, `à la carte`) — ключі лише ASCII.
- `TRANS` — IPA (100% покриття, перевіряється `check-coverage.js`)
- `HINTS` — уточнення відтінку значення в дужках (**1163 записи**; сесія 45 додала 6 про неправильні форми + `chips`):
  показується в рядку правильної відповіді, коли на один укр. глос припадає кілька
  англійських слів. Деталі й правило поповнення — розділ «💡 HINTS» нижче.
- `EXAMPLES` / `EXAMPLES_UA` — EN/UA речення (~2590 пар, 100% hover-покриття слів)
- `DEFS` — визначення (~476) для Def-режиму
- `SYNS` — синоніми (~187) для Syn-режиму
- `EXPRESSIONS` — вирази/колокації `{en,ua,note?,tag?}` (сесія 30; `tag:"slang"` — сесія 41) — НЕ окремі слова, ТРИМАТИ ОКРЕМО від WORDS (як slang у піснях). **З сесії 32 має UI** — розділ «Вирази і сленг» (флеш-картки; див. окремий розділ). **89 виразів** (сесія 41: +25 ідіом/колокацій B2-C1 за ревізією GPT + `tag:"slang"` для 9 розмовних; сесія 40: +12 медичних зворотів; сесія 37 +31 за ПРАВИЛОМ РОЗПОДІЛУ, див. нижче): 11 первісних (4 усмішки, make amends, excuse me, 5 ідіом) + life-events (fall in love, make a will, make friends, get married, have a baby, get a job) + 24 побутові рутини стор.28 (alarm goes off, make the bed, brush your teeth, have breakfast/lunch/dinner, go to bed vs go to sleep, take a break, to be late, do homework, put the children to bed, go out…) + to get your hair cut.
- `ANTS` — антоніми (~143) для Ant-режиму
- `EN_TO_UA` — Map для hover-перекладів

### localStorage ключі (НЕ перейменовувати!)
- `oxford_scores_v1` — топ-10 ігор
- `oxford_days_v1` — `{date: {correct, wrong, skipped, games, wordCount, reviewCorrect}}`
- `oxford_today_best_v1` — найкраща гра сьогодні
- `oxford_latest_v1` — остання гра
- `oxford_day_mistakes_v1` — помилки `{date: [enWords]}` (ключ-слово з сесії 18; було `[wordIndices]`)
- `oxford_word_mastery_v1` — mastery `{enWord: streak | {s,c,w}}` (ключ-слово з сесії 18, було
  `{wordIndex: streak}`; з сесії 40 значення = число АБО об'єкт із лічильниками ✓/✗ — читати
  через `mStreak`/`mTally`, деталі в розділі «Mastery tracking»)
- `oxford_mastery_history_v1` — знімки `{date: {k, l, t}}`
- `oxford_backup_date_v1` — дата backup
- `oxford_progress_open_v1` — стан панелі ("1" = відкрита)
- `oxford_xp_v1` — загальний XP
- `oxford_xp_day_v1` — `{date: xpGained}` щоденний приріст
- `oxford_duolingo_v1` — `{"YYYY-MM-DD": count}` Duolingo сесії
- `oxford_lingohut_v1` — `{lessonNum: "YYYY-MM-DD"}` LingoHut уроки
- `oxford_lh_adj_v1`, `oxford_lh_adj_v2` — прапори одноразових коригувань LingoHut XP (застосовано)
- `oxford_xp_formula_v13` — актуальний прапор повного перерахунку XP (v2…v12 — старі; v13 = mastery-XP **3·ΔВивчаю + 8·ΔЗнаю** (брутто-еквівалент; v12 з max(0,·) з'їдала +3 «швидких» слів) + XP за 🎧20/🎬40/📒15-за-пункт — сесія 39)
- `oxford_songs_status_v1` — `{songId: "queue"|"learning"|"learned"}` статус вивчення пісень
- `oxford_songs_xp_v1` — `{songId: "YYYY-MM-DD"}` дати нарахування XP за пісні (одноразово при →"learned")
- `oxford_song_sessions_v1` — `{"YYYY-MM-DD": count}` заняття піснями 🎧 (сесія 23; +2 пункти «Плани»/сесія; **+20 XP — сесія 39**; у BACKUP_KEYS)
- `oxford_gpt_v1` — `{"YYYY-MM-DD": count}` запити GPT 🤖 (сесія 25; +1 пункт «Плани» за КОЖНІ 2 запити — з сесії 34 пари НАСКРІЗЬ через дні (`gptPointsByDate`, хвіст переноситься); **15 XP/запит — сесія 32**, «−» знімає стільки ж; у BACKUP_KEYS)
- `oxford_mistake_reviews_v1` — `{"YYYY-MM-DD": к-сть закритих слів}` пройдені помилки 📒
  (сесія 30-31: +1 пункт «Плани» за кожні **15** закритих, залишок переноситься кумулятивно).
  ⚠️ **З СЕСІЇ 45 — ЗАМОРОЖЕНИЙ:** кнопки «📒 Помилки» нема, ключ більше НЕ поповнюється,
  але читається (щоб історичні пункти/XP лишились на місці). У BACKUP_KEYS — НЕ прибирати.
- `oxford_due_seed_v1` — прапор одноразового засіву боргу зі старого «відкритого боргу»
  помилок (сесія 45, `seedDueFromMistakes`). Не в BACKUP_KEYS: це разова технічна відмітка.
- `oxford_achievements_v1` — `{achId: найвища_оголошена_ліга 0-5}` claimed-ліги ачівок (дедуп сповіщень). ⚠️ Додано в BACKUP_KEYS у сесії 24 (місячне досягнення не перевиводиться з даних, тож має бекапитись)
- `oxford_games_log_v1` — масив УСІХ зіграних ігор (сесія 33; для реального місця «останньої гри»; кап `GAMES_LOG_MAX` = 20000 із сесії 57; у BACKUP_KEYS)
  ⚠️ **Кап 5000 → 20000 і ЗАПИС, СТІЙКИЙ ДО КВОТИ (сесія 57, за запитом).** Стеля потрібна
  лише як страховка від квоти localStorage (~5 МБ у Safari, ~10 МБ у Chrome/Firefox — і її
  ділять УСІ ключі гри); запис ≈90-100 байт, тож 20000 ≈ 2 МБ і ~7 років при 5-8 іграх/день.
  Головне в правці — не число, а `try/catch` навколо `setItem` у `appendGamesLog`: лог
  пишеться в `endGame`, тож виняток по квоті забирав би з собою запис денної статистики.
  Тепер при відмові лог ріжеться вдвічі й пробує знову (до 100 записів), гірший сценарій —
  «частина старих ігор зникла», а не «гра не дорахувалась». НЕ прибирати try/catch.
- `oxford_movies_v1` — `{"YYYY-MM-DD": count}` сеанси кіно 🎬 2sub.movie (сесія 36; **+2 пункти «Плани»/сеанс і +40 XP — сесія 39**, було 1 пункт без XP; у BACKUP_KEYS)
- `oxford_self_reviews_v1` — `{"YYYY-MM-DD": count}` повторення слів 📚/📖 (сесія 40; **+1 пункт
  «Плани» і +15 XP за повторення**, «−» симетричний; у BACKUP_KEYS). Рахує ЛИШЕ ручне «+» —
  перегляд списку через головну кнопку НЕ рахується.
- `oxford_xp_formula_v16` — **актуальний** прапор (сесія 41): 2 XP за закрите слово рахуються
  ЛИШЕ за post-game повторення. `computeDayStatsXp` дістав опційний 12-й параметр `mrWords`
  (= `loadMistakeReviews()`, слова закриті через меню) і рахує `2 × max(0, reviewCorrect − mrWords)`.
  ⚠️ Параметр ОПЦІЙНИЙ свідомо — старі блоки міграцій (v3…v15) кличуть функцію без нього.
- `oxford_xp_formula_v15` — прапор перерахунку XP (v14 і старіші — історія; v14 додав
  джерело 📖 повторень слів; **v15 (сесія 41) — перебалансування ставок під XP/хв**: 🦉30→25,
  🎧20→30, 🎬40→60, 📒пункт 15→30, 📖повторення 15→30. Тіло міграції НЕ мінялось — воно читає
  константи DUO_XP/SONGSESS_XP/MOVIE_XP/MISTAKE_REVIEW_XP/SELF_REVIEW_XP)
- `oxford_expr_status_v1` — `{exprEn: "learning"|"learned"}` статус вивчення виразів/сленгу (сесія 32; дефолт "queue" не зберігається; у BACKUP_KEYS). Ключ = англ. текст виразу (`en`), НЕ індекс.

### Хелпери
- `getEn(w)`, `getUa(w)` — string|array → array
- `isCorrect(input)` — з "to" та г→ґ нормалізацією
- `lookupExample(table, enWord, uaWord)`
- `wrapSentence(sentence)`, `wrapEnWord(words)` — wrapEnWord (сесія 18) пріоритезує повну
  форму «to X» у EN_TO_UA (чистий переклад дієслова), далі fallback на bare-стем
- `stemEn(word)` — стемінг + irregular форми
- `speak(word)` — озвучує ПОВНУ форму, включно з «to» (сесія 18; раніше зрізав «to»)
- `wordKey(w)=getEn(w)[0]`, `indexOfWordKey(key)` — ключ-слово для mastery/помилок (сесія 18)
- `thermoColor(t)` — інтерполяція кольору градієнта термометра 0..1 → [r,g,b] (сесія 18)
- `checkThermoMilestones()` (сесія 24) — денні milestone'и 🎯/💪 за пунктами термометра (перетин 4/8…, 2/6…)
- `thermoPointsThisMonth()` (сесія 24) — сума СИРИХ пунктів термометра за поточний UTC-місяць (місячна ачівка)
- `loadGptRequests`/`saveGptRequests`/`getTotalGptRequests`/`renderGptBtn`, `GPT_KEY` (сесія 25) — трекер запитів GPT 🤖 (деталі — розділ «Запити GPT»)
- `setMode(m)` — m=ua-en/en-ua/mix/en-def…; ставить `selectedMode`, резолвить `mode` (mix→випадковий конкретний)
- `sfxStart()`, `sfxEnd()`
- `fmtPool(n)`, `fmtElapsed(s)`
- `_progMetaHtml(games, lh, duo, songs, songSess, gpt)` — meta-значки рядка прогресу (порядок — сесія 26)
- `_progAggregate(dates, history, allDates, days)` — агрегація тижнів/місяців

### Візуальна система кольорів
- `#ffd166` — жовтий (XP, highlight)
- `#87b4ff` — синій (review, остання гра, LingoHut у прогресі)
- `#ff6b6b` — червоний (помилки)
- `#7be07b` — зелений (Знаю)
- `#ffa94d` — оранжевий (Нові)
- `#06d6a0` — зелений (daily XP gain pill)
- `#7fdd40` — зелений (Duolingo у прогресі)

---

## Розділ «Пісні» (сесія 11 — старт; розширено в 12)

Окремий розділ для вивчення текстів пісень (NEFFEX тощо): паралельний текст
оригінал‖переклад (рядок-у-рядок), YouTube, статус вивчення, hover-переклади, сленг.

**⚠️ СТАН: 38 пісень NEFFEX + 3 комерційні (Lollipop, Phenomenon, Devil in Her Eyes)** = 41 у колекції. NEFFEX —
усі з повним текстом, перекладом,
glossary та сленгом. Остання додана — «Bryce Savage — Devil in Her Eyes 😈» (`ytId="RakR7T2W-NI"`, 700 XP, сесія 39).

- **Дані:** окремий файл **`songs.js`** (підключений `<script src="songs.js">` ПЕРЕД
  основним скриптом). `SONGS = [{id, artist, title, ytId, xp, sections:[{label,
  lines:[{en,ua}]}], glossary:{...}, slang:[{en,ua,note}]}]`.
- **XP за пісню (поле `xp`):** нараховується **реально** у загальний XP при першому
  переключенні статусу на «✅ Вивчено» (одноразово). Зберігається в
  `oxford_songs_xp_v1 = {songId: "YYYY-MM-DD"}`. Картка показує `⚡ NNN XP` (`.sc-xp`).
  **⚠️ Шкалу масштабовано тричі: сесія 13 (100→200..600), сесія 15 (200→400..1200),
  сесія 15 (розмах розширено до 400..1600, розмах 4×); **сесія 38 — ПЕРЕКАЛІБРУВАННЯ
  з дедупом повторів.** Вивчення складної пісні = декілька годин; розмах 4× відображає
  НЕлінійне зростання зусиль. Поточний діапазон **400–1600 XP**, крок 50.
  **⚠️ score рахує УНІКАЛЬНІ рядки (сесія 38, за запитом):** приспів × 3 — це той самий
  матеріал, вивчається один раз, тож зусилля не потрійні. Дедуп за текстом `en` (без
  регістру/крайніх пробілів) НАСКРІЗЬ по пісні; `scoreOf` у `build-song.js` теж дедупить.
  `score = унікальні_рядки + унікальні_слова/10 + slang×2`;
  формула **`xp = round50(400 + (score − 52.8) / 108.4 × 1200)`**
  (мін score 52.8 = Inspired/підлога 400; макс 161.2 = Things Are Gonna Get Better/стеля 1600).
  Разом **37950 XP** по всіх 41 пісні; ПОТОЧНІ значення xp — прямо в songs.js (джерело істини).
  ⚠️ **Сленг ВХОДИТЬ у score** (`slang×2`) — рахуй xp ПІСЛЯ заповнення slang, інакше вийде
  занижено (кейс Closer to Heaven: без сленгу score 47.8=підлога 400, зі сленгом 57.8→450).
  ⚠️ Стара шкала була `(score − 59.6) / 121.4` з рядками ВКЛЮЧНО з повторами — не повертати.
  Наслідок перекалібрування: найповторюваніші просіли (Soldier 1600→1200 при 45% повторів,
  Lollipop 900→500 при 56%), а щільні без повторів піднялись (Dangerous 900→1200, Failure
  1300→1450, Hope 1050→1200). Сума по колекції 37750→36250.
  **Міграція `oxford_xp_formula_v7`** (сесія 15): перераховує stats XP + додає нові
  значення XP для вже зарахованих пісень (`oxford_songs_xp_v1`). Ідемпотентна.
  **Для нових пісень:** `node build-song.js` → калібрувальна таблиця score→xp по всіх
  піснях + відформатований блок для вставки. `node build-song.js <файл>` → рекомендований
  xp за 3 найближчими сусідами за score. ⚠️ На межі мода 3 сусідів буває неточна (у
  сесії 15 для Failure score 148.3 давала 1200 замість 1300 — баг tie-breaking
  виправлено: тепер при нічиєї бере xp найближчого сусіда). **Завжди звіряй із формулою
  шкали вище — вона джерело істини.** Прапор актуальний: `oxford_xp_formula_v8`.
  Якщо змінювати шкалу знову — додавай `formula_v8` (нова формула + songs XP перерахунок).
  Орієнтири score: Soldier≈181, Best of Me≈178, Forget'em≈173, Summer≈172, Baller≈167,
  Grateful≈141, Hungover≈134, Self Made≈130, Numb≈123, Cold≈115, Till I Let Go≈108, Inspired≈64, Dangerous Woman≈60.
- **Сортування списку за статусом (сесія 12):** `renderSongsList` сортує через
  `STATUS_SORT_ORDER = {learning:0, queue:1, learned:2}` — спершу «📖 Вчу», далі
  «📋 На черзі», внизу «✅ Вивчено».
- **⚠️ Сленг тримається ОКРЕМО** від `WORDS` (поле `slang` у пісні) — під майбутній
  **окремий тренажер сленгу**. НЕ змішувати з основним словником гри.
- **UI:** повноекранний overlay `#songs-overlay` (потрібна ширина для 2 колонок, тому
  НЕ вузька права панель). Вхід — кнопка `#songs-btn` 🎵 на стартовому екрані (фіолетова
  `#c89bff`). Список → деталь (back). Делегований клік по `#songs-overlay` через
  `data-act`. Esc закриває (capture-phase listener, щоб не чіпати ігрові обробники).
- **Статус:** localStorage `oxford_songs_status_v1` = `{songId:
  "queue"|"learning"|"learned"}` (на черзі/вчу/вивчено), дефолт `queue`. Додано в
  `BACKUP_KEYS`.
- **Hover:** EN-рядки рендеряться через `wrapSongLine(text, glossary)` — спершу
  `glossary` пісні (поле в songs.js, для слів яких немає в EN_TO_UA), далі fallback на
  основний `EN_TO_UA`, інакше TTS-only. Дає майже повне покриття перекладів у тексті.
  Клік по слову або 🔊 поряд = TTS. Сленг-рядки теж мають 🔊 (озвучка).
- **Термінологія секцій:** ВСТУП · КУПЛЕТ 1/2/3 · ПРИСПІВ · ПОСТ-ПРИСПІВ · ПРЕД-ПРИСПІВ ·
  РЕФРЕН · БРИДЖ · ПРОГРАШ · КІНЦІВКА (НЕ «ХОР»). РЕФРЕН — для окремого повторюваного
  хука, що не є приспівом (напр. «I won't give up» у Things Are Gonna Get Better, сесія 15).
- **YouTube:** реалізовано ОБИДВА варіанти (користувач обере) — вбудований iframe
  (`youtube-nocookie`, lazy, toggle «Показати/Сховати відео») + кнопка-посилання
  «Відкрити на YouTube ↗».
- **Контент:** користувач дає оригінал → Claude робить адаптивний укр. переклад
  рядок-у-рядок + заповнює `glossary` (слова поза EN_TO_UA) + витягує сленг/вирази
  + ставить `xp` за складністю. Додавання нової пісні: сформувати блок локально
  (Python для вирівнювання колонок ua), вставити одним Edit перед `]` у `SONGS`.
- Функції: `loadSongStatus/saveSongStatus/getSongStatus/setSongStatus`,
  `openSongs/closeSongs`, `renderSongsList/renderSongDetail`, `escAttr`. Константи
  `SONG_STATUS_META`, `SONG_STATUS_ORDER`, `SONGS_STATUS_KEY`.
- **Список — сітка 2 колонки (`.songs-grid`, сесія 15):** картки `.song-card` мають
  клас статусу + кольоровий лівий бордер (queue синій / learning оранж / learned зелений).
  На <620px — 1 колонка.
- **Scroll-restore (сесія 15):** `songsBody` — спільний скрол-контейнер списку й деталі.
  Змінна `songsListScroll` зберігає `scrollTop` списку при кліку на пісню (`open`) і
  відновлює його при поверненні (`list`). `renderSongDetail` робить `scrollTop=0` (деталь
  згори), `openSongs` скидає `songsListScroll=0`. Раніше вихід із пісні кидав список на
  позицію скролу деталі.
- Перевірено мок-DOM тестом (17 кейсів): рендер списку/деталі, статус round-trip +
  persist, сленг-блок, YT-кнопки. Браузерного тесту не було (немає playwright у cloud).

---

## Розділ «Вирази і сленг» (сесія 32; XP + два напрямки + чистка — сесія 47)

Окремий тренажер **флеш-карток** для ідіом/виразів і сленгу. Об'єднує `EXPRESSIONS`
(ідіоми/вирази) + `slang` з УСІХ пісень у єдиний банк. **702 картки** (145 ідіом + 557 сленгу;
дедуп за `en`, ігнор регістру). ⚠️ Було 696 → 683 (сесія 47 прибрала 13 простих слів із
пісенного `slang`, див. «Чистка сленгу» нижче) → 702 (+19 із xlsx «Одяг і зовнішність»).

- **Вхід:** кнопка **`#expr-btn` «💬 Вирази і сленг»** — у ряду «Щодня» (`#daily-row`).
  Ховається під час гри разом зі стартовим екраном.
- **UI:** повноекранний overlay **`#expr-overlay`** — reuse класів `.songs-panel`/`.songs-head`/
  `.songs-body` (та сама рамка/хедер/скрол, що в піснях). ⚠️ Має парне `#expr-overlay.hidden{display:none}`
  (CSS-пастка: ID б'є `.hidden`).
- **Два види (`exprView`):**
  - **`list`** — **ДВА ряди вкладок** (сесія 47):
    - ряд 1 `.expr-tabs` — категорії (Усі / 💬 Ідіоми та вирази / 🎵 Сленг і розмовне) +
      кнопка «▶ Тренувати картки (N)» справа;
    - ряд 2 `.expr-tabs2` — перемикач напрямку `.expr-dir` (**US→UA / UA→US**) +
      **вкладки статусів `.expr-stab` із лічильниками** (● На черзі / ↗ Вчу / ✓ Вивчено).
      Клік по статусу фільтрує, **повторний клік скидає** (`exprStatusFilter=""`), плюс
      кнопка «✕ Скинути», коли фільтр активний.
    - Далі сітка карток `.expr-card`; клік по статус-пілу = **цикл**
      `queue→learning→learned→queue` (`EXPR_STATUS_CYCLE`). ⚠️ **Цикл зі списку XP НЕ дає** —
      його можна крутити по колу, це був би нескінченний фарм.
  - **`card`** — флеш-картка. **US→UA:** спереду EN + 🔊, розкриття → UA + `note`.
    **UA→US:** спереду УКРАЇНСЬКИЙ бік, розкриття → EN + 🔊 + `note`.
    ⚠️ **У UA→US 🔊 (і Tab) ДО розкриття недоступні** — озвучка видала б відповідь.
    Колода перемішується Fisher–Yates. Кінець колоди → «🎉 Готово».
- **Клавіші (capture-listener, глушить Enter/Пробіл щоб гра НЕ стартувала позаду):**
  до розкриття Enter/Пробіл — показати; після розкриття **Enter — «Знаю»**, **Пробіл — «Ще вчу»**;
  **Tab — озвучити** англ. вираз; Esc — вийти. Розкриття з клавіатури йде через `exprReveal()`
  (не через `exprRevealed = true` напряму) — інакше XP не нараховувався б.

### ⚠️ ДВА НАПРЯМКИ = ДВА НЕЗАЛЕЖНІ СХОВИЩА СТАТУСІВ (сесія 47, за запитом)

Слова користувача: «можу знати їх у перекладі з англ., але якщо це буде укр. вислів, то
англійською можливо вже і не зможу сказати». Тому:
- `oxford_expr_status_v1` — статуси **US→UA** (історичний ключ, дані користувача збереглись);
- `oxford_expr_status_ua_v1` — статуси **UA→US** (новий, у `BACKUP_KEYS`).
- Перемикач: `exprDir` (`en2ua` | `ua2en`), ключ обирає `exprStatusKeyFor(dir)`; усі
  `loadExprStatus`/`getExprStatus`/`setExprStatus` приймають ОПЦІЙНИЙ `dir` (за замовчуванням — поточний).
- **НЕ зливати в один ключ** — «вивчено» в один бік нічого не каже про інший, у цьому вся суть.
- Дефолт "queue" не зберігається (`setExprStatus` видаляє ключ при "queue").

### ⚠️ «ВИВЧЕНО» НЕ ПОТРАПЛЯЄ В КОЛОДУ (сесія 47, за запитом)

«щоби вони мені більше не попадалися, оскільки і так їх вже знаю». `exprPracticeList()` =
`exprVisible()` (категорія + фільтр статусу) **мінус усе зі статусом `learned` для ПОТОЧНОГО
напрямку**. Тому вивчене в US→UA спокійно трапляється в UA→US — заради цього й розділені сховища.
Лічильник на кнопці «▶ Тренувати картки (N)» показує саме це число; коли N=0 — кнопка
`disabled` (`.expr-practice-btn.off`) з поясненням у `title`.

### ⚡ XP за картки (сесія 47, за запитом) — джерело з логом по днях

Раніше розділ був **свідомо без XP** (узгодження сесії 32). Тепер:

| Подія | XP |
|---|---|
| «Показати переклад» (кожна картка) | **+1** (`EXPR_CARD_XP`) |
| «✓ Знаю» | **+3** (`EXPR_KNOW_XP`) |

- **⚠️ +3 лише за ПЕРЕХІД у «вивчено»** (`prev !== "learned"`) і **лише з тренажера карток**.
- **🌡️ ПУНКТИ «ПЛАНИ»: кожні 12 ПРОЙДЕНИХ карток = 1 пункт** (сесія 47, за запитом
  «за кожні 12 пройдених виразів зараховуй 1 пункт»). Механіка 1-в-1 як у закритих
  «📒 Помилок»: `EXPR_POINT_EVERY`=12, `exprPointsByDate(log)` рахує КУМУЛЯТИВНО наскрізь
  через дні, залишок < 12 переноситься вперед (13 сьогодні = 1 пункт, наступні 11
  будь-коли = ще 1). **⚠️ Рахуються лише ПОКАЗИ (`r`), не «знаю» (`k`)** — «пройдена
  картка» = та, яку показали.
  ⚠️ `thermoRawForDate` отримав **10-й параметр `exprPts`** — оновлені ВСІ виклики
  (`getThermoCarryIn` — і сигнатура, і набір дат, і рекурсивний виклик; `getThermoPointsToday`,
  `thermoPointsThisMonth`, `avgThermoPerDay`, `renderThermo`, `bestMonthPoints`,
  `computeRecords`). `getBestDayPoints` — легасі, рахує інлайном і не викликається, не чіпав.
  ⚠️ Термометр перемальовується НЕ на кожну картку, а в `exprFlushXpUi()` (там же
  `checkThermoMilestones`) — пункт капає раз на 12 карток, частіше нема сенсу.
- **⚠️ ПОДІЇ ПИШУТЬСЯ ПО ДНЯХ:** `oxford_expr_xp_v1` = `{date:{r:показів, k:«знаю»}}`
  (`loadExprXpLog`/`saveExprXpLog`/`exprXpForDate`). **Без цього лога джерело зникло б при
  першій же наступній міграції** — `computeDayStatsXp` перераховує XP з нуля по джерелах.
- `computeDayStatsXp` отримав **13-й опційний параметр `exprXp`**; старі блоки міграцій
  (v3…v16) кличуть без нього → стара поведінка. Новий прапор **`oxford_xp_formula_v17`**
  (для наявної історії це нуль, але наступна міграція без цього джерела з'їла б зароблене).
- **⚠️ Константи й `loadExprXpLog` оголошені ПЕРЕД `computeDayStatsXp` і ПЕРЕД блоком
  міграцій** — інакше TDZ усередині `try{}` міграції, прапор мовчки не ставиться (та сама
  пастка, що з GPT/selfRev).
- **Продуктивність:** під час тренування нарахування чіпає ЛИШЕ `renderXpBar()`; важкі
  панелі (`renderLbTotals`/`renderProgressPanel`/`checkAchievements`) перемальовуються один
  раз при виході з колоди — `exprFlushXpUi()` з прапорцем `exprXpPending`. НЕ переносити
  їх назад у `awardExprXp` — це ререндер на кожну картку.

### 🧹 Чистка сленгу: 13 простих слів прибрано (сесія 47, за запитом)

Привід: користувач побачив у «Виразах» картку `careless` — «це не сленг, а просте слово».
**Правило відбору:** запис лишається, якщо для розуміння потрібне НЕбуквальне / розмовне /
регістрово-марковане значення; іде, якщо глос — це звичайне словникове значення звичайного слова.

Прибрано з `songs.js.slang`: `careless`, `reckless`, `complacent`, `genuine`, `commit`,
`stall`, `handout`, `wristband`, `intoxicated`, `defenseless`, `standout`, `beginner`, `outwork`.

- **⚠️ ВСІ 13 ЛИШИЛИСЬ У `glossary` своїх пісень** — hover-переклад у тексті пісні працює
  як працював, втрачено рівно нуль. Перевірено скриптом перед видаленням.
- **⚠️ `xp` пісень НЕ перераховувався** (формула score містить `slang×2`). Значення `xp` —
  джерело істини прямо в `songs.js`; перерахунок відібрав би в користувача вже зароблене.
  Якщо колись перераховуватимеш xp — пам'ятай, що ці пісні «переоцінені» на 2 бали score.
- **СВІДОМО ЛИШЕНІ** (сленгові значення звичайних слів — саме те, заради чого банк існує):
  `hot` (приваблива), `rock` (це вогонь), `tight` (крутий), `flex`, `filthy` (схвально),
  `medicine` (евфемізм трави), `hook` (приспів), `trap`, `chemistry` (хімія між людьми),
  `vet` (бувалий), `coup` (купе), `ditch` (кинути), `hungover`, `frisky`, `jacked`, `second-guess`.
- Скан робився по **однослівних** записах (93 з 608); багатослівні звороти не чіпав —
  двослівна колокація сама по собі варта вивчення.

### Решта

- **Дані НЕ змішуються з WORDS** — банк будується з `EXPRESSIONS` + пісенного `slang`
  (`buildExprBank()`). Сленг лишається в `songs.js` (кожна пісня), вирази — в `EXPRESSIONS`.
- **Функції:** `buildExprBank`, `loadExprStatus`/`saveExprStatus`/`getExprStatus`/`setExprStatus`
  (усі з опційним `dir`), `exprStatusKeyFor`, `openExpr`/`closeExpr`, `renderExprHead`/
  `renderExprList`/`renderExprCard`, `startExprPractice`, `exprReveal`, `exprRate`,
  `exprByCat`/`exprVisible`/`exprPracticeList`, `awardExprXp`/`exprFlushXpUi`.
  Конст `EXPR_STATUS_KEY`, `EXPR_STATUS_KEY_UA`, `EXPR_XP_KEY`, `EXPR_CARD_XP`, `EXPR_KNOW_XP`,
  `EXPR_BANK`, `EXPR_STATUS_META`, `EXPR_STATUS_CYCLE`, `EXPR_FILTERS`, `EXPR_STATUS_TABS`, `EXPR_DIRS`.
  Стан: `exprDir`/`exprFilter`/`exprStatusFilter`/`exprView`/`exprDeck`/`exprPos`/`exprRevealed`/`exprXpPending`.
  ⚠️ `exprFiltered()` перейменовано на `exprByCat()` — якщо десь спливе старе ім'я, це залишок.
- **Перевірено:** `test-expr.js` — 39 перевірок (браузерний стенд, сесія 47) + історичний
  прогін сесії 32.
- **Ідеї на потім:** quiz-режим (вибір з 4), індикатор вивчено/всього на кнопці,
  пункти «Плани» за картки.

---

## Picture Mode (пілот ВІДКОЧЕНО — сесія 4)

Режим «фото → вводиш слова, що бачиш» був реалізований і ВІДКОЧЕНИЙ (revert 4288928+369de89):
end screen показувався порожнім, дебаг наосліп у cloud не дав результату (JS чистий → runtime/CSS).
Повертатись ЛИШЕ з консоллю браузера (F12); підозри: зображення Wikimedia 403/CORS → виняток,
або overlay-CSS ховав контент. Референс даних/UI — у git-історії (прибрано в сесії 29).

---

## 🎨 РЕДИЗАЙН v2 «Космос» (сесія 32) — ✅ ЗМЕРЖЕНО В MAIN, це прод

Користувач подивився прев'ю гілки `redesign` (htmlpreview) і сказав «сміливо запускаємо»
→ змержено в `main`. Гілка `redesign` лишилась в origin (історична, можна видалити).
Старий «небесний» дизайн — у git-історії (до коміту e73a3da).

Що входить у редизайн:
1. **Рівні = космічна шкала** (реф: інфографіка «масштаби всесвіту»): 20 назв
   Струни→Кварк→Атом→…→Всесвіт→Мультивсесвіт + `LEVEL_ICONS`. Пороги XP НЕ змінені.
   Оверлей **«🌌 Сходи рівнів»** (`#levels-overlay`, клік по `#xp-section`): всі 20 рівнів
   згори-вниз, кольори ліг (`--lg`/`--lg2` з ACH_LEAGUES), поточний підсвічений + автоскрол.
2. **Загальна статистика**: рядки-плашки з іконками-медальйонами (`row(icon,label,val,color)`,
   `.lb-stat-ic`), заголовок «✦ Загальна статистика ✦» золотом.
3. **Оверлей «🏆 Рекорди»** (`#records-overlay`, кнопка `#lb-records-btn` внизу статистики,
   делегований клік бо innerHTML перемальовується): `computeRecords()` — 11 максимумів за день
   (Знаю/Вивчаю з mastery_history-дельт, XP з xp_day, пункти з thermoRawForDate по всіх датах,
   правильні/відповіді/ігри/повторення з days, Duolingo/пісні/GPT) + дата рекорду. Стиль
   інфографіки: чергування вліво/вправо (`.rec-row.alt`), медальйони, акцент `--rc`.
4. **Тема «Космос»**: CSS-блок `/* РЕДИЗАЙН v2 «Космос» */` ОСТАННІМ у `<style>` — перекриває
   лише кольори/фони (зоряне небо+туманності, золоті рамки карток, золоті active-кнопки).
   Геометрію/display НЕ чіпає → `.hidden`-пастки цілі. `color-mix()` у CSS рівнів/рекордів
   (Chrome 111+, ок для користувача).
5. **«Прогрес по днях» — комірки (2-га ітерація, за фідбеком після прев'ю):** кожна
   активність/дельта — вертикальна комірка `_pgCell(ic,val,cls)` (значок зверху, число
   знизу, `.pgc`/`.pgc-ic`/`.pgc-v`), розділені напівпрозорими рисками (`.pgc + .pgc`
   border-left); блок дельт відділено золотистою рискою (`.prog-delta` border-left).
   Ігри — значком 🎮 (замість «N ігор»). Структура рядка: `.prog-date` (фікс.) +
   `.prog-cells` (flex, wrap) + `.prog-delta`. **Рік у заголовку секції** «Місяці · 2026»,
   місяці підписані лише назвою; **завершені роки → один рядок «YYYY рік»** (секція «Роки»).
   ⚠️ Це СВІДОМО повертає річне групування (прибране в сесії 27) — у новій формі, за явним
   запитом користувача в сесії 32.
6. **Пост-мерж фікси (за фідбеком з проду):** `#bottom-left-btns` — контейнер БЕЗ
   рамки/фону (стилізовані лише самі кнопки: темна підкладка + золота обводка кожної;
   НЕ повертати рамку контейнера); плашки блоку «Плани · поточне» — повні бічні відступи
   (⚠️ старе правило `.lb-variable .lb-stat-row { padding:1px 0 }` лишилось вище в CSS,
   його перекриває редизайн-блок — не видаляти перекриття, інакше цифри знову
   притиснуться до рамки).
- **Вітання «Новий рекорд!» (сесія 32, за запитом; уточнено 2-ю ітерацією):**
  `checkRecordMilestones()` (виклик наприкінці `renderLbTotals` — її звуть усі точки зміни
  даних). Порівнює `computeRecords()` зі збереженою базою `oxford_records_best_v1` `{id:v}`
  (плоскі числа; старий формат `{v,ad}` мігрується тихо); побиття → `enqueueMilestone`
  type:"rank" (фанфара) з «label: N (було M)». Перший запуск — ТИХИЙ сід бази (без спаму
  історією). ⚠️ Вітає за КОЖНЕ побиття (денний дедуп ПРИБРАНО за запитом — рекорди б'ються
  нечасто). ⚠️ База синхронізується В ОБИДВА боки: «−» (Duo/GPT) знижує її разом із даними →
  повторний «+» чесно привітає знову. ⚠️ **Рекорд «Досвіду (XP) за день» рахується БЕЗ XP
  пісень** (пісня «вибухає» +400..1600 у день відмітки «вивчено», хоч вчилась тижнями —
  рекорд ставав недосяжним; віднімання через `oxford_songs_xp_v1`+`SONGS[].xp` ЛИШЕ в
  computeRecords — «Прогрес по днях» і загальний XP пісні враховують як раніше). Ключ
  СВІДОМО не в BACKUP_KEYS (виводиться з даних; на новому пристрої пересідиться тихо).
- Esc закриває обидва нові оверлеї (capture). Перевірено Playwright: 0 помилок.

---

## 🌗 Фон за порою дня + сезони (сесія 33)

Фон сторінки змінюється за фазою доби; межі фаз залежать від СЕЗОНУ (укр. широти).
- **4 фази:** morning (світанковий персик-лаванда) / day (блакитне небо з хмарами — реінкарнація
  старого дизайну) / evening (фіолетово-помаранчевий захід) / night (космос редизайну = ДЕФОЛТ).
  CSS: `html[data-tod="..."] body { background: ... }` у редизайн-блоці (ніч = базовий body).
- **Сезонні межі (ЗАТВЕРДЖЕНІ користувачем через артефакт-редактор — НЕ міняти без нього):**
  зима [7,9,15,17] · весна [6,8,18,21] · літо [5,7,20,23] · осінь [6,8,18,21]
  (масиви = початки фаз [ранок, день, вечір, ніч]; сезони: 12-2/3-5/6-8/9-11).
- **JS у РАННЬОМУ інлайн-скрипті** (перед songs.js, разом із fallback .ready): `todFor(m,h)` +
  `todNow()` + IIFE ставить `document.documentElement.dataset.tod` до першого кадру (без
  нічного «блимання» вранці) + переоцінка кожні 10 хв (довго відкрита вкладка).
- **`?tod=morning|day|evening|night`** — форс фази для перевірки (вимикає автооновлення).
- Картки/панелі лишаються темними у всіх фазах (перевірено скріншотами всіх 4 фаз).
- **⚠️ Ліві кнопки — СОЛІДНИЙ темний фон, не скло (фікс сесії 33):** на світлих фазах
  напівпрозорі кнопки вимивались у невидимість. У редизайн-блоці був ДУБЛЬ-баг (друге
  правило з 0.07-золотом перебивало темне) — видалено; duo/gpt — тінт поверх солідної
  підкладки (пізніший !important виграє порядком); `#progress-btn.active` — темний+золото.
- **Центрування картки (фікс сесії 33):** прогрес (440) ширший за лідерборд (420) → картка
  з'їжджала на 10px. Клас `.layout.balance-center { margin-right:20px }` (media ≥1500px),
  тогл у `updateLayoutAlign()` коли видимі ОБИДВІ бічні панелі.
- Бета `_theme_preview.html` видалена після переносу в прод (не була в git).

---

## Що залишилось / ідеї

- **Picture mode** — пілот відкочено (див. секцію вище); повертатись лише з консоллю браузера
- ~~**CEFR фільтр у грі**~~ — ✅ **ЗРОБЛЕНО в сесії 46** у вигляді 📖-ряду чипсів
  (рівень без уже засвоєних слів); див. розділ «📖 Два ряди чипсів…»
- **XP множники** для складніших режимів (×2, ×3) — підготовлено в архітектурі
- **DEFS_UA** — українські переклади визначень
- **Полісемія** — точкові правки по ходу
- ~~Прохід English for Everyone~~ — ✅ **ЗАВЕРШЕНО в сесії 41** (усі 402 сторінки; останні
  розділи 24-180 закрито через xlsx користувача). Трекер прибрано.
- **`cafe` = дві картки (кафе / кав'ярня)** — помічено в сесії 45: це не омограф, а
  синоніми одного сенсу, напрошується один запис + `uaAlt`. Не робив без узгодження.
- **Тренажер виразів/колокацій** — ✅ **ЗРОБЛЕНО в сесії 32** (розділ «Вирази і сленг», флеш-картки;
  `EXPRESSIONS` + пісенний slang). Далі: quiz-режим, XP, індикатор вивчено/всього на кнопці. Не змішувати з WORDS.

---

## ВЕЛИКИЙ ВІДБІР зі стор. 58+ (сесія 41) — конвеєр і правила

Користувач приніс **xlsx** (`Додати` 3029 / `На перевірку` 930 / `Не додавати` 5526),
що покриває ВЕСЬ залишок EFE (розділи 24-180, стор. 58+). Файл лежить лише в аплоадах
сесії — у репо його НЕМА; кандидати в `scratchpad/add.json` теж сесійні.

**⚠️ ГОЛОВНЕ ПРАВИЛО: глоси з файлу НЕ БРАТИ.** Там сирий дамп старого словника з
помилками й переносними значеннями поперед основного: `good evening`=«доброго ранку»,
`hi`=«Гай|Хай», `dawn`=«Дон», `colon`=«товста кишка», `old-fashioned`=«коктейль з віскі»,
`movies`=«прожекторист», `one-way ticket`=«небезпечне завдання», `bidet`=«конячка».
З файлу беремо ЛИШЕ **англійські слова + тематичну розбивку** (вони якісні),
переклади/приклади/IPA пишемо самі.

**Конвеєр на партію (~60-100 слів):**
1. `theme.js <тема>` — відкидає те, що вже є в WORDS/EXPRESSIONS (по `en`+`enAlt`+bare-формі)
   і показує колізії основних глосів.
2. Куруємо глоси руками; вузьке/зіпсоване — пропускаємо (не «лікуємо» наосліп).
3. **Передвставний скан** (`scan.js`): не-ASCII у ключі та задовгі фрази. ⚠️ Уведений після
   того, як у прод потрапило `à la carte menu` (французька фраза) і `soufflé` (діакритика
   ЛАМАЄ токенізатор — те саме правило, що `fiancé`→`fiance`).
3b. **⚠️ СКАН ГЛОСІВ НА ЧУЖІ СИМВОЛИ** — регексом `[^Ѐ-ӿ\s'\-()a-zA-Z]` по українських
   перекладах партії. Уведений у сесії 41 після того, як у глос `watercress` затесався
   КИТАЙСЬКИЙ ІЄРОГЛІФ («水крес»): жодна інша перевірка такого не бачить — ні покриття,
   ні синтаксис, ні стенд завантаження.
4. `gcheck.js` — колізії глосів зі словником і всередині партії.
5. Вставка (WORDS+TRANS+EXAMPLES+EXAMPLES_UA) → `node check-coverage.js` до ✅ OK
   (каскад: приклади тягнуть ще ~10% базових слів) → `loadtest.js` → коміт.

**СТАН (кінець сесії 41): усі 10 ВЕЛИКИХ тем закрито** — Час 88 · Дім і побут 246 ·
Їжа 194 · Культура 265 · Робота 200 · Спорт 178 · Навчання 183 · Місто 118 ·
Транспорт 136 · Природа 120 = **~1730 слів**. WORDS 3543 → 5270.

**✅ ДРІБНІ ТЕМИ ТЕЖ ЗАКРИТО (+106):** 24 дрібні теми (гроші й право, робота й бізнес,
подорожі, здоров'я, технології, довкілля, інструменти, фразові) опрацьовано окремим
пакетом — mortgage, boarding pass, artificial intelligence, climate change, faucet,
to check in/out тощо. **Аркуш «Додати» покрито на 87% (2642 з 3029).**

**✅ АРКУШ «НА ПЕРЕВІРКУ» ТЕЖ ЗАКРИТО (+569, 8 пакетів A-B/C/D-F/G-J/K-M/N-P/Q-S/T-Z):**
⚠️ У ТОМУ аркуші xlsx **перекладів не було взагалі** (лише англійські слова + частина мови),
тож усі глоси написані з нуля. З 930 рядків 140 уже були в словнику, 569 додано, ~220
відкинуто (вузькі спортивні терміни, рідкісні продукти, абревіатури, власні назви).
⚠️ Перевірка глосів ловила ДУБЛІ, яких не бачив фільтр за ключем (`fillet`, `firefighter`,
`cave`, `grape`, `puzzle`, `spice`) — вони додані як `enAlt` або пропущені.

**✅ ДОБІР ПРОГАЛИН (+93, фінальний пакет):** пройдено список раніше відкинутих і повернуто
те, що випало НЕ за суттю, а через колізії глосів — овочі/риба/спеції (lettuce, plum, pea,
lobster, octopus, mussel, mackerel, quail, pheasant, nutmeg, paprika, parsley, rosemary),
квіти (azalea, begonia, carnation, chrysanthemum, daffodil, daisy, dandelion, hydrangea),
інструменти й деталі авто (sander, scraper, solder, solvent, valve, visor, washer, wiper,
windscreen, suspension, throttle, transmission, tread), yacht, tram, trunk, fortnight.

**⚠️ ЩО ЛИШИЛОСЬ (свідомо, НЕ борг):**
1. ~290 рядків з «Додати» + ~220 з «На перевірку» — вузькі терміни окремих видів спорту,
   рідкісні продукти, абревіатури, власні назви, діакритика, рядки з хибним глосом у файлі.
   Переглянуто вручну — додавати нема чого.
2. Аркуш «Не додавати» (5526) — свідомо поза межами.
3. ~~**Технічний борг:** пари-майже-дублі, розведені уточненнями в глосі~~ —
   ✅ **ЗАКРИТО В СЕСІЇ 52** (17 пар зведено через `enAlt`, див. розділ нижче).

**⚠️ `verify.sh` (у репо, сесія 41) — ЄДИНИЙ правильний передкомітний прогін:**
чекер БЕЗ пайпа + синтаксис + стенд завантаження. Причина появи: ланцюжок
`node check-coverage.js | tail -3 && git commit` **МАСКУЄ код виходу** (pipe повертає
успіх tail-а), через що двічі пройшов коміт із непокритим словом. НЕ комітити словник
інакше як через `./verify.sh`.

**Допоміжні стенди (сесійні, у scratchpad — за потреби відтворити):**
`loadtest.js` — прогін ГОЛОВНОГО inline-скрипта під мок-DOM; ловить TDZ/ReferenceError,
яких `new Function` (перевірка синтаксису) НЕ бачить. ⚠️ Створений після того, як enAlt-пас
поклав прод. `irr.js` — які неправильні форми не резолвляться при наявній базі.

---

## OXFORD-ДОБІР (сесія 42) — покриття офіційного Oxford 3000/5000

Після завершення EFE взялись за **офіційний список Oxford** з CEFR-мітками.
Усі дані й інструменти — **В РЕПО** (на відміну від сесійних стендів сесії 41):

- **`oxford-cefr.json`** — 4954 слова з мітками, витягнуті з Oxford 3000/5000
  (Oxford Learner's Dictionaries). ⚠️ **У Oxford НЕМАЄ рівня C2** — його шкала
  закінчується на C1. Для C2 існує лише сторонній список (Octanove/CEFR-J,
  876 слів) — **користувач вирішив C2 поки НЕ чіпати** (сесія 42).
- **`oxford-gap.js`** — рахує покриття й видає кандидатів: без аргументів
  таблиця, `node oxford-gap.js B2` — список нової лексики рівня, `--all` —
  з розбивкою по категоріях. Використовує СПРАВЖНІ `stemEn`/`HW_SKIP` із гри.
- **`gloss-check.js`** — перевірка кандидатів перед вставкою: чи слово вже є,
  чи вільний укр. глос (`⛔осн:X` = глос уже основний у X, `~alt:X` = лежить у uaAlt).
- **`add-batch.js`** — вставка партії в усі 4 блоки одним проходом + enAlt-доповнення
  до наявних карток. Формат партії: `[{en,ua:[],uaAlt:[],enAlt:[],ipa,ex,exUa}]`.
- **`loadtest.js`** — стенд завантаження (був сесійним, тепер у репо, див. нижче).
- **`c2-candidates.json`** (875 слів C2) — **ВІДПРАЦЬОВАНО в сесії 44** (736 з 875 стали
  картками), лишились тільки механічні `-ly` й покручі — див. розділ «C2-ДОБІР».
- **`cefr-add.js`** (сесія 44) — дописує ключі партії в блок `CEFR_DATA`:
  `node cefr-add.js batch.json C2`. ⚠️ БЕЗ цього кроку нові слова падають у кошик
  «без рівня» в грі 🎓 «Рівні», бо `CEFR_DATA` згенеровано разово в сесії 42.
  (`oxford-derived.json` ВИДАЛЕНО в сесії 42 — усі 269 похідних додані.)

**Стан покриття (кінець сесії 42):**

| рівень | в Oxford | у нас | % | лишилось нової лексики |
|---|---|---|---|---|
| A1 | 898 | 797 | 88.8% | 0 |
| A2 | 792 | 728 | 91.9% | 0 |
| B1 | 690 | 628 | 91.0% | 0 |
| B2 | 1297 | 1183 | 91.2% | 1¹ |
| C1 | 1277 | 1234 | **96.6%** | 0 |
| разом | 4954 | **4570** | **92.2%** | 1¹ |

¹ єдиний залишок — `rid`: поза зворотом `get rid of` не вживається, тож карткою
доданий сам зворот, а `rid` має hover через `m.set`.
**⚠️ Службові слова ТЕЖ додані картками (за запитом користувача, сесія 42):**
after, around, away, before, behind, between, during, without, against, along,
among, anywhere, except, hers, less, such, due, plus, whatever, whenever, lot +
абревіатури cd/dvd/id + `o'clock` (апостроф у ключі чекер пропускає — перевірено).
Логіка запиту: «хоч такі слова знайомі кожному, але їх теж треба врахувати, щоб я
знав свій ЗАГАЛЬНИЙ словниковий запас». НЕ прибирати їх як «службові».

Було на старті сесії: 2804 / 56.6%. **✅ OXFORD 3000/5000 ЗАКРИТО ПОВНІСТЮ** (сесія 42):
уся нова лексика всіх п'яти рівнів додана, 4301 з 4954 слів у грі. Решта 653 —
службові, що лишились у `HW_SKIP` (125), вигуки (7), словоформи, які резолвить
`stemEn` (242), британські написання в `enAlt` (9) і похідні від наявних баз (269).

**ЩО ДАЛІ:**
1. **`c2-candidates.json` — ЗАКРИТО в сесії 44** (деталі — розділ «C2-ДОБІР» нижче).
2. **`oxford-derived.json` — ЗАЛИШИЛОСЬ 97 похідних, УСІ C1** (було 269; дві партії
   сесії 42 додали +172, рівні A1/A2/B1/B2 по похідних закриті ПОВНІСТЮ), формат `[{L,w}]`,
   де `w` = `слово←база`. ⚠️ Файл ПЕРЕГЕНЕРОВУЄТЬСЯ з `oxford-gap.js <рівень> --all`
   (секція `deriv`) — після кожної партії оновлюй його, щоб не пропонувати вже додане. ⚠️ **Це НЕ «сміття» і НЕ «складніші слова»** (я спершу так сказав —
   помилково): `-ly`-прислівників там лише **9**, решта 260 — повноцінні лексеми
   (`assessment`, `commitment`, `awareness`, `contribution`, `acceptance`, `adaptation`).
   Рівні: A2 9 · B1 27 · B2 84 · C1 149. Частина «баз» у детекторі хибна
   (`factor←fact`, `image←age`, `import←port`, `relate←late`) — ці слова взагалі
   незалежні. Потрібен поштучний перегляд, але 200+ карток звідти реально варті додавання.

**⚠️ ЩО З ДІРОК НЕ ДОДАВАТИ** (`oxford-gap.js` розкладає автоматично):
службові (артиклі/займенники/прийменники — вони в `HW_SKIP`), вигуки,
**словоформи** (dancing, written — гра резолвить через `stemEn`),
**британські написання** (colour, lorry, rubbish → в `enAlt` до наявної картки,
патерн сесій 38/40), похідні від наявних баз — дивитись поштучно.

**⚠️ ПРО СПІЛЬНІ ГЛОСИ (ключове правило добору):** `gloss-check.js` показує `⛔осн`,
коли укр. глос уже основний в іншому слові. Це **НЕ завжди дефект**: для справжніх
синонімів спільний глос — норма (крос-логіка ua-en приймає обидва), тому
`particularly`/`especially`, `possibly`/`probably`, `theme`/`topic`, `bush`/`shrub`,
`compulsory`/`mandatory` лишені як є. Розводити треба лише НЕсинонімів
(«позичений глос» — методика в розділі про аудит сесії 38): звідси
`anxiety`→«тривожність» (тривога=alarm), `to blame`→«винуватити» (звинувачувати=accuse),
`basis`→«підстава» (основа=foundation), `identity`→«ідентичність» (особистість=personality),
`guy`→«хлопака» (хлопець=boy), `reference`→«довідка» (посилання=link).

**⚠️ `loadtest.js` ПЕРЕНЕСЕНО В РЕПО (сесія 42):** `verify.sh` викликав його за шляхом
scratchpad сесії 41, якого в новому контейнері вже немає, — тобто третій крок перевірки
падав із `MODULE_NOT_FOUND` у КОЖНІЙ новій сесії. Стенд виконує головний inline-скрипт
під мок-DOM (ловить TDZ/ReferenceError, яких `new Function` не бачить). Сесійні стенди
для словника більше не робити — класти в репо.

---

## 💡 HINTS — уточнення відтінку значення (сесія 44, за запитом користувача)

**Привід:** у грі рядок правильної відповіді показував «складний = hard / difficult /
complex / challenging / complicated / intricate / tricky» — сім слів без жодного
пояснення, чим вони різняться. Користувач: «нехай кожний український варіант у дужках
писав би якесь пояснення: difficult — складний (розумово), heavy — складний у плані
важкий».

**Формат (узгоджено з користувачем):** підказка показується СКРІЗЬ, де видно глос —
і в UA→US, і в US→UA — але **тільки в рядку правильної відповіді, ПІСЛЯ відповіді**.
Промпт лишається чистим і нічого не підказує наперед.

```
UA→US:  складний = hard (важкий, потребує зусиль) / difficult (розумово важкий) /
                   complex (із багатьох частин) / …
US→UA:  difficult = складний (розумово важкий) / важкий / тяжкий
```

**Механіка (де що лежить):**
- `const HINTS = { ... }` — окремий блок даних одразу після `TRANS`. Ключ = wordKey
  (перша форма `en`, з «to » для дієслів) **АБО складений «en:ua» — сесія 45**,
  значення = 1-4 слова українською.
- `withHint(en, ua)` + `hintFor(en, ua)` + `getCorrectAnswer()` — дописують « (підказка)»
  до варіанта. У UA→US до кожного англійського слова (ua береться з ЙОГО картки),
  у US→UA — до ОСНОВНОГО глоса показаної картки.

**⚠️ КЛЮЧ-КАРТКА «en:ua» (сесія 45, фікс) — читати перед поповненням HINTS.**
Спершу ключем було голе `en`, і для 23 слів із ДВОМА картками (tip=чайові/порада,
hot=гарячий/гострий, cool=прохолодний/крутий, fine=чудовий/штраф, run, light, bank,
kind, degree, content, square, nail, present) підказка одного сенсу приклеювалась і до
другого: «чайові (практична підказка)», «крутий (приємно прохолодний)» — 6 із 13 таких
підказок відверто збивали. Тепер:
- ключ картки `"tip:порада"` має ПРІОРИТЕТ (той самий патерн, що в `EXAMPLES` —
  `"nail:цвях"`), `ua` = ПЕРШИЙ глос картки;
- **голий ключ для слова-пари СВІДОМО ігнорується** (`hintFor`: `sameKeyWords(en).length > 1`
  → порожньо). Він завжди про один із двох сенсів, тобто на другому бреше;
- `hints-add.js` це знає: голий ключ пари ВІДХИЛЯЄ з підказкою, які складені ключі
  можливі; `test-hints.js` рахує такий ключ сиротою.
- **⚠️ ПРАВИЛО, КОЛИ ПІСАТИ ПІДКАЗКУ:** лише якщо за той самий укр. глос конкурує інше
  англ. слово (`порада` = tip vs advice; `гострий` = hot vs spicy/sharp/acute). Якщо
  конкурентів нема — виходить тавтологія «цвях (цвях)». Саме тому `nail` і `present`
  лишились БЕЗ підказок узагалі, а 10 інших пар отримали її лише на одну картку
  (виняток `kind` — обидва глоси мають конкурента: `kind:тип` vs type,
  `kind:лагідний` vs gentle).
- `wrapEnWord()` відрізає дужку від слова регуляркою: інакше пошук у `EN_TO_UA`/`TRANS`
  ішов би по рядку «difficult (розумово важкий)» і слово втратило б hover та озвучку.
  Підказка рендериться окремим спаном `.ans-hint` (курсив, opacity .62).
- ⚠️ Гілка en-ua в `showCorrection` перейшла з `textContent` на `innerHTML` з ручним
  екрануванням — інакше спан підказки показувався б як текст. **Не повертати textContent.**
- ⚠️ У жодній картці немає дужок у полі `en`, тому розбір «слово (підказка)» однозначний.
  22 картки мають дужки в `ua` — вони НЕ конфліктують (рендеряться як звичайний текст).
- **⚠️⚠️ У ТЕКСТІ САМОЇ ПІДКАЗКИ ДУЖОК БУТИ НЕ МОЖЕ (сесія 50, привід «jealousy»).**
  Гра рендерить підказку САМА ЯК дужку і розбирає її регексом
  `^(.+?)\s*\(([^)]+)\)$` — у ДВОХ місцях: `wrapEnWord` (UA→US) і гілка en-ua
  `showCorrection` (US→UA). Дужка всередині підказки дає вкладеність
  «jealousy (страх утратити своє (ревнощі))», `[^)]+` такого не матчить, і **весь
  рядок падає в один `.hw`-спан**: підказка не тьмяніє, слово втрачає hover, IPA й
  озвучку, а клік читає англійським голосом український текст. Саме це користувач
  і побачив («переклад йде суцільним»).
  - **⚠️ Розширити регекс НЕ МОЖНА, і це головне:** він мусить чіплятися за
    **ОСТАННЮ** дужкову групу (щоб працював випадок «глос із дисплейною дужкою +
    підказка»), а жадібне `(.+)` ловило б першу. «Остання група» і вкладеність
    регексом одночасно не виражаються — тому інваріант тримаємо НА ДАНИХ.
  - **Замість дужок — тире:** «спечений у духовці — тісто, запіканка»,
    «розм. — бадьорий і налаштований позитивно», «поводитися погано — про дітей
    і тварин». Такий стиль у HINTS уже був (`chips` → «у Британії — картопля фрі»).
  - Виправлено 6 підказок: `baked`, `upbeat`, `jealousy`, `to zone out`,
    `to misbehave`, `to act up`. У `jealousy` дужка ще й дублювала сам глос.
  - **Вхід стереже `hints-add.js`** (падає з підказкою «заміни на тире»), дані —
    4 перевірки в `test-hints.js` (інваріант по всьому блоку + рендер `jealousy`
    у двох спанах). Перевірено, що набір ловить регресію: на зламаних даних падають
    усі 4.
- ⚠️ **Дрібний баг `hints-add.js`, знайдений тоді ж:** на «порожньому» прогоні (усі
  ключі — дублі) він усе одно дописував кому й порожній рядок у кінець блоку HINTS.
  Синтаксис від цього не падає, тож слід накопичувався непомітно. Тепер при
  `lines.length === 0` скрипт виходить, не переписуючи файл.

**⚠️ КЛЮЧ-ВАРІАНТ З `enAlt` (сесія 51, привід «пончик = doughnut / donut»).** Користувач:
«якщо є 2 варіанти відповідей, то варто уточняти, який з них американський, а який
британський». Виявилось, що механізм це ВЖЕ вміє: `getCorrectAnswer` у UA→US жене
enAlt-синоніми через той самий `withHint(e)`, тож голий ключ `HINTS["donut"]` — робочий,
просто ніхто ним не користувався. Заважали лише вартові, які вважали такий ключ сиротою:
навчено обидва (`hints-add.js` приймає, `test-hints.js` не лається). Тепер рядок відповіді:
`пончик = doughnut (повне написання — і брит., і амер.) / donut (спрощене амер. написання)`.
⚠️ Тавтологія перевіряється проти глоса КАРТКИ-ВЛАСНИКА (`altOwner`), а не порожнечі.
**⚠️ АВТОПОЗНАЧКА «амер. / брит.» — ОБЧИСЛЮЄТЬСЯ, А НЕ ЛЕЖИТЬ У ДАНИХ (сесія 51).**
Користувач: «дошках писати US або GB… додай, якщо цього ще не робив». Руками це було б
~140 однакових записів у HINTS. Замість цього `isBritSpelling(amer, brit)` застосовує
регулярні перетворення (`-or→-our`, `-ize→-ise`, `-er→-re`, `-se→-ce`, подвоєння `l`) плюс
короткий словник нерегулярних (`jewelry`/`jewellery`, `aluminum`/`aluminium`, `tire`/`tyre`…),
а `spellingRegion(word)` вирішує, що показати біля конкретного слова. Результат:
`прикраси = jewelry (амер.) / jewellery (брит.)`. **Позначку дістають 144 пари, у даних — нуль записів.**
- **⚠️ ПРАВИЛА ПРАЦЮЮТЬ І ВСЕРЕДИНІ СЛОВА (сесія 53).** `\b` після «or» вимагає кінця
  слова, тож цілий клас — `favorite`/`favourite`, `neighborhood`, `colorful`, `armory`,
  `savory`, `colored` — мовчки лишався без мітки. Додано глобальні `or→our`, `er→re`,
  `iz→is`, `yz→ys`, `sulf→sulph` і окремо КІНЕЦЬ ФРАЗИ (`er$`, `or$`): у `pace per
  kilometer` глобальне `er\b` ламало ще й «per» → «pace pre kilometre», і пара не сходилась.
  Плюс список нерегулярних виріс на медичні `-ae-`/`-oe-` (diarrhea/diarrhoea,
  hemorrhage, pediatrics, fetus, encyclopedia…) і одиночні пари (disk/disc, yogurt/yoghurt,
  judgment, installment, artifact, jeweler, traveler, skeptical, inquire/enquire…).
- **⚠️ ЧОМУ ШИРОКІ ПРАВИЛА БЕЗПЕЧНІ:** перетворення спрацьовує, ЛИШЕ якщо результат
  ДОСЛІВНО дорівнює наявному `enAlt`. `doctor` ніколи не стане «doctour», бо такого
  `enAlt` не існує. Тому боятись four/tour/flour не треба — гарантію дає дані, не регекс.
- **⚠️ Перевернутий випадок теж покритий:** у `cheque` основним стоїть БРИТАНСЬКЕ написання
  (свідомий виняток), тож він отримує «(брит.)», а `check` — «(амер.)».
- **⚠️ ЧЕРГА ПРІОРИТЕТІВ:** явна підказка → дужка з глоса → автопозначка. Рендер уміє лише
  ОДНУ дужку на слово, тож змістовне пояснення важливіше за позначку норми (`doughnut`
  лишає своє «повне написання — і брит., і амер.»).
- **⚠️ ДРУГИЙ ШАР — БРИТАНСЬКІ ЛЕКСЕМИ (сесія 53, за запитом).** Написання виводить
  `theater→theatre`, але НЕ виводить `A&E` з `emergency room`, `bonnet` з `hood`,
  `chemist's` з `drugstore` — це РІЗНІ СЛОВА. Такі пари лежать списком: `LEX_PAIRS`
  (обидві сторони регіональні) і `LEX_BRIT_ONLY` (ліва живе в обох нормах, мітку дістає
  лише британська: `lawyer / solicitor (брит.)`, бо «lawyer (амер.)» було б неправдою).
  `spellingRegion` пробує написання і, якщо мовчить, падає в `lexemeRegion`.
  **⚠️ ДОБІР ДОРОБЛЕНО ТІЄЇ Ж СЕСІЇ (привід — `jump rope / skipping rope` без мітки).**
  Перший список збирався грепом за словником британізмів, тобто ловив лише те, що я
  згадав; другий захід — суцільний перегляд УСІХ 772 пар `enAlt`, не покритих написанням.
  Разом мітку тепер дістають **398 карток** (з них ~200 через лексемний шар).
  **Правило на майбутнє: список, зібраний «за пам'яттю», завжди неповний — перевіряй
  суцільним проходом по даних.**
  **⚠️ ЧОМУ ЦЕ ПАРИ, А НЕ ДВА СПИСКИ СЛІВ — ГОЛОВНЕ В ЦІЙ ФІЧІ:** мітка з'являється, лише
  коли на картці присутні ОБИДВА слова пари. Це єдиний захист від багатозначності:
  `hood` дістає «амер.» на картці «капот» (поруч `bonnet`) і мовчить на картці «капюшон»,
  `tap` — «брит.» поруч із `faucet` і мовчить на «постукувати», `torch` мовчить на
  «смолоскипі», `jelly` — лише поруч із `gelatin dessert`. Плоским списком слів усі ці
  картки позначались би хибно. Тест `test-hints.js` це й стереже (плюс перевірку
  «мертвих пар немає» — пара, чиїх обох слів немає на жодній картці, це помилка даних).
  ⚠️ Свідомо НЕ внесені: `wallet`/`purse`, `shrimp`/`prawn` (різниця не лише регіональна),
  `shop`/`store`, `underground` (картка — прикметник «підземний»), `whiskey`/`whisky`
  (ірландське проти шотландського), `headlight`/`headlamp` (headlamp уже в парі
  `head torch`), `fringe`/`bangs` (там взагалі підозра на баг даних: глос «бахрома»,
  а `bangs` — це чубчик), `layoff`/`redundancy` (на картці «надлишковість» redundancy
  стоїть у нейтральному значенні, мітка збивала б).
  ⚠️ `pants`/`trousers` ВНЕСЕНО у сесії 53 — мітка на картці «штани» правдива; пастка
  «у BrE pants = білизна» лишається темою для HINTS, а не приводом мовчати.
- **⚠️ ГОЛИЙ КЛЮЧ HINTS БЕРЕТЬСЯ ЛИШЕ ЯКЩО enAlt НЕ Є ВЛАСНОЮ КАРТКОЮ.** Інакше на варіант
  протікає підказка ЧУЖОГО слова: `cheque` показував «check (звірити швидко)» — а це
  пояснення дієслова «перевіряти». Той самий клас витоку, що між картками-парами в сесії 45.
- Не додавати в `BRIT_IRREGULAR`: `whiskey`/`whisky` (ірландське проти шотландського, не US/GB),
  `pantyhose`/`tights` (різні слова), `dialog`/`dialogue` (в амер. нормі живі обидва).
- Закріплено 8 перевірками в `test-hints.js` (39).

**Інструменти:** `node hints-add.js batch.json` — вставка партії (звіряє ключі з WORDS
і **падає**, якщо ключа-картки не існує; дублі пропускає, `--force` перезаписує).
`node test-hints.js` — 11 браузерних перевірок: обидва напрямки, чистий промпт,
слово без підказки, відсутність ключів-сиріт, збережений hover.

**Прогрес наповнення** (544 глоси мають 2+ англійських відповідники, 1156 карток):

| партія | групи | карток | стан |
|---|---|---|---|
| 1 | 4-8 відповідників (27 груп) | 120 | ✅ |
| 2 | по 3 відповідники (69 груп) | 197 | ✅ |
| 3-6 | по 2 відповідники (448 груп) | 822 | ✅ |
| 7 | 17 «хвостів» C2 у парах | 17 | ✅ |

**✅ ЗАКРИТО ПОВНІСТЮ: 1156 підказок, усі 544 групи покриті на 100%** (розмір блоку 37 КБ).
Перевірка покриття (запускати після кожного поповнення словника — нові картки можуть
утворити НОВУ групу зі спільним глосом):

```js
// групуємо WORDS за нормалізованим основним глосом і шукаємо групи без підказки
const g=new Map(); W.forEach(w=>arr(w.ua).forEach(u=>{const k=u.trim().toLowerCase();
  if(!g.has(k))g.set(k,[]); g.get(k).push(w);}));
[...g.entries()].filter(([k,v])=>v.length>1 && v.some(w=>!HINTS[arr(w.en)[0]]))
```

⚠️ **Правило на майбутнє:** якщо нова картка бере вже зайнятий основний глос — одразу
дописуй підказки ОБОМ словам групи, інакше в рядку відповіді знову з'явиться
неанотований список.

---

## C2-ДОБІР (сесія 44) — `c2-candidates.json` відпрацьовано

Користувач: «додай C2-слова з c2-candidates.json — вже можна, оскільки є розбивка
по рівнях». 11 партій, **+857 карток**, WORDS 7789 → **8646**.

| партія | діапазон | карток |
|---|---|---|
| 1 | a-b | 98 |
| 2 | c-d | 93 |
| 3 | d-g | 94 |
| 4 | h-l | 91 |
| 5 | m-o | 71 |
| 6 | p-q | 73 |
| 7 | r-s | 91 |
| 8 | t-z + відкладені раніше | 78 |
| 9 | хвости списку | 46 |
| 10 | `-ly` прислівники, ч. 1 | 60 |
| 11 | `-ly` прислівники, ч. 2 | 62 |

**Стан списку: ВИЧЕРПАНО — 858 з 875 позицій стали картками.**
- Спершу я пропускав **122 механічні `-ly`-прислівники** (hover їх резолвить через
  `stemEn`, окрема картка = дублікат прикметника), але користувач сказав: «давай додамо
  і ці -ly прислівники також» — та сама логіка, що зі службовими словами в сесії 42:
  все, що знаєш, має лічитись у загальному запасі. Додані партіями 10-11.
- **Лишились тільки ~17 покручів списку Octanove:** `porten`, `flatout`, `wordage`, `promotable`,
  `sandwiched`, `standing`, `indiscrete`, `metalled`, `blubbery`, `squirmy`, `wrack`,
  `cordon` (вже є як `to cordon off`) тощо.

**Конвеєр партії (той самий щоразу):**
1. `node scratchpad/c2-left.js` — що зі списку ще не покрито (звіряє WORDS + enAlt + m.set,
   розбиває варіанти через `/`);
2. скласти партію в JSON → **скан на чужі мови/символи в глосах** (пастка сесій 41-42:
   іспанське «método», китайський ієрогліф);
3. `node gloss-check.js batch.json` — розвести лише «позичені» глоси НЕсинонімів;
4. `node add-batch.js batch.json "C2-добір, партія N: …"`;
5. **`node cefr-add.js batch.json C2`** ← новий крок, без нього слово без рівня;
6. `./verify.sh` до чистого OK; після змін у грі — `node test-cefr.js` (30 перевірок).

**⚠️ ЩО ЛОВИВ ЧЕКЕР У ЦІЙ СЕСІЇ (запам'ятати на майбутнє):**
- **Багатослівні й дефісні картки гра розбиває на токени**, тож кожна частина потребує
  свого hover: додані `m.set` для `lib` (ad-lib), `bona`/`fide`, `cordon`, `dole`, `eke`,
  `mull`, `aggrandizement`/`aggrandisement`, `vis` (vis-a-vis).
- **`stemEn` не резолвить подвоєння приголосної** (`mulled`, `brimmed`) і **compare-форми**
  (`bigger`) — у прикладах уживай базову форму.
- Британські написання в прикладах (`rumours`) падають, якщо картка американська (`rumor`).

**⚠️ ОМОНІМИ В УКРАЇНСЬКІЙ — окремий клас «позичених» глосів** (не плутати з синонімією,
де спільний глос дозволений). Розведено: `protrusion` → «випнута частина» («виступ» =
performance/gig), `slab` → «кам'яна плита» («плита» = stove), `resuscitation` →
«оживлення потерпілого» («реанімація» = intensive care unit), `accessibility` →
«безбар'єрність» (доступність = availability), `counsel` → «порада фахівця»
(настанова = guideline), `diffuse` → «м'яко розсіяний» (розсіяний = distracted).

---

### 🎓 Вкладка «Рекорди за рівнями CEFR» — порядок і сума (сесія 45, за запитом)

- **⚠️ ПОРЯДОК РЯДКІВ — ЗА СКЛАДНІСТЮ НАБОРУ, не за рекордом і не за першим рівнем мітки.**
  Ключ (`diffOrder` у `renderTabCefr`): 1) найвищий рівень у наборі; 2) найнижчий;
  3) ширина. Тобто `A1` → `A1+A2` → `A2` → `B1-C1` → `B2+C2` → `C2` → `—`.
  Логіка: чистий `A2` важчий за мішанку `A1+A2` (у мішанці половина слів легкі), а пул
  із C2 важкий, хоч би що ще в ньому було. `—` (слова без рівня) — це індекс `none` у
  `CEFR_LEVELS`, тому завжди останній.
  ⚠️ **Це ЗМІНА правила сесії 43**, де сортувалось за ПЕРШИМ рівнем мітки: тоді `A1-B2`
  стояв одразу після `A1`, тепер — після `B1` (бо містить B2-слова). Перевірку в
  `test-lb-cefr.js` оновлено разом із правилом; НЕ «повертати як було».
- **Сума по всіх іграх набору** поруч із лічильником: `3 ігри · 47 (7/54)` — за аналогією
  з «Найкращими ігровими днями». Клас `.lb-games-sum`, тьмяніший за сам лічильник.
  Показується ЛИШЕ коли ігор >1 — на одній грі сума дослівно дублювала б рахунок рекорду
  ліворуч. Рядок «ост» (остання гра) суми й лічильника не має, як і раніше.

## 📖 Два ряди чипсів у «Тренуванні» = ОДИН вибір (сесія 46, за запитом користувача)

У наборі **«🎓 Рівні CEFR»** тепер ДВА ряди чипсів, і разом вони дають один спільний пул:

- **верхній `#tm-levels`** — рівень ЦІЛКОМ (як було): `A1 904`;
- **нижній `#tm-solo`** (у `#tm-solo-wrap`, заголовок `#tm-solo-h`) — той самий рівень, але
  **тільки слова поза «Знаю»** (`streak < 3`, критерій `toLearnIndices`): `A1 37% 571`, де
  зелений `.lvp` — скільки рівня вже вивчено, тьмяний `.lvn` — скільки лишилось грати.

**Правила вибору (усі — прямі формулювання користувача, НЕ «спрощувати»):**
- пул = об'єднання: повні рівні цілком + невивчені слова 📖-рівнів. Можна `A1 📖+A2+B1`;
- **один рівень не буває в обох рядах одночасно:** щойно обрано `A1`, чипс `A1 📖` отримує
  клас `.taken` (opacity 0.3, пунктирна рамка) і **перестає клікатись**, і навпаки.
  Щоб перенести рівень між рядами — спершу зняти, потім обрати в іншому;
- **кількість рівнів НЕ обмежена.** ⚠️ Ліміт «не більше 3» користувач запропонував і
  СКАСУВАВ у тій же сесії — не повертати. Гаснуть ЛИШЕ протилежності, більше нічого;
- хоча б один рівень завжди лишається обраним (`cefrPickLevel` не знімає останній).

**Стан і код:**
- `cefrSel` (повні, ключ `oxford_cefr_sel_v1`) + **`cefrSoloSel`** (📖, новий ключ
  **`oxford_cefr_solo_v1`**). Обидва — UI-налаштування, у `BACKUP_KEYS` свідомо НЕ входять.
  На завантаженні перетин рядів лікується (`cefrSoloSel` мінус `cefrSel`), порожній вибір
  → `A1`.
- `cefrPickLevel(L, solo)` — єдина точка зміни вибору; `cefrMixedIndices()` — пул;
  `cefrKnownStats()` — {рівень: {known,total}} для відсотків; `cefrMixLabel()` — мітка.
- ⚠️ **Окремого набору `trainSet = "cefrsolo"` НЕМА** (був живий ~годину в цій же сесії й
  прибраний за правкою користувача). `TRAIN_SETS` — знову три: `cefr` / `all` / `tolearn`.
  Обидва ряди належать набору `cefr` і ховаються/показуються разом із ним.

**Мітка й лідерборд:**
- `cefrMixLabel()`: якщо 📖-рівнів нема — віддає РІВНО стару `cefrTagLabel` (зі стисканням
  суцільного діапазону в `A1-B1`), щоб історичні рядки вкладки 🎓 не роз'їхались надвоє.
  Інакше — перелік із маркером при кожному 📖-рівні: `A1 📖+A2+B1`.
- тег гри `cefr:<мітка>` — префікс лишає гру поза топ-10 (правило сесії 43), а 📖 робить
  ОКРЕМИЙ рядок у вкладці 🎓: пул без засвоєних слів помітно важчий, рахунки з повним
  рівнем не порівнювані.
- `diffOrder` зрізає **усі** входження `\s*📖` (`/g`) перед розбором мітки й кладе ознаку
  4-м ключем сортування — тому `A1 📖` стоїть одразу після `A1`, а `A1 📖+B1` сортується
  за найвищим рівнем набору, як і звичайні комбінації.

**Навіщо це взагалі:** за розрахунком сесії 46 (модель покриття) у повному пулі шафл тягне
й уже засвоєні слова, тож близько половини відповідей витрачається намарно. Повний прохід
сходів A1 → A1+A2 → … → «без рівня» до 95% коштує ~5 350 ігор повними пулами і ~3 160 —
📖-пулами; до того ж бейдж рівня в 📖-режимі показує справжні 95%, а не 81% (вивчене слово
не витягується назад і не може регресувати).

**Тест:** `node test-cefr-solo.js` — 33 перевірки (обидва ряди, відсотки з mastery, взаємне
блокування, змішаний пул, перенесення рівня, «останній не знімається», персист, лікування
перетину, тег і сортування у вкладці 🎓).
⚠️ У тестах НЕ рахувати «вивчених слів» як кількість проставлених записів mastery:
20 пар-омонімів (run/fair/bank) ділять `wordKey`, тож слів виходить трохи більше.

## 🎓 Гра за рівнями CEFR (сесія 42, за запитом користувача)

Кнопка **`#cefr-btn` «🎓 Рівні CEFR»** на стартовому екрані (під «💬 Вирази і сленг») →
модалка `#cefr-modal` з **мультивибором 7 пунктів**: A1 · A2 · B1 · B2 · C1 · C2 · **«—»
(слова поза списками CEFR)**. Позначені рівні граються РАЗОМ; біля кожного — кількість слів,
на кнопці — сумарний пул. Далі — та сама модалка напрямку (US→UA / UA→US / Випадковий), що
й у категорій, і звичайна timed-гра з оновленням mastery.

- **Дані: `const CEFR_DATA`** (одразу після `IRREGULAR`, ~45 КБ) — 6 рядків «слово|слово|…»
  у нижньому регістрі. Джерела: **Oxford 3000/5000** (A1-C1, пріоритет) → **CEFR-J + Octanove**
  (A1-C2). Слів у мапі 5626, решта 2140 — кошик "none". ⚠️ Ключ = `wordKey` (перше `en`), тому
  вставка нових слів у WORDS мапу НЕ ламає; нове слово без мітки просто потрапляє в «—».
- **Перегенерувати** після великого добору: `gen-cefr.js` у scratchpad (описаний у розділі
  «OXFORD-ДОБІР»; потребує `oxford-cefr.json` + завантажені cefrj/octanove CSV).
- **Функції:** `cefrLevelOf(w)` (→ рівень або "none"), `cefrIndices(levels)`, `cefrCounts()`,
  `renderCefrModal`/`openCefrModal`/`closeCefrModal`/`startCefrGame`. Конст `CEFR_LEVELS`,
  `CEFR_META`, `CEFR_MAP`, `CEFR_SEL_KEY`.
- **Вибір зберігається** в `oxford_cefr_sel_v1` (масив рівнів; дефолт `["B1","B2"]`).
  ⚠️ СВІДОМО не в `BACKUP_KEYS` — це налаштування UI, не дані прогресу.
- **Клавіші:** Esc — закрити, Enter — грати (з `!e.repeat`, як у модалці напрямку).
  Alt-тогл панелі прогресу перехоплюється, поки модалка відкрита (як для `#cat-modal`).
- ⚠️ **CSS-пастка врахована:** `#cefr-modal` має display через ID, тому є парне
  `#cefr-modal.hidden { display: none }` (правило сесії 20).
- **⚠️ ЛІДЕРБОРД: CEFR-ігри ПОЗА топ-10 (за скаргою користувача одразу після релізу).**
  Перша ж гра рівнем A1 дала 16 і стала №1 у «Найкращих іграх» — пул одного рівня легший
  за повний словник, тож порівняння нечесне. Правило (обране користувачем із трьох
  варіантів — мітка / мітка+виключення / окрема вкладка):
  • тег `"cefr:<рівні>"` (`pendingCatTag`, мітка з `cefrTagLabel`: «A1», «A1-B1», «A1+C2», «—»);
  • у `endGame` окрема гілка `timedPoolTag.indexOf("cefr:") === 0`: **БЕЗ** `saveScore`,
    `updateTodayBest` і rank-вітання, **АЛЕ З** `updateLatest` + `appendGamesLog` — тобто
    гра видима в рядку «остання гра» і лишається в історії/точності;
  • `renderGameRow`: підпис «🎓 A1» замість напрямку **і БЕЗ `data-rank`** (номер місця не
    показуємо — гра поза рейтингом);
  • статистика дня, XP, mastery, пункти «Плани» рахуються ЯК ЗВИЧАЙНО.
  ⚠️ НЕ повертати CEFR-ігри в топ-10 і НЕ ставити їм `tag = null` — це рівно та помилка,
  через яку рейтинг зіпсувався.
- **Одноразова міграція `oxford_fix_cefr_lb_v1`** — прибрала ту першу A1-гру (05.08.2026,
  16 очок, 3 помилки/пропуски, wordCount ≥7700, без тегу) зі scores + games-log + latest;
  todayBest перераховано з логу. Патерн і код — копія `oxford_fix_known_lb_v1` сесії 37.
- **Перевірено реальним Chromium** двічі (playwright-core + `/opt/pw-browsers`): правила
  лідерборду — 14 перевірок (міграція прибирає стару гру й перераховує todayBest, підпис
  «🎓 A1», відсутність номера місця, звичайні ігри не зачеплені, підписи діапазонів,
  тег при старті). Сама фіча — 16 перевірок —
  відкриття, 7 рядків, сума лічильників = `WORDS.length`, дефолт B1+B2, тогл і персист,
  блокування кнопки без вибору, пул = тільки слова обраного рівня, старт гри з правильним
  словом, timed (не review), Esc. 0 помилок консолі.

### Сесія 43 — ремонт топ-10 після історії з CEFR-грою + вкладка 🎓
- **Що зламалось (ланцюжок, важливо для розуміння міграцій):** A1-гра на 16 стала №1 і
  **витіснила тодішнє 10-те місце** → міграція сесії 42 прибрала ту A1-гру → в топі лишилось
  9 записів → вільний слот зайняла **порожня гра** (0 очок, 0 відповідей: старт і одразу
  вихід) і показувалась 10-м місцем із «0».
- **Міграція `oxford_fix_lb_refill_v1`** — перебудовує топ-10 з об'єднання `oxford_scores_v1`
  ∪ `oxford_games_log_v1` (дедуп за `ts`, записи топу в пріоритеті — у них є `id`), викидаючи
  порожні ігри й теги `known`/`cefr:*`. Лог веде ВСІ звичайні ігри з сесії 33, тож витіснена
  гра там знайшлась. Ідемпотентна (прапор + дедуп). Порожній `todayBest` теж прибирається.
- **`endGame`: порожня гра (score+wrong+skipped === 0) НЕ пише** ні в `saveScore`, ні в
  `updateTodayBest`/`updateLatest`/`appendGamesLog` (`emptyGame`). Статистика дня — як була.
- **Тест `test-lb-cefr.js`** (реальний Chromium, 42 перевірки, 0 помилок консолі): міграція
  (відновлення 10-го місця, чистка «0», ідемпотентність, id не перезаписані, known/cefr не
  просочуються), захист від порожньої гри, уся вкладка 🎓. У `verify.sh` НЕ входить —
  потрібен `npm i playwright-core`.

### Сесія 43 — enAlt-синоніми в UA→US (аудит uaAlt)
- **Скарга:** на промпт «бронювати» гра не приймала `reserve` (приймала лише `to book`).
- **Аудит показав, що це системна властивість, а не баг у кількох словах:** ~2800 пар
  «UA-промпт ↔ англійське слово, що має цей переклад у `uaAlt`, але в UA→US не зараховується».
  Так і задумано: `uaAlt` свідомо НЕ бере участі в крос-матчі `ua-en` (інакше «місто» прийме
  town, «дурний» — silly). **НЕ приймати uaAlt масово** — це не «фікс», а зміна складності гри.
- **Зроблено точково:** 70 промптів, де другий варіант — повноцінний еквівалент, отримали
  `enAlt` (119 записів, +163 синоніми). Кандидати відбирались із власної бази `SYNS` + ручний
  відбір. Приклади: адвокат→lawyer, аварія→accident, отримувати→receive, потребувати→require,
  хворий→unwell, хибний→wrong/incorrect/untrue, чудовий→excellent/amazing/fantastic/marvelous.
- **Чому саме `enAlt`:** en→ua ці варіанти вже приймав (вони в `uaAlt`), бракувало лише
  зворотного напрямку. `enAlt` також потрапляє в підпис правильної відповіді («to book / reserve»)
  і безпечний для hover-перекладів (enAlt-hover додає ключ лише якщо він ще не резолвиться).
- Скрипт аудиту — разовий (у репо не лишений); повторити легко: зібрати `byUa`/`byAlt` з
  `WORDS` і зіставити з тим, що приймає `isCorrect` у режимі `ua-en`.

---

## Правило додавання нових слів у словник

**⚠️ ПРАВИЛО РОЗПОДІЛУ: WORDS чи «Вирази і сленг»? (сесія 37, за запитом):** нова лексика
розподіляється між ДВОМА тренажерами, нічого не «пропускається лише тому, що це фраза»:
- **→ `WORDS` (стандартні ігри):** окремі слова І мультислівні НАЗВИ-ПРЕДМЕТИ/поняття, що
  працюють як словникова одиниця (board game, wet wipe, baby formula, hula hoop, plush toy;
  усталені фразові дієслова pick up/drop off). Критерій: «це називає річ/дію», перекладається
  іменником/дієсловом. Складники без hover-перекладу → m.set.
- **→ `EXPRESSIONS` («Вирази і сленг», флеш-картки):** ідіоми, сталі звороти, колокації,
  розмовні формули (fall in love, make a will, break a leg, piece of cake). Критерій: сенс
  НЕ дорівнює сумі слів АБО це готова розмовна конструкція. Формат {en,ua,note}.
- Пропонуючи кандидатів зі сторінки EFE/списків — назви обидва кошики явно.

**⚠️ УРОКИ ВЕЛИКИХ ПАРТІЙ (сесія 48 — п'ять xlsx, 1986 карток за сесію).**
Користувач попросив укрупнити партії («по 60 слів у партії? якось дуже довго буде»),
і на партіях ~110 позицій вилізли чотири помилки, яких на партіях по ~50 не було.
Усі чотири повторювались, тож це не випадковості, а властивість методу:

1. **Порожній рівень CEFR.** `cefr-move` зручно кликати двома списками (B1 і C1) —
   і середній рівень просто випадає. У файлі «Покупки» B2 отримав **0 з 336**,
   у «Подорожах» — **10 з 418**. Обидва рази помітив лише підсумковий підрахунок.
   **ОБОВ'ЯЗКОВО: перед комітом партії рахувати розподіл по рівнях і дивитись,
   чи немає порожнього.**
2. **Дубль `enAlt` + картка.** Позиція стає карткою І передається 4-м аргументом
   `add-batch.js` — два записи претендують на той самий рядок `en`. Траплялося
   **в кожному з трьох останніх файлів** (bestseller, shipping, faulty, expiration date,
   waiting list, cockpit, carriage, daycare centre, real estate agency, doctor's surgery).
   **Звіряти список пар ПРОТИ списку карток партії.**
3. **Загублені рядки.** При складанні великого JSON легко пропустити позицію:
   3 у комп'ютерному файлі, 7 у «Покупках», 5 у «Подорожах». **Після кожної партії —
   скрипт, що звіряє список розділу з тим, що реально пішло у JSON; після файлу —
   повний аудит покриття.** Аудит не формальність: він щоразу щось знаходив.
4. **Апострофи рвуть шел.** Пари для `enAlt` передаються JSON-рядком в аргументі;
   `chemist's`, `barber's`, `doctor's office` ламають одинарні лапки. **Класти пари
   у файл і передавати `"$(cat alt.json)"`.**
5. **(сесія 49) Типографський апостроф `’` у xlsx ≠ `'` у партії.** Аудит рахував
   `farmer’s carry` і `Don’t hold your breath.` загубленими, хоча вони були додані.
   **Нормалізувати `[‘’] → '` ОДРАЗУ при вигрузці xlsx**, а не в скрипті аудиту.
6. **(сесія 49) `bigger` не резолвиться в `check-coverage`** — стемер не знімає подвоєння
   приголосної в порівняльному ступені (`big`+`-er`). `larger` проходить. **У прикладних
   реченнях не писати `bigger`/`hotter`/`thinner`** — брати `larger`, `greater` або
   перебудовувати фразу. Впало тричі за сесію.
7. **(сесія 49) Перед enAlt дивитись ГЛОС наявної картки, а не лише ключ.** `rower` у нас
   «весляр» (людина), а не гребний тренажер; `changing room` — «примірочна» в крамниці,
   а не роздягальня. Обидва мали піти окремими картками, і пішли — але лише тому, що
   глос перевірили. Сліпий enAlt за збігом ключа зіпсував би обидві картки.

**⚠️ ПРОЗОРІ КОМПОЗИТИ — ЩО РАХУЄМО ЗАЙВИМ (сесія 49, за запитом користувача).**
Привід: у грі трапилось `red onion` = «червона цибуля», і користувач спитав, навіщо
картка, якщо `red` і `onion` уже вивчені окремо. Скрипт (`transparent.js` у репо) знайшов **539 карток**, де глос = склейка глосів складників.
**Прибирати цей клас цілком НЕ МОЖНА** — туди потрапляють `solar system`,
`nervous system`, `immune system`, `swimming pool`, `blood pressure`,
`industrial revolution`. Рішення користувача: **чистити ТОЧКОВО**.
- **Критерій на видалення:** глос буквально «прикметник + іменник» І фраза НЕ є
  усталеною назвою сорту / стану / терміна.
- **⚠️ УТОЧНЕННЯ (сесія 49, природний файл): міряти прозорість у напрямку UA→EN.**
  `red onion` тривіальний в обидва боки — прибрали. `heavy rain` виглядає так само
  прозоро, але з української «сильний дощ» тягне на хибне `*strong rain`, тож картка
  ПОТРІБНА. Дзеркальна пара: `strong wind` (не heavy). Так само лишаємо `freezing rain`
  (не *icy), `fresh water` (прісна ≠ свіжа), `tree crown` (крона ≠ корона),
  `natural disaster` (стихійне ≠ natural). **Питання не «чи складається з відомих
  слів», а «чи вгадає користувач саме ці слова з українського глоса».**
- **Прибрано 12:** red onion, white pepper, hard cheese, soft cheese, short hair,
  long sleeve, short sleeve, wide doorway, full tank, young adult, black screen, brown rice.
- **СВІДОМО ЛИШЕНО** (усталені назви, не склейки): green tea, black tea, red wine,
  black pepper, white bread, white chocolate, hot chocolate, red meat, red cabbage,
  full moon, New Year, big toe, hard palate, dark mode, blue screen of death,
  low season, wide stance, narrow stance. **Не пропонувати їх до чистки повторно.**
- **Решту 503 не чіпаємо** — це терміни й поняття.
- Інструмент для видалення: `remove-card.js "<en>" ...` — чистить запис
  у WORDS + ключі TRANS/EXAMPLES/EXAMPLES_UA/HINTS + рівень у `CEFR_DATA`.

**⚠️ ЩО НАСПРАВДІ НАЛЕЖИТЬ EXPRESSIONS, А НЕ WORDS (сесія 49, розбір за запитом).**
Користувач знайшов у WORDS дві картки, які туди не мали потрапити — перенесено:
- `fed up` → ідіома: «ситий по горло» не виводиться з fed + up, ще й вимагає `with`.
- `to change clothes` → колокація: сенс = сума слів, цінність суто граматична
  (clothes завжди множина, без артикля).
**Перевірка при доборі:** якщо мультислівна одиниця НЕ називає річ/поняття, а описує
дію через звичайний додаток — це `EXPRESSIONS`. Фразові дієслова (`pick up`, `to cool
down`, `to loosen up`) лишаються у WORDS, як і раніше.

**⚠️ `everyday` ≠ `every day` (сесія 49, скарга «не приймало щодня»).** `everyday` —
ТІЛЬКИ прикметник («повсякденний»); прислівник «щодня» пишеться ОКРЕМО: `every day`.
Гра формально мала рацію, але поведінка дратувала. Полагоджено НЕ ламаючи семантику:
`щодня` / `кожного дня` дописані в **`uaAlt`** (діють на відповідь у EN→UA, у UA→EN
`uaAlt` не діє, тож «щодня» не тягне на `everyday`), а `HINTS["everyday"]` тепер
пояснює різницю. Окрему картку `every day` НЕ додавали: «щодня» стало б її основним
глосом, і тоді відповідь `daily` у UA→EN перестала б зараховуватись.

**⚠️ `cefr-add.js` НЕ ВМІВ У C2 (баг, полагоджено в сесії 52).** Регулярка рядка рівня
вимагала хвіст `",` — а `C2` у `CEFR_DATA` ОСТАННІЙ, тож його рядок закінчується просто на
`"`. Через це будь-яка партія з C2-словами падала з «не знайшов рядок C2 у CEFR_DATA», і
`cefr-batch.js` обривався на пів-дорозі (перші рівні вже записані, C2 — ні). Полагоджено
на `",?` в обох регулярках. Якщо колись переставиш порядок ключів у `CEFR_DATA` — пастка
переїде на новий останній рівень, а не зникне.

**🔗 ПАРИ-МАЙЖЕ-ДУБЛІ ТА МИЛИЦІ-ГЛОСИ — ПРАВИЛА (сесія 52; розгорнуто — `handoff-log.md`).**
Зведено 17 пар (`pavement`→`sidewalk`, `railway`→`railroad`, `spanner`→`wrench`…) і ще 4 в кінці сесії;
партія «шкільні предмети» +28 карток. Що з цього ПРАВИЛА:
- **Виживач — американська форма основною, друга в `enAlt`** (конвенція сесії 51).
- **Рівень концепту НЕ мусить підскакувати через те, яка картка вижила** (`railway` A2 /
  `railroad` B1 → злиття дає «залізницю» A2, не B1). Звіряй рівні при кожному злитті.
- **⚠️ `remove-card.js` ЗНОСИТЬ УСІ КАРТКИ ЗА КЛЮЧЕМ** (регекс глобальний) — пару з однаковим
  `en` (`cafe`) ним чистити НЕ МОЖНА. І він лишає осиротілий ключ у `TRANS` на ЩІЛЬНОМУ рядку —
  після прогону звіряти всі п'ять блоків, не вірити лічильнику.
- **`soccer`/`football` НЕ зведені свідомо** — в амер. це різні ігри; натомість прибрано
  милицю з глоса і дано HINTS обом.
- **«Дві картки приймають один рядок» саме по собі НЕ баг.** Дефект — лише коли глос
  ВИГАДАНО, щоб обійти колізію: перевернутий порядок слів («валик малярний» / «малярний
  валик»), канцелярит, звужений сенс, маркер «американською»/«(UK)». З 11 підозр сесії 52
  справжніх дублів 4, розведено правкою глоса 4, не дефект 3.
- **⚠️ ЯК ЗНАЙТИ ЦЕЙ КЛАС (два дешеві скани):** (1) згрупувати картки за ВІДСОРТОВАНИМ
  набором слів глоса, лишити групи з >1 різним глосом; (2) регекс по `ua` на
  `американ|британ|\(UK\)|\(US\)`. Скан №1 бачить те, чого не бачить скан за ключем.
- Перед `enAlt` дивись ГЛОС наявної картки, не лише ключ (`ethics` → `enAlt` до `ethic`,
  бо «етика» вже там; `rower` = весляр, не тренажер).

**🎯 ВУЗЬКІ ГЛОСИ БАГАТОСЛІВНИХ КАРТОК (сесія 52, за фідбеком з гри).** Привід —
`bath mat` приймав РІВНО «килимок для ванної», а відповідь «килим для ванни» летіла в ✗.
Те саме `to turn back`: тільки «повернути назад», без «повернутися».

**Розмір класу (скан):** багатослівних карток 4069, з них **3616 без жодного `uaAlt`**,
і в **3302** глос теж багатослівний — тобто фразу треба набрати ЗНАК У ЗНАК.
⚠️ **Але більшість із цих 3302 — НЕ дефект:** «лак для волосся», «крем для рук»,
«датчик диму» не мають інших природних варіантів. Масово дописувати їм `uaAlt` = рівно
те, проти чого користувач заперечив у сесії 40 (розмаїття в панелі відповіді).
Тому чистили ДВА підкласи, де дефект однорідний:

- **✅ ФРАЗОВІ ДІЄСЛОВА — 72 картки, клас ЗАКРИТО ПОВНІСТЮ** (+145 варіантів).
  Дефект однаковий у всіх: у глосі одна видова форма. `to log in` приймав «входити в
  обліковий запис», але не «увійти»; `to shut down` — «вимикати комп'ютер», але не
  «вимкнути». Дописано другу видову форму + живий розмовний варіант там, де він є
  (`залогінитися`, `прикурити двигун`, `спіймати таксі`).
  ⚠️ **ЦЕ СВІДОМИЙ ВИНЯТОК ІЗ ПРАВИЛА СЕСІЇ 39** («НЕ ДОДАЄМО видові пари — це перебір
  кількох букв»). Різниця: для ОДНОСЛІВНОЇ картки промах у виді коштує кількох літер,
  а для ФРАЗИ — усієї відповіді. Користувач сам попросив «повертатися» саме тому.
  Правило сесії 39 лишається чинним для однослівних карток.
- **✅ ПРЕДМЕТНІ КАРТКИ, ПЕРША ПАЧКА — 58 карток** (+112 варіантів). Тут дефект має
  ТРИ осі, і жодна не ловиться регексом — тільки очима:
  1. **синонім головного слова:** килимок/килим, ганчірка/серветка, засіб/рідина,
     резинка/гумка, пилка/пилочка, датчик/детектор/сенсор;
  2. **відмінок залежного:** «для душу»/«для душа», «унітаза»/«унітазу»,
     «бачок унітаза»/«зливний бачок»;
  3. **побутова альтернатива:** «застосунок»/«додаток», «магазин товарів для ремонту»/
     «будівельний магазин», «водомір» замість «лічильник води».

**⚠️ ЩО НЕ РОБИЛИ І ЧОМУ — 338 КАРТОК ІЗ ГЛОСОМ-ОПИСОМ (4+ слова).** Це окремий дефект,
і `uaAlt` його НЕ лікує: «не прийти на домовлене побачення», «топ із зав'язками на шиї»,
«страхування відповідальності перед третіми особами», «керування під впливом алкоголю
чи наркотиків» — такий рядок користувач не набере НІКОЛИ, скільки варіантів не додай.
Лік інший: скоротити основний глос, а опис перенести в `HINTS` (там йому й місце).
Користувач вирішив поки не чіпати — **відкритий хвіст**.

**Залишок після сесії 52:** 3172 багатослівні картки без `uaAlt`, фразових дієслів
серед них **0**. Наступні пачки — предметні картки, по ~50 за раз, очима.

**🩹 КОМА В ГЛОСІ БІЛЬШЕ НЕ ОБОВ'ЯЗКОВА (сесія 52) — знайдено побічно, важливіше за саму пачку.**
`normUa` знімає лише регістр і `г→ґ`, пунктуацію не чіпає. А **47 карток** мають глос виду
«кран, що протікає», «той, хто вижив», «обід, узятий із собою» — і **жодна** з них не мала
безкомового варіанта в `uaAlt`. Тобто промах на одному розділовому знаку коштував усієї
правильної відповіді, і дірка була суцільна по всьому класу.
- **Лік у КОДІ, не в даних:** `normUaSoft` тепер знімає `,;:`. Один рядок накриває всі 47
  і всі майбутні картки; правка в даних лікувала б лише наявні.
- **⚠️ ЖИВЕ САМЕ В `normUaSoft`, а не в `normUa`** — те саме розведення, що з дефісом
  у сесії 50: з `normUa` будується `inp`, а з нього `inpNorm` для АНГЛІЙСЬКОЇ гілки.
- **⚠️ `.trim()` у кінці ОБОВ'ЯЗКОВИЙ** (глос із комою в кінці лишив би хвостовий пробіл).
  Він же виявив мертвий запис `grilled → uaAlt:["гриль-"]` — обірваний дефіс як префікс,
  який неможливо ввести і який після trim зливався з карткою `grill`. Запис прибрано.
- Закріплено **5 перевірками** в `test-homograph.js` (54 → **59**). Перевірено, що набір
  ловить регресію В ОБИДВА боки: прибрати правило → падає, поширити на `normUa` → падає.

**📦 ВУЗЬКІ ГЛОСИ — ПАЧКИ 2-5 (сесія 52): 644 картки, +783 варіанти; розгорнуто — `handoff-log.md`.**
Метод (повторюваний, ним же зроблено пачки 6-8 у сесії 55):
1. **Пул** = багатослівний ключ, глос 2-3 слова, без `uaAlt` (глос 4+ слів = клас «опис», не чіпаємо).
2. **Чанки по ~25 → воркфлоу «шукач → скептик»**; соло — лише якщо воркфлоу впав, і це
   дорого для контексту. Правила в промпті шукача (кожне колись коштувало помилки в проді):
   **A** не слово, що входить у власний глос · **B** не голе головне слово глоса · **C** не
   книжне/застаріле/росіянізм/транслітерація · **D** не «перебір кількох букв» · **E** без
   дужок · **F** абревіатури — в нижньому регістрі · **G** не кабінетні вигадки · **H** колізія
   з чужим глосом → див. п. 4. Плюс: кожен варіант `grep`'нути по `dict.js`; 1-3 варіанти на
   картку і НЕ на кожну — приблизно половина зрізу лишається порожньою, це норма.
3. **МЕХАНІЧНИЙ АУДИТ ПЕРЕД ВСТАВКОЮ — ОБОВ'ЯЗКОВИЙ, агентам не вірити на слово.** П'ять
   перевірок: дужки; тавтологія з власним глосом; **фраза, що входить у власний глос**
   (`walk-in closet` ← «гардеробна»); **збіг із основним глосом ІНШОЇ картки**;
   **абревіатури в НИЖНЬОМУ регістрі** (`service station` ← «СТО» → «сто» = глос `hundred`).
   Він ловив по 1-2 реальні помилки в КОЖНІЙ пачці, включно з тими, де агенти звітували «0».
4. **Правило H при колізії з чужим глосом:** та сама річ іншими словами → зняти й записати в
   кандидати на злиття; справжні синоніми — різні слова (`carry-on` ↔ `hand luggage`) → лишити,
   часто дописати взаємно. Різницю вирішувати РУКАМИ, регексом не береться.
5. `node uaalt-add.js batch.json` → `./verify.sh` → коміт; порахувати пул до/після.
Типові осі варіантів: синонім головного слова, відмінок залежного, побутова альтернатива,
живі скорочення (аварійка, техогляд, пралка), орфографічні дублети, друга видова форма для
дієслівних фраз (свідомий виняток із правила сесії 39 — для ФРАЗИ промах у виді коштує
всієї відповіді).

**🎲 АНТИ-ПОВТОР У ДОБОРІ СЛІВ (сесія 52, за скаргою «три рази підряд одне й те саме»).**
Користувач: «щоб не було закономірності, щоб я наперед не вгадував, що за чим піде,
але й щоб не було три рази підряд одне й те саме».

**⚠️ ДІАГНОЗ — ДВА РІЗНІ ШЛЯХИ, І ЛИШЕ ОДИН ДАВАВ ТРИ ПОСПІЛЬ:**
- **Звичайна гра фізично не може дати три підряд.** Вона йде шафлом БЕЗ повернення
  (`shuffledIndices` + `shufflePos`), тож у межах проходу кожне слово рівно раз, і
  повтор можливий ЛИШЕ на стику проходів — максимум ДВА. Симуляція точної логіки
  (200k прогонів по 10 питань) підтвердила: 2 підряд у **70% ігор при пулі з 3 слів**,
  **94% при пулі з 2**, 20% при 5. Тобто на малих пулах це була не рідкість, а норма.
- **Три і більше давало ЛИШЕ повторення помилок.** Там вибір випадковий із
  `reviewRemaining`, а старий захист `indices.length > 1 && pick === currentWordIndex`
  глушив тільки НЕГАЙНИЙ повтор і повністю вимикався, коли в черзі лишалось одне слово.
  ⚠️ **Для одного слова повтор НЕМИНУЧИЙ і правильний** — закрити його можна лише
  трьома правильними поспіль. Це зафіксовано окремою перевіркою, щоб не «полагодили».

**ЛІК — вікно останніх показаних слів** (`recentIndices`, `REPEAT_WINDOW`=3):
- у звичайній грі слово зі стику міняється місцями з найближчим «свіжим» У ТОМУ Ж
  проході — перестановка всередині масиву лишає його перестановкою пулу, тож
  властивість «у межах проходу кожне слово рівно раз» не ламається;
- у повтореннях кандидати фільтруються вікном, вибір лишається випадковим.
- **⚠️ ВІКНО НАВМИСНО ВУЖЧЕ ЗА ПУЛ: `min(3, poolSize − 2)`.** Якби воно дорівнювало
  `poolSize − 1`, на пулі з 3 слів вийшла б жорстка карусель A→B→C→A→B→C — рівно та
  передбачуваність, якої користувач просив уникнути. Тепер: пул 2 — чергування (інакше
  ніяк), пул 3 — заборонено лише сусідній повтор, порядок далі випадковий, пул 5+ — вікно 3.
- Результат симуляції: сусідні повтори **70% → 0%** на пулі 3, і 96 різних послідовностей
  замість 3 у каруселі; розподіл показів по словах лишився рівномірним.

**Закріплено 9 перевірками в `test-due.js` (31 → 40)**, вони ганяють РЕАЛЬНИЙ `nextWord`
у Chromium. Перевірено, що набір ловить регресію в обидва боки: вимкнути анти-повтор —
падає, розширити вікно до каруселі — падає.
⚠️ **УРОК ПРО САМ ТЕСТ:** перша версія перевірки каруселі рахувала КІЛЬКІСТЬ РІЗНИХ
послідовностей — і регресію Б НЕ ловила, бо карусель на пулі 3 дає 6 послідовностей
(за перестановками стартового шафлу). Правильна ознака — **періодичність**
(`a[i] === a[i-n]` для всіх i): у каруселі періодичні 60 прогонів із 60.

**⚠️ ФОРМАТ ЗВІТУ ПРО ДОДАНУ ПАРТІЮ (сесія 47, прямий запит: «роби так і надалі»):**
у звіті користувачу ЗАВЖДИ давай **розкладку доданих карток по рівнях CEFR** (скільки слів
пішло в A1/A2/B1/B2/C1/C2) — таблицею, а при кількох партіях ще й підсумковий стовпчик.
Це не опція й не «якщо доречно» — це стандартна частина звіту.

**⚠️ ФОРМАТ ПРОПОЗИЦІЙ (сесія 36, за запитом):** пропонуючи слова-кандидати, ЗАВЖДИ подавай:
(1) нові слова З ПЕРЕКЛАДАМИ (основний глос + що піде в uaAlt); (2) які слова зі списку/сторінки
ВЖЕ Є в словнику; (3) які НЕ варто додавати і ЧОМУ (зайняті глоси, guard-и handoff, прозорі
фрази, дублі). Без цього пропозиція неповна.

**Обов'язковий чеклист при додаванні нового слова:**
1. `WORDS` array — `{en:"...", ua:"..."}` (ua може бути масивом для альтернатив)
2. `TRANS` — IPA транскрипція
3. `EXAMPLES` — англійське речення
4. `EXAMPLES_UA` — переклад речення
5. `EN_TO_UA` — hover-переклад (якщо не додано раніше)
6. Якщо в прикладному реченні є слово якого **немає в WORDS** → додати його теж повністю (пп. 1-5)
7. Якщо слово є в WORDS але немає в `EN_TO_UA` hover → додати тільки EN_TO_UA запис

**⚠️ ПРАВИЛО (сесія 11, за запитом користувача):** будь-яке нове слово, до якого
**ще не було перекладу** (тобто його немає ні в `WORDS`, ні в `EN_TO_UA`), додаємо
**одразу повноцінним ігровим словом** за повним чеклистом (пп. 1-5) — з прикладним
реченням. НЕ обмежуватись лише hover-перекладом (`EN_TO_UA`). Hover-only залишаємо
тільки для слів, які фізично не мають сенсу як окрема ігрова одиниця (рідкісні власні
назви тощо) — але за замовчуванням робимо повноцінне слово.

**✅ ПОЗИЦІЯ В `WORDS` ТЕПЕР НЕ ВАЖЛИВА (сесія 18):** mastery й помилки прив'язані до
СЛОВА (`wordKey=getEn[0]`), а не до індексу. Тож нові слова можна вставляти **будь-де**
(біля споріднених) — це більше не зсуває чужий прогрес. Раніше це був критичний баг.

**Полісемія / омографи — приймати обидва значення:** якщо англ. слово багатозначне або
має омограф-пару (`tear`/`to tear`, `waste`/`to waste`, `skiing`), додавай усі релевантні
переклади в масив `ua` ОБОХ записів — щоб гра не штрафувала за валідну відповідь, яку
користувач не міг вгадати з ізольованого слова (сесія 18).

**⚠️ ОБОВ'ЯЗКОВА ПЕРЕВІРКА ПОКРИТТЯ (сесія 19, за запитом):** після БУДЬ-ЯКОЇ зміни
словника або прикладних речень запусти **`node check-coverage.js`** → має бути `✅ OK`.
- Скрипт відтворює точну runtime-логіку hover (`stemEn` → `EN_TO_UA` → `IRREGULAR` →
  суфікси) і знаходить у реченнях `EXAMPLES` слова, які гра покаже БЕЗ перекладу.
- Кожне знайдене слово додаємо **повноцінно** (WORDS + TRANS + EXAMPLES + EXAMPLES_UA),
  не hover-only. Нові прикладні речення пиши з УЖЕ покритих слів — і перезапускай
  чекер до чистого OK (він зловить рекурсію: нове слово в новому прикладі).
- Винятки: **гео/культурні власні назви** (Italy, Columbus, Internet…) → hover-only
  `m.set` в EN_TO_UA IIFE; **імена людей** (Tom, Mary…) → список `ALLOW` у чекері.
  Службові слова типу since/via → теж `m.set` (як during/unless).
- Додатково чекер звіряє повноту всіх записів WORDS (TRANS/EXAMPLES/EXAMPLES_UA).
- **⚠️ Присвійні форми ПЕРЕВІРЯЮТЬСЯ з сесії 41:** раніше рядок `if (raw.includes("'")) continue`
  пропускав БУДЬ-ЯКИЙ токен з апострофом — саме через це баг «baby's без перекладу» жив
  непоміченим при чистому `✅ OK`. Тепер пропускаються лише скорочення (не-присвійні), а
  список баз на `'s` = is/has продубльовано в чекері (`CONTRACTION_S`) дзеркально до `stemEn`.
  Чекер також ВИКОНУЄ enAlt-пас (окремий `sliceBlock`) — інакше не бачить тих ключів.
  Імена в присвійних (Einstein's, Adam's) → `ALLOW`, як і решта імен.
- **Мета (вимога користувача):** він НІКОЛИ не має натрапити на слово без перекладу
  в прикладному реченні. Це траплялось щодня до сесії 19 — більше не повинно.

**Санітарна перевірка після правок:** `node -e "new Function(...)"` на JS-блоці.
Для `index.html` — витягти inline `<script>` без `src` і прогнати через `new Function`.

---

## 🎓 ВЕЛИКИЙ РОЗПОДІЛ РІВНІВ (сесія 46) — кошик «без рівня» розібрано

**Було:** 2142 картки поза `CEFR_DATA` (усе, що додавалось після разової генерації
сесії 42) — вони випадали в кошик «—» у грі «🎓 Рівні». **Стало:** 5.

| | A1 | A2 | B1 | B2 | C1 | C2 | — |
|---|---:|---:|---:|---:|---:|---:|---:|
| до розподілу | 904 | 897 | 932 | 1567 | 1326 | 890 | 2142 |
| після розподілу | 934 | 1107 | 1491 | 2303 | 1846 | 972 | **5** |
| + партія «тіло» | 935 | 1109 | 1516 | 2369 | 1903 | 976 | 5 |
| **+ партія «будинок»** | **935** | **1127** | **1616** | **2521** | **1991** | **977** | **5** |

- **Що лишилось без рівня — свідомо:** `New Year`, `Christmas`, `Easter`, `Halloween`,
  `Thanksgiving`. Це власні назви, рівень їм не має сенсу; вони внесені в `ALLOW`
  у `check-cefr.js`, тож вартовий на них не лається.
- **Як розподіляли:** механічний матч по `oxford-cefr.json` виявився НЕПРИДАТНИМ
  (морфологія дає хибні основи: `easter ← east`, `dresser ← dress`, `widower ← widow`) —
  збігів лише 56 і всі підозрілі. Тому оцінювали моделлю: 12 чанків × 2 незалежні
  оцінювачі різними оптиками (частотність у корпусі vs рік навчання), розбіжність ±1 →
  нижчий рівень, ≥2 рівні → третій вирішальний голос, далі аудит країв шкали (A1/A2 і C2,
  бо там помилка найдорожча) — 23 правки. Покриття 2142/2142, без пропусків і вигадок.
- ⚠️ **Наслідок для «сходів» користувача:** пул «без рівня» більше не існує як ігровий
  набір (5 слів). Розрахунок сесії 46 (розділ нижче) рахувався на старих розмірах пулів —
  етапи «C1+—», «C2+—» і «—» тепер безпредметні, а самі рівні поповнились (особливо B2 і
  C1). Якщо користувач повернеться до планування — перерахувати на нових числах.

## 📊 МОДЕЛЬ ПОКРИТТЯ: скільки ігор до 95% рівня (сесія 46, на запит користувача)

Монте-Карло по реальній механіці гри. Скрипти жили у scratchpad і НЕ комітились —
відтворюються за цим описом (параметри нижче достатні).

**Калібрування** — на фактичних точках користувача: A1 (23 гри) 15.0 відповідей/гру при
81.5% точності; A1+A2 (38 ігор) 12.8 при 73.8%.
- час на відповідь: `t = 2.34 + 5.45·(1−p) + 3.5·[помилка]` секунд, 60 с на гру;
- крива навчання: `p(k) = 0.95 − (0.95−p₁)·0.55^k`, де k — попередніх зустрічей слова;
- `p₁` (точність першої зустрічі): A1 0.82, A2 0.63, B1 0.50, B2 0.38, C1 0.28, C2 0.22.
  A1/A2 — з даних користувача, решта — екстраполяція (перевірено: песимізм/оптимізм
  зсувають підсумок лише на ±10%, бо довжину етапу визначає РОЗМІР ПУЛУ, а не складність).

**⚠️ МОДЕЛЬ ЗБІГЛАСЯ З РЕАЛЬНІСТЮ — головний аргумент, що їй можна вірити.**
У користувача 4600 відповідей на 8658 слів = 0.53 зустрічі на слово. Пуассон дає
`P(k≥3) = 1 − e^−0.53·(1+0.53+0.53²/2) = 1.7%` → ~150 слів мали б дійти до «Знаю».
Фактично в «Знаю» — **170**. Розбіжність 13% і саме в очікуваний бік (борг повертає
провалені слова, CEFR-ігри б'ють по підмножинах). Тобто «лише 2% засвоєного після 406
ігор» — це не проблема навчання, а математика рівномірного шафлу по великому словнику.

**Сходи (фінальна форма, за скріном користувача):** A1 → A1-A2 → A1-B1 → A2-B2 → B1-C1 →
B2-C2 → C1-C2 → C2. Патерн «рівень + рівень через один»; A1 і C2 мають по три появи.
**Правило виходу з етапу (єдине для всіх):** рівень, що тут закривається, → 95% «хоч раз
у Знаю»; рівень, який тут з'являється вперше, → 50%. Етап триває, доки не виконані ОБИДВІ.

| Етап | Пул | Ігор (повний пул) | Ігор (📖) | На виході |
|---|---:|---:|---:|---|
| A1 | 935 | 168 | 141 | A1 50% |
| A1-A2 | 2062 | 451 | 287 | A1 92% · A2 50% |
| A1-B1 | 2551 | 675 | 441 | A1 99% · B1 50% |
| A2-B2 | 3648 | 1320 | 927 | A2 95% · B2 65% |
| B1-C1 | 3607 | 1337 | 859 | B1 95% · C1 63% |
| B2-C2 | 3498 | 1036 | 485 | B2 96% · C2 51% |
| C1-C2 | 2968 | 721 | 161 | C1 95% · C2 93% |
| C2 | 977 | 32 | 2 | C2 95% |
| **разом** | | **~5 740** (96 год) | **~3 300** (55 год) | |

При 14 іграх/день (темп найкращих днів) це 13.5 міс проти 7.8 міс; при 8 — 23.6 проти 13.6.

**Висновки, які варто пам'ятати:**
1. Слову треба в середньому 3.7 (A1) … 5.4 (C2) зустрічей, **але щоб 95% рівня дійшли до
   «Знаю», λ має бути 8-11** — хвіст Пуассона дорожчий за саме навчання. У повному пулі
   ~половина відповідей іде у слова, які вже засвоєні.
2. **95% «одномоментно» у пулі, який граєш, недосяжно:** рівноважна частка «Знаю» = p³, де
   p — точність на вивчених словах. При p=0.95 стеля 86%, у симуляції 81%. Тому ціль —
   «95% слів ХОЧ РАЗ дійшли до Знаю». 📖-режим цю проблему знімає (вивчене не витягується
   → не регресує): там ever = snapshot = 95%.
3. **Сума майже не залежить від розкладу.** Скільки етап не розтягуй, загальна робота
   задана кількістю слів: фіксована історія 23+38 ігор давала 5279, повний прогін з нуля —
   5453. Зниження планки 95%→80% економить лише ~25% з тієї ж причини: недотренований
   рівень приходить у свій другий пул слабшим, і той на стільки ж довшає.
4. **Останній етап майже безпредметний:** C2 має три появи, тож до фінального етапу
   приходить на 93% — «C2 окремо» коштує 34 гри в повному режимі і 3 у 📖.
5. **Розподіл рівнів сесії 46 суму не змінив, але перекинув роботу з хвоста в середину:**
   раніше «C1+—» і «C2+—» коштували 1990 ігор, тепер «C1-C2» і «C2» — 722. Складність
   тепер росте рівномірно, а не вибухає наприкінці кошиком різношерстої лексики.

⚠️ Модель НЕ враховує «борг» (учорашні помилки підмішуються в перші 12 слів) — він б'є саме
по хвості Пуассона, тож реальні числа мають бути на 10-20% меншими.
⚠️ Числа перераховані на пулах станом на кінець сесії 46 (A1 935 · A2 1127 · B1 1616 ·
B2 2521 · C1 1991 · C2 977 — уже з партіями «тіло» і «будинок»). Кожне нове слово зсуває їх
приблизно на **0.4 гри в повному режимі й 0.5 у 📖**: +155 карток «тіла» дали +60 ігор,
+359 карток «будинку» — ще +230. Ціну добору можна прикидати множенням, не запускаючи
симуляцію; повний перерахунок потрібен лише при добірках у тисячу слів.

### 📊 «Сер./день» рахується по КАЛЕНДАРНИХ днях (сесія 53, за вибором користувача)

Було: `avgThermoPerDay` мала `if (pts > 0) { sum += pts; n++; }`, а множина дат збиралася
з КЛЮЧІВ сховищ — дня без активності там просто немає. Тобто показник означав «скільки
роблю, КОЛИ сідаю за гру», хоча підпис обіцяє «на день», і пропуск дня ніяк не тягнув
число вниз. У користувача 222 пункти за серпень давали 8.9 (÷25 активних) замість 8.5 (÷26).

- **Дільник тепер:** «міс.» — число сьогоднішнього дня (скільки днів місяця минуло);
  «всього» — від ПЕРШОГО активного дня до сьогодні включно (дати встановлення ніде немає,
  історія починається першим записом у сховищі). Дні з нульовими пунктами історію не
  відкривають — `pts <= 0` пропускається ще до пошуку `first`.
- **⚠️ Побічно виправлено місяць:** `thermoPointsThisMonth` брала `new Date().toISOString()`,
  тобто UTC — 1-го числа з 00:00 до 03:00 за Києвом місяць вважався ПОПЕРЕДНІМ. Тепер
  `todayKey()`, як і дільник; чисельник і знаменник мусять бути з одного календаря.
- **⚠️ 1-го числа дільник = 1**, тож перший день місяця показує рівно денну суму. Це не баг.
- Обидва рядки дістали tooltip з формулою — раніше підпис нічого не пояснював.
- Стереже `test-panel.js` (4 перевірки): тотожність «сума ÷ минулі дні», окремо —
  що дільник НЕ дорівнює кількості активних днів. Дата жива, не пінована.

### 🔤 У ≡ В на початку слова (сесія 53, за рішенням користувача)

Привід: `downward` має глос «**у**низ», користувач написав «**в**низ» → ✗. Це не інше
слово, а чергування у/в за милозвучністю: обидві форми правильні ЗАВЖДИ. Глоси в словнику
записані то так, то так — **960 карток** не мали форми-близнюка, тож гра вимагала вгадати
саме той варіант, що лежить у даних. Правило в `normUaSoft`, поруч із дефіс≡пробіл і комою.

- **ЛИШЕ перед приголосною** (`!UV_VOWELS.includes(w[1])`): чергування живе тільки там.
  «Виконувати» не має пари «уиконувати» — зведення в→у в такому слові було б сміттям.
- **⚠️ ВИНЯТКИ `UV_KEEP = /^(клад|дач|прав|ступ)/`** — стеми, де у/в РОЗРІЗНЯЄ СЛОВА.
  Замір по всьому словнику: без них правило дало б рівно **2 хибні злиття** —
  `вкладати`(invest) ≠ `укладати`(договір), `вдача`(характер) ≠ `удача`(щастя).
  Решта 7 злиттів безпечні: синоніми (упевнений, уперед, убрання, утішний, удосталь)
  або сам цільовий випадок (униз, угору). `прав`/`ступ` — на виріст: вправа ≠ управа,
  вступ ≠ уступ; таких глосів зараз немає, але картка може з'явитись.
- **⚠️ Живе в `normUaSoft`, НЕ в `normUa`** — з `normUa` будується `inp`, а з нього
  `inpNorm` для АНГЛІЙСЬКОЇ гілки (та сама пастка, що з дефісом у сесії 50).
- Стереже `test-homograph.js` (8 перевірок): і що «учиняти»/«уперед»/«вниз» проходять,
  і що «укладати» за invest та «удача» за character НЕ проходять, і що власні глоси
  тих самих карток працюють як раніше.
- **МЕТОДИКА, ЯКУ ВАРТО ПОВТОРЮВАТИ:** перш ніж додавати правило зведення — ПОРАХУЙ
  злиття на реальних даних (скільки різних карток склеїться) і подивись на них очима.
  Саме так у сесії 50 відхилили дефіс для англійської (19 злиттів), а тут прийняли
  для української (2, обидва закриті винятками).

### 🔤 Сполучник Й / І / ТА необов'язковий у фразі (сесія 56, за рішенням користувача)

Привід: `return ticket` = «квиток туди **й** назад», користувач написав «квиток туди-назад»
→ ✗. Дефіс `normUaSoft` уже зводив до пробіла, а от службова літера лишалась обов'язковою.
Три сполучники взаємозамінні за милозвучністю («бабуся й дідусь» = «бабуся і дідусь» =
«бабуся та дідусь»), тож вимагати саме той варіант, що лежить у даних, — та сама
орфографічна пастка, що дефіс, кома і в/у. Четверте правило тієї ж родини, в `normUaSoft`.

- **Що було до цього:** лікували БРУТФОРСОМ у `uaAlt` — `grandparents` мав 12 перелічених
  комбінацій сполучників, і кожна нова фразова картка починала з нуля. Після правила
  прибрано 13 мертвих `uaAlt`-записів (вони лише засмічували панель правильної відповіді):
  `grandparents` −5, `return ticket` −3, `round trip` −3, `to pull in` −1, `pros and cons` −1.
- **⚠️ ЛИШЕ ЯК ОКРЕМЕ СЛОВО і ЛИШЕ у фразі 3+ слів, де після викидання лишається 2+.**
  Без цієї умови `nor` = «і не» / «і жоден» схлопнувся б у «не» / «жоден» і почав приймати
  чуже. Тест на це є, він падає, якщо умову прибрати.
- **⚠️ ЗАМІР ПЕРЕД ПРАВИЛОМ (методика сесій 50 і 53):** правило чіпає **70 глосів** і дає
  рівно **0** нових злиттів між різними картками. Серед 68 глосів зі сполучником немає
  жодного, де «та» — вказівний займенник, а не сполучник.
- **⚠️ ПОРЯДОК СЛІВ ПРАВИЛО НЕ СКАСОВУЄ:** «дідусь бабуся» ≠ «бабуся дідусь», тож переліки
  ПОРЯДКУ в `uaAlt` лишаються потрібними — прибрані тільки ті, що різнились самим
  сполучником. Розширювати правило до нечутливості до порядку — окреме рішення, не робив.
- **⚠️ Живе в `normUaSoft`, НЕ в `normUa`** — з `normUa` будується `inp`, а з нього
  `inpNorm` для АНГЛІЙСЬКОЇ гілки (та сама пастка, що з дефісом у сесії 50).
- Стереже `test-homograph.js` (8 перевірок): «квиток туди-назад / туди і назад / туди та
  назад» проходять, чужий глос «квиток в один бік» — ні, `nor` = «і не» лишилось і голе
  «не» не проходить, порядок слів не скасовано, англійський бік (`pick-up`) строгий.

### ↗ у «Прогресі по днях» = БРУТТО ВХОДУ (сесія 53, вибір користувача — варіант B)

Було: комірка показувала `sgn(dl)` — нетто-зміну категорії. Користувач сам вивів проблему:
«Вивчаю» — ТРАНЗИТНА категорія (наповнюється з «Нові», спорожняється в «Знаю»), тож щойно
темп випуску перевищить темп набору, нетто стає стабільно ВІД'ЄМНИМ — і найпродуктивніший
день виглядає як провал. Тепер у комірці `F` — скільки слів УПЕРШЕ зайшло в роботу.

- **⚠️ `F = dl + dk` — це ТОЧНА рівність моделі, не оцінка.** Потоки дня:
  `dl = F + R − P` (нові + повернення зі «Знаю» − випуски в «Знаю»), `dk = P − R`
  ⇒ `dl + dk = F`, R і P взаємно скорочуються. Тому число рахується ЗІ ЗНІМКІВ і однаково
  правильне для денних і для групових рядків.
- **⚠️ НЕ брати `lLog` (лог переходів) як брутто:** списки дня диз'юнктні — слово, що за
  один день пройшло Нові→Знаю, з `l`-списку прибирається, тож лог недорахував би.
- **✓ лишилась НЕТТО** — свідомо: регресії зі «Знаю» рідкісні, а від'ємне ✓ якраз має
  впадати в око. Нетто «Вивчаю» нікуди не зникло — воно в тултіпі комірки.
- **⚠️ ТРИ МІСЦЯ МУСЯТЬ ЗБІГАТИСЬ:** `_progDeltaHtml` (панель), `renderMasteryStats`
  (рядок під бейджами на старті) і заголовок модалки в `openDayTransModal` — він тепер
  порівнює список із тим, ЩО СТОЇТЬ У КОМІРЦІ, а не з нетто, інакше сперечався б сам із
  собою. Міняєш одне — перевір усі три.
- Стереже `test-panel.js` (4 перевірки): сценарій «закрив 5, узяв 3» → нетто −2, у комірці
  має бути +3, ✓ лишається +5, а «нетто категорії -2» видно в тултіпі.

### 📝 Багатозначна картка = КІЛЬКА прикладів через ключ «en:ua» (сесія 53)

Привід: `section` (ua: `["секція","підрозділ"]`) мала один приклад — про секцію магазину,
і на промпт «підрозділ» він виглядав чужим. Механізм для цього в грі вже був
(`lookupExample` пробує `table[en + ":" + ua]` перед голим `en`), у `EXAMPLES` таких
складених ключів уже 40 (`bank:берег`, `run:керувати`, `book:бронювати`…).

- Додаєш другий глос у картку — перевір, чи приклад йому пасує; якщо ні, додай
  `"<en>:<ua>"` у **обидва** блоки: `EXAMPLES` і `EXAMPLES_UA`.
- ⚠️ Ключ мусить збігатися з глосом ЗНАК У ЗНАК, інакше мовчки візьметься голий `en`.
- ⚠️ `verify.sh` після цього обов'язковий: покриття перевіряє, що всі слова НОВОГО
  речення мають hover-переклад (лічильник прикладів у стенді має вирости).

**⚠️ ЗАОДНО ПРАВИЛО ПРО ПЕРЕКЛАД ПРИКЛАДУ:** він мусить триматися дієслова оригіналу.
Двічі за сесію ловив зворотне: `savings account pays` → «дає» (ховер на `pays` каже
«платити» — зіставити нема як) і `section sells` → «секція продає» (калька, секція нічого
не продає). Природність української тут програє зіставності: учень читає речення парами
слів, і розбіжність читається як помилка гри.

### ⚠️ УРОК ІНСТРУМЕНТАЛЬНИЙ: правити код ТІЛЬКИ за точним рядком (сесія 53)

Замінюючи `buildReviewDirPlan`, я вирізав блок за ІНДЕКСАМИ (`s.index(...)` від коментаря
до «наступної функції») — і разом із ним знищив ~45 рядків нижче: `waitingForLastAnswer`,
`awaitingAfterWrong`, `correctionTimer`, `MODE_BTNS`, `MODE_HINTS`, чотири білдери пулів
EN→EN і `setMode`.

- **⚠️ `verify.sh` ЦЕ НЕ ЛОВИТЬ.** Код лишився синтаксично валідним, а `loadtest.js`
  виконує лише оголошення — до `startGame()` він не доходить, тож ReferenceError на
  `correctionTimer` не спливає. Зловив браузерний тест (`test-review.js`).
- **Правило:** заміна кодового блоку — завжди `assert s.count(old) == 1` на ПОВНОМУ
  тексті старого блоку. Ніяких зрізів «від індексу до індексу»: межа наступного
  оголошення вгадується неправильно, і сусідній код зникає мовчки.
- Після будь-якої правки логіки гри — ганяти не лише `verify.sh`, а й браузерні набори:
  саме вони запускають `startGame`/`nextWord`.

## Що НЕ змінювати без узгодження
- localStorage ключі (зламає дані користувача)
- ⚠️ **СКОРОЧЕННЯ В `enAlt` ЗАРАХОВУЮТЬСЯ — НЕ «ЧИСТИТИ» (рішення сесії 54, варіант A).**
  У словнику 57 абревіатур (`operating system` ← OS, `virtual private network` ← VPN,
  `cash machine` ← ATM) і 27 усічень (`application` ← app, `laboratory` ← lab,
  `administrator` ← admin, `advertisement` ← ad/advert). Питання ставилось прямо —
  «коротка форма не дає змоги прописати довге слово» — і користувач СВІДОМО обрав
  лишити все як є: `ad` це правильна англійська, карати за неї нема за що.
  Замість заборони — пояснення в `HINTS` («розм. скорочення від advertisement»).
  Список рахується разово: див. фільтр у відповіді сесії 54 (prefix/acronym по `enAlt`).
- ⚠️ **ЗАМОРОЖЕНІ ДЖЕРЕЛА ПУНКТІВ/XP (сесії 45 і 47) — читачі НЕ чистити як мертвий код:**
  `oxford_mistake_reviews_v1` (📒 Помилки, закрито в 45) і `oxford_self_reviews_v1`
  (📚 Повторив, закрито в 47) більше НЕ ПОПОВНЮЮТЬСЯ і НІДЕ не показуються, але їх далі
  читають `thermoRawForDate`/`computeDayStatsXp`/рекорди. Прибрати читання = заднім числом
  обнулити пункти й XP минулих днів. Обидва ключі лишаються в `BACKUP_KEYS`.
- ⚠️ **НЕ повертати в UI кнопку «📚 Повторив», рядок «Повторення слів» у `#lb-totals`
  і значок 📖 у «Прогресі по днях»** (сесія 47, два окремі запити користувача).
- ⚠️ **ДВА НАПРЯМКИ ВИРАЗІВ = ДВА СХОВИЩА СТАТУСІВ (сесія 47):** `oxford_expr_status_v1`
  (US→UA) і `oxford_expr_status_ua_v1` (UA→US) НЕ зливати в один ключ — «вивчено» в один
  бік нічого не каже про інший, у цьому вся суть фічі.
- ⚠️ **XP за картки виразів МУСИТЬ мати лог по днях** (`oxford_expr_xp_v1` `{date:{r,k}}`):
  `computeDayStatsXp` перераховує XP з нуля по джерелах, тож джерело без свого журналу
  зникне при першій же наступній міграції `formula_vN`.
- ⚠️ **КАРТКА-ОМОФОРМА ДО СЛОВА, ЩО ВЖЕ Є В WORDS — МОЖНА, АЛЕ ОБОВ'ЯЗКОВО З `m.set`**
  (сесія 47; спершу я такі слова пропускав — рішення ПЕРЕГЛЯНУТО за запитом користувача
  «точно варто їх пропустити?»).
  **⚠️ КЛЮЧОВИЙ ФАКТ, ЧЕРЕЗ ЯКИЙ ЦЕ ВЗАГАЛІ ПРОБЛЕМА: `EN_TO_UA` будується з поля `ua`
  картки й `uaAlt` НЕ бере.** Тож картка `{en:"used", ua:"уживаний", uaAlt:["використовував"]}`
  дає hover рівно «уживаний» — і в реченні «He used a knife» значення дієслова ЗНИКАЄ,
  скільки б синонімів не лежало в uaAlt. Перевірено в браузері через `stemEn`+`EN_TO_UA`,
  це не здогадка.
  **Порядок дій:** (1) картка з ОДНИМ чітким глосом у `ua` (щоб UA→US лишався однозначним);
  (2) `m.set("<форма>", [усі значення, включно з базовим])` у кінці IIFE `EN_TO_UA` —
  m.set іде ПІСЛЯ пасу по WORDS і перекриває його (той самий механізм, що для `race`).
  Так зроблено для `used`, `washed`, `worn`, `looks`, `fitting`.
  ⚠️ Для `torn`/`ripped`/`stained`/`stretched`/`damaged`/`faded`/`embroidered`/`distressed`
  m.set НЕ потрібен: значення форми там **збігається** зі значенням дієслова, тож hover
  нічого не втрачає. m.set потрібен саме там, де значення **розходяться**.
  ⚠️ Окремий випадок — `roots`/`layers`: замість голої форми зроблено ОДНОЗНАЧНІ фрази
  `hair roots` і `layered haircut`. Голе «roots» як промпт «відросле коріння волосся»
  було б невгадуваним, а фраза і вгадується, і не чіпає hover для root/layer.
- Структуру WORDS array
- TAINTED-логіку в review
- Кольорову схему
- ⚠️ **Режим «📖 Незасвоєні» замість «🔀 Мікс» (сесія 34):** НЕ повертати окрему кнопку Мікс —
  мікс-механіка тепер у «Випадковий» будь-якої модалки напрямку. «Вчити» грає Вивчаю ∪ Нові
  (`toLearnIndices` = `!(s>=3)`, тобто все КРІМ «Знаю»). "mix" лишається валідним внутрішнім
  значенням, але поза ALL_MODES/MODE_BTNS. Пул свідомо БЕЗ засвоєних — не «виправляти» на всі слова.
- ⚠️ **Вітання лише в меню (сесія 34):** НЕ повертати показ milestone'ів під час гри / на екрані
  результатів. Механізм `_gameSessionActive`/`_msDeferred`/`flushDeferredMilestones` — за запитом.
- ⚠️ **Пороги досягнень — ПЕРЕГЛЯНУТО користувачем у сесії 36:** «Слів у Знаю»
  `[100,500,2000,5000,10000]` (було [1000,2500,5000,7500,10000] із сесії 23 — користувач сам
  змінив, щоб перші ліги були досяжні; Королівство = 10000 = повний словник-перспектива);
  «Правильних відповідей» `[2000,7000,15000,25000,40000]`; «Повторень»
  `[1000,5000,10000,20000,30000]`. Інші ачівка-пороги не чіпати без узгодження.
- ⚠️ **Поведінка (сесія 23):** «підгадай/глянь X» = подивитись і доповісти, НЕ змінювати.
  Нічого в проді не міняти без явного «давай»/«ок»; спершу аналіз+рекомендація.
- ⚠️ **Виняток — очевидні дрібниці (сесія 38, за прямим запитом):** «якщо є якісь такі банальні
  і явно очевидні правки — просто їх роби» (без попереднього узгодження). Це саме дрібниці:
  явна прогалина полісемії (paint був лише «малювати» без «фарба»; test лише «тестувати»),
  друкарська помилка, неузгодженість прикладу з глосом, новий правопис (проєкт). **НЕ** підпадають:
  видалення/перейменування слів, зміна промпт-глоса, нові фічі, зміни UI/сховищ, ревізія
  раніше узгоджених рішень — там і далі спершу аналіз+рекомендація і чекати «давай».
- ⚠️ **Перенесення залишку термометра (сесія 23)** (`getThermoCarryIn`/`thermoRawForDate`):
  не повертати `getThermoPointsToday` до «лише сире сьогодні» — це візуальна логіка за запитом.
- ⚠️ **selectedMode vs mode (сесія 24):** `selectedMode` = ОБРАНИЙ режим (вкл. "mix"); `mode` =
  КОНКРЕТНИЙ поточний напрям. Логіка показу/перевірки читає `mode`; Shift-цикл і запис рахунку —
  `selectedMode`. НЕ зливати назад в одну змінну (зламає мікс). `startGame` (нормальна гра) і
  рендер беруть `mode` напряму, тож вони мусять лишатись синхронні (через `setMode`).
- ⚠️ **uaAlt / enAlt (сесія 24; enAlt + чистка — сесія 30):** ДВА симетричні поля прийнятних
  відповідей, що НЕ показуються як промпт:
  • **`uaAlt`** — додаткові УКР. переклади; приймаються ЛИШЕ в en-ua (`isCorrect` en-ua: ua+uaAlt;
    `getCorrectAnswer` показує обидва). `getUa` їх НЕ повертає → **ua-en НЕ зачеплено** (uaAlt
    одного слова НЕ розблоковує англ. відповіді інших слів зі спільним перекладом).
  • **`enAlt`** — додаткові АНГЛ. відповіді (слова-синоніми); приймаються ЛИШЕ в ua-en
    (`isCorrect` ua-en: спершу `enAlt`, далі крос-логіка спільного UA; `getCorrectAnswer` додає enAlt).
  ⚠️ Сесія 30: був тимчасовий хак (uaAlt тік у ua-en через `shownUa.concat(uaAlt)`) — ВІДКОЧЕНО,
  бо ламав gorgeous (на «розкішний» зараховувалися wonderful/lovely/stunning…). Натомість enAlt.
  Не додавати чужий ОСНОВНИЙ глос прямо в `.ua` (колізія ua-en) — для цього й є uaAlt/enAlt.
  • **⚠️ ДУЖКА В ГЛОСІ НЕ ХОВАЄ КАРТКУ В UA→US — `uaBare()` (сесія 45, привід «bill»).**
    Перехресний пошук у UA→US звіряв ua-глоси ТОЧНИМИ рядками, тож картка з дисплейною
    дужкою була невидима під голим промптом: на «рахунок» не проходив `bill`
    («рахунок (до сплати)»), на «центр» — `centre`, на «програма» — `programme`, на
    «пара» — `pair`. Гола форма в `uaAlt` не рятувала: uaAlt свідомо не працює в цьому
    напрямку. З 22 карток із дужками невидимими були **17** — серед них УСІ британські
    варіанти. Тепер `isCorrect` (гілка ua-en) і `getCorrectAnswer` порівнюють через
    `uaBare(u)` = глос без « (…)». Промпт користувач і далі бачить із дужкою — вона й
    існує, щоб розрізняти. `withHint` дістав фолбек на цю ж дужку, щоб у списку
    відповідей `bill` стояв як «bill (до сплати)», а не голим поруч із поясненими.
  • **Дисплей-підказка в дужках (сесія 35, transfer):** для майже-однакових укр. слів
    (перевеСти/перевеЗти) основний глос можна писати з уточненням — `"перевести (гроші)"` —
    воно видно в промпті/correction/hover і знімає плутанину. ⚠️ Точний матчинг означає, що
    рядок із дужками ніхто не набере → ГОЛУ форму («перевести») ОБОВ'ЯЗКОВО додати в uaAlt.
    У uaAlt дужки НЕ писати взагалі (мертвий запис — його неможливо ввести).
  • **⚠️ ВЗАЄМНІ ПАРИ-СИНОНІМИ ЧЕРЕЗ `enAlt` — І ЧОМУ ЇХ ЛИШЕ 20 (сесія 45, «пройдись
    такими парами гуртом»).** Привід: у UA→US промпт «розділ» (від `chapter`) не приймав
    `section`, а «чіткий» (від `lucid`) — `clear`, хоча англійською це правильні відповіді.
    ⚠️ **Порахував перед тим, як робити: таких пар 3591** (глос у `uaAlt` одного слова =
    основний глос іншого). Прийняти їх гуртом = відтворити витік сесії 30 у більшому
    масштабі. Фільтр за `SYNS` теж не рятує — серед його 128 «підтверджених» лежить
    «гарний» (beautiful) ← `gorgeous`, тобто рівно той випадок, який користувач забракував.
    **Критерій, який лишився:** ВЗАЄМНІСТЬ — обидва слова визнають глос одне одного в
    `uaAlt` І стоять поруч у `SYNS` (патерн `idle`↔`lazy`). Таких **20**, і більшість уже
    мала `enAlt`; досиметризовано 13 напрямків: cry↔sob, slow↔sluggish, clear↔lucid,
    shy↔timid, want→wish, reply→answer, famous→celebrated, silly→stupid, colossal→huge.
    ⚠️ НЕ розширювати цей список «бо теж синоніми» — межу тримають контрольні перевірки
    в `test-homograph.js` (на «красивий» НЕ проходить gorgeous, на «темний» — gloomy, на
    «великий» — great, на «ясний» — transparent). Решту випадків лікуємо ТОЧКОВО, коли
    користувач на них натрапляє в грі, а не суцільним проходом.
  • **⚠️ ПАРИ З ОДНАКОВИМ EN — US→UA ПРИЙМАЄ ОБИДВА СЕНСИ (сесія 45, за запитом):**
    у словнику **23 слова мають по дві картки** (match=матч/сірник, run, spring, nail,
    orange, bank, right, light, hot, cool, kind, degree, fair, patient, fine, present,
    content, board, noble, cafe, fan, square, tip). У US→UA промпт — САМЕ англійське
    слово, тож він фізично не каже, який сенс питають: на «match» відповідь «матч»
    правильна, хоч би яка картка випала. Тому `isCorrect` en-ua приймає ua+uaAlt
    **будь-якого запису з тим самим ключем** (`sameKeyWords(wordKey(currentWord))` —
    окрема мемоізована мапа біля `indexOfWordKey`; сам `_keyIdx` не годиться, він тримає
    лише ПЕРШИЙ індекс на ключ). ⚠️ **UA→US НЕ ЗАЧЕПЛЕНО** — там промпт український
    («сірник» і далі вимагає match), сенси лишаються розведеними, як домовлялись.
    Mastery теж не зачеплено: пара ділить один `wordKey`. WORDS НЕ чіпали — жодних
    хрестових uaAlt, панель правильної відповіді показує сенс показаної картки.
    Перевіряє `test-homograph.js`. НЕ «спрощувати» назад до глосів однієї картки.
  • **⚠️ НЕПРАВИЛЬНІ ФОРМИ ЯК ПРОМПТ (сесія 45, привід «saw»).** 37 карток є ще й формою
    іншого слова: `saw` = пилка й водночас минулий час `see`, `rose` = троянда ⟵ `rise`,
    `spoke` = спиця ⟵ `speak`, `rung` = щабель ⟵ `ring`, `broke` = без грошей ⟵ `break`,
    `teeth` ⟵ `tooth`. У US→UA промптом стоїть така форма, тож приймаються й глоси
    БАЗОВОГО слова — через наявну мапу `IRREGULAR` (доти вона працювала лише на ховер).
    ⚠️ **Односторонньо:** промпт `see` НЕ приймає «пилка» — він однозначний.
    ⚠️ **Це НЕ українська морфологія:** приймається глос, як він лежить у словнику
    («бачити»), а не «бачив». Форми минулого часу глосами не бувають — правило сесії 40,
    воно лишається. Саме тому найплутанішим карткам (saw/rose/spoke/rung/broke/running)
    додано HINTS-підказку «не форма see» тощо: гра пояснює фокус у рядку відповіді,
    замість того щоб мовчки зараховувати чи мовчки відхиляти.
  • **⚠️ ПАРИ «X» / «to X» — ТЕ САМЕ, АЛЕ АСИМЕТРИЧНО (сесія 45, привід «mean»).**
    43 слова мають окремі картки для іменника/прикметника й дієслова: `mean = підлий`
    vs `to mean = означати`, `book = книга` vs `to book = бронювати`, watch, play, lie,
    love, ring, fall… ⚠️ `sameKeyWords` їх НЕ зв'язує (ключі різні: «mean» ≠ «to mean»),
    тому в US→UA на промпт «mean» відповідь «означати» зараховувалась як помилка.
    **Правило:** промпт-ГОЛЕ слово не називає частини мови → приймаємо глоси й
    дієслівної картки (`sameKeyWords(k).concat(sameKeyWords("to "+k))`).
    **Промпт «to X» — НАВПАКИ, лишається розведеним**: «to» вже сам каже, що це
    дієслово, тож «книга» на «to book» — чесна помилка. Асиметрія НАВМИСНА, це не
    недогляд; обидва боки закриті перевірками (тест падає і якщо звузити правило назад,
    і якщо зробити його симетричним).
  • **⚠️ СКЛАДЕНИЙ ГЛОС «X АБО Y» — ЗАВЖДИ З ПОЛОВИНКАМИ В `uaAlt` (сесія 50).**
    `single parent` = «одинокий батько або мати», `great-grandparent` = «прадідусь або
    прабабуся», `stepparent`, `godparent`, `only child` = «єдина дитина в сім'ї» —
    такі картки вимагали набрати весь глос цілком, хоча природна відповідь це одна
    половина. Половинки йдуть у `uaAlt` (діють у US→UA), основний глос НЕ міняється —
    він лишається промптом, бо однозначно описує картку. Додавай половинки ОДРАЗУ,
    коли заводиш складений глос.
  • **⚠️ ПРАВИЛА ВІДБОРУ uaAlt (сесія 39, за запитом — суцільний прохід словника):**
    ДОДАЄМО **лексично інші** слова (брати→взяти, love→кохати, зникати→щезати) та **інші сенси**
    (work→робота, letter→літера, seal→тюлень, court→залицятися). **НЕ ДОДАЄМО** морфологічні
    варіанти того самого кореня — видові пари й префіксальні форми (писати/написати,
    відчиняти/відчинити, телефонувати/зателефонувати): за словами користувача це «перебір
    кількох букв», а не інший спосіб сказати. Іменникові сенси до дієслівних карток беремо,
    якщо слово реально вживається іменником. НЕ «доповнювати» пари-омографи (light/run/spring/
    bank/watch/present/board/fine/match/tip…) — у них сенси розведені навмисно.
  • **⚠️ КРИТЕРІЙ «ЧИ КОРИСТУВАЧ ЦЕ НАБЕРЕ» (сесія 40, після чистки 32 варіантів):**
    `uaAlt` НЕ впливає на правильність — він лише ДОДАЄ прийняті відповіді в en-ua і **ПОКАЗУЄТЬСЯ
    в панелі правильної відповіді разом із `ua`**. Тому питання не «чи це правильне українське
    слово» (майже всі правильні), а **«чи користувач колись це набере і чи не заважає на екрані»**.
    Саме через показ у панелі його дратував «розмаїтий», хоч той лежав лише в uaAlt.
    НЕ ДОДАЄМО: книжні/застарілі (звитяга, небіж, вивірка, зубожілий, уста), росіянізми
    (юбка, чулки, чемодан, заколка, варежка), транслітерації англійських слів українськими
    літерами (тішотка, онезі, лептоп, есемес), безособові форми на -но при наявних прикметниках
    (відчинено при відчинений), форми минулого часу як глос (ридала, заблукав).
    ДОДАЄМО живі запозичення й регіоналізми, які реально звучать (ровер, горнятко, деко, софт,
    байк, тінейджер, фест, документалка, лофери, скраби) — їх користувач залишив свідомо.
- ⚠️ **ВІТАННЯ «✨ УСІ ПОМИЛКИ ЗАКРИТІ» — ВИДАЛЕНЕ (сесія 50, прямий запит).** НЕ
  повертати ні тип milestone'а `cleared`, ні `sfxCleared`, ні CSS `.ms-box.cleared`.
  Причина користувача: борг (`oxford_due_v1`) сам замішує помилки в ігри наступного
  дня, тож порожній список — звичайний стан, а не досягнення. Це третій крок тієї ж
  лінії, що сесії 45 (кнопка «📒 Помилки») і 47 (кнопка «📚 Повторив»): помилками
  керує ВИКЛЮЧНО борг, окремих лічильників/входів/вітань під це не заводимо.
  ⚠️ **`allDayMistakeKeys()` і `oxford_day_mistakes_v1` при цьому ЛИШАЮТЬСЯ** —
  живих читачів у них нема, але їх кличуть одноразові міграції боргу
  (`seedDueFromMistakes`, `fixDuePollutedByReview`), а на новому пристрої прапорці
  порожні. Це той самий патерн «заморожено, не стерто», що з `oxford_mistake_reviews_v1`.
- ⚠️ **АМЕРИКАНСЬКА НОРМА В НАПИСАННІ (сесія 51):** основне `en` картки — американське,
  британське живе в `enAlt`. Тримає `check-brit.js` у `verify.sh`. НЕ заводити картку з
  британським ключем; свідомий виняток — `cheque` (амер. `check` зайняте дієсловом
  «перевіряти»), він у списку основ вартового СВІДОМО відсутній.
- ⚠️ **`isCefr` vs `outOfRank` у `renderGameRow` (сесія 51) — НЕ зливати в одну змінну.**
  «Поза рейтингом» — це і CEFR-ігри, і пул «не дались», а от ПІДПИС у них різний. Злиті
  в один прапорець, вони давали `"wrong".slice(5)` === "" → голий «🎓 ·  дата» замість
  «🔁 Не дались». Баг прожив із сесії 42 і його помітив користувач.
- ⚠️ **Shift у меню НЕ відкриває «Тренування» (сесія 51)** — він гортає період вкладки 🕒.
  Гру відкривають Enter і клік. Деталі й три ролі клавіші — у «Гарячих клавішах».
- ⚠️ **ДЕФІС ≡ ПРОБІЛ — ЛИШЕ ПО УКРАЇНСЬКОМУ БОКУ (сесія 50):** зведення живе в
  `normUaSoft` (гілка en-ua), `normEn` строгий. Поширити на англійську = зламати
  19 пар, де дефіс розрізняє сенс (`pick up`/`pick-up`, `check-in`, `take-off`).
  Злити `normUa` і `normUaSoft` в одну функцію = те саме протікання через `inpNorm`.
  ⚠️ **Те саме стосується всіх чотирьох правил родини** — дефіс, кома (52), у≡в (53),
  сполучник й/і/та (56): усі живуть у `normUaSoft`, усі лише en-ua. Кожне нове правило
  зведення — СПОЧАТКУ замір злиттів на реальних даних, потім код (методика сесії 50).
- ⚠️ **Milestone'и/місячна ачівка за термометром (сесія 24)** — НЕ повертати milestone'и на
  лічильник ігор; деталі — розділи «Milestone-вітання» та «Leaderboard → Досягнення».
- ⚠️ **Кнопка GPT — навігація ≠ підрахунок (сесія 25, уточнено 30):** ГОЛОВНА «🤖 GPT» (меню)
  ЛИШЕ відкриває ChatGPT (навігація, НЕ рахує). Рахують: «+» (будь-коли) і **фінал-екранний
  `#gpt-link`** (клік = реальний запит під час розбору помилки, +10 XP — сесія 30). Спільна
  `countGptRequest()`. НЕ зливати ГОЛОВНУ кнопку меню у «відкрити+порахувати».
- ⚠️ **КНОПКА «📒 ПОМИЛКИ» І ПУНКТИ ЗА ЗАКРИТІ СЛОВА — ВИДАЛЕНІ (сесія 45, прямий запит).**
  НЕ відновлювати ні кнопку `#review-today-btn`, ні прапор `reviewFromMenu`, ні правило
  «15 закритих = 1 пункт «Плани»». Помилкові слова повертає в ігри ВИКЛЮЧНО борг
  (`oxford_due_v1`) — окремого лічильника й окремого входу під це бути не повинно.
  Наслідки, які теж не «виправляти» назад: `addXp(2)` за закрите слово повторення тепер
  безумовний (правило подвійної оплати з сесії 41 втратило сенс — другого джерела нема);
  `oxford_mistake_reviews_v1` заморожений, але читається; `removeDayMistake` стоїть поруч
  із `clearDue` (інакше «відкритий борг» ріс би вічно). Деталі — розділ «🗑 Сесія 45».
- ⚠️ **Повторення з ФІНАЛЬНОГО екрана — ЗАВЖДИ мікс (сесія 24):** `startReview` ставить
  `reviewDir="random"` і НЕ чіпає `selectedMode` (лише стартовий `mode`); по завершенні
  `displayEndScreen` робить `setMode(selectedMode)` — мікс не «прилипає». ⚠️ Другий флоу
  (старт-екранна «📒 Помилки (N)» з модалкою напряму, `allDayMistakeKeys` за всі дні)
  ВИДАЛЕНО в сесії 45 — post-game повторення лишилось єдиним.
- XP-логіку в `updateMastery` / `recordAnswer` — баги перенарахування виправлено в сесії 8,
  логіку +3 уточнено в сесії 9 (див. розділ XP). Тепер: +3 за **будь-яку** першу зустріч
  слова (`prev===undefined`, правильна/неправильна/пропуск) — рівно раз на слово; review дає
  +1 за закрите слово. Не «відкочувати» ці фікси — зокрема, **+3 у `else`-гілці `updateMastery`
  додано свідомо** (за бажанням користувача), це не баг.
- Логіку mastery категоризацій (`if (s >= 3) known++; else learning++`) — streak 0 = Вивчаю, це правильно
- ⚠️ **Лічильники ✓/✗ у mastery (сесія 40):** значення запису = число АБО `{s,c,w}` — читати
  ТІЛЬКИ через `mStreak`/`mTally` (голе порівняння з числом дасть false на об'єкті). Категорії,
  XP і дельти рахуються виключно по `s`; c/w — довідковий шар. НЕ підключати лічильники до
  повторень / «📒 Помилки» (за прямим рішенням користувача: там уже проваленe слово, це лише
  засвоєння). Міграції немає СВІДОМО — старий формат читається як є.
- ⚠️ **Формат mastery/помилок = ключ-СЛОВО** (`wordKey=getEn[0]`), НЕ позиція (сесія 18).
  Не повертати на позиційні індекси — це ламалось при вставці слів. `updateMastery`,
  `categoryIndices`, `add/removeDayMistake` приймають index і конвертують у слово; review
  «за день» через `indexOfWordKey`. Міграція `migrateMasteryToWordKeys` скидає старий формат.
- ⚠️ **CSS-пастка (сесія 20):** ставлячи `display:` на елемент через **ID-селектор**
  (напр. `#progress-panel { display:flex }`), якщо цей елемент ховається класом `.hidden` —
  ОБОВ'ЯЗКОВО додай парне `#id.hidden { display:none }`. ID (специфічність 1,0,0) перебиває
  `.hidden` (0,1,0), інакше клас `hidden` НЕ ховає: toggle/Alt/старт гри ставлять клас, а CSS
  його ігнорує. Патерн у файлі: `#thermo.hidden`, `#cat-modal.hidden`, `#lh-screen.hidden`,
  `#songs-overlay.hidden`, `#progress-panel.hidden`, `#bottom-left-btns.hidden`.

---

## Контакти / Особа
- Email: vovabestintheworld@gmail.com
- Рівень англійської: A2-B1, **ціль B2–C2** (уточнено сесія 30). Словник свідомо
  розширюється до **~10 000 слів** (перспектива) — тож при відборі нової лексики беремо не
  лише базове, а й помірно-спеціалізоване, корисне на B2–C2; пропускаємо лише зовсім вузьке
  (латина/терміни поза загальним вжитком).
- Грає щодня, активний продакшен
- Цінує: гарячі клавіші, мінімальний UI, естетику
- Спілкування: українською

---

## Версія документа
- Створено: 2026-05-26
- Останнє оновлення: 2026-09-10 (сесія 57; **WORDS 13766 · EXPRESSIONS 1293 · HINTS 1481**)

- **📌 ПІДСУМОК СЕСІЇ 57 одним блоком.** Сесія БЕЗ словника — усе про механіку боргу й
  лідерборд. **3 коміти.** Спільна нитка всіх трьох правок: фіча була ЗАДЕКЛАРОВАНА, але
  на реальних даних не спрацьовувала, і дізнались про це з гри, а не з тестів.

  | # | Що | Привід користувача |
  |---|---|---|
  | 1 | **Борг: гарантія «у 5-й грі точно»** — `waited` рахує ІГРИ, а не вибори; примусове слово в перші питання черги; ліміт трьох віддається прострочeним | «зіграно 6 ігор, а слово все висить» |
  | 2 | **Чип 🕒 «500 ігор» → «За весь час»** (`kind:"all"`, міграція збереженого `"g500"`) | «500 ігор заміни на "за весь час"» |
  | 3 | **Кап журналу ігор 5000 → 20000 + запис, стійкий до квоти** | «що за стеля, це необхідний максимум?» |
  | 4 | **HINTS про обов'язкову -s** — 10 карток: `crossroads`, `damaged goods`, `headquarters`, `binoculars`, `tweezers`, `customs`, `species`, `series`, `clothes`, `premises` | «там обов'язково має бути S вкінці?» |
  | 5 | **`damaged goods` → «пошкоджені товари»**, стара однина в `uaAlt`, приклад теж у множину | «а тут не конфліктує однина і множина?» |
  | 6 | **+1 картка `galaxy`** (B2) — єдина прогалина в астрономічному наборі LingoHut | скрін уроку, «всі є?» |
  | 7 | `to pop` **+«ляснути»** — доконаний вид до наявного «ляскати» | «ляснути» пішло у ✗ |
  | 8 | **Два довгі глоси скорочено:** `ankle boots` → «ботильйони», `babysitter` → «тимчасова няня»; описові формулювання в `uaAlt`, різницю тримає HINTS | «у нас так ніхто не говорить», «хіба тут потрібне аж таке довге формулювання?» |
  | 9 | `career path` — підказка про кальку `career way` | «тут мій варіант не підійде?» |
  | 10 | **Пачка підказок проти кальок — 17 карток** | «давай ще пачку підказок для калькованих сполучень» |

  **⚠️ ЯК ШУКАЛИСЬ КАЛЬКИ (сесія 57) — щоб не шукати наосліп наступного разу.**
  Спершу пройшовся дієслівними сполученнями `to make/take/do/pay a …` — і виявив,
  що таких карток у словнику майже НЕМА: він побудований на іменникових фразах,
  а `to make a decision`, `to take a photo`, `to pay attention` відсутні як картки.
  Тож улов дали ДВА інші шаблони: (1) прикметник+іменник, де українська підказує
  не той прикметник — `heavy rain` ← «сильний», `thick hair` ← «густе»,
  `strong password` ← «надійний», `low water pressure` ← «слабкий»,
  `heavy traffic` ← «інтенсивний»; (2) складені назви, де послівний переклад дає
  неіснуючу фразу — `ambulance` ← «швидка допомога», `kindergarten` ← «дитячий садок»,
  `marital status` ← «сімейний стан», `dead end` ← «глухий кут», `high beam` ←
  «дальнє світло», `traffic jam` ← «пробка» проти `cork`.
  ⚠️ Пари, де українське слово тягне В РІЗНІ боки, підказуються З ОБОХ боків:
  `heavy rain` і `strong wind` посилаються одна на одну — патерн `on time` / `in time`.

  **⚠️ ОПИСОВИЙ ГЛОС — ОСТАННІЙ ЗАСІБ, А НЕ СПОСІБ РОЗВЕСТИ ПАРУ (сесія 57).** Обидва
  довгі глоси народились однаково: поруч стояла картка з коротким глосом (`nanny` «няня»,
  родина `boots`), і різницю дописали В САМ ГЛОС. Наслідок — промпт, якого користувач
  ніколи не набере, і зіпсований переклад прикладу: «Ми знайшли няню **на короткий час
  на вечір**». Правильний порядок: коротке живе слово в `ua` → описове формулювання в
  `uaAlt` → різниця в `HINTS`. Та сама лінія, що `weekend break` у сесії 55.
  ⚠️ Живе запозичення краще за книжний опис (`ботильйони`, як `лофери`/`скраби` сесії 40),
  але транслітерація самого англійського слова — ні: `бебісітер` свідомо НЕ додавали.

  **⚠️ КАЛЬКА ≠ ПРОГАЛИНА СЛОВНИКА.** `career way`, `damaged good`, `crossroad` — це не
  «мій варіант теж правильний», а хибні форми, зліплені з наших же карток (`way` «шлях»).
  Такі випадки лікуються ПІДКАЗКОЮ, а не розширенням `enAlt`: приймати їх означало б
  закріплювати помилку. `career track` теж не додавали — користувач його не набирав,
  а розширювати прийняті відповіді гуртом ми відмовились ще в сесії 45.

  **⚠️ ПРАВИЛО: УКРАЇНСЬКА ОДНИНА ПРИ АНГЛІЙСЬКІЙ ФОРМІ НА -s = ПАСТКА (сесія 57).**
  Глос-промпт в однині прямо підказує набрати форму без -s, якої або не існує
  (`headquarter`, `serie`), або яка означає ІНШЕ (`custom` звичай, `cloth` тканина,
  `specie` монети, `crossroad` бічна дорога, `premise` засновок). Лікуємо НЕ через `enAlt`
  — приймати `crossroad` означало б учити хибної форми — а через HINTS у рядку відповіді.
  `damaged goods` був ще й неузгоджений із власною родиною: `goods` «товари`,
  `prohibited goods` «заборонені товари», `restricted goods`, `to declare goods` — усі
  в множині, і лише він в однині. Тому тут глос змінено, а не лише додано підказку.
  ⚠️ Картки, де УКРАЇНСЬКОЮ теж множина, пасткою НЕ є і підказки не потребують
  (ножиці, джинси, окуляри, товари, сходи) — рука сама пише -s.

  **⚠️ ГОЛОВНИЙ УРОК СЕСІЇ — «ВИБІР ≠ ПОКАЗ».** Примусове підмішування боргу було в коді
  з сесії 44 і виглядало робочим: `waited >= DUE_FORCE_AFTER` → беремо слово. Але лічильник
  скидався на сам ФАКТ вибору, а вибране слово лягало в чергу на позицію 1-12 — і за 60
  секунд могло не з'явитись на екрані. Слово, якого користувач не бачив, щоразу починало
  відлік заново: гарантія не спрацьовувала НІКОЛИ, а тести цього не ловили, бо перевіряли
  `pickDueIndices` у відриві від таймера. Мораль на майбутнє: якщо лічильник вимірює
  «скільки разів користувач чогось НЕ отримав» — рахуй ігри (події, які точно сталися),
  а не наміри коду; і клади примусове туди, куди 60 секунд гарантовано докручують.

  **Тести:** `test-due.js` 40 → **44**, `test-lb-period.js` 55 → **62** (кап журналу
  й поведінка при переповненій квоті — через підміну `Storage.prototype.setItem`).

  **📦 Блок сесії 54 переїхав у `handoff-log.md`** (довідник тримає дві останні сесії):
  `handoff.md` 413 → 383 КБ.

- **📌 ПІДСУМОК СЕСІЇ 55 одним блоком.** Сесія за фідбеком із гри (десяток скрінів картки
  з питанням «чи підійде мій варіант?») + **дві пачки вузьких глосів**. **11 комітів.**
  Продовження наскрізної теми сесій 53-54, але з ВАЖЛИВИМ уточненням: не кожне «мій
  варіант правильний» лікується `uaAlt` — див. правило про три діагнози нижче.

  | # | Що | Привід користувача |
  |---|---|---|
  | 1 | `campsite` +5 варіантів, `tennis shoes` / `running shoes` / `gym shoes` +7 | «майданчик для кемпінгу піде?», «тут лише цей варіант підійде?» |
  | 2 | **+1 картка `seasickness`** (B1) + `seasick` розведено HINTS'ами, `motion sickness` +2 | скрін `seasick` ← «морська хвороба» |
  | 3 | `flirt` +4 короткі варіанти, HINTS парі `flirt`/`flirting`, виправлено кальку в прикладі | «тут просто "флірт" не підійде?» |
  | 4 | `wafer`: прибрано тавтологію «thin wafer» у прикладі, HINTS парі `wafer`/`waffle` | «якщо wafer це тонка вафля, тоді для чого добавляти thin» |
  | 5 | `to pop` / `to burst`: доконаний вид, HINTS розводить пару | «лопнути піде? а проколоти?» |
  | 6 | `prisoner` / `captive`: HINTS групі, прибрано тавтологію | «заручник піде?» / «купець піде?» |
  | 7 | `urban`/`rural`/`suburban area`, `canned fish`/`food` +11 | «міська зона», «консервована риба» |
  | 8 | `lost passport`/`luggage`/`parcel` +6 | «втрачений паспорт» |
  | 9 | `gate number` +3, `gate` ← «гейт» | «можна додати простіший варіант?» |
  | 10 | **Вузькі глоси, пачка 6** — 85 карток, +103 варіанти | «давай наступну пачку» |
  | 11 | **Вузькі глоси, пачка 7** — 65 карток, +83 варіанти | «давай ще одну пачку» |
  | 12 | `steering` +3 («рульове керування» — термін із ПДР) | «рульове керування підійде?» |
  | 13 | `warranty period` +3 (неповний ряд строк/термін/період) | «гарантійний період піде?» |
  | 14 | **Вузькі глоси, пачка 8** — 81 картка, +89 варіантів | «давай ще одну пачку» |
  | 15 | **`handoff.md` розділено** на довідник + `handoff-log.md`, 688 → 392 КБ | «уже з'їло 455к токенів» |
  | 16 | `public event` +3; **`weekend break` — глос скорочено** до «поїздка на вихідні» | «я б вкоротив переклад» |
  | 17 | `compass point` **+enAlt** cardinal direction / cardinal point | «сторона світу це хіба не quarter?» |
  | 18 | `to mute` +4, `police station` +2, `paragliding` +2 | «піде?» × 3 |
  | 19 | **Скан транслітерацій** — 10 карток, +11 питомих відповідників | «прожени сканом транслітерації» |
  | 20 | **Ховер бере `ua` + `uaAlt`** — 115 речень показували не той сенс | «stored це ж зберігатися?» |
  | 21 | **Термометр: пройдені проміжні цифри теж жовті** | «щоб і решта цифр підсвічувалася» |
  | 22 | **Повторення нічого не фіксує** — знято `recordReviewStats` і +2 XP | «нехай таймер тікає і ні на що не впливає» |
  | 23 | **Сполучник «й/і/та» більше не обов'язковий** у відповіді (70 глосів) | `квиток туди-назад` ← ✗ |
  | 24 | `absorbed` +3, HINTS до `on time`, `paintballing` ← paintball, `return ticket`/`round trip` | фідбек із гри |
  | 25 | **Прибрано картку `hip joint`** (B1) — вузький анатомічний термін | «в загальному вжитку не трапляється» |
  | 26 | **Trim розлитих глосів ×4:** `lift`, `to mute`, `flirt`, `captive`/`prisoner` | «закрий усі чотири» |
  | 27 | **+картка `in time`** (A2), прибрано перекриття `m.set("fire")` | «закрий ті два теж» |
  | 28 | `to pull out` +5 сенсів, `floor mat` +enAlt, `rear`/`side window`, готельні номери +10 | «мій варіант підійде?» × 4 |

- **⚠️ ГОЛОВНИЙ УРОК СЕСІЇ: «мій варіант не прийняло» має ТРИ різні діагнози, і лік
  у кожного інший.** Раніше в handoff був описаний лише перший.
  1. **Інша форма ТОГО САМОГО** (вузький глос багатослівної картки) → `uaAlt`.
     Так пішли `campsite` («майданчик для кемпінгу» при глосі «кемпінг-майданчик» —
     ті самі два слова навпаки) і `tennis shoes` («тенісне взуття» при «тенісні кросівки»).
  2. **Інше слово, якого в словнику НЕМА** → нова КАРТКА, а не `uaAlt`.
     `seasick` = прикметник («з морською хворобою»), а користувач написав `морська хвороба` —
     це іменник `seasickness`, якого в базі не було ВЗАГАЛІ. Запхати «морська хвороба»
     в `seasick.uaAlt` було б рівно тією помилкою, проти якої сесія 54 відхилила
     `rainfall` ← «дощ»: дві різні лексеми злились би в одну картку.
  3. **Інше слово, яке в словнику Є** → нічого не робимо, ✗ чесний (див. `camp` нижче).
  **Питання, яке розрізняє випадки:** «те, що написав користувач — це той самий
  ДЕНОТАТ іншими словами, чи інша лексема?» Якщо інша лексема — шукай її в базі,
  і лише потім вирішуй.
  **⚠️ Випадок №3 траплявся за сесію ЧОТИРИ рази, і щоразу користувач мав рацію
  лексично, але не за карткою:** «кемпінг»/«табір» → `camp`; «флірт» → `flirting`;
  «заручник» → `hostage`; «купець» → `merchant`. У всіх чотирьох правильна реакція —
  НЕ додавати варіант, а **пояснити різницю через `HINTS`**, щоб гра сама показала,
  куди дівся той варіант. Саме так закрито `flirt`/`flirting`, `prisoner`/`captive`,
  `seasick`/`seasickness`, `to pop`/`to burst`, `wafer`/`waffle`.
  ⚠️ Формально це РОЗШИРЕННЯ правила «підказка лише коли за глос конкурує інше англ.
  слово»: тут конкуренція не за глос, а **за помилку користувача**. Такий клас
  анотувати варто — це той самий патерн, що підказки «не форма see» в сесії 45.

- **🏕 `campsite` +5 варіантів `uaAlt`:** майданчик для кемпінгу · місце для кемпінгу ·
  місце для наметів · кемпінгова стоянка · стоянка для наметів.
  ⚠️ **«кемпінг» і «табір» СВІДОМО НЕ додані** — це основні глоси картки `camp`
  (`ua:["табір","кемпінг"]`, dict.js:705). Це випадок №3 вище: різниця «подія/група»
  ↔ «місце» реальна, і саме її картка тренує.

- **👟 Трійка спортивного взуття (той самий однорідний дефект, долито гуртом):**
  `tennis shoes` ← тенісне взуття, кросівки для тенісу ·
  `running shoes` ← кросівки для бігу, взуття для бігу ·
  `gym shoes` ← взуття для спортзалу, кросівки для спортзалу, взуття для фізкультури.
  ⚠️ «тенісні кеди» НЕ додавав — кеди це `plimsolls`/`canvas shoes`, збивало б.

- **🚢 `seasickness` — нова картка B1** («морська хвороба», +`uaAlt` «заколисування на морі»).
  - Рівень **вручну**: слова немає в `oxford-cefr.json`, узятий за парою `seasick` (там B1).
  - `seasick` глос НЕ мінявся (лишається промптом), +3 варіанти прикметника:
    якого нудить у морі · заколисаний морем · якого закачало в морі.
  - **Обом карткам дано HINTS**, і це той самий патерн, що для `saw`/`rose` у сесії 45 —
    гра ПОЯСНЮЄ фокус у рядку відповіді, замість мовчки відхиляти:
    `seasick` → «прикметник — про людину, яку заколисало; сама хвороба — seasickness»,
    `seasickness` → «іменник — назва стану; про людину — seasick».
    ⚠️ Формально це виняток із правила «підказка лише коли за глос конкурує інше англ.
    слово» (глоси тут різні: «з морською хворобою» vs «морська хвороба»). Конкуренція
    не за глос, а **за помилку користувача** — саме цей клас і варто анотувати.
  - `motion sickness` заразом ← «заколисування», «закачування в транспорті»
    (був лише «заколисування в транспорті» знак-у-знак).

- **📦 ВУЗЬКІ ГЛОСИ, ПАЧКИ 6, 7 і 8 (сесія 55) — 231 картка, +275 варіантів.**
  Робочий пул (багатослівний ключ, глос 2-3 слова, без `uaAlt`) **2205 → 1974**.
  Пачка 6 — тіло й здоров'я, дім, кухня, сантехніка; пачка 7 — взуття, сумки, тканини,
  крій і посадка, догляд за одягом; пачка 8 — їжа й напої, харчування, емоції,
  стосунки, робота.
  - **⚠️ ПАЧКИ 6-7 РОБИЛИСЬ СОЛО — ВОРКФЛОУ ВПАВ НА ЛІМІТІ СЕСІЇ** (усі 7 агентів,
    HTTP 429 «session limit»). Пачка 8 через кілька годин пішла воркфлоу нормально.
    **Висновок на майбутнє:** ліміт скидається, тож упалий воркфлоу — привід зачекати,
    а не переходити на соло: дві соло-пачки з'їли стільки ж контексту головної сесії,
    скільки вся решта сесії, тоді як у воркфлоу агенти самі грепають `dict.js` і до
    мого аудиту доходить уже чисте.
  - **⚠️ МЕХАНІЧНИЙ АУДИТ ПЕРЕД ВСТАВКОЮ ВІДПРАЦЮВАВ УСІ ТРИ РАЗИ** (0 фатальних;
    1 + 1 + 2 попередження). Скрипт перевіряє п'ять речей: дужки, тавтологія з власним
    глосом, **фраза, що входить у власний глос** (лазівка сесії 46), збіг із основним
    глосом ІНШОЇ картки, абревіатура в нижньому регістрі. Усі чотири попередження були
    по ділу, і рішення щоразу різне — саме тому цей крок не автоматизується:
    - `clogged drain` ← «забитий злив» = глос `blocked drain`. Правило H: це РІЗНІ
      англійські слова, справжні синоніми → варіант ЛИШЕНО і дописано взаємно;
    - `base layer` ← «термобілизна» = глос `thermals` → та сама річ, ЗНЯТО;
    - `sexual attraction` ← «статевий потяг» = глос `sex drive` → ЗНЯТО (лібідо ≠
      потяг до конкретної людини, тобто варіант ще й просто неточний);
    - `birth control` ← «контрацепція» = глос `contraception` → ЗНЯТО (синоніми).
    ⚠️ **Агент може лишити варіант, який аудит потім знімає** — у пачці 8 обидва зняті
    варіанти шукач свідомо лишив «за правилом H». Аудит НЕ довіряй агентові на слово:
    він працює з текстом, а колізію видно лише в даних.
  - **Осі цих пачок:** живе розмовне (шкірянка, джинсовка, метрика, акустика, вентшахта,
    баул, стрейч, розмірна сітка, собачка блискавки, мінералка, газованка, шоколадка),
    дублети запозичень (екошкіра/еко-шкіра, кондиціювання/кондиціонування), синонім
    головного слова (голосові складки, різкий біль, капітальна стіна, напір води,
    високий каблук), перефразування (рама вікна, труба водопроводу, система травлення,
    з високою талією, хатні справи, проблеми з довірою), «без-» замість «-free»
    (безглютеновий, безлактозний, безмолочний).
  - **⚠️ ВІДКРИТИЙ ХВІСТ — СІМ ПАР-КАНДИДАТІВ НА ЗЛИТТЯ, не чіпані:**
    ```
    relaxed fit «вільна посадка»   ↔ loose fit «вільний крій»
    base layer «базовий шар одягу» ↔ thermals «термобілизна»
    use-by date                    ↔ expiration date
    argument                       ↔ verbal argument
    household chores               ↔ housework          ⚠️ уже взаємозамінні
    emergency contraception        ↔ morning-after pill
    job offer                      ↔ formal job offer
    ```
    ⚠️ `household chores` ↔ `housework` — **не пропозиція, а наявна патологія:**
    «домашні справи» стоїть у `housework.uaAlt` і дослівно дорівнює основному глосу
    `household chores`, тобто той самий рядок уже зараховується обом карткам. Той самий
    клас, що `tape`/`adhesive tape` і `prepayment`/`advance payment` у сесії 52 —
    варто розібрати окремою задачею разом із рештою.
  - **Що СВІДОМО пропускалось (≈половина зрізу — це норма, не недоробка):** картки без
    природних альтернатив («лак для волосся», «датчик диму», «зелений чай», «сонячна
    система») і варіанти, заборонені правилами: «гардеробна» до `walk-in closet`,
    «перекачаний» до `muscle-bound` (входять у власний глос), голі головні слова
    («індекс», «манекен», «біржа», «замикання», «плавки»).

- **⚠️ ІНСТРУМЕНТАЛЬНЕ: `npm i playwright-core` ПОТРІБЕН У КОЖНОМУ НОВОМУ КОНТЕЙНЕРІ.**
  Браузерні набори (`test-hints.js` та ін.) без нього просто пишуть «playwright-core не
  знайдено» і **виходять з кодом 0** — тобто мовчки НЕ перевіряють нічого. Chromium уже
  лежить у `/opt/pw-browsers`, ставиться тільки драйвер. Після встановлення
  `test-hints` дав 49/49.

- **⚠️ ГІТ: ЛОКАЛЬНА `main` У КОНТЕЙНЕРІ БУЛА ДИВЕРГЕНТНА — 50 комітів проти 50.**
  Сесія стартувала на гілці `claude/handoff-reread-a0w9nj`, яка збігалася з актуальним
  `origin/main`; а локальна `main` вказувала на СТАРУ лінію (`110ebb5`), бо історію на
  GitHub колись переписали (`git fetch` показав `+ 110ebb5...9770d46 main (forced update)`).
  **Що робити, якщо побачиш це знову:** НЕ пулити й НЕ мержити локальну `main` —
  комітити поверх поточного HEAD (він і є актуальний прод) і пушити
  `git push origin HEAD:main` (fast-forward). Локальну `main` не чіпати.
  ⚠️ **Stop-хук лається на непушену сесійну гілку `claude/...`** навіть коли коміт уже
  в `main` — лік у тому, щоб пушнути й гілку: `git push origin HEAD:claude/<назва>`.

- **🩹 ТАВТОЛОГІЧНІ ПІДКАЗКИ І ТАВТОЛОГІЧНІ ПРИКЛАДИ — ОКРЕМИЙ КЛАС ДЕФЕКТУ (сесія 55).**
  Знайдено ПОБІЧНО, жоден чекер їх не бачить, ловить тільки око:
  - `captive` мала HINTS «полонений» — дослівний повтор власного глоса, нуль інформації;
  - `to burst` мала HINTS «лопнути, вибухнути» — те саме;
  - приклад до `wafer` був «The ice cream came with a **thin** wafer» при глосі
    «**тонка** вафля» — прикметник дублював те, що вже сказано в глосі
    (замінено на `crunchy`, переклад «з хрумкою вафлею»);
  - переклад прикладу до `flirt` — «Він безневинний **з тих, хто фліртує**» —
    калька з описового глоса (замінено на «безневинний залицяльник»).
  **Правило:** підказка, що повторює глос, гірша за її відсутність — вона займає
  місце й нічого не пояснює. Якщо переписуєш глос або додаєш пару — ПЕРЕВІР її HINTS
  і приклад. Відтворити скан можна грепом: підказка, що збігається з глосом картки
  або є його підрядком.

- **🔗 КОМПАУНД НЕ УСПАДКОВУЄ `uaAlt` СВОГО СКЛАДНИКА (сесія 55, привід `lost passport`).**
  Картка `lost` уже приймала «втрачений» (у `uaAlt`), але `lost passport` = «загублений
  паспорт» про це нічого не знає — кожна картка має власний список. Для користувача це
  виглядає як непослідовність: слово вивчив, а фраза з ним не приймає.
  **Відкритий хвіст — скан, який варто зробити:** для кожної багатослівної картки взяти
  перше слово глоса, знайти однослівну картку з таким основним глосом і непорожнім
  `uaAlt`, запропонувати підстановку. ⚠️ Механічно застосовувати НЕ МОЖНА — підстановка
  не завжди законна («світла кімната» ≠ «легка кімната»), потрібен перегляд очима.

- **✂️ `handoff.md` РОЗДІЛЕНО НА ДВА ФАЙЛИ (сесія 55, за запитом «уже з'їло 455к токенів»).**
  Замір, з якого все почалось: старт сесії коштував ~120k токенів, і **найбільший
  керований важіль — саме цей файл** (688 КБ ≈ 196k токенів). Друге за розміром —
  системні нагадування харнесу (~3.5k на КОЖЕН хід користувача), і вони поза нашим
  контролем; звідси висновок, що короткі сесії з `/clear` вигідніші за довгі.
  - `handoff.md` **392 КБ** — довідник + ДВІ останні сесії, читається на старті цілком;
  - `handoff-log.md` **324 КБ** — чейнджлог сесій ≤54 і розгорнуті наративи пачок сесії 52,
    **на старті НЕ читається**, лише `grep`;
  - `CLAUDE.md` дістав пункт 2 про це.
  - **⚠️ ПРАВИЛО, ЩОБ ФАЙЛ ЗНОВУ НЕ РОЗРІССЯ:** при кожному оновленні handoff найстаршу
    з двох сесій переносити в лог. Довідник має описувати ПОТОЧНИЙ стан і правила, а не
    історію — історія живе в лозі.
  - Заразом стиснуто: дубль абзацу `WORDS` (7 КБ переліку «сесія N: +X слів» — чейнджлог
    усередині довідника), блоки про ВИДАЛЕНІ фічі (`📒 Помилки`, `📚 Повторив`, кнопка
    «Мікс») — з описів «як воно було» до правил і застережень «що НЕ чистити як мертвий
    код і чому».

- **🔤 СКАН ТРАНСЛІТЕРАЦІЙ (сесія 55, за запитом) — 10 карток, +11 варіантів.**
  Привід: `paragliding` приймав лише «параглайдинг», хоча є «парапланеризм». Клас
  однорідний — **картка знає лише запозичення, а питомого відповідника не має**
  (за сесію трапився тричі: `steering`, `police station`, `paragliding`).
  - **Скан:** картки з ОДНИМ однослівним глосом без `uaAlt`, що закінчується на
    `-инг/-інг/-ер` → 71 кандидат, з них реальних дірок **10**: `bodybuilding` ←
    культуризм · `powerlifting` ← силове триборство · `motor scooter` ← моторолер ·
    `stapler` ← зшивач · `influencer` ← лідер думок · `floor lamp` ← підлогова лампа ·
    `bronzer` ← бронзатор · `snowboarding` · `kayaking` · `snorkeling`.
  - **⚠️ Решта 61 — НЕ недоробка, повторно не проганяти:** справжні запозичення без
    відповідника (лазер, сканер, бампер, браузер, трилер), сорти й марки (лагер, пілзнер,
    чедер, камамбер) і хибні спрацювання регексу — слова, що просто закінчуються на `-ер`
    (інженер, вітер, четвер, мер, лікер).
  - **⚠️ Описову розшифровку замість слова НЕ додавати:** `sprinter` ← «бігун на короткі
    дистанції», `interviewer` ← «той, хто бере інтерв'ю» — такого користувач не набере.
  - Аудит: 0 фатальних, 0 попереджень.

- **⚠️ РУСИЗМ У ВІДПОВІДІ КОРИСТУВАЧА = НЕ ДОДАВАТИ, АЛЕ ПЕРЕВІРИТИ ПИТОМЕ (сесія 55).**
  На `police station` користувач написав «поліцейський участок». «Участок» — русизм
  (укр. відповідник *дільниця*), тож у `uaAlt` він не йде: `uaAlt` ПОКАЗУЄТЬСЯ в панелі
  відповіді й закріплював би помилку. **Але хибний варіант вказав на реальну дірку** —
  питомої «поліцейської дільниці» в картці не було. Патерн: русизм відхиляємо, а його
  правильний український відповідник ДОДАЄМО.
  ⚠️ «Відділення міліції» теж не додавав — міліції в Україні немає з 2015 року.

- **🔍 ХОВЕР БЕРЕ `ua` + `uaAlt` (сесія 55) — правка рушія, замір 115 речень.**
  Привід: у «Potatoes are **stored** in the cellar» тултіп показував «магазин».
  Причина: `EN_TO_UA` будувався з `getUa(w)`, а той **віддає лише `ua`** — тож сенс,
  що лежить в `uaAlt`, у ховері не існував. `stemEn` зрізав `-ed`, отримував `store`
  і мапа давала єдине, що знала.
  - **Скан перед правкою (методика, яку варто повторювати):** картки, де в `ua` немає
    дієслова, а в `uaAlt` є → 348; із них таких, де слово РЕАЛЬНО стоїть у прикладі
    в дієслівній формі (`-ed`/`-ing`) → **115**. Тобто це був не поодинокий випадок.
    ⚠️ Форму на `-s` до фільтра НЕ брати — це найчастіше множина іменника
    (`books`, `phones`, `roses`), а не дієслово.
  - **Лік — один рядок:** `const uaList = getUa(w).concat(w.uaAlt || []);`
  - **⚠️ НА ЗАРАХУВАННЯ НЕ ВПЛИВАЄ І НЕ МУСИТЬ:** `EN_TO_UA` живить ЛИШЕ ховер і
    переклад речень; `isCorrect` читає `ua`/`uaAlt`/`enAlt` напряму. Межа сесії 30 ціла —
    перевірено окремо (на промпт «місто» від картки `city` відповідь `town` і далі
    НЕ проходить), `test-homograph` 67/67.
  - **⚠️ ЗАЙВЕ ПЕРЕКРИТТЯ, ЯКЕ ЛИШИЛОСЬ:** `m.set("fire", ["вогонь"])` (`index.html`)
    тепер ховає «пожежа»/«звільняти» — у «Nobody may be **fired**…» знову покаже лише
    «вогонь». Кандидат на видалення, НЕ зроблено (чекає рішення).
  - **⚠️ УРОК ПРО ТЕСТ:** `test-phrase-hover` впав не через регресію фічі, а тому що
    порівнював тултіп `worked out` ТОЧНИМ рядком `=== "тренуватися"`. Переведено на
    намір (значення фрази перше, «працювати» не протікає). Той самий клас, що в сесії 53.
  - **⚠️ ІНСТРУМЕНТАЛЬНЕ: `\b` у JS НЕ ПРАЦЮЄ З КИРИЛИЦЕЮ** — межа слова рахується
    лише за ASCII, тож `/^тренуватися\b/` не матчить перед комою. У перевірках над
    українським текстом писати явну межу: `/^тренуватися(,|$)/`.

- **🔗 СПОЛУЧНИК «Й / І / ТА» БІЛЬШЕ НЕ ОБОВ'ЯЗКОВИЙ (сесія 55) — правило в `normUaSoft`.**
  Привід: `return ticket` = «квиток туди й назад» не приймав «квиток туди-назад».
  Три сполучники взаємозамінні за милозвучністю, тож вимагати саме той, що лежить у
  даних, — та сама орфографічна пастка, що дефіс (сесія 50), кома (52) і в/у (53).
  - **⚠️ ДВІ УМОВИ, БЕЗ ЯКИХ ПРАВИЛО ЛАМАЄ ДАНІ:** сполучник викидається ЯК ОКРЕМЕ
    СЛОВО і лише у фразі 3+ слів, де після викидання лишається 2+. Інакше `nor` =
    «і не» схлопнувся б у «не».
  - **Замір перед правкою:** чіпає 70 глосів, нових злиттів між картками **рівно 0**.
    Це та сама методика, що для в/у: спершу порахувати злиття на реальних даних.
  - Заразом прибрано **13 мертвих `uaAlt`**, що різнились лише сполучником
    (`grandparents` мав 12 перелічених комбінацій). ⚠️ Порядок слів правило НЕ скасовує,
    тож переліки з різним порядком лишились.

- **🌡 ТЕРМОМЕТР: ПРОЙДЕНІ ПРОМІЖНІ ЦИФРИ ТЕЖ ЖОВТІ (сесія 55, за запитом).**
  `.tm-minor.done .tm-n` → `#ffd166` (було тьмяно-біле). Тепер підсвічуються ВСІ
  досягнуті числа, не лише кратні 4. Розмір НЕ мінявся: крупність і далі відрізняє
  основні поділки від проміжних, колір — пройдене від майбутнього.
  ⚠️ Голубі перенесені мітки не зачеплені — `.tm.carry.done .tm-n` має вищу
  специфічність (4 класи проти 3) і виграє.

- **🚫 ПОВТОРЕННЯ БІЛЬШЕ НІЧОГО НЕ ФІКСУЄ (сесія 55, за запитом «нехай таймер тікає
  і ні на що це не впливає»).** Знято ТРИ речі разом: живий `addXp(2)` за закрите
  слово, виклик `recordReviewStats(wordsReviewed)` і саму функцію — єдиного писача
  `reviewCorrect`.
  - **⚠️ ЧОМУ САМЕ РАЗОМ, А НЕ ЩОСЬ ОДНЕ:** якби лишився живий `+2 XP` без запису,
    XP став би невідтворюваним і зник би на першій же міграції `formula_vN`
    (`computeDayStatsXp` рахує з нуля по джерелах). Джерело і його журнал знімаються
    ТІЛЬКИ в парі. Коментар про це стоїть у коді біля обох місць.
  - **⚠️ `reviewCorrect` ЗАМОРОЖЕНЕ, НЕ СТЕРТЕ** — усі читачі лишились
    (`computeRetroXp`, `computeDayStatsXp`, «Повторень» у `#lb-totals`, ачівка `review`,
    «Прогрес по днях», рекорди). Тому XP і статистика минулих днів НЕ зсунулись; число
    просто перестало рости. Патерн той самий, що з `oxford_mistake_reviews_v1` (45) і
    `oxford_self_reviews_v1` (47). Нового прапора `formula_v18` НЕ заводив — історія стабільна.
  - **Рядок «~N сек/слово» ЛИШАЄТЬСЯ** (прямий вибір користувача) — таймер тікає,
    просто нічого не пише.

- **✂️ TRIM РОЗЛИТИХ ГЛОСІВ — ЧОТИРИ КАРТКИ (сесія 55, за запитом).** Той самий патерн,
  що `machine` (сесія 50) і MEDIUM-список (51): у `ua` лишається ОДИН сенс, решта → `uaAlt`.

  | картка | було | стало |
  |---|---|---|
  | `lift` | ua: піднімати · **ліфт**, enAlt: elevator | ua: **піднімати**; ліфт, підйомник → `uaAlt`; **enAlt знято** |
  | `to mute` | вимкнути сповіщення | **вимкнути звук** (сповіщення → `uaAlt`) |
  | `flirt` | той, хто фліртує | **залицяльник** (опис → `uaAlt`) |
  | `captive` | ua: бранець · **полонений** · в'язень | ua: **полонений**; бранець, невільник → `uaAlt` |
  | `prisoner` | uaAlt: в'язень, **полонений** | uaAlt: в'язень, **арештант** |

  - **⚠️ `lift` — НОВИЙ ПІДВИД ДЕФЕКТУ, якого доти в handoff не було: `enAlt` діє на ВСЮ
    картку, а належить одному сенсу.** `enAlt:["elevator"]` стояв заради сенсу «ліфт», але
    в панелі пропонувався як відповідь на промпт «піднімати» — користувач це й побачив.
    **Правило:** якщо картка має два сенси в `ua`, її `enAlt` майже напевно бреше на одному
    з них. Trim лікує обидва разом.
    ⚠️ Нічого не втрачено: на промпт «ліфт» `lift` і далі проходить через `elevator.enAlt`.
  - **⚠️ ПІСЛЯ TRIM'У ОБОВ'ЯЗКОВО ПЕРЕВІР HINTS І ПРИКЛАД** (урок сесії 51, підтвердився):
    підказка `to mute` «вимкнути звук або сповіщення» стала тавтологією з новим глосом —
    переписана на «прибрати звук, не вимикаючи сам пристрій»; приклад `to mute` був про
    чат (сенс, що поїхав у `uaAlt`) — замінено на «He muted the TV during the ads».
    Заразом група «піднімати» лишилась без розрізнення → дописано HINTS `lift` і `to elevate`
    поряд із наявним `raise`.
  - **⚠️ Свідома втрата:** `prisoner` більше НЕ приймає «полонений» — саме через цей глос
    дві картки плутались; різницю тепер пояснює підказка.
  - **Перевірено в браузері в ОБИДВА боки** (не лише «що стало», а й «що не загубилось»):
    `«піднімати» ← elevator` → false · `«ліфт» ← lift` → true · `«полонений» ← prisoner` →
    false · `lift ← «ліфт»`, `to mute ← «вимкнути сповіщення»`, `flirt ← «той, хто фліртує»`
    → true. `test-homograph` 75.

- **➕ КАРТКА `in time` (A2) + ЗНЯТО `m.set("fire")` (сесія 55).**
  - `in time` = «встигнути» (+ не запізнитися, завчасно) — пара до `on time` = «вчасно».
    Рівень A2 взято за парою (`on time` в `oxford-cefr.json` саме A2). Обом дано HINTS,
    що розводять: *in time* — встигнути до строку, із запасом; *on time* — точно за
    розкладом. ⚠️ Перевірено, що вирази НЕ злилися: на «вчасно» `in time` не проходить.
    Це закриває питання, відкрите з першої появи «вчасно» ← `in time` у грі.
  - **⚠️ `m.set("fire", ["вогонь"])` ПРИБРАНО — приклад того, як стара оптимізація стає
    багом.** Перекриття мало сенс, поки ховер брав лише `ua`; після переходу на `ua+uaAlt`
    воно ПРИДУШУВАЛО власні значення картки, і в «Nobody may be **fired**…» знову
    показувало лише «вогонь». Тепер ховер віддає «вогонь, пожежа, звільняти».
    **Правило на майбутнє:** міняючи побудову `EN_TO_UA`, перевіряй увесь блок `m.set` —
    частина перекриттів існує лише як обхід старої поведінки.

- **Стан на кінець сесії 55:** `verify.sh` чистий (13765 записів, 13790 прикладів).
  Браузерні набори: `test-homograph` 67 · `test-hints` 49 · `test-phrase-hover` 18 ·
  `test-panel` 68 · `test-cefr` 28 · `test-wrong` 14 · `test-review` 11 · `test-due` 40,
  0 помилок консолі. Відкриті хвости сесії 54 (полоз у `runner`,
  «передчувати» в `to look forward to`, група B службових слів, вада зарахування UA→EN
  з `currentShown`) — **НЕ чіпані, лишаються в черзі**.
- **📌 ЧЕРГА ПРОПОЗИЦІЙ ЗАКРИТА** — усі п'ять пунктів (trim `flirt`/`to mute`/`prisoner`+
  `captive`/`lift`, картка `in time`, зняття `m.set("fire")`) ВИКОНАНО в цій же сесії,
  див. блоки вище. Лишився один: **таймер у повторенні** — користувач сказав «уточню
  пізніше» (запис уже знято, лишився сам таймер і рядок «~N сек/слово»).
- **✅ Перевірено й дірок НЕМА (не переробляти):** список ландшафтів LingoHut (volcano,
  canyon, marsh, mountain range, peninsula, bay, coast… — усі 19 у словнику, половина вже
  з потрібними `uaAlt`: «гірський хребет», «ріка», «берег», «горб», «бухта», «ущелина»).
  `marsh` «болото» / `swamp` «драговина» розведені коректно — це не дублі.
