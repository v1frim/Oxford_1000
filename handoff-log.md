# Oxford 1000 — Чейнджлог сесій (архів)

> ⚠️ Це АРХІВ. На старті сесії його НЕ читати — лише `grep -n` за номером сесії
> чи ключовим словом, коли треба відновити історію конкретного рішення.
> Актуальний довідник і дві останні сесії — у `handoff.md`.

---

- **📌 ПІДСУМОК СЕСІЇ 53 одним блоком.** Сесія за фідбеком із гри: дві правки UI, дві
  фічі зарахування й багато точкових доробок словника. **17 комітів.**

  | # | Що | Привід користувача |
  |---|---|---|
  | 1 | **Лідерборд відкривається на 🕒 «Рекорди за період»**, а не на 🏆 | «хотілось би, щоб показувало рекорди за останній час» |
  | 2 | «лиця» нарівні з «обличчя» — 7 карток, +8 варіантів | `крем для лиця` полетів у ✗ |
  | 3 | **Позначка «амер./брит.» для британських ЛЕКСЕМ** (76 карток) | «по 2 значення — це брит./амер. чи просто два переклади?» |
  | 4 | `emergency room`: +3 варіанти, прибрано «швидка допомога» | гуглівський варіант не приймався |
  | 5 | **+1 картка `backyard`** (задній двір, B1) | питання «город = vegetable garden чи backyard?» |
  | 6 | `typo`: +3 варіанти (описка · помилка набору · одрук) | «чи підійде варіант "одрук"?» |
  | 7 | **Мітка «амер./брит.»: суцільний добір 76 → 398 карток** + дірка в правилах написання | `jump rope / skipping rope` без позначки |
  | 8 | **«Сер./день» ділиться на КАЛЕНДАРНІ дні**, не на активні (+ фікс UTC-місяця) | «здається, не рахує дні без активності» |
  | 9 | **У ≡ В на початку слова** — правило зарахування (960 карток) | `downward` ← «вниз» полетіло у ✗ |
  | 10 | Доконаний вид: speed up · accelerate · slow down · break down · break | «зламатися» / «пришвидшитися» у ✗ |
  | 11 | «зал» нарівні з «зала» — 7 карток | `waiting room` ← «зал очікування» |
  | 12 | campfire ← вогнище · cooked ← приготовлений (+steamed) | фідбек із гри |
  | 13 | skinny ← худющий, кістлявий (але НЕ худорлявий) | «чи підійдуть ці варіанти?» |
  | 14 | lifestyle ← «life style», downward/upward ← вниз/вгору | фідбек із гри |
  | 15 | **↗ у «Прогресі» = БРУТТО входу, не нетто** (варіант B) | «слова з вивчаю будуть постійно в мінус» |
  | 16 | Приладдя «електро-» приймає й описову форму (5 карток) | `electric kettle` ← «електричний чайник» |
  | 17 | `succeed`, `section` — вид і багатозначність | фідбек із гри |
  | 18 | **`section:підрозділ` — окремий приклад під другий глос** + чистка перекладу | «приклад не відповідає слову» |
  | 19 | **Новий приклад до `savings account`** + «-free» через пробіл (12 карток) | «переклад взагалі не прямий» |
  | 20 | `torn` ↔ `ripped` зведено взаємно, `to tear` — доконаний вид | фідбек із гри |
  | 21 | **24 других приклади під конкретний глос** + інструмент `example-add.js` | «прожени вибірку і зроби партію» |
  | 22 | **+1 картка `wanderer`** (блукач, B2) | «уточни щодо мандрівника і wanderer» |
  | 23 | **Напрямок у повтореннях: дві монетки + гарантія на третьому кроці** | скрин: те саме слово тричі одним боком |

  **`verify.sh` чистий, 475 браузерних перевірок зелені.** Виросли набори:
  `test-hints` 39 → **49**, `test-panel` 60 → **68**, `test-homograph` 59 → **67**,
  новий `test-review` **11**.

- **⚠️ НАСКРІЗНА ТЕМА СЕСІЇ — ✗ НЕ ЧЕРЕЗ НЕЗНАННЯ, А ЧЕРЕЗ ФОРМУ ЗАПИСУ.** Майже кожен
  фідбек із гри був не «не знав слова», а «написав ІНШИЙ ПРАВИЛЬНИЙ варіант того самого»:
  вид дієслова (зламатися ↔ ламатися), чергування у/в (вниз ↔ униз), рід іменника
  (зал ↔ зала), морфологічний дублет (приготовлений ↔ приготований), пробіл у складному
  слові (life style). Розв'язки двох сортів, і вибір між ними — за ЗАМІРОМ: якщо клас
  великий і зведення безпечне → ПРАВИЛО (у/в, 960 карток); якщо клас малий або зведення
  небезпечне → ДАНІ (зал/зала — 7 карток; life style — точково, бо в `normEn` пробіл
  розрізняє `pick up`/`pickup`).

- **⚠️ ЩО ЛИШИЛОСЬ ВІДКРИТИМ (наступній сесії):**
  1. **ВИД ДІЄСЛОВА — найбільший невичерпаний клас.** Тричі за сесію латав точково
     (speed up, break down, break). Глоси записані переважно недоконаним видом, а
     англійський інфінітив виду не має, тож доконана відповідь летить у ✗. Потрібен
     прохід, як із «вузькими глосами» в сесії 52: скрипт знаходить картки-дієслова без
     доконаної пари в `uaAlt`, далі партії з перевіркою. Правилом це НЕ закриється —
     морфологія надто нерегулярна.
  2. **ДРУГІ ПРИКЛАДИ — ЗРОБЛЕНО 24 З ~60 ВАРТИХ.** Вибірка (скрипт у сесії): 334
     багатозначні картки без окремого прикладу, але більшість із них — синоніми
     («робити/зробити»), де один приклад чесний. Відбирати треба ті, де глоси РІЗНІ
     ЗА СЕНСОМ: іменник↔дієслово або різні домени. Що ще лишилось на око: `drop`
     (крапля/кидати), `jump` (стрибок/стрибати), `trigger`, `land`, `view`, `sense`.
     Інструмент готовий — `example-add.js`.
  3. Картка `fringe` = «бахрома» має `enAlt: ["bangs"]`, а `bangs` — це чубчик.
     Схоже на баг даних, не на регіональну пару. (Виявлено при доборі мітки
     «амер./брит.», у списки не вносив.)
  4. **ДЕФІС ≡ ПРОБІЛ в англійській — РОЗГЛЯНУТО Й СВІДОМО ВІДКЛАДЕНО.** У словнику
     **423 картки з дефісом**, а конфлікт при зведенні рівно ОДИН: `pick-up` (пікап)
     проти `pick up` (підбирати). Тобто правило технічно можливе (сесія 50 рахувала 19
     конфліктів по списку фразових дієслів, а не по реальних даних). НЕ робимо, бо:
     (а) даними це не варіант — `enAlt` ПОКАЗУЄТЬСЯ в панелі, 420 доданих варіантів =
     420 захаращених панелей («T-shirt / t shirt»); (б) на відміну від у/в чи виду
     дієслова, тут гра справедливо вчить норми написання. Виняток — `-free`
     (12 карток, зроблено): у предикативі «is sugar free» дефіс регулярно опускають,
     тож пробіл там сам є нормою. Натрапиш ще на такі — закривай точково; стане багато —
     повертайся до правила з винятком на `pick-up`.
     ⚠️ **`free sugar` до `sugar-free` НЕ додавати** — це протилежний за змістом термін
     («вільні цукри» — ті, що в продукті Є).

- **⚠️ УРОК СЕСІЇ — ПАРА, А НЕ СПИСОК СЛІВ.** Друга спроба зробити мітку «амер./брит.»
  для лексем природно тягла на два плоскі списки слів (американські / британські). Це
  зламалось би миттєво на багатозначності: `hood` (капюшон АБО капот), `tap` (кран АБО
  постукувати), `torch` (ліхтарик АБО смолоскип), `jelly`, `coach`, `vest`. Правильна
  форма даних — ПАРА, і вимога, щоб обидві сторони стояли на ОДНІЙ картці: тоді контраст
  на екрані гарантований самою умовою спрацювання. Той самий принцип, що врятував
  автопозначку написання в сесії 51 («результат правила мусить ДОСЛІВНО дорівнювати
  наявному enAlt»): **гарантію дають дані, а не хитріший алгоритм.**

- **⚠️ ДРУГИЙ УРОК — ЗМІНА ДЕФОЛТУ ЛАМАЄ ТЕСТ, ЯКИЙ ПРО НЕЇ НЕ ЗНАЄ.** Перемикання стартової
  вкладки лідерборду завалило дві перевірки в `test-cefr.js`, які читали рядок «остання гра»
  одразу після `page.reload()` — вони мовчки покладались на те, що відкриється 🏆. Не баг
  фічі, а прихована залежність у тесті: якщо перевірка читає щось «за замовчуванням»,
  вона мусить це замовчування виставляти САМА (тепер там явний клік на 🏆).

- **📌 ПІДСУМОК СЕСІЇ 52 одним блоком.** Сесія про **вузькі глоси** — картка приймала
  рівно один рядок знак у знак, і будь-який природний синонім летів у ✗. **15 комітів.**

  | # | Що | Привід користувача |
  |---|---|---|
  | 1 | 28 карток шкільних і університетських предметів + фікс `cefr-add.js` для C2 | «чи є в нас усі предмети» |
  | 2 | hover `either`/`neither` — заперечний сенс «теж» | «глянь, чи є цей варіант» |
  | 3 | **17 пар-майже-дублів зведено через enAlt** (амер. форма основною) | «зведемо ті пари» |
  | 4 | Вузькі глоси, **пачка 1**: 72 фразові дієслова + 58 предметних | «перевір схожі картки» |
  | 5 | **Фікс коми в зарахуванні** — 47 карток вимагали набирати кому | знайдено побічно |
  | 6 | Вузькі глоси, **пачка 3** (соло): 203 картки | «наступну пачку» |
  | 7 | Вузькі глоси, **пачка 4** (воркфлоу): 168 карток | «через воркфлоу» |
  | 8 | `cycling`, `advertising`, `secondary school`, `light rain`, `anyway`, `raindrop` | фідбек із гри |
  | 9 | **Анти-повтор у доборі слів** | «три рази підряд одне й те саме» |
  | 10 | Вузькі глоси, **пачка 5** (воркфлоу): 172 картки | «наступну пачку» |
  | 11 | **Злиття 4 пар-дублів + чистка 4 милиць-глосів** | «доробляй задумане» |

  **Разом по вузьких глосах: 673 картки, +900 варіантів.** Робочий пул
  (багатослівний ключ, глос 2-3 слова, без `uaAlt`) **2865 → 2216**.
  **`verify.sh` чистий, 438 браузерних перевірок зелені.**
  Нові інструменти: **`uaalt-add.js`**. Виросли набори: `test-homograph` 54 → **59**,
  `test-due` 31 → **40**.

- **⚠️⚠️ ГОЛОВНИЙ УРОК СЕСІЇ — АГЕНТИ НЕ ЗАМІНЮЮТЬ МЕХАНІЧНОГО АУДИТУ, І НАВПАКИ.**
  Пачки робились воркфлоу «шукач + скептик» по чанках. Скептики ловили те, чого не бачить
  скрипт (кабінетні вигадки, русизми, «чи так узагалі кажуть»), а мій скрипт — те, чого
  не бачить текст: тавтологію, дужки, збіг з основним глосом іншої картки й **лазівку
  «слово входить у власний глос»**. Найяскравіше: `service station ← «СТО»` — гра знижує
  регістр, «СТО» стає «сто», а це основний глос картки `hundred`.
  **Кожен урок аудиту, дописаний у промпт наступної пачки, прибирав цілий клас помилок:**
  пачка 3 (соло) — 1 фатальна; пачка 4 (правило про абревіатури) — 0 фатальних;
  пачка 5 (правило H про пари-дублі) — **0 фатальних і 0 попереджень**.

- **⚠️ ДРУГИЙ УРОК — ТЕСТ ТРЕБА ПЕРЕВІРЯТИ НА ОБИДВІ РЕГРЕСІЇ, А НЕ НА ОДНУ.**
  Перевірка «пул не став жорсткою каруселлю» рахувала КІЛЬКІСТЬ РІЗНИХ послідовностей —
  і регресію не ловила, бо карусель на пулі з 3 слів дає аж 6 послідовностей (за
  перестановками стартового шафлу). Правильна ознака — **періодичність** (`a[i]===a[i-n]`).
  Те саме з комою: набір перевірявся і на «прибрати правило», і на «поширити його на
  `normUa`». Прожени зламану версію в ОБИДВА боки, інакше тест декоративний.

- **📌 ПІДСУМОК СЕСІЇ 51 одним блоком.** Сесія «за фідбеком з гри»: половина правок
  почалася з того, що користувач натрапив на щось у грі й спитав «а хіба мій варіант
  не підійде?». **17 комітів.**

  | # | Що | Привід користувача | Коміт |
  |---|---|---|---|
  | 1 | `shoe shop` + «взуттєвий магазин» | «мій варіант також правильний» | `ea099f4` |
  | 2 | `well done` → вирази + фразовий ховер | «well done це молодець?» | `e74ccb0` |
  | 3 | `grandparents` — сполучники і/та | «додай ще варіанти в альт» | `242a374` |
  | 4 | **HINTS для enAlt-варіантів** | «варто уточняти, який амер., а який брит.» | `8595caa` |
  | 5 | **Математика: +27 карток, +6 виразів** | «глянь, чи є в нас ці слова» | `09ff530` |
  | 6 | **60 британських ключів → амер.**, 3 дублі злито, `median` | «давай перевернуті пари та median» | `c2f2b52` |
  | 7 | **Вартовий `check-brit.js`** + 10 карток і 23 речення | «давай вартового» | `890cb1c` |
  | 8 | **Вкладка 🕒 «Рекорди за період»** | «не бачу результати за останній період» | `0f7c66a` |
  | 9 | Shift гортає період, рядок «ост», порядок вкладок | «ігри вмикаються Enter, Shift — періоди» | `ceeb281` |
  | 10 | `dumpling` +пельмень, `fire station` +2 глоси | «пельмень також підійде?» | `ee2883c` |
  | 11 | Лівий Shift уперед / правий назад, 🎓 перед 🏆 | прямий вибір | `c6fe0ea` |
  | 12 | Shift працює й на екрані результатів | «тут shift наче не активний» | `97f4c3f` |
  | 13 | **Фікс: пул «не дались» показувався як голий 🎓** | «чому не вказувати значок повторень?» | `91e992d` |
  | 14 | `rather` +3 глоси, підпис пулу скорочено до 🔁 | «чи підійдуть ці 2 варіанти?» | `efc4c41` |
  | 15 | **MEDIUM-список закрито: 34 картки + 14 підказок** | «пройдись рештою MEDIUM-списку» | `6b55d68` |
  | 16 | **8 пар «іменник/дієслово» розділено** | «давай розділимо і ці пари» | `3a3ae4f` |
  | 17 | **Автопозначка амер./брит.** + 6 ключів, які пропускав вартовий | «дошках писати US або GB» | `d9b4a50` |

  **`verify.sh` виріс до ШЕСТИ кроків** (додано `check-brit.js`), **415 браузерних
  перевірок** зелені. Новий набір `test-lb-period.js` (55), `test-hints.js` 26 → 39.
  **Обсяг: WORDS 13683 → 13715 · EXPRESSIONS 1287 → 1293 · HINTS 1384 → 1383.**

- **⚠️⚠️ ГОЛОВНИЙ УРОК СЕСІЇ — МІЙ ПЕРШИЙ ЗАМІР КЛАСУ БУВ ЗАНИЖЕНИЙ УТРИЧІ.**
  Я доповів «30 перевернутих пар», бо детектор шукав британський маркер **у кінці ключа**.
  Насправді їх 60: маркер буває всередині (`licence plate`, `shopping centre`), а частина
  карток узагалі не мала американського варіанта (`favourite`, `flavour`, `colourful`).
  Мало не вийшло гірше за початковий стан: після першої хвилі складені `X centre` стали
  американськими, а базове `centre` лишилось британським — тобто **половинчаста нормалізація
  розсинхронила родину**. Правило на майбутнє: **перш ніж доповідати розмір класу, прогнати
  детектор у ДВОХ формах** (маркер у кінці / будь-де) і окремо перевірити, чи є картки БЕЗ
  парного варіанта. І доводити такі проходи до кінця однією сесією.

- **⚠️ ДРУГИЙ УРОК — ВАРТОВОГО ТРЕБА ПИСАТИ ДО, А НЕ ПІСЛЯ ЧИСТКИ.** `check-brit.js`,
  написаний уже «по чистому», одразу знайшов ще 10 ключів і 23 речення, які обидві ручні
  хвилі пропустили. Ручний прохід не замінює детектор навіть коли здається вичерпним.

- **⚠️ ТРЕТІЙ УРОК — «✅ чисто» ВІД ВАРТОВОГО ЩЕ НЕ ОЗНАЧАЄ, ЩО КЛАС ЗАКРИТО.** Той самий
  `check-brit.js` через кілька годин пропустив ШІСТЬ британських ключів (`personalised`,
  `itemised bill`, `authorised representative`, `wardrobe organiser`, `sulphur`, `sulphuric`):
  правило `ise\b→ize` не матчить закінчення `ised`. Знайшлось випадково — автопозначка
  амер./брит. почала показувати ці картки як «перевернуті». **Правило: написавши вартового,
  прожени його клас ІНШИМ способом** (тут — незалежним обчисленням пар), а не вір зеленому
  рядку. Форми `ised`/`ises`/`iser`/`isers` дописані, `sulphur` — у список основ.

- Зміни сесії 51: **🇺🇸 АМЕРИКАНСЬКА НОРМА В НАПИСАННІ — 60 КАРТОК, 3 ДУБЛІ, 152 РЕЧЕННЯ.**
  Привід — питання про `doughnut / donut`. Конвенція репо (сесія 42) каже: американське
  написання основне, британське в `enAlt`. Аудит показав, що вона трималась НЕ скрізь.
  - **Хвиля 1 (30):** `harbour`→`harbor`, `defence`→`defense`, `epicentre`→`epicenter`,
    13 складених із `centre`, `snorkelling`, `sabre`, `watercolour`, `to notarise`…
  - **Хвиля 2 (30):** маркер УСЕРЕДИНІ ключа (`licence plate`) або взагалі без амер.
    варіанта — `favourite`, `flavour`, `colourful`, `grey`, `aluminium`, `kerb`, `plough`,
    `mouldy`, `fulfilment`, `driving licence`, `criminal offence`, `defence lawyer`.
  - **Хвиля 3 (10):** те, що знайшов уже готовий вартовий — `anaesthetist`,
    `microfibre cloth`, `snowplough`, `pace per kilometre` і шість `-isation`.
  - **Хвиля 4 (6): СЛІПА ПЛЯМА САМОГО ВАРТОВОГО.** Правило `ise\b→ize` не матчить
    «personalised»/«itemised» — там на кінці `ised`, а не `ise`. Через це `personalised`,
    `itemised bill`, `authorised representative` і `wardrobe organiser` спокійно проходили
    повз нього, а `sulphur`/`sulphuric` не було в списку основ. Знайшлось випадково —
    під час роботи над автопозначкою амер./брит. Форми `ised`/`ises`/`iser`/`isers`
    дописані в RULES, `sulphur` — у BRIT_LEX. **Урок: вартовий теж треба перевіряти
    на власному класі, а не лише на тому, що він уже зловив.**
  - **Злито 3 дублі-близнюки** (той самий клас, що `meter`/`metre` того ж дня):
    `centre`+`center`, `tyre`+`tire`, `programme`+`program` — усі мали ОДНАКОВИЙ основний
    глос і були розведені милицею «(UK)» у дужках.
  - **⚠️ `cheque` СВІДОМО лишився британським** — це не дубль: амер. `check` зайняте
    дієсловом «перевіряти», фліп створив би третій сенс на одному ключі.
  - **⚠️ `dialogue` → `dialog` — НЕ правило.** Спершу спрацювало, але Merriam-Webster подає
    `dialogue`/`catalogue` основними в американській: це розщеплена норма, не британізм.
  - **⚠️ ТРИ ПАСТКИ ПЕРЕЙМЕНУВАННЯ КЛЮЧА** (усі три ловились лічильником ключів ДО/ПІСЛЯ):
    1. правки лише в межах СВОГО блоку, по парі «ключ:значення» — урок сесії 50;
    2. `TRANS["grey"]` і `TRANS["favourite"]` лежать на ЩІЛЬНИХ рядках із кількома ключами,
       тож якірний регекс `^\s*"key":` їх не бачить — потрібен окремий прохід;
    3. після фліпу ключа приклад може лишитись британським (`You must notarise…`) —
       і це ловить лише `check-coverage`, бо слово втрачає hover.

- Зміни сесії 51: **🕒 ВКЛАДКА «РЕКОРДИ ЗА ОСТАННІЙ ПЕРІОД» + ПЕРЕПРИЗНАЧЕНИЙ SHIFT.**
  Повний опис — у розділах «Leaderboard» і «Гарячі клавіші». Коротко про рішення:
  - п'ять вікон (7/30 днів · календарний місяць · 100/500 ігор), усе з `oxford_games_log_v1`;
  - CEFR-ігри ВХОДЯТЬ (інакше вкладка була б порожня — користувач грає рівнями);
  - Shift: меню й фінал → період, модалка → напрямок, гра → озвучка. Лівий уперед, правий назад;
  - **⚠️ порядок вкладок і активна за замовчуванням — РІЗНІ речі**: кнопки переставлено на
    `🕒 · 🎓 · 🏆 · …`, а активною лишилась 🏆, бо просили переставити, а не змінити старт.

- Зміни сесії 51: **🩹 ФІКС, ЯКИЙ ЗНАЙШОВ КОРИСТУВАЧ — ПУЛ «НЕ ДАЛИСЬ» ПОКАЗУВАВСЯ ЯК
  ГОЛИЙ 🎓.** Дослівно: «🎓 цей значок означає, так розумію, що це були повторення, тож чому
  не вказувати саме значок повторень (🔁)?». У `renderGameRow` один прапорець `isCefrGame`
  обслуговував ДВІ різні речі — «поза рейтингом» і «підпис рівнем». Для тегу `wrong` він
  теж був true, тож підпис рахувався як `"wrong".slice(5)` === **порожній рядок**, і в
  рейтингу висіло «🎓 ·  20.08», хоча правильне «🔁 Не дались» лежало в `LB_TAG_LABELS`
  і просто ніколи не досягалось. Розведено на `isCefr` (підпис) і `outOfRank` (номер місця).
  ⚠️ Баг жив із сесії 42 і не ловився нічим: рядок рендерився, помилок не було.
  Закрито 4 перевірками в `test-lb-period.js` (кожен пул має СВІЙ підпис).

- **📌 ПІДСУМОК СЕСІЇ 50 одним блоком.** Сесія БЕЗ нового словника — уся про якість
  даних, правила зарахування й верстку. **16 комітів**, з них 3 (`78e9f20`, `6427c9d`,
  `43f3984`) — з паралельної сесії користувача.

  | # | Що | Привід користувача | Коміт |
  |---|---|---|---|
  | 1 | Дефіс ≡ пробіл в укр. відповіді | «бізнес план» не зараховано | `681f023` |
  | 2 | `machine` — trim «розлитого сенсу» | «приклад геть не про механізм» | `dd61768`,`681f023` |
  | 3 | Вітання «✨ Усі помилки закриті» видалено | «вона ніяк не використовується» | `278b6e3` |
  | 4 | `report card` + `chalkboard` із LingoHut | «ці слова у нас також є?» | `b60fff7` |
  | 5 | Дужки в тексті підказки ламали рендер | «переклад йде суцільним» | `541b90e` |
  | 6 | `couch` — глос «кушетка» → «диван» | «кушетка це не зовсім диван» | `5e45b82` |
  | 7 | `new moon` — глос «молодик» віддано `youngster` | «ніхто не говорить про новий місяць як про молодика» | `566d642` |
  | 8 | Глоси «X або Y» приймають половинки | *(паралельна сесія)* | `78e9f20`,`6427c9d` |
  | 9 | Прозорі композити `long hair`/`dark hair` | *(паралельна сесія)* | `43f3984` |
  | 10 | **555 транскрипцій → американська норма** | «транскрипція гугла відрізняється» | `c09ad53` |
  | 11 | 22 тавтологічні підказки | «fortunately = на щастя (на щастя)» | `a1cec61` |
  | 12 | Аудит перекладів прикладів, 12 виправлено | «сумнівний тут переклад» | `6024487` |
  | 13 | Панель прогресу 440 → 480px | «місячним рядкам ледь місця вистачає» | `04017b1`,`c847042` |

  **Обсяг даних на кінець сесії: WORDS 13683 · EXPRESSIONS 1286 · HINTS 1384.**
  `verify.sh` чистий, **356 браузерних перевірок** зелені. Виросли набори:
  `test-homograph` 41→54, `test-hints` 21→26, `test-panel` 43→60.
  **Новий інструмент — `check-ipa.js`** (у `verify.sh`).

- **⚠️⚠️ ГОЛОВНИЙ УРОК СЕСІЇ — ОДИН КЛАС ПОМИЛКИ, ЯКИЙ Я ПОВТОРИВ ЧОТИРИ РАЗИ.**
  Масова заміна рядків у `dict.js`/`index.html` **без обмеження на потрібний блок**:
  1. `rr`→`r` по всьому `TRANS` знищила КЛЮЧІ (`arrow`→`arow`, `carry`→`cary`, 218 замін);
  2. видалення підказок пошуком по всьому файлу зрізало записи з `TRANS`
     (`funny`, `belief`, `to open`);
  3. `hints-add.js --force` дописував ДУБЛЬ ключа замість заміни, а його заміна теж
     шукала по всьому файлу;
  4. коментар `//` **у кінці рядка** конкатенації з'їв завершальний `+` — і половина
     «Загальної статистики» тихо зникла.
  **Спільне в усіх чотирьох: синтаксис лишався валідним, тож ні `new Function`, ні
  `loadtest.js` нічого не бачили.** Двічі рятував `git checkout`, двічі ловив користувач.
  **Правила, які з цього виведені:**
  • правити ТІЛЬКИ в межах потрібного блоку (`const X = {` … `\n};`), ніколи по всьому файлу;
  • працювати зі ЗНАЧЕННЯМИ через регекс по парі «ключ:значення», а не по сирому тексту;
  • у багаторядкових конкатенаціях коментар ставити НАД рядком;
  • після масової правки — рахувати кількість ключів ДО і ПІСЛЯ (мало б збігтись).
  ⚠️ І ще: `git checkout <file>` під час роботи відкочує ВСЕ незакомічене в цьому файлі —
  я так двічі втратив зроблене. Перед «контрольним експериментом» комітити або копіювати.

- Зміни сесії 50 (продовження): **👨‍👩‍👧 ГЛОСИ «X АБО Y» ТЕПЕР ПРИЙМАЮТЬ ПОЛОВИНКИ
  (паралельна сесія користувача, коміти `78e9f20`, `6427c9d`, `43f3984`).**
  Картки на кшталт `single parent` = «одинокий батько або мати» вимагали набрати весь
  глос цілком, хоча природна відповідь — одна половина. Додано `uaAlt` з половинками:
  `single parent` (+ «мати-одиначка», «батько-одинак»), `great-grandparent`,
  `great-grandchild`, `stepparent`, `godparent`, `strict parent`, `only child`
  (+ «єдина дитина»). ⚠️ Основний глос НЕ мінявся — він лишається промптом, бо саме
  він однозначно описує картку; половинки працюють як прийнятні відповіді в US→UA.
  **Правило на майбутнє: складений глос «X або Y» ЗАВЖДИ супроводжуй половинками в `uaAlt`.**
  Заодно прибрано прозорі композити `long hair` і `dark hair` (за критерієм сесії 49).

- Зміни сесії 50 (продовження): **📐 ПАНЕЛЬ «ПРОГРЕС ПО ДНЯХ» 440 → 480px
  (привід: «унизу, де місяці, ледь місця вистачає»).**
  - **⚠️ КЛЮЧОВЕ ВІДКРИТТЯ (заміряно в Chromium, не вгадано): розширення панелі
    прогресу НІЧОГО НЕ КОШТУЄ ПРАВОРУЧ.** Поки центр картки тримається на центрі
    екрана, увесь приріст іде ВЛІВО — правий край лейауту не рухається взагалі.
    На 1920px: ліворуч було 83px запасу до `#bottom-left-btns`, праворуч лише 41px
    до `#lb-totals`. Тому вимога користувача «центр не має зсунутись» не заважала
    розширенню, а навпаки — гарантувала, що вузький правий просвіт не постраждає.
  - **Компенсація центру — ФОРМУЛА, не магічне число:**
    `.layout.balance-center { margin-right }` **== ширина прогресу − ширина лідерборду**
    (було 20 = 440−420, стало 80 = 500−420). Лейаут центрується всередині
    `vw − margin-right`, тож така компенсація повертає центр КАРТКИ на центр екрана.
    ⚠️ Міняєш ширину панелі — перерахуй margin, інакше вся трійця з'їде.
  - **⚠️ ЧОМУ 480 І ЧОМУ ПІД МЕДІА-ЗАПИТОМ.** Проба на РЕАЛЬНИХ місячних рядках
    користувача (серпень 143/6/1/9/15, липень, червень): 440-470 їх переносять, з 480
    усі три лягають в один рядок. ⚠️ Спершу я міряв на ВИГАДАНОМУ щільнішому рядку
    (додав лічильники, яких у тих місяцях нема) і вийшло, що треба 500 — користувач
    заперечив, і на справжніх даних вистачило 480. **Міряй на реальних рядках.**
    Нижче ~1854px лівий край лейауту заходить під кнопки, а біля 1500 лейаут
    переноситься і центрування ламається. Тому `@media (min-width: 1860px)` —
    ширше 480px, вужче стара геометрія 440px. **До 1840px поведінка як була.**
  - **`#lb-totals` («Загальна статистика»):** рядки точності стикались підписом і
    значенням. Полагоджено БЕЗ розширення панелі (вона `fixed right`, тож ріст з'їдав
    би просвіт до лідерборду): `gap` 20→10, підпис `flex:1 1 auto; min-width:0`,
    значення `flex:0 0 auto`, а найдовший підпис скорочено «Точність · останні 300» →
    «Точність · ост. 300». Побічний ефект приємний: просвіт до лідерборду **виріс
    41 → 52px**, бо панель стала вужчою.
  - **Лідерборд НЕ чіпав** — за словами користувача це «можна не чіпати». Місця для
    його розширення практично нема: правий просвіт тримає `#lb-totals`.
  - **⚠️⚠️ ПОМИЛКА, ЯКОЇ Я ПРИПУСТИВСЯ ТУТ-ТАКИ — ЗАПАМ'ЯТАЙ ЦЕЙ КЛАС.**
    Скорочуючи підпис, я дописав пояснення коментарем `//` **У КІНЦІ РЯДКА** всередині
    конкатенації `row(...) + row(...) + …` у `renderLbTotals`. Коментар з'їв завершальний
    `+`, вираз обірвався на сьомому рядку — і **половина «Загальної статистики» тихо
    зникла** (Повторень, Duolingo, LingoHut, GPT, пісні, кіно, XP, блок «Плани», кнопка
    «Рекорди»). ⚠️ **Ні `new Function`, ні `loadtest.js` цього НЕ ловлять:** код
    лишається синтаксично валідним, просто коротшим. Помітив користувач на скріншоті.
    **Правило: у багаторядкових конкатенаціях коментар ставиться НАД рядком, ніколи
    в кінці.** Закрито перевіркою повноти панелі в `test-panel.js`.
  - **Перевіряє `test-panel.js` (43 → 60):** центр картки == центр екрана і
    компенсація == різниця ширин на 1920 і 1600, ширина 500/440 по обидва боки порогу,
    відсутність переносу реального місячного рядка **і повнота `#lb-totals` (≥16 рядків +
    наявність підписів «Досвід», «Кіно англійською», «Уроки LingoHut», «Пісень вивчено»)**.
    Перевірено, що тест падає і на зламаній компенсації, і на проковтнутому «+».

- Зміни сесії 50 (продовження): **🔍 АУДИТ ПЕРЕКЛАДІВ ПРИКЛАДІВ — 12 виправлено,
  але проблема ШИРША (привід: `playful` → «У цуценяти грайлива вдача»).**
  - **⚠️ МЕХАНІЧНО ЦЕЙ ДЕФЕКТ НЕ ЛОВИТЬСЯ — я перепробував чотири детектори, усі
    провалились. Не витрачай час повторно:**
    1. «кожне значуще англ. слово мусить відлунювати в перекладі» → 10124 з 13680
       (укр. морфологія: `стіл`→`столом` префікс не збігається);
    2. «глос картки мусить бути у власному прикладі» → 538, майже всі хибні
       (дієслівна словозміна: `клястися`→`клянуся`, `брати`→`візьми`);
    3. «рідкісні укр. слова» → 2172, це просто словоформи;
    4. «асиметрія довжини» → 378, усі законні (українська компактніша: нема
       артиклів і допоміжних дієслів).
    **Робочий метод лишився один — читати.** Систематична вибірка кожної 91-ї пари
    (151 шт.) дала **~6% проблемних**, тобто по всьому словнику це **≈800 речень**.
  - **Виправлено 12** (вибірка + звіт користувача): `playful`, `breast`, `firm`,
    `movie`, `front`, `loose`, `shipping`, `menial`, `equable`, `intrinsically`,
    `place of residence`, `repair estimate`.
  - **⚠️ ГОЛОВНЕ СПОСТЕРЕЖЕННЯ: частина кривих перекладів — наслідок КРИВОГО ГЛОСА,
    а не поганого перекладу.** Приклад чесно віддзеркалює картку:
    `breast` мав основним глосом клінічну «молочну залозу» (звичайні «груди» лежали
    в `uaAlt`) → виправлено на «груди», за прецедентом `couch`/«кушетка»;
    `shipping` = «морські перевезення», а приклад був «Shipping costs went up» —
    це сенс «доставка», тож замінено САМЕ РЕЧЕННЯ під глос картки.
    **Правило: спершу дивись, чи винен глос, і лише потім переписуй переклад.**
  - **⚠️ Окремий клас — КАРТКОВЕ УТОЧНЕННЯ, вклеєне в речення дослівно:**
    `menial` «чорновий за характером» → «Він виконував чорнову за характером роботу»,
    так само `equable`, `intrinsically`, `place of residence`. Уточнення в глосі
    існує для розрізнення НА КАРТЦІ; у прикладі воно перетворюється на канцелярит.
    Пошук: глос із « за характером/вдачею/документами/суттю », що дослівно входить
    у переклад — таких було рівно 3 (+ `menial` через відмінювання).
  - **⚠️ ЩО ЛИШИЛОСЬ:** ≈800 речень за оцінкою вибірки. Розумний спосіб — проходити
    ТЕМАТИЧНИМИ пакетами (як робились самі партії), а не суцільно. Оцінка отримана
    на вибірці кожної 91-ї пари; повторити легко.
  - `breast` дістав спільний глос «груди» з `chest`, тож за правилом обом дописано
    HINTS: `chest` → «уся грудна клітка», `breast` → «одна молочна залоза; у птиці — грудка».

- Зміни сесії 50 (продовження): **🧹 ТАВТОЛОГІЧНІ ПІДКАЗКИ ПРИБРАНО
  (привід: «fortunately = на щастя (на щастя)… це вже просто дублювання тексту»).**
  Скан усього блоку знайшов **22 підказки, що дослівно повторюють власний глос**.
  Розбір по кожній — чи є за цей глос конкурент серед англійських слів:
  - **6 без конкурента** (`to open`, `beautiful`, `countryside`, `belief`, `funny`,
    `personality`) — підказку ВИДАЛЕНО: за правилом HINTS її там і не мало бути.
  - **16 із конкурентом** (`fortunately` vs `thankfully`, `buy` vs `purchase`,
    `child` vs `kid`, `weak` vs `feeble`…) — підказку ПЕРЕПИСАНО так, щоб вона
    розрізняла: `fortunately` → «нейтрально; звичайне слово», `grandson` → «син сина
    або доньки». Конкурент у кожної пари свою підказку вже мав.
  - ⚠️ **271 «частковий перетин» НЕ чіпав** — там підказка додає сенс, а не повторює:
    `гострий` → «гострий як лезо; різкий», `брудний` → «до огиди брудний».
  - **Вартові:** `hints-add.js` тепер відхиляє підказку, що дорівнює глосу; у
    `test-hints.js` 25 → **26** перевірок (інваріант по всьому блоку). Перевірено, що
    тест падає на поверненій тавтології.
  - **⚠️ ДВА БАГИ `hints-add.js`, знайдені по дорозі (обидва тихі, синтаксис не падав):**
    1. **`--force` дописував ДУБЛЬ ключа в кінець блоку** замість заміни. У JS-літералі
       виграє останній ключ, тож усе «працювало» — я побачив лише перерахувавши рядки
       (1400 при 1384 унікальних). Тепер наявний ключ переписується НА МІСЦІ.
    2. **Заміна шукала по всьому файлу, а не в блоці HINTS.** Ключі `funny`, `belief`,
       `to open` є ще й у `TRANS`/`EXAMPLES` — я цією ж помилкою вручну знищив три
       транскрипції й відкочував `dict.js`. Тепер усе скоупнуто на блок.

- Зміни сесії 50 (продовження): **🔊 ТРАНСКРИПЦІЇ ПРИВЕДЕНО ДО АМЕРИКАНСЬКОЇ НОРМИ
  (привід: «транскрипція гугл-перекладача відрізняється від нашої… і таке вже не вперше»).**
  - **⚠️ ПЕРШЕ, ЩО ТРЕБА ЗНАТИ: `TRANS` НЕ ВПЛИВАЄ НА ОЗВУЧКУ.** `speak()` передає
    рушію САМЕ СЛОВО (`new SpeechSynthesisUtterance(toSpeak)`, `u.lang="en-US"`), і той
    вимовляє його за ВЛАСНИМ словником. Транскрипція — суто те, що бачить око. Тобто це
    два незалежні джерела, і розійтись вони можуть будь-коли. Не шукай тут причинності.
  - **⚠️ САМ `kebab` виявився ПРАВИЛЬНИМ** — я перевірив джерела, перш ніж «виправляти».
    Наша `/kɪˈbɑːb/` = Cambridge US дослівно; Wiktionary GA дає `/kəˈbɑb/` І `/kɪˈbɑb/`;
    Google (Oxford) пише `kəˈbäb`. Перший склад НЕНАГОЛОШЕНИЙ, а там діє weak vowel
    merger — `ɪ` та `ə` злиті, тож це різниця традицій запису, а не вимови.
  - **Але аудит знайшов справжнє:** 555 записів із 13625 приїхали з БРИТАНСЬКИХ джерел.
    Виправлено все: `ɒ`→`ɑː` (424), `əʊ`→`oʊ` (79), `eə`→`er` (9), `ɜː`→`ɜːr` (20),
    `ɐ` (18), неротичні (44), BATH (10).
  - **⚠️ ɒ → ɑː НЕ УНІВЕРСАЛЬНЕ.** Словник (у незайманій частині) розрізняє **LOT**
    `hospital /ˈhɑːspɪtəl/` і **CLOTH** `cost /kɔːst/`, `office /ˈɔːfɪs/`, `dog /dɔːɡ/`.
    62 записи довелось довести до `ɔː` окремим проходом. Список CLOTH-лем — у скрипті
    чистки; спосіб перевірки: звірити фразу зі **standalone-формою того самого слова**
    у власному словнику, а не з зовнішнім джерелом.
  - **⚠️ ТРИ ПАСТКИ, В ЯКІ Я ВЛІЗ — щоб наступний не влазив:**
    1. **Заміна по всьому блоку TRANS нищить КЛЮЧІ.** `rr`→`r` «щоб прибрати артефакт»
       перетворило `arrow`→`arow`, `carry`→`cary`, `sorry`→`sory` — 218 замін, з них
       побитих ключів більшість. **Працювати ЛИШЕ зі значеннями**, через регекс по парі
       «ключ:значення».
    2. **`eə`→`er` дає `err`**, якщо далі вже стоїть `r` (`swear /sweər/`). Спершу
       `eər`→`er`, потім решта.
    3. **`lɑːs`→`lɔːs` з'їдає `klɑːs`** (class) і робить `klɔːs`. Потрібен `(?<!k)`.
  - **Вартовий `check-ipa.js` доданий у `verify.sh`** — без нього кожна нова партія
    тягла б британські форми далі. У `ALLOW` — `sommelier`/`hotelier` (французькі
    запозичення з НІМИМ кінцевим `r`, тобто хибне спрацювання детектора неротичності).
  - ⚠️ **BATH-правило у вартовому — ТІЛЬКИ перевірені лексеми** (`hɑːf`, `klɑːs`,
    `brɑːntʃ`, `vɑːst`). Дописувати «загальні» шматки типу `ɑːsk`/`pɑːθ` НЕ МОЖНА —
    вони ловлять законний LOT (`kiosk`, `mosque`, `hypothesis`). Перевірено, впало одразу.
  - ⚠️ **ЩО СВІДОМО НЕ ЧІПАВ:** 145 записів із дивним місцем наголосу (`youth /jˈuːθ/` —
    звуки правильні, лише позначка стоїть після початкової приголосної) і дрібні
    розбіжності конвенцій (`holiday /ˈhɑlədeɪ/` без `ː`, `alter /ˈɔltɝ/` через `ɝ`
    замість `ər`). Вартовий на них не лається — це косметика, не вимова.

- Зміни сесії 50 (продовження): **🌙 `new moon` — глос «молодик» віддано `youngster`
  (привід: «ніхто у нас не говорить про новий місяць як про молодика»).** Було
  `new moon = "молодик"` (hint «фаза місяця») і `youngster = "молодик"` — той самий
  глос у двох НЕсинонімів, тобто справжня колізія, а не синонімія. Плюс на промпт
  «молодик» випадав приклад про місяць («The new moon is hard to see» / «Молодик важко
  побачити»), хоча користувач думав про юнака.
  Стало: `new moon` → `ua:"молодий місяць", uaAlt:["новий місяць"]` — форма в пару до
  наявного `full moon = "повний місяць"`; переклад прикладу → «Молодий місяць важко
  побачити». `youngster` лишився ЄДИНИМ власником «молодик», і його приклад уже був
  саме про людину («A youngster showed us the way»).
  - **Обидві підказки ПРИБРАНО як тавтологічні:** щойно конкурент за глос зник, hint
    порушує власне правило («писати лише коли за той самий глос конкурує інше англ.
    слово»). У `youngster` вона до того ж дослівно дублювала `uaAlt`, який і так видно
    в панелі відповіді.
  - **`youngster` дістав `enAlt:["youth"]`** — користувач написав `youth` і отримав ✗,
    а це законна відповідь на «молодик» (countable `youth` = юнак). Точково, саме за
    правилом сесії 45: такі пари лікуємо коли користувач НАТРАПИВ у грі, а не суцільним
    проходом по 3591 кандидату.

- Зміни сесії 50 (продовження): **🛋 `couch` — виправлено ХИБНИЙ основний глос
  (привід: «кушетка це не зовсім диван, тому і прикладне речення дає не вірний
  переклад»).** Було `ua:"кушетка", uaAlt:["диван","софа"]` при прикладі
  «He fell asleep on the couch» / «Він заснув на **дивані**» — тобто і глос хибний,
  і приклад йому суперечив.
  ⚠️ **ЧОМУ ГЛОС БУВ ТАКИЙ:** «диван» уже основний у `sofa`, і хтось (я в минулих
  сесіях) обійшов колізію, підсунувши `couch` інший іменник. Це ПОМИЛКОВЕ застосування
  правила: розводити треба лише НЕсинонімів, а `couch`/`sofa` — справжні синоніми,
  для них спільний глос НОРМА (те саме, що `particularly`/`especially`,
  `bush`/`shrub`). Крос-логіка UA→US і так приймає обидва.
  Стало: `couch` → `ua:"диван", uaAlt:["софа"]`; «кушетка» прибрано зовсім (інші меблі,
  у панелі відповіді вчила б хибну рівність — критерій сесії 40).
  За правилом «спільний глос → підказки ОБОМ словам групи» додано HINTS:
  `couch` → «розм. — частіше в амер.», `sofa` → «звичайне слово — і брит., і амер.».
  Приклад `couch` замінено на «We watched a movie on the couch» — інакше дві картки
  з тим самим глосом мали майже однакові речення («fell asleep on the couch/sofa»).
  ⚠️ Наслідок: промпт «кушетка» зник зі словника. Окрему картку не заводив —
  англійською це `day bed` / `examination couch`, вузько.

- Зміни сесії 50 (продовження): **🩹 ДУЖКИ В ТЕКСТІ ПІДКАЗКИ ЛАМАЛИ РЕНДЕР
  (привід: «тут переклад йде суцільним»).** У 6 підказок HINTS дужка стояла всередині
  тексту, а гра рендерить підказку САМА ЯК дужку → вкладеність
  «jealousy (страх утратити своє (ревнощі))». Регекс `^(.+?)\s*\(([^)]+)\)$` такого
  не матчить, і весь рядок падав в ОДИН `.hw`-спан: підказка не тьмяніла, слово губило
  hover, IPA й озвучку, клік читав український текст англійським голосом.
  Регекс розширити не можна — він мусить брати ОСТАННЮ дужкову групу, тож інваріант
  тримаємо на даних: дужки → тире. Виправлено `baked`, `upbeat`, `jealousy`,
  `to zone out`, `to misbehave`, `to act up`; вхід тепер стереже `hints-add.js`,
  дані — 4 нові перевірки в `test-hints.js` (21 → 25). Деталі — розділ «💡 HINTS».
  ⚠️ Тоді ж знайдено дрібний баг `hints-add.js`: на прогоні без нових ключів він
  дописував кому й порожній рядок у кінець блоку HINTS (синтаксис не падав, слід
  накопичувався непомітно) — виправлено ранім виходом.

- Зміни сесії 50 (продовження): **🗑 ВІТАННЯ «✨ УСІ ПОМИЛКИ ЗАКРИТІ» ВИДАЛЕНО
  за прямим запитом користувача.** Привід: «вона зараз ніяк не використовується і усі
  помилкові слова замішуються автоматично на наступний день». Відколи борг
  (`oxford_due_v1`, сесія 44) сам повертає помилки в ігри, порожній список — не
  досягнення, а звичайний стан. Прибрано гілку в `endGame`, `sfxCleared`, гілку
  `"cleared"` у `showMilestone` і обидва CSS-правила `.ms-box.cleared`.
  ⚠️ **`allDayMistakeKeys()` і `oxford_day_mistakes_v1` ЛИШИЛИСЬ** — живих читачів у
  них більше нема, але їх кличуть одноразові міграції боргу (`seedDueFromMistakes`,
  `fixDuePollutedByReview`), а на новому пристрої прапорці порожні. НЕ чистити.
  Перевірено: `verify.sh` + 334 браузерні перевірки зелені (зокрема `test-due.js` 31
  і `test-wrong.js` 14 — уся механіка боргу й архіву помилок не зачеплена).

- Зміни сесії 50: **ДВІ ПРАВКИ ПРАВИЛ ЗАРАХУВАННЯ (обидві за скаргами з гри).**
  - **➖ Дефіс ≡ пробіл в українській відповіді** (привід: «бізнес план» не зараховано).
    `normUaSoft` у гілці en-ua зводить `[-\s]+` до одного пробілу. Зачіпає 152 картки,
    злиттів укр. глосів 0. **Англійський бік СВІДОМО не чіпали** — там 19 пар, де дефіс
    розрізняє сенс (`pick up`/`pick-up`, `check-in`, `take-off`, `warm-up`).
    ⚠️ **Перша спроба дописала правило прямо в `normUa` — і воно протекло в ua-en**
    через `inpNorm = normEn(inp)`: `pick-up` почав приймати «pick up». Зловила нова
    перевірка в `test-homograph.js`, звідси й розділення на дві функції. Деталі —
    розділ «Нормалізація відповідей».
  - **🔧 `machine` — trim «розлитого сенсу»** (привід: «приклад геть не про механізм»).
    Було `ua:["механізм","машина","пристрій"]`; промпт у UA→US береться ВИПАДКОВО з усіх
    глосів, тому приклад «Машина знову зламалася» не пасував двом промптам із трьох,
    а глос «машина» ще й затягував **`car` у список правильних відповідей на «механізм»**
    (панель збирає всі слова, що ділять БУДЬ-ЯКИЙ глос картки). Патерн сесії 36:
    `ua:["механізм","пристрій"], uaAlt:["машина"]` + переклад прикладу → «Механізм знову
    зламався» (англійське речення не міняли — воно годиться для обох сенсів).
    ⚠️ Побічний ефект, прийнятий свідомо: на промпт «машина» (картка `car`) відповідь
    `machine` більше не зараховується — «машина» в тому промпті означає автомобіль.
  - **Перевірено:** `verify.sh` чистий + **334 браузерні перевірки** зелені
    (homograph 54 — з них 13 нових, hints 21, panel 43, cefr 28, cefr-solo 33, due 31,
    wrong 14, expr 50, phrase-hover 18, lb-cefr 42), 0 помилок консолі.

- Зміни сесії 49: **⚖️ П'ЯТНАДЦЯТА ПАРТІЯ — «ДОКУМЕНТИ, ПРАВО Й ДЕРЖАВНІ УСТАНОВИ»:
  +435 карток і +115 виразів (WORDS 13249 → 13684, EXPRESSIONS 1171 → 1286).**
  xlsx користувача, **902 позиції, 22 розділи — НАЙБІЛЬШИЙ ФАЙЛ ЗА ВСЮ ІСТОРІЮ**
  (попередній рекорд 798, комп'ютерний). Розкладка: **338 уже були (37%)**, **9 частково**,
  **12 уже у виразах**, **543 нові** → 5 партій (85 / 80 / 65 / 93 / 112).

  | партія | розділи | карток | виразів |
  |---|---|---|---|
  | 1/5 | право й закон, документи, заяви, договори | 85 | 13 |
  | 2/5 | органи влади, місцева влада, податки, права | 80 | 14 |
  | 3/5 | поліція, правопорушення, розслідування, суд | 65 | 20 |
  | 4/5 | кримінальне провадження, цивільні спори, юристи, праця | 93 | 13 |
  | 5/5 | житло, сім'я, міграція, вибори, скарги, фрази | 112 | 55 |

  - **CEFR:** A2 **4** · B1 **85** · B2 **186** · C1 **160** (A1/C2 — жодної).
    Найважчий файл сесії: понад третина карток на C1.
  - **⚠️ ГОЛОВНА СПЕЦИФІКА: українське «право» = І `law`, І `right`.** Тому весь блок
    `legal X` (`legal right`, `legal protection`, `legal procedure`, `legal remedy`…)
    і `criminal law` / `civil law` / `family law` СВІДОМО лишені попри формальну
    прозорість — у UA→EN це справжня пастка. Те саме з «державний» = `state` І `public`
    (`state body`, `public institution`, `public sector`, `public official`).
    **Пропущено лише беззаперечне (25 позицій)** — список у `final-audit.js`.
  - **Розділ «Корисні фрази в установах» (36) — цілком у `EXPRESSIONS`**; разом із
    зворотами інших розділів партія 5/5 дала 55 виразів — рекорд для однієї партії.
  - **Зведено в enAlt (11 пар):** `identity card`/`ID card`→id, `driver's licence`→driving
    licence, `public service centre`→administrative service centre, `contract of
    employment`→employment contract, `employment notice period`→notice period,
    `immigration authority`→immigration office, `real property`→real estate,
    `electoral district`→constituency, `council member`→councillor, `layoff`→redundancy.
  - **Розведені глоси:** `place of residence` → «місце проживання за документами»
    (residence), `nationality` → «громадянство (у документах)», `acknowledgement` →
    «письмове підтвердження» (recognition), `safeguard` → «гарантія захисту»
    (⚠️ «запобіжник» уже належить `fuse` — електричному!), `town council` → «рада
    містечка» (city council), `legal proceedings` → «юридичне провадження» (trial),
    `respondent` → «сторона, що відповідає на скаргу» (defendant), `circumstantial` →
    «непрямий (про доказ)» (indirect), `legal action` → «юридичні дії» (lawsuit),
    `domestic abuse` → «жорстоке поводження в сім'ї» (domestic violence),
    `inheritance` → «спадщина (майно)» (heritage).
  - **Hover-only (`m.set`):** `majeure`, `mitigating`, `aggravating`, `declaratory`,
    `authorised`, `notarial`, `probationary`, `marital`, `restraining`, `residency`,
    `allegiance`, `stateless`, `supervisory`, `germany`.
  - **⚡ Заведено `cefr-batch.js`** (див. індекс інструментів): рівень пишеться прямо
    в картці партії полем `cefr`, скрипт групує й кличе `cefr-add`. Пастка «порожній B2»
    тепер неможлива — скрипт лається сам. Використано з партії 2/5 і далі.

- **📌 ПІДСУМОК СЕСІЇ 49 одним блоком:**

  | Файл xlsx | Позицій | Карток | Виразів |
  |---|---|---|---|
  | 🏋️ Спорт, тренування та рухи тіла | 871 | 455 | 113 |
  | 🌿 Природа, погода та місцевість | 810 | 304 | 77 |
  | ⚖️ Документи, право й державні установи | 902 | 435 | 115 |
  | **Разом** | **2583** | **1194** | **305** |

  **WORDS 12504 → 13684 · EXPRESSIONS 979 → 1286.** Усі три файли покриті на **100%**
  (фінальні аудити: 871/871, 810/810, 902/902). Плюс **точкова чистка 12 прозорих
  композитів** і перенесення `fed up` / `to change clothes` у вирази — за запитом
  користувача, деталі окремими пунктами нижче.

- Зміни сесії 49: **🌿 ЧОТИРНАДЦЯТА ПАРТІЯ — «ПРИРОДА, ПОГОДА ТА МІСЦЕВІСТЬ»:
  +304 картки і +77 виразів (WORDS 12945 → 13249, EXPRESSIONS 1094 → 1171).**
  xlsx користувача, **810 позицій, 21 розділ.** Розкладка: **398 уже були (49% —
  найбільший збіг з усіх файлів, природа проходилась ще в сесії 41)**, **6 частково**,
  **1 уже у виразах**, **405 нових** → 4 партії (81 / 81 / 88 / 54).

  | партія | розділи | карток | виразів |
  |---|---|---|---|
  | 1/4 | довкілля, погода й прогноз, сонце, хмари, дощ | 81 | 8 |
  | 2/4 | сніг і мороз, вітер і бурі, клімат, гори, рівнини | 81 | 10 |
  | 3/4 | річки, море, ліси, тварини, екологія, стихійні лиха | 88 | 8 |
  | 4/4 | село, орієнтування, безпека, краєвид, фрази | 54 | 51 |

  - **CEFR:** A2 **32** · B1 **128** · B2 **57** · C1 **87** (A1/C2 — жодної).
  - **⚠️ ГОЛОВНЕ УТОЧНЕННЯ ПРАВИЛА ПРО ПРОЗОРІ КОМПОЗИТИ (важливіше за саму партію).**
    Прозорість треба міряти **в напрямку UA→EN, а не EN→UA**. `red onion` тривіальний
    в обидва боки — його прибрали. А `heavy rain` виглядає так само прозоро
    («важкий + дощ»), але з української «сильний дощ» тягне на хибне `*strong rain` —
    і саме тому картка ПОТРІБНА. Такі лишаємо:
    `heavy rain` / `light rain` (не strong), `strong wind` (не heavy — дзеркальна пара!),
    `freezing rain` (не *icy rain), `fresh water` (прісна ≠ свіжа), `tree crown`
    (крона ≠ корона), `natural disaster` (стихійне ≠ natural), `high tide` (приплив,
    не «висока вода»), `sea spray` (бризки ≠ spray).
    **Пропущено за правилом 36 позицій** — список у `final-audit.js` (масив `SKIP`).
  - **Розділ «Корисні фрази про погоду й місцевість» (35) — ЦІЛКОМ у `EXPRESSIONS`**,
    жодної картки. Разом із фразами орієнтування й безпеки партія 4/4 дала 51 вираз
    при 54 картках — єдина партія, де виразів майже стільки ж, скільки слів.
  - **Апострофи нормалізовані ОДРАЗУ при вигрузці xlsx** (`’ → '` у скрипті дампу) —
    урок зі спортивного файлу спрацював, аудит жодного разу не дав хибного «загублено».
  - **Зведено три пари-майже-дублі через enAlt** (технічний борг із розділу «Що
    лишилось»): `fossil fuel`→fossil fuels, `hive`→beehive, `rain jacket`→waterproof
    jacket. Плюс `foothills`→foothill.
  - **⚠️ `sank` не було в `IRREGULAR`** — приклад «The wheels sank into the mud» лишався
    без hover. Додано `sank:"sink"`. Заразом додано картку `to sink` (у словнику був
    лише іменник `sink` = «раковина»).
  - **Пастка, яку зловив gloss-check:** `rower` = «весляр», а не гребний тренажер;
    `changing room` = «примірочна» в крамниці. Те саме правило спрацювало й тут:
    перед enAlt дивитись ГЛОС наявної картки, не лише ключ.
  - **Розведені глоси:** `wilderness` → «безлюдна місцевість» (дика природа = wildlife),
    `sunshine` → «сонячна погода» (сонячне світло = sunlight), `hazy` → «затягнутий
    серпанком» (імлистий = misty), `temperate` → «помірний (про клімат)» (помірний =
    moderate), `humid` → «вологий і задушливий» (вологий = damp), `mountain range` →
    «гірська система» (гірський хребет = ridge), `ravine` → «глибокий яр» (яр = gully),
    `marshy` → «заболочений» (болотистий = muddy), `high tide` → «пік припливу»
    (приплив = tide), `woodland` → «рідколісся» (лісиста місцевість = woods).
  - **Свідомі спільні глоси** (справжні синоніми, крос-логіка приймає обидва):
    `brook`/stream, `headland`/cape, `beetle`/bug, `picturesque`/scenic,
    `torrential rain`/downpour.
  - **Hover-only (`m.set`):** `unseasonably`, `sub`, `eco`, `topographic`.

- Зміни сесії 49: **🏋️ ТРИНАДЦЯТА ПАРТІЯ — «СПОРТ, ТРЕНУВАННЯ ТА РУХИ ТІЛА»:
  +455 карток і +113 виразів (WORDS 12504 → 12959, EXPRESSIONS 979 → 1092).**
  xlsx користувача, **871 позиція, 22 розділи.** Розкладка: **313 уже були (36%)**,
  **16 частково**, **1 уже у виразах**, **541 нова** → 5 партій (97 / 95 / 108 / 90 / 65).

  | партія | розділи | карток | виразів |
  |---|---|---|---|
  | 1/5 | загальні поняття, змагання, структура заняття, підходи, тренажери | 97 | 10 |
  | 2/5 | положення й рухи, техніка й команди тренера, вправи верх/ноги | 95 | 22 |
  | 3/5 | кор і вага тіла, кардіо, рухливість, анатомія | 108 | 8 |
  | 4/5 | спортивні якості, прогрес і дані, відновлення, травми | 90 | 25 |
  | 5/5 | харчування, командні ігри, види спорту, вода/зима/єдиноборства, фрази залу | 65 | 48 |

  - **CEFR:** A2 **22** · B1 **179** · B2 **178** · C1 **76** (A1/C2 — жодної).
    ⚠️ **Пастка порожнього B2 цього разу НЕ спрацювала** — від першої ж партії рівні
    роздавав ЧОТИРМА списками одразу (A2/B1/B2/C1) і рахував розподіл перед кожним
    комітом. Метод працює: тримати B2 окремим списком, не «залишком між B1 і C1».
  - **Розділ «Корисні фрази в залі» (38 позицій) — ЦІЛКОМ у `EXPRESSIONS`**, жодної
    картки: це готові речення (`Can I work in with you?`, `Can you spot me?`,
    `Watch your form.`). Разом із командами тренера з розділу «Техніка» дало 113 виразів —
    найбільший внесок у `EXPRESSIONS` за одну тему.
  - **Аудит зловив 3 загублені позиції** (`Smith machine`, `Bulgarian split squat`,
    `fire hydrant`) — знову підтвердив, що аудит по розділу не формальність.
  - **⚠️ НОВЕ: типографські апострофи в xlsx ламали аудит.** Позиції `farmer’s carry`,
    `Don’t hold your breath.` рахувались «загубленими», бо у файлі `’` (U+2019), а в
    партії `'`. Полагоджено нормалізацією `[‘’] → '` у скрипті аудиту.
    **Наступного разу нормалізувати апострофи ОДРАЗУ при вигрузці xlsx.**
  - **⚠️ НОВЕ: стемер не бере подвоєння в порівняльному ступені.** `bigger` тричі впало
    в `check-coverage` (big → -er з подвоєнням `g` не резолвиться), хоча `large`/`larger`
    проходить. **У прикладних реченнях уникати `bigger`/`hotter`/`thinner`** — писати
    `larger`, `greater` або перебудовувати фразу.
  - **Дублі enAlt — цього разу жодного.** Допомогло те, що скан партії звіряв список пар
    проти списку карток ПЕРЕД вставкою (а не після).
  - **Позиції, що пішли в enAlt, бо картка вже була:** `supporter`→fan, `sports center`→
    sports centre, `abdominals`→abs, `kilometre`→kilometer, `pectorals`/`pecs`→chest,
    `delt`→deltoid, `quads`→quadriceps, `calf muscle`→calf, `physical therapist`→
    physiotherapist, `rehab`→rehabilitation, `touchline`→sideline, `goalie`→goalkeeper,
    `track and field`→athletics, `racquet`→racket, `ice skate`→skate, `lineup`→line-up.
  - **Розведені глоси:** `heart rate` → «частота серцевих скорочень» (пульс = pulse),
    `sideways` → «боком» (убік = aside), `breathing` → «дихання (процес)» (дихання = breath),
    `brisk` → «швидкий і бадьорий» (жвавий = lively), `refreshed` → «посвіжілий»
    (добре відпочилий = well-rested), `to radiate` → «віддавати (про біль)».
  - **Пастка `rower` ≠ гребний тренажер.** У словнику `rower` = «весляр» (людина), тож
    `rowing machine` пішов ОКРЕМОЮ карткою, а не enAlt. Так само `changing room` у нас
    «примірочна» (крамниця), тому `locker room` = «роздягальня» — теж окрема картка.
    **Перед тим як вішати enAlt, дивитись ГЛОС наявної картки, а не лише ключ.**
  - **Свідомо пропущено (6 позицій):** `heavy weight`, `light weight` (прозорі фрази),
    `AMRAP`, `EMOM`, `VO2 max` (жаргонні абревіатури), `current body weight`
    (дубль до доданого `body weight`).
  - **Латинська анатомія й жаргон — hover-only (`m.set`), не картки:** `dorsi`, `abdominis`,
    `gluteus`, `gluteal`, `maximus`, `erector`, `latissimus`, `lat`, `pulldown`, `pushdown`,
    `delt`, `plyometric`, `abduction`, `adduction`, `overhand`, `underhand`, плюс
    прикметники-власні назви `bulgarian`, `romanian`, `russian`, `american`, `olympic`,
    `smith`, `roundhouse`, `rink`, `somebody`.
- **📌 ПІДСУМОК СЕСІЇ 48 одним блоком** (деталі кожного пункту — нижче по чейнджлогу):

  | Файл xlsx | Позицій | Карток | Виразів |
  |---|---|---|---|
  | 🚗 Автомобіль, водіння та дорога | 685 | 475 | 47 |
  | 💻 Комп'ютер, інтернет і ПЗ | 798 | 474 | 93 |
  | 🏙 Місто, вулиці та громадські місця | 567 | 283 | 81 |
  | 🛒 Покупки, замовлення та доставка | 688 | 336 | 134 |
  | ✈️ Подорожі, готель, вокзал, аеропорт | 753 | 418 | 122 |
  | **Разом** | **3491** | **1986** | **477** |

  **WORDS 10518 → 12504 · EXPRESSIONS 502 → 979.** Усі п'ять файлів покриті на 100%
  (фінальний аудит по кожному). Плюс **⚡ великий фікс продуктивності** — індекси
  словника й винесення даних у `dict.js` (перший пункт чейнджлогу нижче).

- Зміни сесії 48 (продовження): **⚡ ПРОДУКТИВНІСТЬ — ІНДЕКСИ + ВИНЕСЕННЯ ДАНИХ У `dict.js`.**
  Привід: користувач поскаржився, що вікно «періодично зависає, а буває по хвилині не
  вантажиться». Заміри підтвердили дві РІЗНІ проблеми.
  - **1. Підвисання в грі — лінійні скани словника.** Кожна відповідь у режимі UA→EN
    перебирала всі 12 504 картки з нормалізацією рядків: `isCorrect` 5.5 мс +
    `getCorrectAnswer` 20.5 мс + `renderMasteryStats` 26.5 мс ≈ **52 мс на відповідь**,
    і цифра росла лінійно з кожною партією. Додано три мемоізовані `Map` одразу після
    `getEn`/`getUa`: `_uaBareIdx` (укр. глос → картки), `_enNormIdx` (англ. слово →
    картки), `_uniqKeys` (унікальні wordKey). Результат: **52 → 7 мс**
    (isCorrect ×39, getCorrectAnswer ×410, renderMasteryStats ×4).
    ⚠️ Ключі індексів повторюють формули на місці виклику ЗНАК У ЗНАК (`uaBare` БЕЗ
    зміни регістру) — інакше зміниться логіка зарахування. Кеші живуть на тій самій
    умові, що наявні `_keyIdx`/`_sameKeyIdx`: **WORDS за сесію не змінюється**.
  - **2. Довге завантаження — 2.9 МБ inline-скрипта.** Браузер **НЕ кешує скомпільований
    байткод inline-скриптів** (на відміну від зовнішніх `.js`), тож парс+виконання
    платились при КОЖНОМУ відкритті, навіть із повного кешу. Плюс сторінка не малювала
    нічого, поки не завантажиться весь файл. Дані винесені в **`dict.js`**:

    | | було | стало |
    |---|---|---|
    | index.html | 3.78 МБ | **0.53 МБ** |
    | inline-скрипт | 2.91 МБ | **0.37 МБ** |
    | компіляція inline | 712 мс | **10 мс** |
    | виконання | ~600 мс | **113 мс** (+107 мс dict.js, кешується браузером) |

  - **Оновлено 10 інструментів + `verify.sh`** (окремий крок перевірки синтаксису
    `dict.js`) + `loadtest.js` (виконує `dict.js` перед головним скриптом) +
    `check-cefr.js` (порівняння з `git show HEAD:dict.js`).
  - **Перевірено:** `verify.sh` чистий і **321 браузерна перевірка** зелена
    (homograph 41, hints 21, panel 43, cefr-solo 33, cefr 28, due 31, wrong 14,
    expr 50, phrase-hover 18, lb-cefr 42), 0 помилок консолі — тобто `const` з
    `dict.js` справді видимі головному скрипту в реальному Chromium.
  - ⚠️ **Ризик, про який пам'ятати:** тепер сторінка залежить від ДВОХ файлів. Якщо
    GitHub Pages віддасть новий `index.html` зі старим `dict.js` (або навпаки) під час
    деплою, можлива коротка розсинхронізація. Обидва файли завжди міняються разом
    і в одному коміті — цього достатньо, але при дивних скаргах «слова зникли»
    перше, що перевіряти, — Ctrl+F5.
- Зміни сесії 48 (продовження): **✈️ ДВАНАДЦЯТА ПАРТІЯ — «ПОДОРОЖІ, ГОТЕЛЬ, ВОКЗАЛ
  ТА АЕРОПОРТ»: +418 карток і +122 вирази (WORDS 12086 → 12504, EXPRESSIONS 857 → 979).**
  xlsx користувача, **753 позиції, 20 розділів — найбільший файл сесії.**
  Розкладка: **200 уже були (27%)**, **27 частково**, **525 нові** → 5 партій
  (91 / 104 / 92 / 82 / 49). Покрито всі 753.
  - **CEFR:** B1 **134** · B2 **209** · C1 **75**.

    ⚠️ **Та сама пастка з порожнім B2 повторилася** — попри те, що я записав її після
    файлу «Покупки». Перший прохід дав B2 лише 10 з 418. Полагоджено проходом C1→B2
    на ~195 позицій. **Це вже системна помилка методу, а не випадковість: `cefr-move`
    зручно кликати двома списками (B1 і C1), і середній рівень просто випадає.
    Рахувати розподіл ОБОВ'ЯЗКОВО перед комітом партії.**
  - **Дублі enAlt — уже вп'яте за сесію.** Знято: `expiration date`←expiry date,
    `waiting list`←standby list, `cockpit`←flight deck, `carriage`←railroad car.
    Патерн незмінний: я створюю картку І передаю ту саму позицію 4-м аргументом
    `add-batch.js`. **Перевіряти список пар ПРОТИ списку карток партії.**
  - **Позиції, які пішли в enAlt, бо картка вже була під іншим написанням:**
    `guesthouse`→guest house, `hairdryer`→hair dryer, `first officer`→co-pilot,
    `restaurant car`→dining car, `booking`→reservation, `lost property`→lost property office.
  - **Фінальний аудит знову зловив 5 загублених позицій** (airport entrance, airport exit,
    bad weather + двi вище). Аудит по файлу — не формальність.
  - **Розведені глоси:** `travel guide` → «туристичний довідник» (путівник = guidebook),
    `carry-on bag` → «салонна сумка» (ручна поклажа = hand luggage),
    `flight cancellation` → «скасування авіарейсу» (скасування = cancellation).
  - **HINTS:** нова група `вилучати` = seize / to confiscate — підказки дописані.
- Зміни сесії 48 (продовження): **🛒 ОДИНАДЦЯТА ПАРТІЯ — «ПОКУПКИ, ЗАМОВЛЕННЯ ТА ДОСТАВКА»:
  +336 карток і +134 вирази (WORDS 11750 → 12086, EXPRESSIONS 723 → 857).** xlsx користувача,
  **688 позицій, 19 розділів.** Розкладка: **202 уже були (29%)**, **25 частково**,
  **458 нові** → 5 партій (88 / 71 / 95 / 41 / 41). Покрито всі 688.
  - **CEFR:** B1 **85** · B2 **124** · C1 **127** (A1/A2/C2 — жодної).

    ⚠️ **Пастка, яку зловив лише підсумковий підрахунок:** перші прогони `cefr-move`
    розкидали КОЖНУ картку між B1 і C1, і рівень B2 вийшов **порожнім** (0 з 336).
    Виглядало як системний збій, а не як судження. Полагоджено окремим проходом
    C1→B2 на 134 позиції. **Після кожного файлу рахувати розподіл по рівнях і дивитись,
    чи немає порожнього рівня.**
  - **Найбільший внесок у EXPRESSIONS за історію проєкту — 134 вирази.** Причина: у файлі
    70 дієслівних фраз плюс цілий розділ готових реплік («Where is my order?»,
    «The payment hasn't come through yet.», «Your parcel has been shipped.»). Близько
    15 виразів уже були з тем «Робота» й «Комп'ютер» — `expr-add.js` їх пропустив сам.
  - **⚠️ ДУБЛІ enAlt ПОВТОРИЛИСЬ ЩЕ ТРИЧІ** (те саме, що в міському файлі): позиція
    ставала і карткою, і `enAlt` до чужої картки. Знято: `bestseller`←best seller,
    `shipping`←delivery address, `faulty`←defective. **Правило: якщо позиція йде карткою,
    НЕ передавати її ж у 4-му аргументі `add-batch.js`.**
  - **Пари з реальною різницею значень лишені ОКРЕМИМИ картками, а не enAlt:**
    `waterproof` (водонепроникний) vs `water-resistant` (водостійкий), `fake` vs
    `counterfeit` (юридичний термін), `custom-made` vs `bespoke`, `outlet` vs
    `outlet store`, `promotion` vs `promo`, `faulty` vs `defective`.
  - **Фінальний аудит знайшов 7 позицій, що загубилися між великими партіями**
    (`contents`, `phone number`, MSRP, `cash-on-delivery amount`, `at the local depot`,
    `proof of purchase for warranty`, `replacement item under warranty`). На партіях
    по ~50 такого не було — це ціна укрупнення. Аудит по файлу обов'язковий.
  - **Розведені глоси:** `contents` → «вміст і комплектація» (вміст = content),
    `inconvenience` → «клопіт для когось» (незручність = embarrassment),
    `proof of identity` → «документальне підтвердження особи» (verification),
    `prepayment` → «попередня оплата» (передоплата = advance payment).
  - **HINTS:** нова група `безглуздий` = silly / senseless / ridiculous / **pointless** —
    підказку дописано.
- Зміни сесії 48 (продовження): **🏙 ДЕСЯТА ПАРТІЯ — «МІСТО, ВУЛИЦІ ТА ГРОМАДСЬКІ МІСЦЯ»:
  +283 картки і +81 вираз (WORDS 11467 → 11750, EXPRESSIONS 642 → 723).** xlsx користувача,
  **567 позицій, 17 розділів.** Розкладка: **184 уже були (32% — найвище покриття з трьох
  файлів сесії)**, **50 частково збігались (рекорд)**, **333 нові** → 3 великі партії
  (83 / 114 / 86). Покрито всі 567.
  - **CEFR:**

    | Рівень | п.1 | п.2 | п.3 | Разом |
    |---|---|---|---|---|
    | B1 | 31 | 35 | 24 | **90** |
    | B2 | 12 | 1 | — | **13** |
    | C1 | 40 | 78 | 62 | **180** |
    | **Карток** | 83 | 114 | 86 | **283** |

  - **⚠️ ЗМІНА РОЗМІРУ ПАРТІЙ — ПРЯМИЙ ЗАПИТ КОРИСТУВАЧА:** «по 60 слів у партії? якось
    дуже довго буде». Перейшов на **~110 позицій за партію** замість ~50. Працює, але
    JSON доводиться писати двома файлами (`t2a`/`t2b`) — інакше один heredoc задовгий.
    **Обов'язковий страхувальний крок при великих партіях:** скрипт, що звіряє список
    розділу з тим, що реально пішло в JSON (я двічі губив рядки, коли партія велика).
  - **⚠️ ПАСТКА ШЕЛА З АПОСТРОФАМИ:** пари для `enAlt` передаються 4-м аргументом
    `add-batch.js` як JSON-рядок. Якщо в них є апостроф (`chemist's`, `barber's`,
    `doctor's office`), одинарні лапки шела рвуть рядок. **Класти пари у файл**
    і передавати `"$(cat alt.json)"`.
  - **⚠️ ДУБЛІ enAlt vs ОКРЕМА КАРТКА:** тричі створив і картку, і `enAlt` на те саме
    (`daycare centre`, `real estate agency`, `doctor's surgery` — останній уже лежав
    в `enAlt` у `clinic`). Знято: якщо позиція стає карткою, `enAlt` до чужої картки
    прибирати, інакше два записи претендують на той самий рядок `en`.
  - **Хибні enAlt-пропозиції аналізатора в цьому файлі:** `street sign` (наявний
    `road sign` — дорожній знак, а не табличка з назвою вулиці), `litter bin`
    (`trash` = сміття, не урна), `barricade` (`barrier`), `landlady` (`landlord` —
    це стать, не синонім), `public toilet` (`restroom`), `hairdresser's` (`hair salon`).
  - **Справжні брит./амер. пари, що пішли в `enAlt` (19 у першій партії):** capital city,
    city centre, alleyway, lamppost, storm drain, manhole cover, office block, row house,
    first floor, foyer, streetcar, metro, subway, single ticket, chemist's, takeout,
    movie theater, daycare centre, bureau de change.
  - **`café` з наголосом НЕ додавати** — картка `cafe` вже має `coffee shop` в `enAlt`,
    а é ламає токенізатор (інцидент сесії 19). Позиція покрита без accent-форми.
  - **HINTS:** три нові групи (`нерівний` = rough/unequal/uneven, `дозвілля` =
    leisure/recreation, `оновлення` = update/renewal) — підказки дописані.
- Зміни сесії 48 (продовження): **💻 ДЕВ'ЯТА ПАРТІЯ — «КОМП'ЮТЕР, ІНТЕРНЕТ І ПРОГРАМНЕ
  ЗАБЕЗПЕЧЕННЯ»: +474 картки і +93 вирази (WORDS 10993 → 11467, EXPRESSIONS 549 → 642).**
  xlsx користувача, **798 позицій, 17 розділів — найбільший файл за всю історію.**
  Розкладка: **226 уже були (28%)**, **46 частково збігались**, **525 нові** → 15 партій.
  Покрито всі 798 (фінальний аудит чистий).
  - **CEFR:**

    | Рівень | п.1-3 | п.4-6 | п.7-9 | п.10-12 | п.13-15 | Разом |
    |---|---|---|---|---|---|---|
    | A2 | — | — | — | 2 | — | **2** |
    | B1 | 32 | 25 | 22 | 29 | 7 | **115** |
    | B2 | 42 | 24 | 19 | 10 | 18 | **113** |
    | C1 | 50 | 39 | 58 | 78 | 19 | **244** |
    | **Карток** | 124 | 88 | 99 | 119 | 44 | **474** |

    C1 більше за половину — тема технічна; A1 узагалі немає, A2 лише `whether` і `per`.
  - **⚠️ ПРОТИЛЕЖНОСТІ — НЕ enAlt.** Файл дає пари `mute/unmute`, `block/unblock`,
    `follow/unfollow`, `enable/disable`, `check/uncheck`, `lock/unlock`, `encrypt/decrypt`,
    `zoom in/zoom out`, `undo/redo`. `enAlt` означає СИНОНІМ, тож кожна половина —
    ОКРЕМА картка. Один раз я помилився в інший бік (`underline` дописав у `enAlt` до
    `bold`) — відкотив і зробив карткою.
  - **Хибні enAlt-пропозиції аналізатора (перевіряти очима ЗАВЖДИ):** `computer case`
    (наявний `tower` = вежа), `docking station` (`dock` = док/причал), `recycle bin`
    (`trash` = сміття), `device driver` (`driver` = водій), `command line` (`terminal`).
  - **Бренди — hover, а не картки:** `Windows`, `macOS`, `Linux`, `Bluetooth` пішли в
    `m.set`. Логіка: вони не є англійською лексикою, яку можна «знати» чи «не знати»
    (на відміну від службових слів сесії 42, які додавали картками).
  - **Абревіатури основною формою:** `PC`, `RAM`, `GPU`, `ECU`, `BIOS`, `DNS`, `RPM`,
    `ECU`, `API key`, `IP address` — повні назви в `enAlt` (патерн сесії 47).
  - **Розділ «Повсякденні дії та корисні фрази» майже цілком пішов у EXPRESSIONS** —
    32 з 35 позицій, включно з готовими репліками техпідтримки («The screen is frozen»,
    «Have you tried restarting it?», «It works on my machine»). Це перший розділ у
    практиці проєкту, де речення цілком стали картками виразів.
  - **🩹 УРОК, ЩО ПОВТОРИВСЯ (сесія 41 → сесія 48):** один коміт пройшов із двома
    непокритими словами, бо `./verify.sh` викликався **через пайп у `tail`** — код виходу
    з'їдається, `&& git commit` спрацьовує на «успіх» пайпа. Це рівно та причина, через яку
    `verify.sh` узагалі з'явився. **Запускати ГОЛИМ рядком** (`./verify.sh; echo $?`),
    ніколи не через `| tail`. Виправлено наступним комітом.
  - **HINTS:** три нові групи зі спільним глосом (`шаблон` = pattern/template,
    `цифра` = figure/digit, `глядач` = spectator/viewer) — усі шість карток отримали підказки.
  - **Слова-опори, яких бракувало в базовому словнику** (знайшов `check-coverage`):
    `whether`, `per`, `font`, `grammar`, `digit`, `viewer`, `module`, `connector`,
    `verification`, `authentication`, `shortcut`, `to refresh`, `to tick`, `to reset`,
    `to zoom`, `thermal`, `numeric`.
- Зміни сесії 48: **🚗 ВОСЬМА ПАРТІЯ — «АВТОМОБІЛЬ, ВОДІННЯ ТА ДОРОГА»: +475 карток
  і +47 виразів (WORDS 10518 → 10993, EXPRESSIONS 502 → 549).** xlsx користувача,
  **685 позицій, 16 розділів.** Розкладка: **160 уже були (23% — НАЙНИЖЧЕ покриття з
  усіх файлів; для порівняння «Робота» 42%, «Сім'я» 35%)**, **34 частково збігались**,
  **489 нові** → 14 партій. Покрито всі 685.
  - **CEFR:**

    | Рівень | п.1-3 | п.4-6 | п.7-9 | п.10-12 | п.13-14 | Разом |
    |---|---|---|---|---|---|---|
    | B1 | 58 | 21 | 23 | 24 | 14 | **140** |
    | B2 | 67 | 20 | 21 | 16 | 5 | **129** |
    | C1 | 34 | 51 | 35 | 48 | 38 | **206** |
    | **Карток** | 159 | 92 | 79 | 88 | 57 | **475** |

    A1/A2/C2 — жодної: тема технічна, найлегші позиції (`car`, `road`, `wheel`) уже були.
  - **🆕 Новий інструмент у репо: `expr-add.js`** — вставка партії у `EXPRESSIONS` (раніше
    вирази дописувались вручну). Формат `[{en,ua,note,tag}]`, дублі за `en` пропускає.
  - **⚠️ УРОК ПРО enAlt ПОВТОРИВСЯ (той самий, що в «Роботі»): аналізатор пропонує enAlt
    за збігом ОДНОГО слова, і майже чверть пропозицій хибні.** Перевірено очима, відхилено
    й зроблено ОКРЕМИМИ картками: `bodywork` (наявний `body` = тіло), `rocker panel`
    (`sill` = підвіконня), `brake disc` (`rotor` = ротор), `coil spring` (`spring` =
    весна/пружина), `hard shoulder` (`shoulder` = плече), `repair shop` (`garage` = гараж),
    `deductible` (`excess` = надлишок), `down payment` (`deposit` = завдаток),
    `dashboard` (`dash` = тире). **Ніколи не приймати enAlt-пропозицію без погляду на глос
    наявної картки.**
  - **Розподіл WORDS ↔ EXPRESSIONS за правилом сесії 37:** іменникові вузли й фразові
    дієслова (`to buckle up`, `to pull over`, `to skid`, `to conk out`) → картки;
    колокації «дієслово + об'єкт» (`to apply the brakes`, `to run a red light`,
    `to exchange details`, `to book a service`) → 47 виразів. Прийменники винесені в ноти:
    crash **INTO**, skid **OFF** the road, stuck **IN** traffic, keep **TO** the right.
  - **Абревіатури основною формою** (патерн сесії 47): `SUV`, `ECU`, `RPM`, `VIN`, `CV joint`,
    `TPMS` — повні назви в `enAlt`. У технічній мові скорочення частіше за розгорнуту форму.
  - **21 enAlt-доповнення до наявних карток** (брит./амер. пари): automobile←car,
    saloon←sedan, people carrier←minivan, splash guard←mudguard, number plate←licence plate,
    turn signal←indicator, gear lever←gearshift, parking brake←handbrake, gas pedal←accelerator,
    safety belt←seat belt, reverse gear←reverse, headlamp←headlight, GPS navigation←satnav,
    freeway←motorway, crosswalk←pedestrian crossing, traffic circle←roundabout,
    road construction←roadworks, slip road←ramp, tailback←traffic jam, line of traffic←queue,
    traffic lights/traffic signal←traffic light, give way←to yield.
  - **Слова-опори з прикладів окремими картками (+15):** wagon (→ m.set), gauge, to glow,
    coil, displacement, fumes, gasket, exhaust, adaptive, daytime, to beep, amber, sobriety,
    flap, to squeak, reflective, roadside, unlimited. Решта складників багатослівних карток
    пішла в `m.set`: panoramic, fob, infotainment, injector, catalytic, lug, xenon,
    prohibitory, oncoming, multi, valet, octane, misfire, conk, diagnostic, aftermarket.
  - **Омоніми українською розведено** (клас «позичених» глосів): `coupe` → «дводверне купе»
    (купе поїзда = compartment), `rental agreement` → «договір прокату авто» (договір
    оренди = lease), `instrument panel` → «щиток приладів» (панель приладів = dashboard),
    `off-road vehicle` → «авто для бездоріжжя» (позашляховик = SUV).
  - **HINTS:** нова група зі спільним глосом лише одна — «необмежений» = unlimited /
    unrestricted; обидві картки отримали підказки. Решта 9 «груп без підказки» у перевірці —
    успадковані пари, не з цієї сесії.
  - Конвеєр щоразу: `gloss-check` → `add-batch`/`expr-add` → `cefr-add` (+ переніс рівнів)
    → `./verify.sh` до чистого ✅. 14 партій, 14 комітів, усі перевірки зелені.
- Зміни сесії 47: **🗑 ЗАКРИТО ФІЧУ «📚 Повторив» (трекер повторень слів).**
  Привід (слова користувача): «повторення ми робили такими, що вони зараховуються, коли
  виходить 15 з 15 слів (помилкових), але зараз цього немає і можна це добавляти лише
  вручну, то думаю, і це можна прибрати». Після закриття 📒-джерела в сесії 45 лічильник
  міряв тільки натискання «+» — самопідтверджувана цифра.
  - Прибрано: блок кнопок `#selfrev-wrap` (`📚 Повторив` / `+` / `−`) з `#bottom-left-btns`
    разом із CSS (вкл. пілюлю `#selfrev-count`), рядок **«📖 Повторення слів»** з `#lb-totals`.
  - Видалено з JS: `saveSelfReviews`, `getTotalSelfReviews`, `renderSelfRevBtn`,
    `countSelfReview`, три слухачі й init-виклик.
  - **⚠️ Дані ЗАМОРОЖЕНО, а не стерто — той самий патерн, що з `oxford_mistake_reviews_v1`
    у сесії 45.** Лишились `SELF_REVIEW_KEY` (у BACKUP_KEYS), `SELF_REVIEW_XP` і
    `loadSelfReviews()`; увесь історичний тракт читає їх далі (`thermoRawForDate` з
    параметром `selfRev` — усі 8 викликів, carry-in, місячна ачівка, avg/день,
    `bestMonthPoints`/`computeRecords`, `computeDayStatsXp`) — але вже НІДЕ не показує.
    Прибрати читачі = заднім числом обнулити пункти й XP минулих днів. НЕ чистити як мертвий код.
  - **Другим кроком, за тим самим запитом, прибрано й історичний значок 📖N у «Прогресі
    по днях»** («не потрібна інфа»): з `_progMetaHtml`/`_progAggregate` знято параметр
    `selfRev`, видалено клас `.prog-selfrev`. **У UI не лишилось ЖОДНОГО сліду фічі**;
    `loadSelfReviews()` живе тільки всередині розрахунку пунктів «Плани» та XP.
- Зміни сесії 47 (продовження): **👕 ВЕЛИКА ПАРТІЯ «ОДЯГ І ЗОВНІШНІСТЬ» — +365 карток
  і +19 виразів (WORDS 9172 → 9544, EXPRESSIONS 135 → 154).** xlsx користувача,
  701 позиція, 19 розділів. Розкладка: **284 вже були** в словнику, **8 пішли в `enAlt`**,
  **409 справді нові** → 5 партій (82 / 75 / 84 / 54+12 виразів / 70+5 виразів) + добір 7.
  - **Правила, за якими розводились глоси** (щоб UA→US лишався однозначним): `formal wear`
    → «офіційний одяг» (бо «офіційний» = formal), `trendy` → «трендовий» («модний» =
    fashionable), `jumpsuit` → «суцільний комбінезон» («комбінезон» = overalls),
    `underpants` → «чоловічі труси» («труси» = panties), `gemstone` → «дорогоцінний камінь»
    («дорогоцінний» = precious), `linen` → «лляна тканина» («льон» = flax), `crease` →
    «залом» («складка» = pleat), `dial` → «шкала приладу» («циферблат» віддано watch face),
    `scruffy` → «недоглянутий» («неохайний» = messy), `headband` → «пов'язка для волосся»
    («обруч» = hula hoop), `good-looking` → «гарний на вигляд» («гарний» = beautiful).
  - **⚠️ ДОБІР +7 КАРТОК: ОМОФОРМИ ВСЕ Ж ДОДАНО** (за запитом користувача «точно варто їх
    пропустити?» — початкове рішення пропустити ПЕРЕГЛЯНУТО). `used`, `washed`, `worn`,
    `looks`, `fitting` + однозначні фрази `hair roots`, `layered haircut`.
    Механізм і чому без `m.set` це не працює — розділ «Що НЕ змінювати без узгодження».
  - **⚠️ ЛИШЕ 2 ПОЗИЦІЇ СВІДОМО НЕ ДОДАНО:** `pair of shoes` — прозора фраза (сума слів);
    `item of clothing` — теж прозора, пішла в `enAlt` до `garment`.
    Разом покрито **699 із 701** позиції файлу.
  - **Пари BrE/AmE й синоніми — ОДНА картка + `enAlt`, не дві:** sneakers +trainers,
    high heels +heels, pumps +court shoes, sliders +slides, pantsuit +trouser suit,
    clothes shop +clothing store, shoe shop +shoe store, changing room +fitting room,
    shop assistant +sales associate, wrinkled +creased, viscose +rayon, elastane +spandex,
    microfiber +microfibre, waist bag +fanny pack, keychain +key ring, watch strap
    +watch band, snap fastener +press stud, costume jewelry +costume jewellery,
    turtleneck +roll-neck, fringe +bangs.
  - **⚠️ БРИТАНСЬКІ НАПИСАННЯ — ЛИШЕ в `enAlt`, основне американське** (`solid-colored`,
    `matching colors`, `contrasting colors`, `microfiber`, `costume jewelry`): у словнику
    ключ `color`, тож BrE-форма основною зламала б hover.
  - **19 позицій пішли у EXPRESSIONS, а не в WORDS** (за правилом розподілу): готові репліки
    з примірочної («Can I try this on?», «It does not suit me.», «The sleeves are too long.»),
    сталі звороти (`in stock`, `out of stock`, `missing a button`, `to break in shoes`,
    `to tie your shoelaces`) і дієслівні колокації догляду (`to cut your hair`,
    `to grow a beard`, `to apply makeup`, `to remove makeup`, `to put on perfume`).
  - **~40 складників багатослівних карток пішли в `m.set`** (hover, не окремі ігрові слова):
    mid/mini/midi/maxi, puffer, varsity, sheepskin, attire, detachable, lightweight,
    bridesmaid, johns, resistant, Chelsea/Derby/Oxford (гео-назви моделей взуття),
    breathable, crossbody, duffel, elastic, eyeglass, fastener, faux, geometric, merino,
    signet, tote, trouser, washable, buzz, coily, foam, hourglass, lotion, salon, shaven,
    straightener, upright.
  - **Складники, що варті окремої картки, зроблено картками одразу** (`fleece`, `skinny`,
    `waterproof`) — вони й далі трапляються у файлі окремими позиціями.
  - `turtleneck` файл дає ДВІЧІ (як «водолазку» і як «високий комір») — це одна картка,
    другий сенс дописано в `uaAlt`.
  - **CEFR по всіх партіях** (⚠️ користувач попросив ЗАВЖДИ давати цю розкладку у звіті —
    сесія 47: «роби так і надалі». Не забувати в наступних партіях.):

    | Рівень | п.1 | п.2 | п.3 | п.4 | п.5 | добір | Разом |
    |---|---|---|---|---|---|---|---|
    | A2 | 12 | 11 | 15 | 12 | 9 | — | **59** |
    | B1 | 36 | 32 | 39 | 25 | 34 | 2 | **168** |
    | B2 | 33 | 29 | 30 | 17 | 27 | 5 | **141** |
    | C1 | 1 | 3 | — | — | — | — | **4** |
    | **Карток** | 82 | 75 | 84 | 54 | 70 | 7 | **372** |
- Зміни сесії 47 (продовження): **💼 СЬОМА ПАРТІЯ — «РОБОТА, БІЗНЕС, ПРОДАЖІ ТА ГРОШІ»:
  +255 карток і +108 виразів (WORDS 10263 → 10518, EXPRESSIONS 394 → 502).** xlsx користувача,
  **628 позицій — найбільший файл сесії**, 14 розділів. Розкладка: **261 вже була (42%)**,
  **7 у `enAlt`**, **360 нові** → 6 партій (48 / 41 / 53 / 40 / 28 / 45). Покрито всі 628.
  - **CEFR:**

    | Рівень | п.1 | п.2 | п.3 | п.4 | п.5 | п.6 | Разом |
    |---|---|---|---|---|---|---|---|
    | A2 | 6 | 6 | 6 | 6 | 3 | 8 | **35** |
    | B1 | 29 | 26 | 25 | 23 | 21 | 26 | **150** |
    | B2 | 13 | 9 | 22 | 11 | 4 | 11 | **70** |
    | **Карток** | 48 | 41 | 53 | 40 | 28 | 45 | **255** |

  - **⚠️ АБРЕВІАТУРА ЯК ОСНОВНА ФОРМА — свідоме рішення цієї теми.** SKU, MOQ, CTR, CPC,
    CPA, ROAS, CTA, KPI, AOV, COGS, ROI, VAT, HR, CEO: у бізнесі повну форму майже не
    вживають, тож картка йде від скорочення, а повна назва — в `enAlt` (і далі
    зараховується як відповідь). НЕ «виправляти» назад на повні форми.
  - **⚠️ ПРИЙМЕННИКИ — головна цінність нот у цій темі.** Українською їх часто немає
    взагалі або вони інші: `work FOR` компанію але `work AT` місце, `apply FOR` роботу
    але `apply TO` компанію, `pay IN cash` але `pay BY card`, `invest IN`, `entitled TO`,
    `spend money ON`, `follow up WITH`. **Окремо: `borrow FROM` (брати) vs `lend TO`
    (давати) — українською обидва «позичати», це класична помилка.**
  - **Дієслівні колокації з фіксованим партнером** (теж у нотах): MEET a deadline
    (не «reach»), GIVE a presentation (не «make»), MAKE a decision (не «take»),
    PLACE an order (не «make»), RUN a business (не «lead»), ENTER a market
    (англійською «входять», а не «виходять»).
  - **⚠️ УРОК ПРО АНАЛІЗАТОР:** три позиції (`to fulfill an order`, `to honor a warranty`,
    `to apologize for the inconvenience`) скрипт відніс до `enAlt`, бо ОДНЕ слово з них
    збіглося з наявною карткою (`to fulfil`/`to honour`/`to apologise`). Це цілі звороти,
    а не варіанти написання. **Перевіряй `enAlt`-кандидати очима — не кожен збіг частини
    означає варіант написання.**
  - **⚠️ ЩЕ ОДИН УРОК:** `training` мало не загубилося — я помилково вирішив, що воно вже
    в словнику (там був лише `train` = «поїзд»), і викинув його зі списку CEFR. Спіймав
    фінальний аудит покриття файлу. **Робити фінальний аудит по КОЖНОМУ файлу обов\u0027язково.**
  - **Порівняльні й найвищі форми знову вилізли:** `bigger` (переписав приклад),
    `biggest` (пішов у `m.set` — слово надто часте, щоб оминати щоразу).
- Зміни сесії 47 (продовження): **👨‍👩‍👧 ШОСТА ПАРТІЯ — «СІМ\u0027Я, СТОСУНКИ, ЗНАЙОМСТВА ТА СЕКС»:
  +179 карток і +62 вирази (WORDS 10084 → 10263, EXPRESSIONS 332 → 394).** xlsx користувача,
  **389 позицій — найбільший файл сесії**, 10 розділів. Розкладка: **137 вже були (лише 35% —
  найнижче покриття з усіх файлів)**, **7 у `enAlt`**, **245 нові** → 3 партії
  (65+11 виразів / 49+31 вираз / 65+20 виразів). Покрито всі 389.
  - **CEFR:**

    | Рівень | п.1 | п.2 | п.3 | Разом |
    |---|---|---|---|---|
    | A2 | 14 | 8 | 5 | **27** |
    | B1 | 36 | 29 | 30 | **95** |
    | B2 | 15 | 12 | 30 | **57** |
    | **Карток** | 65 | 49 | 65 | **179** |

  - **⚠️ НОВИЙ ТИП ПРОБЛЕМИ — ТЕРМІН БЕЗ ОДНОСЛІВНОГО УКРАЇНСЬКОГО ВІДПОВІДНИКА.**
    `brother-in-law` — це і швагер, і дівер, і зять залежно від того, чий це брат;
    `sister-in-law` — зовиця, своячка, невістка. Узяти котрийсь один основним глосом не
    можна (карало б за два інші валідні варіанти), а «невістка» ще й закріплена за
    `daughter-in-law`. **Рішення: основний глос ОПИСОВИЙ і однозначний («брат чоловіка
    або дружини»), усі побутові варіанти — в `uaAlt`.** Так само `half-brother` →
    «брат по одному з батьків», `godparent` → «хрещений батько або хрещена мати».
  - **Прийменники — головна цінність нот у цій темі** (українською їх немає взагалі):
    `flirt WITH`, `feelings FOR`, `cheat ON` (без ON — «списувати»), `lie TO`,
    `argue ABOUT` щось vs `argue WITH` когось, `file FOR` divorce, `close TO`,
    `attracted TO`, `get engaged TO`, а `marry` — БЕЗ прийменника (marry her, не «marry with»).
  - **Сучасний пласт увійшов повністю:** dating app, bio, to ghost, to swipe right,
    to match with someone, red flag / green flag, one-night stand, friends with benefits.
  - **Розділ про секс і сексуальне здоров\u0027я — нейтральна практична лексика** (як і в
    самому файлі): контрацепція, ІПСШ, фертильність + **формули згоди** («Are you
    comfortable with this?», «Do you want me to stop?», «No means no.», to withdraw consent).
    Ноти пояснюють неочевидне: `test positive` = «виявлено» (не «добре»), consent можна
    відкликати будь-коли.
  - **`bridesmaid` перестав бути `m.set`-складником і став повноцінною карткою** —
    у партії одягу він був лише частиною «bridesmaid dress», тепер це самостійне слово.
    ⚠️ Патерн на майбутнє: коли складник із `m.set` доростає до окремої картки,
    старий `m.set` треба ПРИБРАТИ, інакше він перекриє глос картки.
- Зміни сесії 47 (продовження): **🗣 П\u0027ЯТА ПАРТІЯ — «СПІЛКУВАННЯ, СУПЕРЕЧКИ ТА КОНФЛІКТИ»:
  +63 картки, **+78 виразів** і +17 підказок (WORDS 10021 → 10084, EXPRESSIONS 254 → 332).**
  xlsx користувача, 296 позицій, 9 розділів. Розкладка: **151 вже була** (51%),
  **3 у `enAlt`**, **142 нові** → 2 партії (25+42 виразів / 38+36 виразів). Покрито всі 296.
  - **CEFR:**

    | Рівень | п.1 | п.2 | Разом |
    |---|---|---|---|
    | A2 | 5 | 8 | **13** |
    | B1 | 16 | 19 | **35** |
    | B2 | 4 | 11 | **15** |
    | **Карток** | 25 | 38 | **63** |

  - **⚠️ ТУТ СПІВВІДНОШЕННЯ ВПЕРШЕ ЗВОРОТНЄ: виразів БІЛЬШЕ, ніж карток (78 проти 63).**
    Тема складається переважно з **готових мовленнєвих формул** («I beg to differ.»,
    «With all due respect…», «Correct me if I\u0027m wrong.», «Let me put it another way.»),
    а не з лексики. **Правило на майбутнє:** для тем «спілкування / переговори / етикет /
    сервіс» одразу закладай, що більша частина піде в EXPRESSIONS, і не намагайся
    втиснути цілі репліки в WORDS — у грі-словнику вони безглузді.
  - **У нотах фіксувались саме помилконебезпечні місця:** `IN my opinion` (не «to»),
    `appreciate IT if` («it» обов\u0027язкове), після `Would you mind` — -ing форма,
    `argue that` = доводити тезу (не «сваритися»), `draw` a conclusion (не «make»),
    `swear AT` vs `swear TO`, `call someone names` (у множині), `excuse` = виправдання,
    а не «вибачення».
  - **Британські написання — в `enAlt`:** apologize +apologise, criticize +criticise,
    rumor +rumour. `politeness` → `enAlt` до `courtesy` (той самий глос).
  - Глоси розведено: `persuasion` → «мистецтво переконувати» («переконання» = belief),
    `tactful` → «делікатний у словах» («тактовний» = considerate), `to deceive` →
    «обдурювати» («обманювати» = to cheat), `mediator` → «медіатор у конфлікті»
    («посередник» = intermediary).
  - **⚠️ ДРІБНИЦЯ ПРО `add-batch.js`:** виклик із порожнім списком (лише заради `enAlt`)
    лишає в `WORDS` порожній коментар-маркер партії — прибирати вручну.
- Зміни сесії 47 (продовження): **🙋 ЧЕТВЕРТА ПАРТІЯ — «ХАРАКТЕР, ПОВЕДІНКА ТА ОПИС
  ЛЮДИНИ»: +101 картка, +35 виразів і +88 підказок (WORDS 9920 → **10021 — рубіж 10 000
  пройдено**, EXPRESSIONS 219 → 254).** xlsx користувача, 299 позицій, 10 розділів.
  Розкладка: **155 вже були** (52%), **5 у `enAlt`**, **139 нові** → 2 партії
  (55+17 виразів / 46+18 виразів). Покрито всі 299.
  - **CEFR:**

    | Рівень | п.1 | п.2 | Разом |
    |---|---|---|---|
    | A2 | 7 | 5 | **12** |
    | B1 | 35 | 30 | **65** |
    | B2 | 13 | 11 | **24** |
    | **Карток** | 55 | 46 | **101** |

  - **✅ УРОК ПРО HINTS ІЗ ПАРТІЇ ЕМОЦІЙ ЗАСТОСОВАНО:** підказки планувались ОДРАЗУ
    разом із глосами, а не після. Сходи тут не менш щільні: perceptive/insightful/
    observant, resourceful/inventive, level-headed/sensible, self-controlled/restrained/
    reserved, conceited/vain/egotistical/snobbish, defiant/rebellious, thick-skinned/
    thin-skinned. Так і робити далі для будь-якої партії про риси, оцінки чи відтінки.
  - **Британські написання — в `enAlt` до наявних американських:** behavior +behaviour,
    organized +organised, disorganized +disorganised, judgment +judgement (уже був),
    self-centered +self-centred, judgmental +judgemental, honorable +honourable,
    likeable +likable. Той самий принцип, що з `color`.
  - **Чисті синоніми — `enAlt`, не окремі картки:** selfless → unselfish,
    dependable → reliable, obstinate → stubborn.
  - **35 позицій у EXPRESSIONS:** ідіоми оцінки (`to have a heart of gold`,
    `to think outside the box`, `to have a chip on one\u0027s shoulder`,
    `to judge a book by its cover`), «ярлики» на людину (`a lone wolf`, `a control freak`,
    `a people pleaser`, `a team player`, `good company`) і звороти з фіксованим
    прийменником (`respectful **towards**`, `look down **on**`, `take advantage **of**`,
    `easy to talk **to**`, `easy to get along **with**`).
  - Глоси розводились так само щільно: `quick-witted` → «із швидким розумом»
    («дотепний» = witty), `resilient` → «здатний відновлюватися» («стійкий» = steady),
    `persevering` → «той, що не кидає почате» (одразу два зайняті: «наполегливий» =
    persistent, «завзятий» = eager), `short-tempered` → «швидко зривається»
    («дратівливий» = annoying), `conceited` → «пихатий від самовдоволення»
    («самовдоволений» = complacent), `high-maintenance` → «той, що потребує багато
    уваги» («вимогливий» = demanding).
- Зміни сесії 47 (продовження): **🧠 ТРЕТЯ ПАРТІЯ — «ЕМОЦІЇ, ПОЧУТТЯ ТА ПСИХІЧНІ СТАНИ»:
  +119 карток, +43 вирази і **+92 підказки HINTS** (WORDS 9801 → 9920, EXPRESSIONS 176 → 219).**
  xlsx користувача, 337 позицій, 11 розділів. Розкладка: **170 вже були** (50%),
  **168 нові** → 2 партії (58+17 виразів / 61+26 виразів). Покрито 336 із 337.
  - **CEFR:**

    | Рівень | п.1 | п.2 | Разом |
    |---|---|---|---|
    | A2 | 6 | 6 | **12** |
    | B1 | 39 | 35 | **74** |
    | B2 | 13 | 20 | **33** |
    | **Карток** | 58 | 61 | **119** |

  - **⚠️ ГОЛОВНИЙ УРОК ПАРТІЇ: емоції — це суцільні СХОДИ СИНОНІМІВ, тож HINTS тут не
    бонус, а умова придатності.** Один український глос покриває по 3-5 англійських слів:
    sorrow/despair/heartache, fury/rage/irritation/annoyance/resentment,
    astonished/stunned/amazed, puzzled/baffled/bewildered/confused,
    uneasy/apprehensive/nervous/tense, closeness/intimacy, envy/jealousy.
    Без підказки в дужках гра карала б за валідну відповідь. **Правило на майбутнє:
    додаєш партію емоцій, оцінок або відтінків стану — HINTS плануй ОДРАЗУ, разом
    із глосами, а не «якщо лишиться час».**
  - **`down` СВІДОМО не став карткою** — високочастотний прийменник, картка перебила б
    йому hover у кожному реченні (та сама логіка, що з `used`/`washed`). Пішов у `enAlt`
    до `depressed`, тож відповідь зараховується. Так само `thankful` → `grateful`
    (файл сам зазначає взаємозамінність), `grief-stricken` → `heartbroken`,
    `devoted` → `dedicated`.
  - **43 позиції у EXPRESSIONS** — найбільша частка з усіх партій сесії: ідіоми стану
    (`to be on cloud nine`, `to feel blue`, `to have butterflies in one\u0027s stomach`,
    `to save face`/`to lose face`), звороти саморегуляції (`to keep one\u0027s cool`,
    `to come to terms with`, `to bottle up one\u0027s emotions`) і — окремо цінне —
    конструкції з ФІКСОВАНИМ прийменником, якого українською немає взагалі:
    `afraid **of**`, `snap **at**`, `get **on** someone\u0027s nerves`, `relate **to**`,
    `feel **for**`, `fall **for**`, `proud **of**`.
  - Глоси розводились найщільніше за сесію: `fury` → «нестримна лють» («лють» = rage),
    `resentment` → «затаєна образа», `enjoyment` → «задоволення від процесу»
    («насолода» = delight), `uneasy` → «з відчуттям тривоги» («неспокійний» = restless),
    `craving` → «непереборний потяг» («сильне бажання» = urge), `anticipation` →
    «передчуття приємного» («очікування» = expectation), `numb` → «емоційно нечутливий»
    («заціпенілий» = petrified), `mindful` → «уважний до теперішнього» («усвідомлений» = aware).
- Зміни сесії 47 (продовження): **🍽 ДРУГА ВЕЛИКА ПАРТІЯ — «ЇЖА, ПРОДУКТИ ТА ПРИГОТУВАННЯ»:
  +257 карток і +22 вирази (WORDS 9544 → 9801, EXPRESSIONS 154 → 176).** xlsx користувача,
  731 позиція, 19 розділів. Розкладка: **421 вже були** (58% — базова харчова лексика в
  словнику вже щільна), **15 у `enAlt`**, **295 нові** → 4 партії (66 / 79 / 68+7 / 44+15).
  Покрито **729 із 731**; не додано лише `to cook at home` і `to add salt` — прозорі фрази.
  - **CEFR:**

    | Рівень | п.1 | п.2 | п.3 | п.4 | Разом |
    |---|---|---|---|---|---|
    | A2 | 13 | 17 | 8 | 6 | **44** |
    | B1 | 37 | 43 | 38 | 28 | **146** |
    | B2 | 16 | 19 | 22 | 10 | **67** |
    | **Карток** | 66 | 79 | 68 | 44 | **257** |

  - **⚠️ НОВА ПАСТКА — ДІАКРИТИКА (партія 3/4):** `sauté` розбивач слів токенізує як
    **`saut`** (é зрізається), тож ключ картки `to sauté` такому токену НЕ відповідає і
    hover у реченні мовчить. Полагоджено `m.set("saut", …)`. **Те саме перевіряти для
    `café`, `purée`, `flambé`, `jalapeño`** — будь-яке слово з акцентом.
  - **⚠️ ПОРІВНЯЛЬНІ ФОРМИ З ПОДВОЄННЯМ І y→i (партія 2/4):** `heavier`, `hotter` вилізли
    як непокриті, хоча `darker`/`softer` проходять. Простіше **переписати приклад**, ніж
    заводити m.set на кожну форму — саме так і зроблено.
  - **Глоси розводились так само щільно, як в одязі** (харчова лексика перетинається з
    наявною ще сильніше): `cuisine` → «кулінарна традиція» («кухня» = kitchen), `supper` →
    «пізня легка вечеря» («вечеря» = dinner), `serving` → «одна порція» («порція» = portion),
    `aroma` → «приємний запах» («аромат» = flavour), `moist` → «м'який і не сухий»
    («вологий» = damp, «соковитий» = juicy), `crispy` → «хрусткий зверху» («хрусткий» = crisp),
    `to warm up` → «розігріти їжу» («підігрівати» = to heat), `spoonful`/`cupful` → «повна
    ложка»/«повна чашка», `opened` → «початий» («відкритий» = open).
  - **`sweet cherry` — окрема картка з `enAlt` `cherry`,** бо `cherry` тримає «вишню» з
    uaAlt «черешня»; `sour cherry` пішов у `enAlt` до `cherry`. Так обидві відповіді
    зараховуються й різниця вишня/черешня не губиться.
  - **`wafer` — окрема картка «тонка вафля», НЕ `enAlt` до `waffle`:** український глос у
    файлі однаковий, але це різні вироби. Злиття стерло б різницю назавжди.
  - **22 позиції у EXPRESSIONS:** рецептурні формули (`to preheat the oven`, `to crack an
    egg`, `over low/medium/high heat` — саме `over`, не `on`), написи з етикеток
    (`keep refrigerated`, `store in a cool, dry place`) і повний набір ресторанних реплік
    (`Could we have the bill, please?`, `I am allergic to…`, `for here`).
  - **~30 складників у `m.set`:** chia, Chinese, fatty, freshwater, saltwater, iodine, leafy,
    roe, airy, cayenne, cocoa, Greek, Mexican, rye, virgin, wholemeal, herbal, saut, airtight,
    Asia, Asian, Mediterranean.
- Зміни сесії 47 (продовження): **💬 РОЗДІЛ «ВИРАЗИ І СЛЕНГ» — XP, ДВА НАПРЯМКИ, ЧИСТКА.**
  Повний опис і всі правила — розділ «Розділ «Вирази і сленг»» вище. Коротко, чотири запити:
  - **⚡ XP:** +1 за показ картки, +3 за «✓ Знаю» (лише за перехід у «вивчено» і лише
    з тренажера). Новий лог по днях `oxford_expr_xp_v1` + 13-й параметр `computeDayStatsXp`
    + прапор `oxford_xp_formula_v17`. Пунктів «Плани» розділ і далі не дає.
  - **🔁 Два напрямки US→UA / UA→US з ОКРЕМИМИ статусами** (новий ключ
    `oxford_expr_status_ua_v1`) — бо «знаю з англійської» ≠ «зможу сказати англійською».
  - **🚫 «Вивчено» більше не потрапляє в колоду** (для поточного напрямку).
  - **📊 Вкладки статусів із лічильниками** (● На черзі / ↗ Вчу / ✓ Вивчено) другим рядом.
  - **🌡️ Пункти «Плани»: 12 пройдених карток = 1 пункт** (`EXPR_POINT_EVERY`,
    `exprPointsByDate` із перенесенням залишку, як у 📒). `thermoRawForDate` отримав
    10-й параметр `exprPts`, оновлені всі 8 викликів. У «Прогресі по днях» — новий
    значок **💬N** (`.prog-expr`, рожевий #f78fb3).
  - **🧹 13 простих слів прибрано з пісенного сленгу** (`careless` і компанія) — усі
    лишились у `glossary` пісень, `xp` пісень свідомо НЕ перераховувався. Банк 696 → 683.
  - **Новий тест:** `test-expr.js` (39 перевірок, браузерний).
  - Перевірено: `loadtest.js` + `test-panel.js` (43), `test-due.js` (31), `test-cefr.js` (28),
    `test-hints.js` (21), `test-wrong.js` (14) — усі зелені, 0 помилок консолі (це й доводить,
    що не лишилось звертань до видалених `#selfrev-*`).
- Зміни сесії 46: **📖-РЯД ЧИПСІВ У «ТРЕНУВАННІ» + МОДЕЛЬ ПОКРИТТЯ СЛОВНИКА.**
  WORDS без змін (8650). Коміти `407dd4b` … `df391f2`.
  - **Фіча:** другий ряд чипсів у наборі «🎓 Рівні CEFR» — ті самі рівні, але лише слова
    поза «Знаю», з відсотком вивченого на чипсі. Обидва ряди = один спільний вибір
    (`A1 📖+A2+B1`), однаковий рівень у двох рядах одночасно неможливий.
    Повний опис і всі правила — розділ «📖 Два ряди чипсів…» вище.
    ⚠️ Шлях був покроковий: спершу окремий набір `cefrsolo` з одиничним вибором → ряд
    мультивибору почав ховатись (користувач: «старі рівні не повинні зникати») → злилось
    в один вибір; ліміт «3 рівні» запропоновано й скасовано в тій же сесії. Кінцевий стан
    описаний у розділі; проміжні варіанти НЕ відроджувати.
  - **Розрахунок «скільки ігор до 95%»** (на запит користувача) — окремий розділ нижче.
  - **Словник, точкові правки:** `cycle` +«велосипед» (cycle lane), `quiz` +«тест»,
    `cheque` — основний глос «банківський чек» + «чек»/«платіжний чек» у `uaAlt`,
    `coding` +enAlt `programming`, нова картка `programmer` (B1, +enAlt `coder`),
    `roast` — додано прикметникове значення «запечений (у духовці)», `ever` +«колись»,
    +«хоч раз». ⚠️ Точні відповідники скрізь ішли в `uaAlt`, а не в `ua`: промптом у
    UA→US стають лише значення `ua`, тож дубль основного глосу («чек» = receipt,
    «колись» = sometime, «запечений» = baked) зробив би промпт неоднозначним.
    ⚠️ `ever` СВІДОМО не приймає «якось» — це `somehow` (спосіб дії), не час.
  - **`HINTS` +14 записів:** верхні щаблі сходів синонімів (outraged/incensed/indignant/
    heartbroken/despondent/exultant/erudite/superlative), способи готування
    (roast/baked/fried/grilled — український «смажений» покриває три різні слова)
    і ever/sometime.
  - **Новий тест:** `test-cefr-solo.js` (33 перевірки, браузерний).
  - **+155 карток «тіло й здоров'я»** (WORDS 8658 → **8813**) за xlsx користувача (384
    позиції, 182 вже були). Розділи: голова/обличчя, рот і зуби, шкіра, кінцівки, тулуб,
    кістки й м'язи, внутрішні органи, кров та імунітет, сечостатева система, функції тіла,
    симптоми, у лікаря. **+18 реплік у `EXPRESSIONS`** (111 разом) — «It hurts here»,
    «The pain comes and goes», «How serious is it?»: це розмовні конструкції, у WORDS їм
    не місце за правилом розподілу.
    ⚠️ **Свідомо ВІДКИНУТО (не повертати без запиту):** 8 надто вузьких термінів (uvula,
    perineum, glans, phalanx, flexor, extensor, antigen, microbiome), 12 прозорих складанок
    (upper/lower body, body part, internal organ, front tooth, upper/lower jaw, armpit hair,
    body hair, back of the hand, inner thigh, upper back), 9 дублів у іншому написанні
    (tooth/teeth, foot/feet, moustache, Adam's apple, esophagus, diarrhea, uterus/womb,
    bacteria, chills) — з них `baby tooth` пішов у `enAlt` до наявного `baby teeth`.
    ⚠️ **Чотири глоси переписані через колізію з НЕсинонімами:** `upper arm` → «верхня
    частина руки» («плече» зайняте shoulder), `lower back` → «нижня частина спини»
    («поперек» — це прислівник across!), `belly` → «живіт (розмовно)» (abdomen),
    `sense of taste` → «смакове відчуття» (taste).
    ⚠️ Складники багатослівних карток (tract, duct, canine, pectoral, lymph, circulatory,
    fallopian, menstrual, throbbing, shortness, marrow, achilles, pubic, clot) закриті
    через `m.set` — окремими картками вони не потрібні. `swollen` і `hormone` натомість
    додані повними картками (самостійні слова).
  - **+7 карток (WORDS 8650 → 8657)** — верхні щаблі «сходів синонімів» зі скріна
    користувача (good/big/happy/sad/smart/angry × A1-C2): `superlative` (C2),
    `exultant` (C2), `heartbroken` (C1), `despondent` (C2), `erudite` (C2),
    `outraged` (C1), `incensed` (C2). Решта 29 слів таблиці вже були.
    Проставлено рівні й двом «безрівневим»: `colossal` → C1, `shin` → B1.
    ⚠️ Головні глоси добирались із перевіркою на колізію UA→EN: «обурений» зайнятий
    `indignant`, «розлючений» — `furious`/`enraged`, «пригнічений» — `depressed`,
    «неперевершений» — `unsurpassed`. Тому outraged = «розгніваний», incensed =
    «оскаженілий», despondent = «зневірений», superlative = «найвищого ґатунку»;
    точні відповідники пішли в `uaAlt`. Додано 8 записів у `HINTS` (разом з
    `indignant`), щоб у рядку правильної відповіді було видно різницю відтінків.
  - **Анатомія ноги** (foot/ankle/shin/calf/knee/thigh/leg) — перевірено, всі 7 були.
  - **+359 карток «будинок і побут»** (WORDS 8813 → **9172**) за другим xlsx користувача
    (789 позицій, 332 вже були) — п'ять партій: житло й конструкція, меблі й освітлення,
    кухня й посуд, ванна й прибирання, комунікації та ремонт. **+24 вирази** (135 разом):
    сталі колокації домашніх справ (make the bed, do the laundry) і скарги на поломки
    (the tap is leaking, the fuse has blown). **+23 enAlt** до наявних карток.
    ⚠️ **Дві справжні прогалини, які підсвітив файл:** `storey` (поверх) і `draught`
    (протяг) не приймалися взагалі — у словнику були лише `story` = розповідь і
    `draft` = чернетка. Плюс `neighbour` — тепер enAlt до `neighbor`, як давно було
    в `neighbourhood` і `neighbouring`.
    ⚠️ **Свідомо ВІДКИНУТО:** 20 прозорих складанок (hot/cold water, wooden floor,
    apartment number, main/rear entrance, top floor, exterior wall, living space…),
    вузьке (UPS, pram room, meter room, silicone sealant), 19 варіантів написання
    наявних карток і 11 внутрішніх дублів файлу (apartment building = block of flats,
    dryer = tumble dryer…) — злиті через enAlt.
    ⚠️ **Розведені колізії глосів:** power strip «подовжувач із розетками» vs surge
    protector «мережевий фільтр»; dish rack «сушарка для посуду» vs drying rack
    «сушарка для білизни»; shoe rack «полиця для взуття» vs spice rack «полиця для
    спецій». У файлі кожна пара мала однаковий глос.
    ⚠️ **Формат:** дужки з `alternating current (AC)` / `direct current (DC)` перенесені
    в enAlt (у UA→US їх довелося б набирати буквально); 12 фразових дієслів оформлені
    як `to plug in`, `to tidy up`, `to declutter`…; описові прикметники `leaking`
    «той, що протікає» → «протікає», `flickering` → «мерехтливий»; обрізані глоси
    файлу (`laundry detergent` = «засіб», `towel rail` = «тримач») доповнені.
  - **Виправлено зарахування синонімів «підніматися»** (за скаргою: на промпт
    «підійматися» не проходили rise і climb). Причина не в семантиці, а в правописі:
    крос-логіка UA→US звіряє ЛИШЕ головні глоси, а в словнику жили два варіанти того
    самого дієслова. Канонічним промптом стало «підніматися» / «піднімати», другий
    варіант — у uaAlt. Тепер «підніматися» → climb, rise, to go up; «піднімати» →
    raise, lift. ⚠️ Якщо синонім «не зараховується» — перевіряти саме збіг ГОЛОВНИХ
    глосів (`ua`), а не сенс: `uaAlt` у напрямку UA→US не діє взагалі.
  - **⚠️ ВІДКАТ послаблення багатослівних карток (за запереченням користувача).**
    Пробували приймати головне слово замість цілого словосполучення (`bath mat` →
    «килимок»). Виявилось лазівкою: «килимок» став проходити для чотирьох карток
    (rug, mat, bath mat, doormat) — половина відповіді закривала всю сімʼю.
    **Правило на майбутнє: колізію глосів перевіряти з ОБОХ боків** — не лише «чи не
    ділять картки спільне головне слово англійською», а й «скільки карток УЖЕ приймають
    цей український глос». Зі списку 75 кандидатів у 21 глос уже приймали ≥2 картки.
    НЕ повертати.
  - ⚠️ `programming` саме `enAlt`, а не картка: глос «програмування» вже основний у
    `coding`, дві картки з тим самим глосом зробили б промпт UA→US неоднозначним.
- Зміни сесії 45: **ОДИН МЕХАНІЗМ ПОВТОРЕННЯ ПОМИЛОК + 5 ЗАКРИТИХ ДІР У ПРАВИЛАХ
  ЗАРАХУВАННЯ ВІДПОВІДЕЙ + ДАНІ Й UI (змістові коміти `9b4cd0d` … `040c07f`).**
  Словник майже не ріс: WORDS 8646 → **8650** (+`chips`, +`fries`, +`air conditioner`,
  +`to work out` — прогалини побутової лексики, знайдені за фідбеком); решта правок — поля прийнятих відповідей у 20 картках
  (`cool`, `too`, `also`, `doubles`, `singles`, `double`, `receptionist`, `chip`
  + 13 взаємних синонімів — п.9).
  Нижче індекс подій, повні деталі — у профільних розділах вище.

  **A. ПОМИЛКИ — ОДИН МЕХАНІЗМ ЗАМІСТЬ ДВОХ**

  1. **🗑 Кнопку «📒 Помилки (N)» і пункти «15 закритих = 1» ВИДАЛЕНО** (`9b4cd0d`,
     розділ «🗑 Сесія 45» у блоці про борг). Сесія 44 додала борг ЗВЕРХУ, нічого не
     прибравши, тож помилка писалась і в `oxford_due_v1`, і в `oxford_day_mistakes_v1`
     і одразу спливала в кнопці. Вирізано кнопку, CSS, Пробіл на старт-екрані,
     `updateReviewTodayBtn`, `pickReviewDay`, `reviewFromMenu`, `recordMistakeReviews`
     / `saveMistakeReviews` / `_totalMistakePoints`. `addXp(2)` став безумовним.
     `oxford_mistake_reviews_v1` ЗАМОРОЖЕНО (не пишеться, але читається — інакше
     обнулились би історичні пункти й XP). Засів `seedDueFromMistakes()`.
  2. **⚠️ ФІКС: борг ставила й сесія ПОВТОРЕННЯ** (`a06453e`). `scheduleDue` стояв без
     умови, а в дрилі «3 правильні поспіль» затинки нормальні — черга роздулась до
     35 слів. Тепер `if (!reviewRemaining)`. Разовий ремонт — `fixDuePollutedByReview()`.
  3. **⚠️ НОВЕ ПРАВИЛО БОРГУ, що СКАСОВУЄ домовленість сесії 44** (`49a9dba`): борг =
     помилки, НЕВІДПРАЦЬОВАНІ після post-game повторення, а не «борг зверху». Чисте
     закриття слова знімає борг, затинка лишає на завтра, не пішов у повторення —
     їдуть усі. Дослівне формулювання користувача — у блоці про борг.

  **B. ПРАВИЛА ЗАРАХУВАННЯ ВІДПОВІДЕЙ — 5 дір, усі про US→UA**
  Спільний корінь: промптом стоїть англійське слово, яке НЕ називає, якої саме картки
  від тебе хочуть. Деталі всіх п'яти — у «Що НЕ змінювати → uaAlt/enAlt».

  4. **Пари з однаковим EN** (`7c294d0`): 23 слова з двома картками (match=матч/сірник,
     cool, bank, light, tip). Приймаються глоси обох карток — `sameKeyWords()`.
  5. **Пари «X» / «to X»** (`c3e4268`): 43 слова (mean=підлий vs to mean=означати,
     book, watch, play). ⚠️ АСИМЕТРИЧНО: голий промпт приймає обидві картки, промпт
     «to X» лишається розведеним — «to» сам називає частину мови.
  6. **Неправильні форми як промпт** (`d4c9c0f`): 37 карток є ще й формою іншого слова
     (saw ⟵ see, rose ⟵ rise, spoke ⟵ speak, teeth ⟵ tooth). Приймаються глоси
     базового слова через наявну мапу `IRREGULAR`. Односторонньо. ⚠️ Морфології це НЕ
     дає: «бачити» проходить, «бачив» — ні (правило сесії 40 про форми лишається), тому
     6 найплутанішим карткам додано HINTS «не форма see».
  7. **Дужка в глосі ховала картку** (`51cdcdc`): `bill = «рахунок (до сплати)»` був
     невидимий під голим промптом «рахунок»; так само ВСІ британські варіанти
     (centre, programme, tinned). 17 карток із 22. Лік — `uaBare()`.
  8. **Взаємні пари-синоніми через `enAlt`** (`9b2c1d8`): запит «пройдись гуртом».
     ⚠️ Кандидатів 3591, фільтр `SYNS` (128) пропускає «гарний» ← gorgeous = витік
     сесії 30. Критерій взаємності лишив **20** пар, досиметризовано 13 напрямків.
     Решту — ТОЧКОВО за фідбеком, не суцільним проходом.

  **C. ДАНІ Й ІНТЕРФЕЙС**

  9. **HINTS стали підказками КАРТКИ** (`cca02fb`, розділ «💡 HINTS»): ключ «en:ua»
     як у `EXAMPLES`, голий ключ слова-пари ігнорується. Було 6 із 13 підказок, що
     відверто брехали («чайові (практична підказка)»).
  10. **Словник** (`c28f744`, `74effcc`, `4a255d2`, `091ea44`, `cd275e5`, `7c97b03`):
     `cool` +класний/кльовий; `too` +також і взаємний `enAlt` `too`↔`also`;
     `doubles`/`singles` +«розряд», `double` +двійник/дублер; `receptionist`
     +адміністратор/рецепціоніст; **`chip`: промпт-глос уточнено** на «відколотий
     шматочок» (голе «шматочок» → `uaAlt`, інакше в UA→US було оманливе завдання);
     **+3 нові картки `chips`/`fries`/`air conditioner`** — побутової лексики бракувало
     повністю. `cooler` дістав `uaAlt` кулер/охолоджувач/сумка-холодильник.
     ⚠️ `air conditioner`: компонент «conditioner» додано в `EN_TO_UA` через `m.set` —
     інакше стемер зводив його до `condition` і ховер у реченні давав «умова, стан»
     (той самий патерн, що gall/bladder для медичних термінів).
  11. **Ряд «Щодня»: однакова висота + кольори** (`538f6af`, розділ «🎛 Стартовий
     екран»). Легасі-`margin` у `#expr-btn` розпирав grid-ряд, а кольори з голого ID
     програвали `#daily-row button` за специфічністю.
  12. **Заголовок «Найкращі ігри · весь словник»** (`b7f29fe`): вкладка читалась як
     «усі мої ігри», хоча CEFR/«Знаю»/«не дались» у неї не входять. Заведено
     `setLbTitle(html, tip)` — `textContent` не знімав би `title` із сусідніх вкладок.
  13. **Полагоджено 2 провали `test-cefr.js`** (`88c34d5`) — це була ГНИЛА ПЕРЕВІРКА,
     не баг гри: пін `TODAY="2026-08-05"` використовувався й для рядка «остання гра»,
     який рендериться лише за `latest.key === todayKey()`. Тепер ключ береться з
     `todayKey()` самої сторінки. ⚠️ Мораль: пінуємо дату лише там, де пінований і код.

  14. **Приклади без свого слова** (за фідбеком: «тут прикладне речення взагалі не про
     те»). Прогнав усі 8645 прикладів на те, чи містять вони своє ключове слово в
     будь-якій формі (морфологія + `IRREGULAR`). Реально биті — **2**: `ancient`
     («The temple is over two thousand years old» — слова немає взагалі) і `tooth`
     («I have a terrible toothache» — слово лише всередині складного). Обидва
     переписані. ⚠️ Наївний детектор (без морфології) дає 55 хибних спрацювань
     (`organized`, `bees`, `broke down`), тому разова перевірка — з формами:
     +s/+es/+ed/+ing, подвоєння приголосної, y→ies, зворотна мапа `IRREGULAR`;
     складені ключі («pick up», «Adam's apple») — кожна частина окремо.
     Постійного чекера НЕ заводив: 2 випадки на 8645 не варті ще одного кроку
     у `verify.sh` з allow-списком під складені форми.

  15. **🎓 Рекорди CEFR: порядок за складністю + сума по всіх іграх набору**
     (`649a9b2`, розділ «🎓 Вкладка Рекорди за рівнями CEFR» вище). Рядки сортуються
     за складністю набору (найвищий рівень → найнижчий → ширина): A1 → A1+A2 → A2 →
     B1-C1 → B2+C2 → C2 → «—». ⚠️ Це ЗМІНЮЄ правило сесії 43, де ключем був ПЕРШИЙ
     рівень мітки: `A1-B2` тепер стоїть після `B1`, а не одразу за `A1`. Поруч із
     лічильником ігор — сума по всіх іграх набору («3 ігри · 47 (7/54)»), лише коли
     ігор >1. ⚠️ Користувачу здавалось, що сортування було «за рекордом» — насправді
     за першим рівнем; на його даних обидва правила давали однаковий вигляд.

  16. **Фразовий ховер у прикладах** (розділ «🔗 Фразовий ховер» вище). `wrapSentence`
     розбирала речення послівно — 508 речень показували багатослівний ключ частинами
     («worked out» → «працювати»). Тепер спершу шукається фраза 2-4 слова.
     ⚠️ Список-виняток `PHRASE_HOVER_SKIP` (7 фраз) — там, де частка насправді
     прийменник місця; знайдено прогоном по корпусу, не здогадкою. Новий
     `test-phrase-hover.js` (18), головна перевірка — рендер не втрачає ані символу
     в усіх 8647 прикладах. Заодно +картка `to work out`.

  **D. ⚠️ ІНЦИДЕНТ ІЗ ДЕПЛОЄМ (не код, але з'їв дві години)**
  Три деплої поспіль впали з `deployment_queued` → «Timeout reached». Головне, чим
  закінчилось: **`failure` у Pages Actions ≠ «не опублікувалось»** — черга доповзла
  вже після таймауту, зміни виїхали. Перш ніж пушити повторно, СПОЧАТКУ просити
  користувача оновити сторінку. Деталі — у шапці файлу, під інцидентом сесії 32.

  **Стан перевірок на кінець сесії — УСЕ ЗЕЛЕНЕ, незакритих хвостів немає:**
  `verify.sh` чистий; нові **`test-homograph` 41** і **`test-phrase-hover` 18**;
  `test-panel` 35→**43**, `test-due` 9→**31**, `test-lb-cefr` 32→**42**,
  `test-hints` 11→**21**, `test-cefr` **28/28**, `test-wrong` 14.
  Разом **238 браузерних перевірок** (18+21+41+43+31+28+42+14).
  ⚠️ Браузерні набори потребують `npm i playwright-core` (Chromium уже в `/opt/pw-browsers`).

  💡 **Помічене принагідно, не робив:** `cafe = кафе / кав'ярня` лежить двома картками,
  хоча це не омограф, а синоніми одного сенсу — напрошується один запис + `uaAlt`.
  Записано і в «Що залишилось / ідеї».
- Зміни сесії 44: **C2-ДОБІР ЗАКРИТО + 💡 ФІЧА «HINTS» (WORDS 7789 → 8646, +857 карток,
  1156 підказок, 23 коміти).** Повні деталі — розділи «C2-ДОБІР» і «💡 HINTS» вище;
  тут індекс подій.
  - **Привід:** «додай C2-слова з c2-candidates.json — вже можна, оскільки є розбивка
    по рівнях (від A1 до C2 і проміжки)». Список лежав відкладеним із сесії 42.
  - 11 партій по 46-98 карток, у кожній: gloss-check → add-batch → **cefr-add** → verify.
    858 з 875 позицій списку стали картками; лишились ~17 покручів (porten, flatout,
    wordage, standing…). `-ly`-прислівники додані окремими партіями 10-11 за прямим
    проханням користувача — рішення те саме, що зі службовими словами сесії 42.
  - **Новий інструмент `cefr-add.js`** — дописує ключі партії в `CEFR_DATA`. Без нього
    нові слова потрапляють у кошик «без рівня» в грі 🎓 «Рівні» (перевірено). Рівні тепер:
    A1 891 · A2 890 · B1 928 · B2 1567 · C1 1322 · **C2 885** = 6483 з міткою, 2163 без.
  - **Дві правки за скаргами з гри (обидві — від користувача під час партій):**
    `to pardon` тепер приймає «пробачати / прощати / прощення / вибачення» (uaAlt);
    `true` перевели на глос «істинний», а «правдивий» лишився за `truthful` + `enAlt:["true"]`,
    щоб крос-логіка ua-en і далі зараховувала true на цей промпт.
  - `test-cefr.js` ганявся після кожної зміни блоку рівнів — 30/30, 0 помилок консолі.
  - **💡 НОВА ФІЧА «HINTS»** (окремий розділ вище). Привід: у грі рядок відповіді показував
    «складний = hard / difficult / complex / challenging / complicated / intricate / tricky»
    без жодного пояснення. Тепер кожен варіант має уточнення в дужках. Формат узгоджено
    питанням: показувати В ОБОХ напрямках, але ЛИШЕ після відповіді (промпт чистий).
    Наповнено за 7 партій, від найбільших груп до пар: **1156 підказок, 544 групи, 100%**.
    Найцінніші — омоніми українською: мати (have/mother), ніж (knife/than), лютий
    (February/fierce), метр (meter/metre), рецепт (recipe/prescription).
  - Нові інструменти в репо: `cefr-add.js`, `hints-add.js`, `test-hints.js` (+ `test-cefr.js`
    із сесії 42). Усі — з коментарем-інструкцією в шапці файлу.
  - ⚠️ **Робочий висновок сесії:** щоразу, коли партія додає слова, лишається ДВА кроки,
    які легко забути: `cefr-add.js` (інакше слово без рівня) і перевірка груп HINTS
    (інакше неанотований список у рядку відповіді). Обидва описані у своїх розділах.
  - **🎛 ПЕРЕБУДОВА СТАРТОВОГО ЕКРАНА + 🔁 БОРГ ЗА ПОМИЛКИ** (окремі розділи вгорі).
    Привід: «багато режимів і всі розбиті». Тепер одна кнопка «Тренування» → модалка
    з двома осями (набір × напрямок), ряд «Щодня» (Duolingo і Кіно переїхали з лівої
    панелі), дрібний ряд English-only. Помилки підмішуються самі, окремої гри не треба.
    Прибрано: 4 кнопки режимів, модалку `#cefr-modal`, Shift-цикл, «Випадковий».
    Поріг кіно 500 → 1500. Модалку перемальовано під тему «Космос», Tab циклить набори.
  - ⚠️ **Пастка сесії:** чистка старого CEFR-коду зрізом «від функції до функції» знесла
    щойно вставлений блок «Тренування», який лежав між ними. Зловив `loadtest.js`.
  - Правки словника за скаргами з гри: `to like` → «подобатися» («любити» лишилось за
    `to love` + enAlt), `away` приймає «далеко / на відстані».
  - **🔁 Комірка «повтори» в «Прогресі по днях» стала клікабельною** (розділ вище):
    новий архів `oxford_day_wrong_v1`, бо старий «борг» стирався після повторення;
    модалка показує УНІКАЛЬНІ слова, гра з них іде з тегом `wrong` поза топ-10.
  - Кілька ітерацій дошліфовування панелі й модалки за фідбеком: ряд «Щодня» на grid
    (2 ряди замість 4), лічильники слів унизу карток, `plWords`, чистий «Напрямок».
- Зміни сесії 42: **📚 OXFORD ЗАКРИТО + 🎓 ГРА ЗА РІВНЯМИ CEFR (WORDS 6038 → 7789, +1751 картка,
  ~25 комітів).** Найбільша словникова сесія проекту. Повні деталі — розділи «OXFORD-ДОБІР»
  і «Гра за рівнями CEFR» вище; тут — індекс подій.
  - **Привід:** користувач попросив порахувати, скільки в нас слів кожного рівня A1-C2, і
    добрати відсутні за офіційним списком Oxford. Джерела витягнуті з мережі й покладені
    в репо (`oxford-cefr.json`), бо egress ріже oxfordlearnersdictionaries.com — беремо
    дзеркала на raw.githubusercontent.com.
  - **Стан на старті:** покриття Oxford 2804/4954 = **56.6%**, провал саме в цілі користувача —
    B2 47.3%, C1 22.7%. Наш словник (6015 слів) розкладався так: A1 793 · A2 675 · B1 472 ·
    B2 616 · C1 290, **поза Oxford 5000 — 3169 (52.7%)** — тематичний шар з EFE.
  - **Стан на кінець: 4570/4954 = 92.2%** (A1 88.8% · A2 91.9% · B1 91.0% · B2 91.2% · C1 96.6%).
    **Нової лексики Oxford не лишилось жодної.** Залишок 384 позиції не потребує карток:
    словоформи, які резолвить `stemEn` (242), службові в `HW_SKIP` (125), брит. написання
    в `enAlt` (9), вигуки (7) і `rid` (є карткою `to get rid of`).
  - **22 партії:** базові дірки A1-A2 (+60) → B1 (+61, +60) → B2 шістьма партіями (+75, +79,
    +82, +83, +87, +74) → C1 дев'ятьма (+87, +88, +88, +84, +88, +83, +92, +99, +87) →
    службові слова й абревіатури (+25) → «похідні» трьома (+86, +86, +97).
    ⚠️ Дірки були навіть на A1-B1: не було `game`, `wash`, `taxi`, `chart`, `paragraph`,
    `ice cream`, `context`, `benefit`, `vehicle`, `thief`, `doubt`, `lack`, `stuff`, `analysis`.
  - **⚠️ Службові слова додані КАРТКАМИ за прямим запитом** (after, between, during, without,
    against, among, except, such, less, plus, whatever, whenever, hers, lot + cd/dvd/id/o'clock):
    «хоч такі слова знайомі кожному, але їх теж треба врахувати, щоб я знав свій ЗАГАЛЬНИЙ
    словниковий запас». НЕ прибирати як «службові».
  - **19 enAlt-доповнень замість карток-дублів:** cab←taxi, police officer←policeman,
    television←tv, alright←ok/okay/all right, mom←mum, truck←lorry, trash←rubbish,
    neighborhood←neighbourhood, to analyze←to analyse, favor←favour, colored←coloured,
    disk←disc, inquiry←enquiry, to emphasize←to emphasise, to fulfill←to fulfil, honor←honour,
    globalization←globalisation, license←licence, information←info (+ ще ~15 у пізніших партіях:
    catalogue, counselling, spokesman/spokeswoman, sceptical, solicitor, traveller, judgement,
    to characterise, councilor, favourable, organisational, realisation).
  - **🩹 +6 неправильних форм у `IRREGULAR`** (clung, fled, upheld, overcame, rebuilt, undertook) —
    усі витяг `check-coverage` з нових прикладів. Патерн сесії 41 працює.
  - **🩹 `verify.sh` був ЗЛАМАНИЙ у кожній новій сесії:** третій крок кликав `loadtest.js` зі
    scratchpad сесії 41 → `MODULE_NOT_FOUND`. Стенд відтворено і покладено В РЕПО.
    Урок: сесійні стенди для словника більше не робити.
  - **🩹 Скан глосів на чужі символи спрацював удруге за історію:** в `uaAlt` до `methodology`
    затесалося іспанське «método» (у сесії 41 був китайський ієрогліф у `watercress`).
    Жодна інша перевірка такого не бачить — не прибирати цей крок з конвеєра.
  - **📌 «Похідні» — виправлення моєї ж оцінки:** спершу я назвав 269 слів із колонки `deriv`
    сміттям («-ly від наших прикметників»). Порахував на вимогу користувача: `-ly` там **9**,
    решта 260 — повноцінні лексеми (`awareness`, `commitment`, `location`, `reality`,
    `translation`), а частина «баз» узагалі хибна (`factor←fact`, `image←age`, `import←port`).
    Усі додані. Колонку `deriv` у `oxford-gap.js` НЕ вважати сміттям.
  - **🎓 НОВА ФІЧА «Рівні CEFR»** (окремий розділ вище): мультивибір рівнів A1-C2 + кошик
    «без рівня» → timed-гра з обраного пулу. Виникла з питання «наші всі слова можна розбити
    на рівні?» — відповідь: **5626 з 7766 (72%)** мають мітку, решта 2140 це тематичний шар,
    якого немає в жодному частотному списку.
    ⚠️ Одразу після релізу — фікс за скаргою: A1-гра стала №1 у «Найкращих іграх». Тепер
    CEFR-ігри поза топ-10, з підписом рівня і без номера місця (деталі — у розділі фічі),
    плюс одноразова міграція `oxford_fix_cefr_lb_v1`.
  - **🖥️ У цьому оточенні Є БРАУЗЕР** (на відміну від запису сесії 32 «немає playwright у
    cloud»): Chromium у `/opt/pw-browsers`, драйвер ставиться `npm i playwright-core` у
    scratchpad, запуск через `executablePath: "/opt/pw-browsers/chromium-<версія>/chrome-linux/chrome"`
    + `args:["--no-sandbox"]`, сторінка — `file:///home/user/Oxford_1000/index.html`.
    Фічу CEFR перевірено так двічі (16 + 14 перевірок). Обидва набори злиті в `test-cefr.js`
    у репо — **30 перевірок, 0 помилок консолі**; запуск: `npm i playwright-core && node test-cefr.js`
    (або `PW_CORE=<шлях>/node_modules/playwright-core node test-cefr.js`, якщо драйвер деінде).
    У `verify.sh` НЕ входить — потребує зовнішнього драйвера; ганяти вручну після змін
    у блоці CEFR або в лідерборді.
  - **Інструменти, покладені в репо цією сесією:** `oxford-cefr.json`, `oxford-gap.js`,
    `gloss-check.js`, `add-batch.js`, `gen-cefr.js`, `loadtest.js`, `test-cefr.js`,
    `c2-candidates.json`. Мета — щоб наступна сесія не переганяла аналіз наново.
  - 🩹 `m.set("ukrainian")` — чекер витяг власну назву з прикладу до `native`.
- Зміни сесії 41: **ФРАЗОВІ ДІЄСЛОВА — 3 партії (+63 слова) + ревізія від ChatGPT.**
  Користувач віддав список наявних фразових/виразів на ревізію GPT і приніс його правки.
  - **Партія 1 (побутові, +11):** to turn on/off, to try on, to throw away, to run out,
    to look after, to take out, to wake up, to give up, to put away (+trash від чекера).
    Звільнено глоси: `enable` «вмикати»→«уможливлювати», `wake` «прокидатися»→«будити»
    (перехідне) — обидва точніші за словником; фразові забрали звільнені глоси.
  - **Партія 2 (B2, +19)** і **партії 3-4 (B2-C1 22 + C1-C2 11)** — повний список у
    чейнджлозі комітів. +10 базових слів, яких вимагав `check-coverage` на нових прикладах
    (director, to dwell, to fend, phase, single, stem, to lash, to cheat, to boil, stick).
  - **Виправлено за ревізією:** `to turn up` основний глос «підкочувати»→«заявлятися»
    (реальне вживання = несподівано прийти); `to hang up` розведено на «класти слухавку»
    (основне) + «повісити» в uaAlt; `to throw away` без «жбурляти»; ноти виразів
    (get a cold теж природно, «чекати на дитину» = be expecting, measure temperature
    можливе, gonna/wanna залежать від речення); `give a smile`→`give someone a smile`.
  - **⚠️ ВІДХИЛЕНО з пропозицій GPT (не повертатись без узгодження):**
    (а) **прибрати `to` перед дієсловами** — ключ mastery = сам рядок `en`, mastery НІХТО
    не санує → перейменування обнулило б streak; `to` — конвенція всіх 114 дієслівних
    записів; гра й так приймає обидві форми при вводі (`isCorrect`);
    (б) **записи-шаблони з плейсхолдерами** (`pick something up`, `wake someone up`) —
    у ua-en гравець НАБИРАЄ англійську відповідь, шаблон набрати неможливо. Багатозначність
    розводимо як раніше: ua/uaAlt або два записи з однаковим `en` + дизамбіг-приклади.
  - **EXPRESSIONS 64→89** (+25 ідіом/колокацій B2-C1) і **`tag:"slang"`** для 9 записів
    (ain't/gonna/wanna/ya/lil'/shorty/swag/thug/lady lumps) — вони більше НЕ в «Ідіомах»,
    а у вкладці **«🎵 Сленг і розмовне»** (перейменована з «Сленг пісень»). `buildExprBank`
    читає `e.tag`. НЕ повертати ці записи в "idiom".
  - **📚 ВЕЛИКИЙ ВІДБІР: словник 3543 → 6038** (+2495, +70%): 10 великих тем + 24 дрібні
    (аркуш «Додати») + увесь аркуш «На перевірку» + добір прогалин.
  - **🎓 ПРОХІД ENGLISH FOR EVERYONE ЗАВЕРШЕНО.** xlsx користувача покривав розділи 24-180
    (стор. 58+), тобто ВЕСЬ залишок книжки — отже 402 сторінки пройдено. Тимчасовий трекер
    `english-for-everyone-progress.md` прибрано за його ж правилом (історія — в git).
    «Культура»; конвеєр і guard-и — окремий розділ «ВЕЛИКИЙ ВІДБІР» вище).
  - **🩹 +17 неправильних форм у `IRREGULAR`** (swept, shone, sprang, dug, tore, torn,
    sworn, shrank, stung, crept, dealt, dwelt, forbade, leapt, mown, sped, sunk): без них
    слово в прикладі лишалось без hover, і приклади доводилось писати лише в теперішньому
    часі. Стенд `irr.js` показує 0 непокритих.
  - **📚 СЛОВНИК 3425 → 3543 за сесію.** Крім фразових (вище): **+18 «покупки й реклама»**
    (receipt/bill/invoice/commercial/billboard/poster/banner/brand/slogan/cashier/refund/
    warranty/coupon/leaflet/to advertise/to promote/instant/channel; advertisement дістав
    `enAlt:["ad","advert"]`) — привід: слова з матчинг-вправи уроку, з яких бракувало лише
    `receipt`. **+38 EFE стор. 56-57 «Diet and nutrition»** — деталі у трекері
    `english-for-everyone-progress.md` (лічильник сторінок: 57/402).
  - **📊 АУДИТ СТРУКТУРИ XP (на запит користувача, числа станом на 04.08):** з 15 507 XP
    **ігри дають 67.7%** (10 492 = правильні 1829 + post-game повторення 3608 + бонуси
    mastery 5055) — це **≈31.7 XP за гру**. Головний висновок: ⅔ «ігрового» XP — НЕ самі
    відповіді, а повторення й бонуси за рух слів по категоріях. Невитрачений резерв бази —
    ~5 800 XP лише за перші зустрічі з рештою «Нових» (1943 слова).
  - **⚖️ ПЕРЕБАЛАНСУВАННЯ СТАВОК XP (за розрахунком XP/хв, прапор `formula_v15`):**
    порахували фактичний дохід: гра ≈32 XP за 5-7 хв (4.7-6.5 XP/хв) — і підтягнули решту
    під неї за РЕАЛЬНИМИ зусиллями, а не лише часом: 🦉Duolingo 30→**25** (легше за гру),
    🎧заняття 20→**30** (було 1.0-1.3 XP/хв — найдешевша хвилина), 🎬кіно 40→**60**,
    📒пункт помилок 15→**30**, 📖повторення слів 15→**30**. ⚠️ Мінялись ЛИШЕ константи —
    жива логіка і `computeDayStatsXp` читають їх; тултіпи кнопок 🎧/🎬/📖 оновлені вручну.
    Довідка по структурі XP користувача на той момент: 68% усього досвіду дають ігри
    (з них ~⅔ — не самі відповіді, а повторення + бонуси за рух слів по категоріях).
  - **🩹 ДВА ФІКСИ HOVER (за скаргами з проду; деталі — розділ «Hover translations»):**
    (а) присвійні форми (`baby's` та ще 13 токенів) не мали тултіпа — `stemEn` тепер зрізає
    `'s`/`s'`; (б) 55 `enAlt`-синонімів (`nappy`, `trousers`, `pram`…) не мали перекладу
    в панелі відповіді — доданий enAlt-пас у `EN_TO_UA` з guard-ом проти омографів
    checked/spotted. `check-coverage.js` більше не пропускає присвійні (через цей пропуск
    баг і не ловився) — правило оновлено в розділі «Обов'язкова перевірка покриття».
  - **Рекорди (UI):** порядок рядків дзеркалить «Загальну статистику» (ігри→відповіді→
    правильні→повторення→Duolingo→GPT→пісні→mastery→XP→пункти; коментар-guard у коді);
    плашки симетричні — `width:55%` + `align-self` flex-start/flex-end замість
    `max-width`+`margin-left:auto` (той стискав праві рядки до контенту); підпис
    «Досвіду (XP) за день» → «XP за день».
- Зміни сесії 40 (продовження): **📚 НОВА ФІЧА «Повторив» — трекер повторень слів.**
  Причина (слова користувача): «я сам інколи нажимаю на пройдені слова, що попали у Вивчаю, але
  це ніде не фіксується, так як я лише поглядом проходжуся і про себе повторяю». Плашка під 🤖 GPT
  за тим самим патерном; деталі й точки інтеграції — розділ «#bottom-left-btns».
  - **Одиниця = одне повторення** (сесія перегляду, не окреме слово) — вибір користувача.
  - Значок і підпис доопрацьовані за фідбеком із проду: 🧠→📚 на кнопці (📖 у прогресі/статистиці,
    бо 📚 там = LingoHut), лічильник-пілюля, «Самостійні повторення» → **«Повторення слів»**.
  - **Ставка = як пункт «📒 Помилки»**: 1 пункт «Плани» + 15 XP (його формулювання: «це аналог,
    відповідно і стільки ж повинно давати»). Нове джерело XP → **новий прапор `formula_v14`**
    (правило handoff: змінив формулу — додай прапор; жива логіка і `computeDayStatsXp` синхронні).
  - **⚠️ Сигнатура `thermoRawForDate` розширена ВОСЬМИМ параметром `selfRev`** — оновлено всі
    8 викликів (getThermoCarryIn, getThermoPointsToday, thermoPointsThisMonth, avgThermoPerDay,
    renderThermo, bestMonthPoints, computeRecords). `getBestDayPoints` рахує пункти інлайном і
    НЕ викликається ніде — легасі, свідомо не чіпав.
  - Перевірено стендом (справжні функції у vm, мок-DOM; 22 перевірки, 0 помилок): «+» дає 15 XP
    і 1 пункт, кілька складаються, «−» симетричний і не йде в мінус (запис дня видаляється),
    `computeDayStatsXp` бачить нове джерело і не падає без нього (старі виклики міграцій v3…v13),
    значок 🧠 у прогресі, рядок у статистиці, перерахунок детермінований.
- Зміни сесії 40 (продовження): **🧹 ЧИСТКА РІДКОВЖИВАНИХ ВАРІАНТІВ у `uaAlt` — прибрано 32.**
  Привід: користувач помітив «розмаїтий» на картці `varied` («в нас взагалі не застосовується») —
  слово лежало в uaAlt, але панель відповіді показує ua+uaAlt разом. Прибрано з varied/diverse,
  далі — суцільний скан на його запит.
  - **Метод скану (відтворити при потребі):** зібрати всі значення uaAlt (було 3622 унікальних
    у 2508 записах), відфільтрувати однослівні, що НІДЕ не є основним глосом (2535) — саме там
    ховаються книжні/діалектні/помилкові; переглянути список очима, згрупувати кандидатів.
  - **Прибрано 32:** форми-дублі (ридала, заблукав, прокинутий, відчинено, відкрито, розблоковано),
    росіянізми (юбка, чулки, чемодан, заколка, варежка), рідкісні/книжні (звитяга, бігунка,
    трюхати, вивірка, небіж, небога, зубожілий, уста, облудний), діалектні (лоточник, хідник),
    транслітерації (лептоп, есемес, пул, тішотка, онезі, снорхель, логувати, джерсі, слайдери)
    і «бубна» з `diamond` (картярська масть замість каменю; САМА картка лишилась).
  - **ЛИШЕНО за рішенням користувача** (не «недогляд», НЕ прибирати наступною сесією): рітейлер,
    екранувати, нічнушка, горнятко, деко, ровер, пращур, невіглас, немічний, тінейджер, фест,
    документалка, а також усі сучасні запозичення (софт, байк, денім, боді, скраби, лофери, сліпони).
  - **Процес рішення:** мій скан → ревізія ChatGPT (він відстояв живі регіоналізми й запозичення) →
    фінальні правки користувача (додав хідник/логувати/джерсі/облудний/слайдери; повернув
    рітейлер/екранувати/нічнушка). ⚠️ ChatGPT не знав механіки: він оцінював «чи слово правильне»,
    а вирішує «чи його наберуть» — критерій у розділі uaAlt/enAlt вище.
  - **Безпека:** скрипт-чистильник має ЖОРСТКУ перевірку — якщо кандидат виявиться основним
    глосом (`ua`), він падає й нічого не змінює (жодного разу не спрацювала). Порожні `uaAlt:[]`
    прибираються разом із полем. Після чистки: 3414 записів, 0 карток без ua, check-coverage OK.
  - 🩹 Дорогою виправлено ДРУКАРСЬКУ ПОМИЛКУ: `weary` мав uaAlt «знужений» (без «д») — правильно
    **«знуджений»**; через точний матчинг правильну відповідь неможливо було ввести. Знайшов
    користувач, звірившись із Google Translate. ⚠️ Урок: «слова немає в мові» і «слово написане
    з помилкою» — різні діагнози, формулювати точно.
- Зміни сесії 40 (продовження): **💬 +12 медичних зворотів у «Вирази і сленг» (52→64 картки).**
  have surgery / undergo surgery / operate on somebody / catch a cold / run a fever /
  be on medication / get better / feel under the weather / take somebody's temperature /
  make an appointment / be in good shape / pull through. Примітки пояснюють саме конструкцію
  (operate вимагає прийменника ON; have surgery БЕЗ артикля, але have AN operation — з ним).
  ⚠️ Користувач засумнівався («не дуже лягаються під наш словник, це скоріше вирази») — саме тому
  вони в `EXPRESSIONS` (окремий тренажер флеш-карток), а НЕ у `WORDS`: у звичайну гру не потрапляють.
  Це рівно правило розподілу сесії 37. Медичних зворотів там до цього не було жодного.
- Зміни сесії 40 (продовження): **🩹 `to lie` — розведено дієслівні й іменникові сенси (за питанням
  користувача «to lie це хіба більше не брехати?»).** Було: `to lie` = лише «лежати», а «брехати»
  і «лежати» помилково висіли в `uaAlt` ІМЕННИКА `lie` — тобто дієслівні сенси на іменниковій картці,
  а «брехати» не мало промпт-глоса взагалі. Стало: **`to lie` = ["лежати","брехати"]** + uaAlt
  [прилягти, обманювати, говорити неправду]; **`lie` = «брехня»** + uaAlt [неправда, обман, вигадка].
  Доданий дизамбіг-приклад `to lie:брехати`; сенс «лежати» далі бере `lie:лежати` через bare-fallback.
  Укр. приклад іменника вирівняно з глосом («Не бреши» → «Не кажи неправди»). Перевірено емуляцією
  `lookupExample`: кожен із 3 сенсів тягне свій приклад.
- Зміни сесії 40 (продовження): **🏥 МЕДИЧНИЙ БЛОК EFE стор. 48-55 — 4 розвороти, +154 слова
  (WORDS 3256→3410, 4 коміти).** Хвороби/травми (44), візит до лікаря + аптечка + дієслова (44),
  лікарня + відділення (35), стоматолог + оптика (31). Повний перелік і рішення — у трекері
  `english-for-everyone-progress.md`. **Наступна сторінка EFE — 56.** Ключове:
  - **UK-дублети — через `enAlt`, без окремих карток** (підтверджено користувачем; патерн сесії 38):
    plaster, cotton wool, false teeth, milk teeth, short-/long-sighted, physiotherapy, A&E,
    theatre, doctor's surgery, diarrhoea, haemorrhage, paediatrics.
  - **`medication`/`tablet`/`inoculation` — ПОВНОЦІННІ картки за рішенням користувача**
    («і ті і ті зустрічаються»), а НЕ enAlt до medicine/pill/vaccination. `tablet` = таблетка +
    **планшет**. НЕ «виправляти» назад на дублі.
  - **⚠️ Спільні глоси свідомо НЕ уникали** (перевірено на `isCorrect`): у ua-en приймається
    будь-яке слово зі спільним глосом, тож `рецепт` (recipe/prescription) і `таблетка`
    (pill/tablet) — норма, а не колізія. Обхід робили ЛИШЕ там, де глос належить НЕ-синоніму
    (sling≠«шина»=tire, safety pin≠«шпилька»=pin). Багатослівні точні глоси («медична маска»)
    ОБОВ'ЯЗКОВО мають голу форму в uaAlt — інакше en-ua відхиляв би «маска».
  - **Чекер знову витяг базові дірки** (слів не було ВЗАГАЛІ): wire, shock, pharmacy,
    examination, adhesive, nasal, pad, compression, label, passage, oxygen, surgical, intensive,
    unit, cancer, therapy, kit, length. Патерн стабільний — нові приклади щоразу витягують
    кілька відсутніх базових слів; це нормальна частина процесу, а не помилка добору.
  - Пропущено ~32: прозорі когнати-відділення (радіологія/дерматологія/урологія/гінекологія/
    патологія/офтальмологія/ендокринологія/гастроентерологія/ортопедія) і вузьке обладнання
    (phoropter, Snellen chart, retinal camera, whiplash, astigmatism, monocle, opera glasses,
    bifocal…) — та сама категорія, що декуттер анатомії сесії 35.
  - m.set-компоненти: runny, hay, ray, brace, compressions, hygienist, easier (порівняльний
    ступінь — патерн `prettier` сесії 39).
- Зміни сесії 40: **📊 кумулятивні лічильники відповідей на слово (за пропозицією користувача).**
  - **Проблема (з гри):** streak має стелю 3 і обнуляється будь-якою помилкою, тож слово, яке
    ти 12 разів узяв правильно й раз збився, виглядає у «Вивчаю» так само, як щойно побачене.
    При 1467 словах у категорії це «плутанина» — не видно, ЩО саме збиває.
  - **Зроблено:** запис mastery `число` → `{s,c,w}` (streak + правильні/помилки), БЕЗ міграції
    (старий формат читається як є, апгрейд на місці при першій відповіді). Категорії/XP/дельти
    лишились виключно на `s`. Хелпери `mStreak`/`mTally`, усі 8 читачів переведені.
    Деталі й guard-и — розділ «Mastery tracking».
  - **Рішення користувача:** лічильники рухає ЛИШЕ звичайна гра з першої спроби; повторення
    і «📒 Помилки» не рахуються («я на них уже відповів неправильно, це лиш засвоєння»).
    Пропуск = помилка (в `w`), як і в бейджі «✗N».
  - **UI:** у модалці категорій бейдж `✓N ✗N`, «Вивчаю» сортується за проблемністю
    (розділ «Гра по категоріях»). Нюанс сортування безпомилкової зони — там же.
  - **Перевірено двома стендами** (справжні функції гри у vm-контексті, мок-DOM; 32 перевірки,
    0 помилок): цикл Нові→Вивчаю→Знаю з +3/+5/−3 XP, стеля streak при живому лічильнику,
    помилка не стирає історію, старий числовий формат у всіх читачах + апгрейд, same-en пара
    (run) ділить лічильник, рендер і сортування модалки, інваріант K+L+N.
  - **Відкладено СВІДОМО:** ігровий пул «📕 Проблемні слова» (`w>=3` / `w>c`) — поріг обирати
    після 1-2 тижнів реальних даних, інакше наосліп.
  - Словникові дрібниці за грою: `application` +uaAlt [застосунок, програма, заява, звернення]
    (глос «застосунок» — ОСНОВНИЙ у `app`, тож лише uaAlt); `renovation` +**enAlt [repair]**
    +uaAlt [оновлення, переобладнання] — на промпт «ремонт» проходив лише renovation, бо
    «ремонт» у repair лежить в `uaAlt`, а той у ua-en НЕ діє (той самий урок, що
    bracelet/wristband у сесії 38 — для англ. синонімів потрібен `enAlt`).
- Зміни сесії 39 (продовження): **🔬 повний прогін словника через ігрову логіку (за пропозицією
  користувача) + 🔴 фікс XP-формули mastery + семантика дельт (formula_v13).**
  - **Прогін-стенд (headless, справжні функції гри):** життєвий цикл усіх 3236 слів
    Нові→Вивчаю→Знаю — 0 збоїв; 20 same-en пар ділять прогрес коректно; приймання
    відповідей: всі ua + uaAlt + en усіх 3256 записів — 0 хибно відхилених; бейджі=модалки.
  - **🔴 БАГ (знайдений воркфлоу-верифікацією, підтверджений стендом):** жива логіка платить
    +3 за КОЖНУ першу зустріч (брутто), а `computeDayStatsXp` рахувала `max(0,Δl)·3 + Δk·5`
    (нетто) → кожне слово, що за один день проходило Нові→Вивчаю→Знаю, ВТРАЧАЛО свої +3 при
    будь-якому перерахунку (v12 так і перерахувала історію). **Фікс: `3·Δl + 8·Δk`** —
    алгебраїчний еквівалент живого нарахування (інваріант Δl+Δk=перші зустрічі; 8=3+5 бо
    промоушн покидає «Вивчаю»). Працює на ВСІ історичні дні (лог не потрібен). Свідома
    неточність: регресія коштує −5 замість живих −3 (2 XP на рідкісну подію; R зі снапшотів
    не відновлюється). Міграція **formula_v13** повертає з'їдене. Перевірено стендом: 4
    сценарії live==formula точно, регресія — рівно задокументовані −2.
  - **Снапшоти mastery:** (а) `saveMasterySnapshot` тепер рахує по унікальних ключах WORDS
    (та сама метрика, що бейджі/модалки з фіксу цієї ж сесії; орфани не потрапляють);
    (б) виклик додано в `endGame` (timed-гілка) і на `pagehide` — раніше закриття вкладки
    з екрана результатів лишало знімок доігровим і переходи «перетікали» в наступний день.
  - **Семантика дельт ✓/↗ пояснена в UI** — див. оновлений блок «СЕМАНТИКА ДЕЛЬТ» вище
    (title на комірках, «(у прогресі +N)» у модалці, комірка при нетто 0). `_pgCell` отримав
    4-й параметр attrs (замість лапкового хака в cls).
  - **⚠️ Відомий нюанс (НЕ чіпали, задокументовано):** mastery-дати (снапшоти, лог переходів)
    — UTC (`toISOString`), а ігри/повторення/Duo/GPT/кіно (`todayKey`) — ЛОКАЛЬНІ. Для Києва
    (UTC+3) заняття 00:00–03:00 лягають у різні рядки прогресу. Уніфікація = зсув ключів
    сховищ + міграція — лише окремою задачею за узгодженням.
- Зміни сесії 39 (продовження): **⚡ XP за «безXP-ові» активності (за запитом), міграція `formula_v11`.**
  Нові константи біля `DUO_XP`: `SONGSESS_XP=20`, `MOVIE_XP=40`, `MISTAKE_REVIEW_XP=1`.
  • 🎧 заняття піснями: +20 XP (симетрично на «−»); • 🎬 кіно: +40 XP і **2 пункти «Плани» замість 1**
  (`thermoRawForDate` ×2); • 📒 **+15 XP за ПУНКТ** (= за закриття 15-ки слів), а НЕ по 1 XP за слово.
  ⚠️ Це принципово: 7 закритих слів = 0 XP, 15-те слово замикає пункт і дає одразу +15; залишок
  переноситься між днями, тож XP «падає» в день замикання пари (як самі пункти). Жива логіка в
  `endGame` платить за ПРИРІСТ пунктів (`_totalMistakePoints()` до і після `recordMistakeReviews`),
  а `computeDayStatsXp` приймає вже пораховані `mrPts` (`mistakeReviewPointsByDate`), НЕ сирі слова.
  ⚠️ Перша ітерація (прапор v11) давала +1 XP за КОЖНЕ слово — користувач одразу уточнив, що хотів
  «15 XP за 15 слів», тож правило замінено, а прапор піднято до **v12** (v11 кілька хвилин був у проді).
  `computeDayStatsXp` отримала 3 нові ОПЦІЙНІ параметри (songSess, movies, mrPts) — старі блоки міграцій
  v3…v9 кличуть її без них і не ламаються; `computeXpFromStats` вантажить нові джерела й додає їх у allDates.
  Перевірено headless: живі ± симетричні (20/40/15), кіно дає +2 пункти; пороги 14/15/29/30 слів дають
  0/15/15/30 XP, перенесення 8+7 через два дні кладе +15 у день замикання, міграція ідемпотентна, 0 помилок.
  🩹 **Виправлено ПОМИЛКУ ДОКУМЕНТАЦІЇ:** handoff стверджував, що GPT «−» знімає 10 при «+» 15 —
  код завжди був симетричний (`addXp(-GPT_XP)`), застряг текст сесії 27. Не «виправляти» код під старий опис.
- Зміни сесії 39 (продовження): **41-ша пісня «Bryce Savage — Devil in Her Eyes 😈»**
  (`ytId="RakR7T2W-NI"`, 700 XP, score 79.0; 7 секцій / 40 рядків, глосарій 13, сленг 11).
  Комерційний реліз → патерн Lollipop/Phenomenon (я каркас, текст і переклад вставив користувач).
  Мітки секцій вирівняно під термінологію розділу: ІНТРО→ВСТУП, БРІДЖ→БРИДЖ, АУТРО→КІНЦІВКА.
  Для hover **+14 слів у WORDS**: devil, curse, hearse, moonlight, sunlight, naughty, sinner, sin,
  tattoo, trap, extra, reborn, god + **priest** (його витяг чекер із мого ж прикладу до sinner).
  `m.set("prettier")` — стемер не зводить порівняльний ступінь pretty→prettier. WORDS 3242→3256.
  Сленг пісні багатий: ride-or-die, no cap, bedroom eyes, tats, to make it out, the other side.
  ⚠️ При вставці з Genius користувач правильно відсік врізку «You might also like» — при
  наступних піснях перевіряти це автоматично (скрипт-перевірка стоїть у процедурі приймання).
- Зміни сесії 39 (продовження): **🔢 фікс розбіжності лічильників mastery (скарга: бейджі
  57/1448/1737, модалки 58/1455/1729).** Причина: бейджі рахували КЛЮЧІ mastery, модалки —
  ЗАПИСИ WORDS; 20 same-en пар ділять ключ (у користувача 8 таких у mastery: 1 known +
  7 learning → ΔK+1/ΔL+7/ΔN−8, збіглося з нульовою свободою). Фікс «слова скрізь»:
  renderMasteryStats ітерує унікальні ключі WORDS (імунітет до осиротілих), тотал-бейдж =
  3222 слова (тултіп: 3242 записи, 20×2 сенси), openCategoryModal групує рядки за wordKey
  у всіх категоріях (патерн сесії 38). Діагноз і план верифіковано ВОРКФЛОУ (3 адверсарні
  лінзи: математика/регресії/UX — підтверджено, 2 major-поправки враховані: ітерація по
  ключах WORDS замість Object.values(m); групування і в «Нових», де висіло 12 дублів).
  Емуляція: старий код відтворює обидва скріни точно; новий — бейджі=модалки. Headless-смоук:
  групований рядок, TTS, старт гри, 0 помилок. Деталі — розділи «Mastery tracking» і «Гра
  по категоріях». Очікувані числа користувача після деплою: 57 / 1448 / 1717 · 3222.
- Зміни сесії 39 (продовження): **39-та пісня «NEFFEX — Closer to Heaven 🕊️»**
  (`ytId="3zPiacz_X2U"`, 450 XP, 6 секцій / 38 рядків, глосарій 15, сленг 5).
  NEFFEX = copyright-free → повний текст + переклад (на відміну від комерційного Lollipop-каркаса).
  Для hover додано **+3 слова у WORDS** (їх не було ВЗАГАЛІ): heaven(рай), everybody(усі),
  alright(гаразд) — глоси були вільні, колізій немає. WORDS 3223→3226. Сума XP колекції 36250→36700.
  ⚠️ Урок про шкалу: `slang×2` входить у score, тож xp рахуй ПІСЛЯ заповнення slang.
- Зміни сесії 39 (продовження): **40-ва пісня «Thousand Foot Krutch — Phenomenon ⚡»**
  (`ytId="x4EpbuqJcRw"`, 550 XP, score 64.8; 6 секцій / 55 рядків, глосарій 17, сленг 6).
  **Комерційний реліз → патерн Lollipop:** я зробив каркас (id/ytId/мітки секцій/коментарі),
  рядки й переклад вставив САМ КОРИСТУВАЧ через веб-редактор GitHub; далі я добив глосарій,
  сленг, hover і XP. Межа лишається: тексти комерційних пісень я у файли/чат не пишу.
  Для hover **+16 слів у WORDS** (не було ВЗАГАЛІ): soul, shine, switch, praise, unite, invasion,
  navigation, communication, phenomenon, surround, inject, righteous, contamination, fighter,
  rider, lighter — усі глоси були вільні. +`m.set("anymore")`. WORDS 3226→3242.
  Мітку секції «БРІДЖ» виправлено на «БРИДЖ» (термінологія розділу).
- **⚠️ HW_SKIP — важливо для перевірки пісень (з'ясовано в сесії 39):** `check-song-words.js`
  показує у списку «ВЗАГАЛІ НЕМАЄ перекладу» ще й службові слова (and/the/you/being/down/might…),
  для яких гра hover **навмисно не показує** — вони в `HW_SKIP` (97 слів, index.html).
  Додавати їх у EN_TO_UA чи m.set МАРНО — `wrapSongLine` для них одразу повертає null.
  Чекер також НЕ читає пісенний `glossary`, тож покриті ним слова теж лишаються в списку.
  Реальна перевірка «0 слів без перекладу» = залишок чекера МІНУС HW_SKIP МІНУС glossary.
- Зміни сесії 39 (СУЦІЛЬНИЙ ПРОХІД uaAlt по всьому словнику; 15 комітів):
  - **Проблема (з гри):** гра відхиляла чесні синоніми в 🇺🇸→🇺🇦, бо ~2805 із 3223 записів мали
    лише 1-2 прийнятні варіанти. Користувач ВІДХИЛИВ часткові підходи («перша 1000», пробні
    партії): «яке саме слово попадеться — невідомо, тому якщо братися, то одразу за всі».
  - **Зроблено:** переглянуто всі 2805 записів із ≤2 варіантами, доповнено **~1760 записів
    ~2900 варіантами** (14 партій, після кожної `check-coverage` + JS-синтаксис + коміт).
    Розподіл після проходу: 3+ варіанти — 1502 записи, рівно 2 — 997, один — 724 (свідомо:
    числа, дні тижня, місяці, однозначні предмети kettle/scissors/penguin).
  - **Правила відбору — у розділі «Що НЕ змінювати → uaAlt/enAlt»** (лексично інші слова та
    інші сенси ТАК; морфологічні варіанти кореня НІ; пари-омографи не чіпати). Тимчасовий
    журнал проходу `uaalt-sweep-progress.md` видалено по завершенні (правила перенесено сюди).
  - **Інструмент (відтворити при потребі):** скрипт у scratchpad будує список записів із ≤2
    варіантами, патч подається як JSON `{індекс_у_WORDS: [нові варіанти]}`, застосовувач
    парсить блок WORDS з урахуванням рядків/лапок і вставляє/зливає `uaAlt` з дедупом.
  - **Безпека:** `uaAlt` діє ЛИШЕ в en-ua (`getUa` його не повертає), тож ua-en і крос-логіка
    спільних глосів НЕ зачеплені — новий варіант не може створити колізію промптів.
  - Дрібні фікси по дорозі: `instead` +[замість того, замість цього, замість]; приклад
    `aftershave` («нанався» → «наніс»).
- Зміни сесії 38 (словниковий добір + чистка колізій глосів + фікс модалки; WORDS 3002→3223, 32 коміти):
  - **EFE стор. 36 «13 Clothes» (+30):** матеріали/патерни/крій (leather, denim, woolen+woollen,
    striped, checkered+checked, loose, baggy, fitted, crumpled, cropped, polka dot+spotted,
    plaid, paisley); робочий одяг (uniform, military, chef, apron, overalls+dungarees, scrubs,
    lab coat, high-visibility jacket, cargo pants); дитячий (hood, costume, bib, mitten,
    snowsuit, onesie, booties, snap+popper). Збагачено plain(+однотонний)/tight(+тісний,
    облягаючий)/vintage(+вінтажний). m.set: polka, dot, lab, visibility, cargo. Пропущено
    прозорі композити (chef's hat/uniform, school shirt/tie…) і вузьке (tabard, sleepsuit,
    bodysuit, romper suit); UK-дублети → enAlt. **Наступна сторінка EFE — 38.**
  - **Шкільне приладдя / клас (+33, зі скріншота користувача + його таблиці, НЕ з EFE):**
    scissors, ruler, glue, eraser, folder, pencil sharpener, lunch box, backpack, pencil case,
    marker, highlighter, crayon, paintbrush, tape, stapler, paper clip, hole punch, calculator,
    chalk, compass(+компас), globe, classroom, classmate, blackboard, whiteboard, locker, bell,
    workbook, worksheet, quiz, recess, timetable, stationery(⭐ парасолькове + пастка-омофон
    stationary). «На межі» (staple, colored pencil, school bag, glue stick, binder clip,
    protractor, set square, correction fluid/tape) і board marker — ВІДХИЛЕНО користувачем.
    ⚠️ Глоси, зайняті сусідами: «клас»=class → classroom «класна кімната»; «перерва»=break →
    recess «шкільна перерва»; «аудиторія»=audience і «дошка»=board — НЕ використовувати.
  - **+4 базові слова, які зловив check-coverage** у нових прикладах (їх взагалі не було
    в словнику): hole(дірка), mark(позначати/позначка), case(**чохол** — «випадок» зайнято
    accident/occasion), clip. m.set: sharpener.
  - **+obstetrician** (акушер/акушер-гінеколог) — раніше значився пропущеним на стор.26 EFE як
    «вузько-мед.»; журнал EFE виправлено. ⚠️ **midwife(акушерка) й obstetrician — РІЗНІ
    професії, не пара за родом** (акушерка приймає пологи, obstetrician — ЛІКАР): «акушер»
    СВІДОМО не в uaAlt до midwife. Захисний коментар стоїть у WORDS.
  - **🔧 Модалка «слова дня» — групування пар з однаковим EN** (скарга «дубль run»): рядок =
    СЛОВО, а не запис; деталі — розділ «Клікабельні дельти ✓/↗».
  - **Нове правило поведінки:** банальні очевидні правки робити ОДРАЗУ без узгодження —
    розділ «Що НЕ змінювати», пункт «Виняток — очевидні дрібниці».
  - **Словникові правки за грою:** приклад `stale` спрощено («This bread is old and stale»);
    elaborate → ua [детальний, докладний, ретельно розроблений, вишуканий] («вигадливий»
    прибрано); advanced(+передовий); remarkable → ua [видатний, дивовижний] (приклад про
    одужання не пасував «видатному»); timid → ua [боязкий, несміливий] («полохливий» рідко
    вживане → в uaAlt; узгоджено з укр. прикладом); paint(+фарба — був лише дієслівний),
    test(+тест/контрольна — був лише «тестувати»), project(+проєкт); lunch box («контейнер
    для їжі» переведено з uaAlt в **ua**, щоб ПОКАЗУВАВСЯ), highlighter(+маркер/текстмаркер
    у uaAlt — безпечно, бо uaAlt не діє в ua-en), clip → ua [кліп, затискач] + 8 uaAlt.
  - **🔴 Кластер «зір / вид» — розведено колізію (за скріном GPT):** `sight` мав ОСНОВНИЙ гліс
    «вид» — той самий, що в `species` (на промпт «вид» приймалися обидва, sight промптився
    семантично хибно; укр. приклад узагалі казав «картина»). Тепер sight → ua [зір,видовище],
    uaAlt [погляд,вид,картина,зрілище]; приклад «прекрасне видовище». **species лишається
    єдиним власником «вид».** Заодно: view → ua [вигляд,краєвид] (ядро значення бракувало)
    + uaAlt [вид,погляд,думка,перегляд,оглядати]; look → +uaAlt [виглядати,вигляд,погляд]
    («you look tired» і іменникові сенси були непокриті); **+4 слова:** vision(бачення),
    eyesight(зір), sort(сортувати/різновид), scenery(пейзаж/декорації).
    ⚠️ **eyesight і scenery СВІДОМО ділять гліс** із sight(«зір») і landscape(«пейзаж») —
    це справжні синоніми (прецеденти: approachable≈friendly, to stand up≈to get up,
    prevail≈to dominate). Захисний коментар стоїть у WORDS. Не «виправляти» на унікальні глоси.
  - **ask** → ua [питати,**просити**] (сенс «ask for» був відсутній) + uaAlt запитувати/
    попросити/прохати/спитати.
  - **🔴 `grave`** мав ОСНОВНИЙ гліс «серйозний» = той самий, що в `serious`, а іменниковий
    сенс «могила» був ВІДСУТНІЙ; приклад іще й містив обидва слова водночас. Тепер
    ua [могила,тяжкий], uaAlt [серйозний,поважний,похмурий,гріб]; ДВА приклади через
    дизамбіг-ключ: `grave` (могила) і `grave:тяжкий`.
  - **🔴 `section`/`chapter`** обидва були «розділ» (а приклад section використовував обидва
    слова, перекладаючи chapter як «глава»). Тепер section → ua [секція,підрозділ] + uaAlt
    [розділ,частина,відділ,ділянка,сегмент,параграф], новий приклад про відділ магазину;
    **chapter лишається єдиним власником «розділ»** (+uaAlt глава/частина).
  - **🩹 `chemical` — БАГ «склеєного» глоса:** було `ua:"хімічний; хімічна речовина"` ОДНИМ
    рядком, а матчинг точний → правильну відповідь неможливо було ввести взагалі. Тепер масив
    [хімічний,хімічна речовина] + uaAlt [хімікат,хімічна сполука]. **Просканував увесь словник
    на `;`/`,`/`/` всередині глоса — інших таких НЕМАЄ.** Якщо колись додаватимеш кілька
    значень — ЛИШЕ масивом, ніколи не через роздільник у рядку.
  - **🩹 `bracelet` +enAlt `wristband`** (кейс: «браслет»→wristband не зараховувався). Урок,
    який легко забути: у wristband «браслет» лежало в **uaAlt, а uaAlt НЕ діє в ua-en**
    (`isCorrect` ua-en читає лише getUa + enAlt). Для англійських слів-синонімів — **enAlt**.
  - **🔴 ПАЧКА «позичених глосів» (8):** слово мало ЄДИНИЙ гліс, що насправді належить іншому.
    Старі значення пішли в uaAlt (відповіді не зламались), приклади підтягнуто під нові глоси:
    `fever`(гарячка/жар ← було «температура»=temperature), `meal`(прийом їжі/трапеза ← «їжа»=food),
    `eager`(охочий/завзятий ← «нетерплячий»=impatient), `peculiar`(своєрідний/дивний ←
    «особливий»=special), `photography`(фотографування/фотомистецтво ← «фотографія»=photograph),
    `seat`(сидіння ← «місце»=place), `bathroom`(ванна кімната ← «ванна»=bath),
    `elated`(окрилений/у піднесенні ← «щасливий»). Також `issue` → ua [проблема,випуск] + uaAlt.
  - **🔬 МЕТОДИКА СКАНУ (відтворити при потребі) + головний висновок:** скрипт групує WORDS за
    основним глосом → знайшов **356 груп** зі спільним глосом. ⚠️ **Переважна більшість — НОРМА,
    не баг:** синонімічні сім'ї (`складний`→hard/difficult/complex, `швидкий`→fast/quick/swift).
    **НЕ «виправляти» їх на штучно унікальні глоси.** Дефект — лише коли слова НЕ синоніми
    (гліс «позичений»), і ховається він майже завжди в ПАРАХ, не у великих кластерах.
    Спроба відловити автоматично «приклад не містить глоса» дала 310 хибних спрацювань
    (укр. морфологія: «досягати» vs «досягти») — **автоматом не береться, потрібна семантика.**
  - **⚠️ КЛЮЧОВЕ ПРО КРОС-ЛОГІКУ (перевірено на `isCorrect`, index.html):** у ua-en приймається
    **БУДЬ-ЯКЕ** слово, що ділить показаний гліс. Тому спільний гліс = НЕ помилка для
    користувача, а лише неоднозначність промпту. Наслідок: **дисплей-підказки в дужках для
    омонімів ВІДХИЛЕНО користувачем** (`fierce`«лютий (розлючений)», `than`«ніж (порівняння)»,
    `girlfriend`) — вони не розвʼязували б реальної проблеми, лише додавали шуму. НЕ повертати.
  - **🟢 Легітимна полісемія — НЕ чіпати:** `місяць`(month/moon), `північ`(midnight/north),
    `ручка`(pen/handle), `чоловік`(man/husband), `легкий`(light/easy), `кидати`(throw/quit),
    `складати`(fold/compose), `годинник`(clock/watch), `предмет`(subject/item),
    `лютий`(February/fierce), `ніж`(knife/than), `hot`(гарячий/гострий — пара-омограф).
  - **🫱 `hand`/`arm` — розведено «руку» (за узгодженням):** англійська розрізняє те, чого
    українська не лексикалізує, і це ВАРТО вчити («I broke my hand» замість arm — класична
    помилка). `hand` → ua «кисть руки», uaAlt [кисть,рука]; **`arm` лишається власником «рука»**.
    Перевірено емуляцією: промпт «кисть руки» приймає лише hand, «рука» — лише arm.
    ⚠️ Дужок НЕ використано (за вимогою) — замість підказки взято ТОЧНІШЕ укр. слово.
  - **🫁 `chest`/`breast` — розведено (за узгодженням):** первісний задум `chest`→«грудна клітка»
    НЕ спрацював — цей гліс уже ОСНОВНИЙ у `rib cage`. Зроблено навпаки: **`chest` лишає
    побутове «груди»** (він частотніший — chest pain) + uaAlt [грудна клітка,грудна порожнина];
    **`breast` → «молочна залоза»** + uaAlt [груди,грудка,жіночі груди]. Приклади розведено
    (breast: «Лікар оглянув її молочну залозу»; chest: біль у грудях). Перевірено емуляцією:
    промпт «груди» приймає лише chest, «молочна залоза» — лише breast.
    ⚠️ `thigh`/`hip` (обидва «стегно») — свідомо НЕ чіпаємо: укр. не розрізняє, а точні
    терміни («кульшовий суглоб») — медичний жаргон. Це прийнятна неоднозначність.
  - **🧹 ПРИБРАНО «комбіновані» картки (кілька en на одній) — WORDS 3074→3068.** Рудимент
    раннього словника: 8 записів виду `{en:["big","large"],ua:"великий"}`, чиї складники ВЖЕ
    існували окремими картками → слово фізично двоїлось (могло випасти двічі за гру).
    Функціонально вони нічого не давали: крос-логіка й так приймає обидва синоніми на спільний
    гліс. **Видалено 6:** big/large, small/little, fast/quick, easy/simple, sick/ill,
    afraid/scared. **Конвертовано в окремі 2** (у них другий складник мав ІНШИЙ гліс, тож
    видалення осиротило б слово): `fall/autumn`→`fall`=осінь (autumn лишився окремо),
    `unusual/uncommon`→`unusual`=незвичайний (uncommon=нечастий лишився).
    ⚠️ Перед видаленням `afraid/scared` глоси **боятися/злякатися ЗЛИТО** в окремий `afraid`
    (ua [наляканий,боятися], uaAlt [злякатися,…]) — інакше дієслівний сенс зник би.
    ⚠️ `fall`=осінь отримав дизамбіг-приклад `fall:осінь` — бо голий ключ `"fall"` належить
    ДІЄСЛОВУ `to fall` («Be careful not to fall») і підхопився б через bare-fallback.
    **Перевірено після чистки:** усі 17 задіяних слів на місці, приклади резолвляться коректно,
    пар-омографів 20 і в кожної РІЗНІ приклади на сенс. Комбінованих карток у словнику 0.
  - **✅ Аудит «дубль-омографи ↔ приклади» ПЕРЕПЕРЕВІРЕНО (питання користувача):** структурна
    проблема «обидва значення отримують один приклад» **закрита ще в сесії 37** і тримається:
    скрипт-емуляція рантаймової `lookupExample` показує 20/20 пар із власним прикладом на кожен
    сенс (run бігти|керувати, bank банк|берег, hot гарячий|гострий, watch, play, like, lead,
    spring, light, kind, fine, match, present, content, cool, orange, fan, nail, right, square,
    tip, board, fair, patient). НЕ переробляти — механізм дизамбіг-ключів `en:ua` працює.
  - **👗 EFE стор. 38-46 — 5 РОЗВОРОТІВ ОДРАЗУ (+150 слів, WORDS 3068→3218).** Користувач
    надіслав 5 скрінів разом (суміжні теми: Clothes / Accessories / Shoes / Beauty), опрацьовано
    ТРЬОМА партіями з `check-coverage` після кожної. Повний перелік — у журналі
    `english-for-everyone-progress.md`. **Наступна сторінка EFE — 48.** Ключові рішення:
    • **UK-варіанти — через `enAlt` + `m.set`, БЕЗ окремих карток** (jumper→sweater,
      trousers→pants, rucksack→backpack, purse→wallet, pyjamas, knickers, waistcoat, zip,
      trainer, flippers…). Свідомо, щоб НЕ відтворити картки-дублі, прибрані цією ж сесією.
    • **⚠️ `vest` — US/UK пастка:** vest(US)=waistcoat(UK)=**жилет**, АЛЕ vest(UK)=
      undershirt(US)=**майка**. Захисний коментар стоїть у WORDS — не «спрощувати».
    • **Обхід зайнятих глосів** (щоб не плодити нові колізії): «знімати»=to withdraw →
      `take off` [скидати,злітати]; «вішати»=hang → `hang up` [повісити,класти слухавку];
      «шафа»=cupboard → `closet` «шафа для одягу»; «блискавка»=lightning → `zipper`
      «застібка-блискавка»; «жирний»=greasy → `oily` «масний»; «шнурок»=shoelace → `lace`
      «мереживо»; «приталений»=fitted → `tailored` «пошитий на замовлення».
    • **Чекер знову виявив базові дірки** (слів не було ВЗАГАЛІ): belt, mask, closet, silver,
      to roll, elegant, festival, to polish, lace, ski. Патерн стабільний — нові приклади
      щоразу витягують кілька відсутніх базових слів; це нормальна частина процесу.
    • Пропущено вузьке/етнографічне (agbada, thawb, deerstalker, tabi boots, espadrilles,
      corset, garter, solarium-лексика) — перелік у журналі EFE.
  - **🎵 38-ма пісня + ПЕРЕКАЛІБРУВАННЯ XP-шкали пісень (formula_v10).**
    • **Пісня «Framing Hanley — Lollipop»** додана як **КАРКАС БЕЗ ТЕКСТУ** (id/ytId/секції/
      glossary/slang), бо це комерційний реліз — на відміну від решти колекції (NEFFEX
      випускає музику copyright-free). Рядки тексту й переклад вставив САМ КОРИСТУВАЧ через
      веб-редактор GitHub. ⚠️ Я тексти комерційних пісень у файли/чат не пишу — ні під іншою
      назвою, ні «лише оригінал без перекладу»; це стала межа, не разова відмова.
    • Для пісні добито hover: WORDS +5 (lollipop, wrapper, mama, lady, cute), m.set mine/hers,
      glossary 14 записів → **0 лексем без перекладу при наведенні**. EXPRESSIONS +10
      сленг-одиниць (shorty, thug, swag, lady lumps, to back it up, wanna, gonna, ain't,
      lil', ya) → банк флеш-карток 591.
    • **🔴 ШКАЛА XP ПІСЕНЬ ПЕРЕКАЛІБРОВАНА (за запитом):** повторювані рядки тепер рахуються
      ОДИН раз (приспів × 3 = той самий матеріал). Деталі формули — розділ «Пісні».
      Нова: `round50(400 + (score − 52.8) / 108.4 × 1200)`. Оновлено ВСІ 38 значень xp
      у songs.js + `scoreOf` у build-song.js (дедуп) + міграція `oxford_xp_formula_v10`
      (тіло як v9 — перерахунок з нуля зі статистики й актуальних SONGS[].xp).
      Сума колекції 37750→36250. НЕ повертати підрахунок «з повторами».
  - **🔎 Пошук у розділі «Пісні»** (`#songs-search`): по назві/виконавцю/мітках секцій/
    РЯДКАХ тексту (en+ua)/сленгу/glossary; кілька слів = І. Хейстек кешується
    (`_songHaystack`), `renderSongsGrid` відокремлено від `renderSongsList` (інакше input
    перестворювався б і губив фокус), keydown-capture не пускає друк до ігрових обробників.
  - **Перевірено, без змін:** числа 11–20, десятки 10–100, thousand/million/billion — усі вже
    були; складені 21–29 свідомо відсутні (утворюються з покритих частин).
- Зміни сесії 37 — продовження 20.07 (WORDS 2912→2998, EXPRESSIONS 11→42):
  - **ПРАВИЛО РОЗПОДІЛУ WORDS ↔ EXPRESSIONS** (окремий розділ вище) — фрази НЕ пропускаються:
    назви-предмети → WORDS, ідіоми/звороти/рутини → «Вирази». Старе «2+ слів пропускаємо» скасовано.
  - **EFE стор. 28/30/32/34** (+9+15в, +23, +9, +33+1в) + ретро-розподіл пропусків 24/26
    (+11 WORDS, +4 вирази). Деталі — журнал `english-for-everyone-progress.md` (наступна стор. 36).
  - **Клікабельна «+N ●»** у «Прогресі по днях» — слова, ДОДАНІ В СЛОВНИК за день; самопідтримуваний
    реєстр `oxford_words_added_v1` із сідінгом (окремий розділ). Нюанс нетто-дельти ≠ модалка — там же.
  - **Категорійні ігри: теги в лідерборді + «Знаю» ПОЗА рейтингом** (окремий розділ) + одноразова
    міграція запису 14-го місця.
  - **Фікс «вічної» помилки** — sanitizeDayMistakes + бейдж рахує лише резолвні (окремий розділ).
  - Словникові правки за грою: wristband(+браслет), to tear(+розриватися — полісемія burst/tear),
    sometimes(+інколи/часом), worry(+турбуватися), to bear(+зносити…), exhibit(+демонструвати…),
    show(+вистава), applaud(+clap), belief(+віра), management(+керівництво), upgrade(+оновлення),
    proposal(+offer/suggestion), eyebrow/eyelash(+множини); розкрито кластер «зупиняти»
    (stop+зупиняти — була дірка; halt/cease/pause/suspend/freeze розширені, крос-матч);
    +detailed (слова не було, «детальний» тримав лише elaborate; elaborate розширено);
    above(+вище/над), highway(+швидкісна дорога/автомагістраль), finish(+завершувати/закінчити),
    obey(+слухатися).
  - **Mastery ua-en: прогрес отримує ВВЕДЕНЕ слово** (masteryTargetIndex; кейс authentic —
    три картки ділять «справжній») — деталі в розділі Mastery tracking.
  - **Кластер страху — фікс + 4 дієслівні пари:** приклад startled узгоджено з прикметником
    (пасив «was startled by», не актив); +to frighten/to scare(лякати/налякати), to terrify
    (жахати), to startle(сполохати) — дієслова-причини, приклади АКТИВНІ (на противагу пасивним
    прикладам прикметників frightened/scared/terrified/startled). Дієслова (лякати/сполохати)
    і прикметники (наляканий/сполоханий) НЕ перетинаються в крос-матчі.
  - dear(+любий/милий/шановний — звертання в листі).
- Зміни сесії 37 (статистика + прогрес + словникова чистка/марафон; WORDS 2861→2912):
  - **🎯📈 Показник «Точність»** у #lb-totals — ДВА рядки «весь час» (baseline з oxford_days_v1)
    і «останні 300» (recentAccuracy з oxford_games_log_v1), десяткові з КОМОЮ (51,2%), БЕЗ
    стрілки тренду (прибрано за запитом). Деталі — розділ #lb-totals.
  - **#lb-totals: перейменування+порядок** (за запитом): «Заняття піснями»→«Пісні англійською»,
    «Пісні вивчено»→«Пісень вивчено», «Запити GPT» вгору між LingoHut і піснями (пісні+кіно поряд).
  - **Клікабельні дельти ✓/↗ у «Прогресі по днях»** → модалка слів дня (EN—UA + гра); лог
    `oxford_mastery_trans_v1` (BACKUP_KEYS). Деталі — окремий розділ. Ведеться З МОМЕНТУ фічі.
  - **Чистка «розлитих сенсів» (клас civil):** кейс civil → великий аудит (338 записів, workflow
    89 confirmed) → split 16 пар «to X»/«X» (approach/charge/dash/drop/guide/land/order/record/
    sign/tie) + другі значення (fan/nail/patient/right/square/tip) + чистка 43 HIGH. Деталі —
    розділ «ВЕЛИКА ЧИСТКА» + «Аудит глос↔приклад». Split дав +16 у «Нові» (стара половина зберегла streak).
  - **EFE стор. 24 «Life events» (+11 +2 вирази) і стор. 26 «Pregnancy & childhood» (+25)** —
    деталі у `english-for-everyone-progress.md` (наступна стор. 28). Мультислівні назви-предмети
    (wet wipe/baby formula/board game/hula hoop/plush toy) → у WORDS, складники hover через m.set.
  - **~15 розширень прийнятних перекладів** (uaAlt/enAlt, за зауваженнями під час гри): fan(+шануваль-
    ник), fly(+літати/летіти), start(+початок), aim(намагатися→мати на меті/прагнути — БУЛА хибна
    крос-логіка), proposal(+offer/suggestion), management(+керівництво/менеджмент), upgrade(+оновлен-
    ня), belief(+віра), show(+вистава/шоу/виступ), applaud(+clap), worry(+турбуватися), to bear
    (+зносити/витримувати/нести), exhibit(+демонструвати/експонат), sometimes(+інколи/часом), imaginative(приклад).
- Зміни сесії 36 (словниковий марафон + 🎬 кіно-трекер + пороги ачівок; WORDS 2782→2861, ~50 комітів):
  - **🎬 НОВА ФІЧА «Кіно англійською» (2sub.movie)** — трекер за патерном GPT-кнопки, ЗАБЛОКОВАНИЙ
    до 500 у «Знаю». Деталі — розділ #bottom-left-btns. Ключ `oxford_movies_v1`; +1 пункт «Плани»/
    сеанс, БЕЗ XP; прапор розблокування `oxford_movie_unlocked_v1`.
  - **Пороги 3 ачівок переглянуто (за запитом):** Знаю [100,500,2000,5000,10000] (500=срібло
    збігається з розблокуванням кіно), Правильних [2000,7000,15000,25000,40000], Повторень
    [1000,5000,10000,20000,30000]. Розділ «Що НЕ змінювати».
  - **Кластери (кожен великий верифіковано воркфлоу — draft + адверсарна перевірка):**
    «страх» +6 (worried/panic/horrified/alarmed/startled/petrified; terrified→нажаханий —
    градація замість 4× «наляканий»); «шкала сили» +5 (tap/pat/whack/slam/bang; slap/smack/hit/
    punch вже були); «відчуття» +2 (sensation/perception; sense=[відчуття,чуття,сенс] — «сенс»
    make sense був непокритий); «будівництво» +3 (builder/construction/construction site;
    компонент site → m.set); ЛОГІСТИКА +12 (haul/tow/load/unload/wrap/mail/drop off/pick up/
    convey/relay/escort/deploy) — load=вантажити (завантажувати=download!), convey=«доносити
    (думку)», relay=переказувати (передавати=transfer); drop off/pick up — ПЕРШІ ФРАЗОВІ
    (багатослівні ключі, патерн construction site); «плач/скорбота» +3 (grieve/lament/death —
    зловив чекер); ЧИСЛА +14 (eleven/twelve — 11-12 випали між ten і thirteen!/billion/порядкові
    fourth,sixth-tenth/dozen/double/triple/pair/once); ЧАС +5 (today/tomorrow/yesterday/tonight —
    були лише hover-only, всупереч правилу!/weekday); EFE стор.20-21 СІМ'Я +9 (couple/marriage
    базові були відсутні!/divorce/-d/widow/-er/to grow up/toddler/triplets); EFE стор.22-23
    ПОЧУТТЯ +19 (surprised/confused/bored/guilty/shocked/indifferent та ін.); old/elder/elderly/aged
    (elder enAlt older); mom/mother і backward/back — взаємні enAlt; +mayor=мер (пара до major).
    ⚠️ compassionate=[милосердний,співчутливий] vs sympathetic=співчутливий (GPT-нюанс).
  - **Патерн «дисплей-підказка в дужках» (задокументовано в uaAlt/enAlt правилах):** основний
    глос з уточненням — transfer «перевести (гроші)» (vs перевеЗти=transport), slack «млявий
    (про справи)» (vs sluggish/lethargic=млявий-енергія), convey «доносити (думку)», deploy
    «направляти (війська)». Гола форма ОБОВ'ЯЗКОВО в uaAlt; у uaAlt дужок не писати (мертвий запис).
  - **Полісемії/uaAlt за фідбеком з гри (~30 слів):** cough(+кашель), treat(+лікувати/частувати/
    пригощати/ласощі), working=[робочий,працюючий]+enAlt worker / worker / employee(+співробітник),
    cherish=[дорожити,плекати] / treasure(+берегти), major(+серйозний/майор/мажор/спеціальність +
    hover), elderly(+похилого віку), shift=[зміна,зсув](+робоча зміна), smooth(+рівний/плавний),
    seize=[захоплювати,вилучати](+конфіскувати), spread(+поширювати/поширення), email=[електронна
    пошта,електронний лист](+імейл), renowned(+відомий), amazing=[дивовижний,разючий],
    impossible(+неможливо), transfer(+перевезти), to transport(+перевезти/транспортувати),
    pick/choose/select(+обирати; pick+зривати), perform=[виконувати,виступати], master(+оволодівати/
    майстер; «набувати»=acquire і «добувати» свідомо ВІДХИЛЕНО — не розмивати пари), fuss/fidget
    і scramble — раніше в сесії. ship(+відправляти/пересилати — дієслово було непокрите),
    forward(+пересилати/переадресовувати), move(+рухати/переміщати/пересувати), drag(+волочити
    в основні — розведено з pull), guard=[охороняти,охоронець].
  - **GPT-пункти «Плани» — пари наскрізь через дні** (`gptPointsByDate`; було floor/день) —
    розділи «Запити GPT»/«Термометр» (зроблено на початку сесії, до словникового марафону).
  - 🔴 Фікс регресії `fmtProgressDate` («07 undefined» у датах прогресу — з'їдена кома-пропуск
    деструктуризації в декуттер-коміті сесії 35; захисний коментар додано).
- Зміни сесії 35 (хоткеї-модифікатори; деталі — «Гарячі клавіші» та «Режими гри»):
  - **Shift-цикл включає «📖 Незасвоєні»:** `"tolearn"` в `ALL_MODES` як лаунчер (підсвітка
    кнопки; Enter/«Почати» → модалка напрямку). `setMode("tolearn")` не чіпає `mode`.
  - **«Чисті» модифікатори:** Shift-цикл і Alt-тогл панелі — на KEYUP, скасовуються будь-якою
    іншою клавішею (Ctrl+Shift/Alt+Shift зміна розкладки не циклить режим; Alt+PrtSc не ховає
    панель — PrtSc ловиться по keyup). `pendingShiftCode`/`pendingAltToggle` + blur-reset.
  - **Декуттер анатомії (−20 слів, WORDS 2802→2782):** прибрано вузькі терміни зі стор. 12–19 EFE
    за запитом користувача (larynx/trachea/lymphatic/adrenal gland/breastbone/instep/cuticle/
    quadriceps/pectoral/spleen/cervix/pancreas/gall bladder/thyroid gland/gland/ovary/navel/pelvis/
    cartilage/palate). Панель суддів (3 лінзи) класифікувала 91 анат. слово; користувач затвердив
    список. Лишено: esophagus/endocrine/small intestine/large intestine/deltoid/scrotum/urinary.
    «gland» → hover-only. check-coverage OK, browser-smoke 0 помилок. Деталі — розділ «Дані».
  - **Мікро-фікс Enter на `#answer`:** guard `gameEl hidden → return` перед
    submitAnswer+stopPropagation — прихований інпут із «застряглим» фокусом більше не ковтає
    Enter переходу з фіналу в меню (передіснуюча гонка ~1 кадр, знайдена headless-тестом).
  - Перевірено Playwright headless: 28 кейсів × 4 прогони, 0 помилок консолі + адверсарійна
    ревізія діфа воркфлоу. (Регресію цього коміту — з'їдена кома-пропуск у `fmtProgressDate` —
    виправлено в сесії 36, див. вище.)
- Зміни сесії 34 (ПОВНІ деталі — розділи фіч):
  - **Вітання лише в меню:** термометр/рекорди/ачівки/level-up більше НЕ вискакують під час
    гри та на екрані результатів — відкладаються в буфер (`_msDeferred`/`_gameSessionActive`)
    і виливаються при поверненні в меню (`flushDeferredMilestones`). Розділ «Milestone-вітання».
  - **Новий режим «📖 Незасвоєні»** (ЗАМІНИВ кнопку «🔀 Мікс»): грати Вивчаю ∪ Нові разом (усе, крім
    засвоєного), з вибором напрямку (US→UA/UA→US/Випадковий). Мікс-механіка живе у «Випадковий».
    `toLearnIndices`/`startToLearnGame`; "mix" прибрано з ALL_MODES/MODE_BTNS. Розділ «Режими гри».
  - **🎬 Трекер «Кіно англійською» (2sub.movie)** — нова кнопка за патерном GPT (головна =
    відкрити сайт; «+» = сеанс = +1 пункт «Плани», БЕЗ XP; «−» скасовує). 🔒 Заблоковано до
    500 у «Знаю» (кнопка-ціль N/500 + вітання при розблокуванні; лок — СОЛІДНА темна підкладка
    з оранжевим піл-лічильником, прозора вимивалась на світлих фазах). Розділ #bottom-left-btns.
  - **Пороги ачівок переглянуто (за запитом):** Знаю [100,500,2000,5000,10000], Правильних
    [2000,7000,15000,25000,40000], Повторень [1000,5000,10000,20000,30000] — перші ліги досяжні.
  - **GPT-пункти «Плани» — пари НАСКРІЗЬ через дні** (`gptPointsByDate`, мемоізація; хвіст
    непарного дня переноситься, пункт у день замикання пари; раніше floor за день, хвости
    згорали). Розділи «Запити GPT» і «Термометр».
  - **Словник (+61, WORDS 2741→2802):** стор. 18-19 EFE (внутрішні органи/голова/репродуктивні,
    +41); fuss/fidget (+2, повернули «метушитися» під коректним словом); торговельний кластер
    (+11, client/buyer/consumer/vendor/seller/merchant/retailer/wholesaler/supplier/dealer/
    distributor — глоси розведено, спільні → uaAlt); кластер «вплив/переконання» (+7, sway/
    persuade/convince/inspire/motivate/manipulate/to shape-омограф + збагачено uaAlt impact/
    influence/effect/affect/encourage/pressure). Обидва кластери верифіковано ВОРКФЛОУ
    (відхилено хибні глоси ChatGPT: довести=prove, використовувати=use). Полісемії/uaAlt:
    destination(+пункт призначення), grab(+схопити), notebook(+записник/блокнот),
    scramble(прибрано хибне «метушитися» → збивати/дертися), guard(омонім охороняти+охоронець),
    customer(+покупець/замовник), coat(+шар/шерсть), unlocked/unlock(+відчинений/розблокований…),
    prevail(+«переважати» в основні — дубль із to dominate). EFE: остання стор. 19, наступна 20.
    Деталі — трекер english-for-everyone-progress.md.
- Останнє оновлення (попереднє): 2026-07-05 (сесія 33)
- Зміни сесії 33 (продовження після стиснення handoff; 5 комітів; ПОВНІ деталі — розділи фіч):
  - 🌗 **Фон за порою дня + сезони** (новий розділ вище): 4 фази (ранок/день/вечір/ніч-космос),
    межі залежать від сезону — ЗАТВЕРДЖЕНІ користувачем через артефакт-редактор
    (зима [7,9,15,17] · весна [6,8,18,21] · літо [5,7,20,23] · осінь [6,8,18,21]). Ранній
    інлайн-скрипт `todFor`/`todNow`, `?tod=` — форс. НЕ міняти межі без узгодження.
  - **Світлі фази — фікси видимості:** ліві кнопки СОЛІДНІ темні (скло вимивалось на світлому;
    знайдено дубль-баг 0.07-золото); центрування картки `.layout.balance-center` (прогрес 440 >
    лідерборд 420 → +20px справа). Деталі — розділ #bottom-left-btns / «Реальне місце».
  - **Лідерборд — реальне місце під рискою:** дні/XP-дні — точно по всій історії; ігри — новий
    лог `oxford_games_log_v1` (усі ігри, кап 5000, у BACKUP_KEYS), `gameRanks()` (позиція у спільному списку → без дублів номерів). Лог з моменту
    фічі → перші тижні місце ігор «серед відомих» занижене. Розділ «Leaderboard».
  - **Словник:** neat/tidy +«акуратний»; fail +«зазнавати невдачі»/«невдача»(uaAlt);
    fair — ОБИДВА записи (справедливий+ярмарок) приймають усі значення через uaAlt (омограф);
    repair +ремонт(uaAlt)/лагодити; foolish +дурний/безглуздий(uaAlt); jog +пробіжка(uaAlt);
    bake +випікати/запікати; noisy +шумний; establish +встановлювати(uaAlt); uncertain +невизначений;
    baby +малюк/дитина(uaAlt); +to withdraw (НОВЕ слово, знімати/відкликати, IRREGULAR withdrew→withdraw);
    pulp — ПЕРЕНАСТРОЄНО зі стомат. «пульпа» на побутове «м'якоть» (приклад про фрукт; пульпа в uaAlt);
    plot — промпт лише «сюжет» (ділянка/змова→uaAlt), бо приклад про сюжет фільму; fail +«зазнавати невдачі»/«невдача»(uaAlt); fair — ОБИДВА
    записи (справедливий adj / ярмарок n) приймають усі значення через uaAlt (чесний/ярмарок/світлий/
    русявий/погожий та навпаки); (раніше того ж дня, ще під міткою «сесія 32»: to stare,
    lock+замок, nail+цвях, shallow→мілкий, blush+рум'янець, massive+величезний, operate+діяти,
    machinery hover-фікс, likely+ямовірно, known-hover, patient+пацієнт, well, interrupt,
    trustworthy). check-coverage OK.
- Зміни сесії 32 (МЕГА-сесія, ~45 комітів; ПОВНІ деталі — у розділах фіч вище):
  - 🎨 **Редизайн v2 «Космос» → прод** (рівні-всесвіт + «Сходи рівнів», статистика з
    медальйонами, оверлей «🏆 Рекорди», темна тема, прогрес комірками значок/число,
    рік у заголовку + річна згортка) — розділ «Редизайн v2».
  - **Нова фіча «Вирази і сленг»** (флеш-картки, EXPRESSIONS+slang пісень; Enter=знаю,
    Space=ще вчу, Tab=озвучити, ← прибрано) — свій розділ.
  - **Вітання «Новий рекорд!»** (кожне побиття; XP без пісень; двобічний синхрон бази —
    «−»/«+» повторно вітає) — у розділі «Редизайн v2».
  - **Лідерборд 5 вкладок:** «Найкращі ігрові дні» (перейменовано, без 🔁) + нова ⚡
    «Найкращі XP-дні» (xpDayNoSongs) — розділ «Leaderboard».
  - **Гра по категоріях — вибір напрямку** (модалка US→UA/UA→US/мікс; pendingCatPool).
  - **Термометр:** перенесені пункти — холодним голубим ЗВЕРХУ заливки (#thermo-carry).
  - **#lb-totals:** плашки з іконками, блок «Плани · поточне», кнопка Рекорди, ховається
    під час гри; статистика ЛИШЕ в меню.
  - **XP:** ставки Duo 25→30 і GPT 10→15 (міграція formula_v9); зелений сегмент
    сьогоднішнього приросту на смужці (#xp-today-fill).
  - **Словник:** +31 сімейна лексика (docx; brother/sister-in-law ДОДАНІ Й ВИДАЛЕНІ — не
    повертати) +5 тема вибачень +to stare (фікс ховера stared→«зірка») +5 ідіом у
    EXPRESSIONS; полісемії/uaAlt: interrupt, patient, well, trustworthy, likely, known,
    shallow (основний глос → мілкий), blush, massive, operate, machinery (hover-фікс),
    lock (+замок), nail (+цвях), cousin (+двоюрідна сестра). WORDS 2699→2736.
  - Прибрано фразу «Oxford 1000 — найважливіші слова англійської» з меню.
  - **Деплой-інцидент:** ліміт Pages ~10 білдів/год — плейбук у розділі «Проект».
  - handoff стиснуто (~35%): чейнджлог 24–31 → однорядкові, XP-таблиця пісень → songs.js.
- Зміни сесії 31: лічильник «N/15» на кнопці «📒 Помилки» — тьмяний/відокремлений (.rt-prog); поріг «пройдені помилки → пункт Плани» 10→15 (дані сирі, перерахунок дільником).
- Зміни сесії 30: uaAlt/enAlt-модель (idle↔lazy, gorgeous, livid); GPT-лінк фінал-екрану РАХУЄ (+10 XP тоді); «📒 Помилки» = ВСІ дні (allDayMistakeKeys); закриті помилки → пункти «Плани» (лише кнопка меню, oxford_mistake_reviews_v1); +welcome/green/elevator/unexpectedly; омографи handle/transport; стор.12–13 EFE +20 слів; кнопка «🏠 На головну». WORDS 2600→2603.
- Зміни сесії 29: handoff −33% (чейнджлог 5–23 → однорядкові); 5 словникових правок (certainly/curious/wander/afternoon); лічильник «✗N» live у всіх режимах (wrong+skipped).
- Зміни сесії 26: панель прогресу — видимість значків (фікс подвійної opacity), новий порядок meta, без накладань; ліві кнопки width:max-content; «🤖 GPT» замість «Запити GPT»; ВІДКОЧЕНО 2-рядкову сітку груп (не відроджувати).
- Зміни сесії 25: трекер «Запити GPT» 🤖 (кнопка+«+»/«−», 2 запити = 1 пункт, тоді БЕЗ XP); вирівняно ширини лівих кнопок; фікс duo-«−» (renderThermo); crisp/march/thoughtless; аналіз дублів-промптів UA→US — лишили як є.
- Зміни сесії 24: milestone'и 🎯/💪 за пунктами термометра (не за іграми); місячна ачівка «Пунктів за місяць» 🌡️; режим «🔀 Мікс» (selectedMode vs mode); механізм uaAlt (town+«місто»); повторення з фіналу — завжди мікс.
- ⚠️ Чейнджлог сесій 5–31 згорнуто до однорядкових (5–23 — сесія 29; 24–31 — сесія 32). Повні деталі вплетені у розділи фіч і «Що НЕ змінювати» — цей лог лише індекс «що коли з'явилось». Сесія 32 — компактний список вище.
- Зміни сесії 23: трекер занять піснями 🎧 (кнопка біля «Пісні»; +2 пункти «Плани», БЕЗ XP; `oxford_song_sessions_v1`); термометр — перенесення залишку плану на наст. день (carry-in, лише візуально); полісемія lofty/express/stuffed. WORDS 2599. ⚠️ Пороги ачівки «Слів у Знаю» [1000…10000] НАВМИСНІ (під розширення до ~10000) — не «виправляти». ⚠️ Поведінка: «підгадай/глянь X» = подивитись і доповісти, не міняти; нічого в проді без явного «давай».
- Зміни сесії 22: +10 слів (лексика пісень Fight/Lit) + 7 полісемій-фіксів (great/condition/guide/alarm/messy/chat/authorize); новий committed-інструмент `check-song-words.js`. WORDS 2589→2599. ⚠️ Не додавай переклад, що вже є ОСНОВНИМ глосом іншого слова (колізія UA→EN — перевіряй грепом).
- Зміни сесії 21: +9 слів зі словника LingoHut (привітання/свята); +1 пісня «Scars 🩹» (→37 пісень, 36850 XP); фікс `to spin` (прибрано хибне «плести»). ⚠️ Cloud-egress ріже lingohut.com (403 host_not_allowed) → користувач шле скріни, Claude зчитує слова з картинки.
- Зміни сесії 20: 🎉 свято level-up (`sfxLevelUp` + оверлей `levelup` + годинне підсвічування `xp-fresh`, ключ `oxford_levelup_at_v1`); фікс прикладів to-дієслів у `lookupExample` (fallback на голу форму); фікс TDZ `SONGS_XP_KEY` (значок 🎵 зникав після reload — підняв оголошення const); фікс ховання `#progress-panel` (CSS-пастка: ID-селектор б'є `.hidden` → потрібен парний `#id.hidden{display:none}`); полісемія probably/abnormal/overnight/filled/stress + розбито склеєні слешем dash/entire/required.
- Зміни сесії 19: +185 слів — 100% покриття прикладів + новий ОБОВ'ЯЗКОВИЙ чекер `check-coverage.js` (запускати після будь-яких змін словника до ✅ OK); панель «Прогрес» згорнуто (7 днів→тижні→місяці→роки) + скрол; +1 пісня (36-та); розблоковано ачівку «Пісень вивчено» [5…40]; café→cafe (é ламав токенізатор).
- Зміни сесії 18: 🔴 КРИТИЧНО — mastery + помилки дня тепер за СЛОВОМ (`wordKey=getEn[0]`), не за позицією в WORDS (вставка слів більше не «передає» streak чужим словам; міграція `migrateMasteryToWordKeys` скидає старий формат і в бекапах); термометр перероблено (фікс. шкала topMark=16/maxPts=18, мітки 1–17, підсвітка пройдених, кулька міняє колір); `speak()` читає повну «to X»; `wrapEnWord` пріоритезує «to X»; омографи tear/waste/skiing приймають обидва значення; +5 слів (expression/funeral/exception/silence/uncomfortable) + jump/hurt іменникові.
- Зміни сесій 5–17 (рання історія, стисло — повне вже перекрито розділами фіч вище): інтеграції Duolingo/LingoHut (🦉/📚, XP-ставки → formula_v4); розділ «Пісні» 1→35 (NEFFEX, `build-song.js`); термометр «Плани» `#thermo` (с.14); гра по категоріях (с.10); стеля XP 20-го рв. →180000 (с.17, крива масштабована динамічно); mastery/помилки за СЛОВОМ а не позицією (с.18 — критично, деталі в розділі Mastery); численні полісемії/словникові фікси. ⚠️ Два застереження цих сесій, продубльовані у фіч-секціях: НІКОЛИ не видаляти `const XP_DAY_KEY` (ламає per-day XP — інцидент с.8); коммітити ЛИШЕ в `main`, не в dev-гілку `claude/...` (інцидент с.11-12).

---

## Сесія 52 — розгорнуті наративи (винесено з довідника в сесії 55)

> Правила, виведені з цих блоків, лишились у `handoff.md` у стислому вигляді.

**📚 ПАРТІЯ «ШКІЛЬНІ ТА УНІВЕРСИТЕТСЬКІ ПРЕДМЕТИ» (сесія 52, за запитом).** Привід — урок
LingoHut «Шкільні предмети»: користувач спитав, чи всі предмети покриті. Аудит ~200 позицій
показав, що предметне ядро вже було (math, physics, chemistry, biology, history, geography,
literature, art, music, philosophy, psychology, sociology, economics, engineering…), бракувало
28 позицій — їх і додано: 19 окремих слів (calculus, geology, genetics, physiology,
linguistics, anthropology, pedagogy, dentistry, criminology, neuroscience, robotics,
vocabulary, bachelor, dean, alumni, elective, jurisprudence, philology) + компаунди
(computer science, political science, social studies, international relations, data science,
public health, high school, middle school, elementary school, fine arts).
- **`ethics` пішло `enAlt` до наявної картки `ethic`** («етика» вже був її основним глосом) —
  за правилом «перед enAlt дивись ГЛОС наявної картки»; окрема картка дала б два записи на
  один глос.
- **`public health` = «громадське здоров'я», НЕ «охорона здоров'я»** — останнє зайняте
  карткою `healthcare`.
- **Американська система шкіл:** `elementary school` / `middle school` / `high school`
  основними, британські `primary school` / `secondary school` — в `enAlt` (норма сесії 51).
- **Мови (English, Ukrainian, French…) СВІДОМО не додані** — власні назви, їм місце в
  `EN_TO_UA`, а не карткою. `informatics`, `gymnasium`, `headmaster`, `rector` теж пропущені
  як британізми/європеїзми на користь `computer science`, `high school`, `principal`.
- Підказок `HINTS` не знадобилось: скрипт-перевірка показала, що жоден новий глос не
  ділиться з іншою карткою.


**🔗 ПАРИ-МАЙЖЕ-ДУБЛІ ЗВЕДЕНО ЧЕРЕЗ `enAlt` (сесія 52, за запитом) — 17 пар, WORDS 13743 → 13727.**
Технічний борг сесії 41 («колись варто пройтись»). Клас виявився ВТРИЧІ більшим за ті
4 пари, що були в списку, — знайшовся двома незалежними сканами, і це головний урок:

1. **глоси з милицею-маркером** — регекс по `ua` на `американ|британ|\(UK\)|\(US\)`.
   Дав 8 карток: `тротуар американською`, `поштова скринька американською`,
   `залізниця американською`, `пральня самообслуговування американською`,
   `футбол американською`, `консервований (UK)`, `плінтус (UK)`, `гайковий ключ (UK)`.
2. **ключі-близнюки** — нормалізація ключа (зріз `to `, видалення дефісів і пробілів)
   і групування. Дав 108 груп, з них **справжніх дублів 9**, решта — навмисні розведення
   іменник/дієслово (`login`/`to log in`, `warm-up`/`to warm up`).

**Правило виживача — американська форма основною, друга в `enAlt`** (конвенція сесії 51).

| прибрано | вижило | що дописано виживачу |
|---|---|---|
| `pavement` | `sidewalk` | глос → «тротуар» |
| `letterbox` | `mailbox` | глос → «поштова скринька», enAlt |
| `railway` | `railroad` | глос → «залізниця», enAlt, рівень B1→**A2** |
| `launderette` | `laundromat` | глос → «пральня самообслуговування», enAlt |
| `tinned` | `canned` | uaAlt «у бляшанці», enAlt |
| `skirting board` | `baseboard` | enAlt |
| `spanner` | **`wrench`** (нова картка) | перевернуто: амер. форма стала ключем, `spanner` → enAlt |
| `hard-working` | `hardworking` | uaAlt «старанний у роботі», enAlt |
| `sunblock` | `sunscreen` | enAlt |
| `redcurrant` | `red currant` | enAlt |
| `heatwave` | `heat wave` | uaAlt «хвиля спеки», enAlt |
| `best seller` | `bestseller` | uaAlt «хіт продажів», enAlt |
| `set-up` | `setup` | uaAlt «облаштування/налаштування/організація справи», enAlt |
| `en-suite bathroom` | `en suite bathroom` | uaAlt «ванна кімната в номері», enAlt, рівень C1→**B2** |
| `high-definition` | `high definition` | uaAlt «високої чіткості», enAlt, рівень C1→**B1** |
| `round-trip` | `round trip` | uaAlt «в обидва боки», enAlt |
| `cafe` (2-га картка) | `cafe` | uaAlt «кав'ярня» — відкладене з сесій 45 і 49 |

- **⚠️ `soccer`/`football` СВІДОМО НЕ ЗВЕДЕНО.** В американській це РІЗНІ ігри
  (`football` = американський футбол), тож enAlt був би брехнею. Натомість прибрано
  милицю з глоса (`soccer` = «футбол»), і за правилом «спільний глос → підказки ОБОМ»
  обидві картки дістали HINTS. Заразом переписано приклад: «В Америці кажуть соккер»
  → «Він грає у футбол щовихідних» (так українською ніхто не каже).
- **⚠️ РІВЕНЬ КОНЦЕПТУ НЕ МУСИТЬ ПІДСКАКУВАТИ ТІЛЬКИ ЧЕРЕЗ ТЕ, ЯКА КАРТКА ВИЖИЛА.**
  Пара часто мала різні рівні (`railway` A2 / `railroad` B1), і сліпе злиття зробило б
  «залізницю» словом B1. Три рівні перенесено вручну — звіряй це в кожному злитті.
- **⚠️ `remove-card.js` ЗНОСИТЬ УСІ КАРТКИ ЗА КЛЮЧЕМ** (регекс глобальний), тож пару
  з ОДНАКОВИМ `en` (`cafe`) ним чистити НЕ МОЖНА — знесе обидві разом зі спільними
  `TRANS`/`EXAMPLES`. Другу картку `cafe` видалено вручну.
- **⚠️ Він же лишає осиротілий ключ у `TRANS`, якщо той стоїть на ЩІЛЬНОМУ рядку**
  (якір `^\s*"key":` його не бачить — пастка сесії 51). Так лишився `"pavement"`;
  після кожного прогону звіряти ключі всіх п'яти блоків, а не вірити лічильнику правок.
- **HINTS:** підказка `railway` поїхала разом із карткою — переписана на `railroad`
  («залізниця як мережа колій»), бо глос «залізниця» ділить із ним `rail`.
- Утрата, прийнята свідомо: прогрес mastery на 17 прибраних ключах обнуляється
  (`wordKey` = перший `en`), і `pavement` втратив сенс «бруківка» — в амер. англійській
  це взагалі дорожнє покриття, а не тротуар.


**📦 ВУЗЬКІ ГЛОСИ, ПАЧКА 2 (сесія 52) — 101 картка, +132 варіанти.** Зроблено ВОРКФЛОУ:
пул 2865 предметних кандидатів (глос 2-3 слова; 4+ = клас «опис», не чіпаємо) розбито
на 6 чанків, по кожному шукач → скептик-верифікатор. 12 агентів, 0 помилок.
- **Скептики відкинули 69 варіантів**, і це головна цінність схеми. Найбільша категорія —
  **колізії з іншими картками (10)**: напр. `power cut ← «відключення світла»` — це
  ОСНОВНИЙ глос картки `blackout`. Далі: 8 «перебір кількох букв» (`прийняття рішення`
  при вцілілому `прийняття рішень`), **5 голих головних слів** (правило сесії 46),
  5 кабінетних вигадок, 1 книжне («червоноголова капуста»).
- **6 варіантів повернуто вручну** як зрізані надто суворо: `power cut ← відключення
  світла` (спільний глос із `blackout` для `uaAlt` НЕ шкідливий — він лише додає
  прийняття в US→UA, промпт у UA→US і далі веде до `blackout`), `car rental ← оренда
  автомобіля`, `travel agency ← турагентство`, `pros and cons ← переваги і недоліки`,
  `frequently asked questions ← поширені питання`, `diesel fuel ← дизпаливо`.
- **⚠️ УРОК ПРО САМУ СХЕМУ:** механічний аудит ПІСЛЯ агентів обов'язковий — я звіряв
  кожен варіант із `dict.js` на тавтологію, дужки, збіг з основним глосом іншої картки
  й на однослівність. Агенти працюють текстом і файл бачать не завжди.
- **Залишок:** 3071 багатослівна картка без `uaAlt` (було 3172). Наступні пачки — тим самим
  воркфлоу, чанки беруться з того самого пулу.


**📦 ВУЗЬКІ ГЛОСИ, ПАЧКА 3 (сесія 52) — 203 картки, +248 варіантів.** Зроблено СОЛО
(без воркфлоу), прохід очима по зрізу пулу: побут, кухня, ванна, меблі, освітлення,
електрика, авто, тіло й здоров'я, довкілля, готель. Залишок **3071 → 2868**.

**⚠️ ГОЛОВНЕ — МЕХАНІЧНИЙ АУДИТ ПЕРЕД ВСТАВКОЮ ЗЛОВИВ ТЕ, ЧОГО ОКО НЕ БАЧИТЬ.**
На 251 запропонованому варіанті: 1 фатальна помилка + 8 попереджень. Скрипт перевіряє
чотири речі — тавтологія з власним глосом, дужки, **голе ГОЛОВНЕ слово глоса** (лазівка
сесії 46) і **збіг із основним глосом ІНШОЇ картки**. Прибрано три:
- `walk-in closet ← «гардеробна»` — слово входить у власний глос «гардеробна кімната»,
  тобто рівно та лазівка;
- **`service station ← «СТО»` — найковарніше.** `normUa` знижує регістр, тож «СТО»
  перетворюється на «сто», а це основний глос картки `hundred`. Відповідь «сто» почала б
  зараховуватись на промпт «service station». **Абревіатури в uaAlt перевіряти в НИЖНЬОМУ
  регістрі** — це новий клас пастки, регістр приховує омонім;
- `hand shower ← «душова лійка»` — основний глос картки `showerhead`; лишено «лійка для душу».

Решта 6 попереджень лишені свідомо — справжні синоніми, для яких спільне прийняття
є нормою: `carry-on`↔`hand luggage` (амер./брит. пара, дописано взаємно),
`insect spray`↔`insect repellent`, `oven glove`↔`potholder`, `surgical mask`↔`face mask`.

**Типові осі цієї пачки:** «унітаза»/«унітазу» (відмінок), «сукня»/«плаття»,
«ахіллове»/«ахіллесове», «кишечника»/«кишківника», «угнута»/«увігнута»,
«ремінь»/«пасок безпеки», «пральна машина»/«пралка», «АЕС», «автопром», «далекобійник».


**📦 ВУЗЬКІ ГЛОСИ, ПАЧКА 4 (сесія 52) — 168 карток, +200 варіантів.** Знову ВОРКФЛОУ,
7 чанків × (шукач + скептик) = 14 агентів, 0 помилок. Залишок **2868 → 2700**.
Промпт підсилено трьома уроками пачки 3, і це дало **0 фатальних помилок** у моєму
механічному аудиті (у пачці 3, зробленій соло, була одна):
- **правило про абревіатури в НИЖНЬОМУ регістрі** — скептики самі відкинули `2FA`, `API`,
  `IDE`, `SDK`, `VPN`, `ссд`, `гбц`, `пз` (латиниця в полі для української + транслітерації);
- правило A переформульовано з «голе головне слово» на «слово, що ВХОДИТЬ У ВЛАСНИЙ ГЛОС»;
- скептикам наказано ГРЕПАТИ `dict.js` по кожному варіанту конкретною командою.

**⚠️ НОВИЙ КЛАС ДЕФЕКТУ, ЗНАЙДЕНИЙ ПОБІЧНО: ГЛОС-ПЕРЕСТАВЛЯНКА.** Аудит показав, що
`post office` = «поштове відділення», а `post office branch` = «відділення пошти» —
та сама фраза, переставлена місцями, щоб обійти колізію. Перехресні варіанти зробили б
картки повністю взаємозамінними, тож обидва зняті. Скан «глоси = той самий набір слів
у різному порядку» знайшов ще дві пари:
```
roller = «валик малярний»   ||  paint roller  = «малярний валик»
coach  = «автобус міжміський» || intercity bus = «міжміський автобус»
```
⚠️ **Інвертований порядок слів — це така сама милиця, як «тротуар американською»**, і
скан злиття сесії 52 їх НЕ бачив: там ключі різняться цілим словом, а не написанням.
Усі три пари — кандидати на злиття, **не чіпані, відкритий хвіст**. Відтворити скан:
згрупувати картки за відсортованим набором слів глоса, лишити групи з >1 різним глосом.

**9 попереджень аудиту лишені свідомо** — справжні синоніми, для яких спільне прийняття
є нормою: `hair salon`↔`hairdresser's`, `security camera`↔`CCTV`, `ray of sunlight`↔`sunbeam`,
`luggage storage`↔`left-luggage office`, `service fee`↔`service charge`, `dairy produce`↔`dairy`.

**Типові осі пачки:** живі скорочення (аварійка, техогляд, соцмережі, запчастина, жуйка,
енергетик, держслужбовець, бананка, павербанк), орфографічні дублети
(«автентифікація»/«аутентифікація», «фаллопієва»/«фалопієва»), відмінок
(«для душу»/«для душа»), професійний жаргон (АКПП, шестигранник, блочний тренажер).


**📦 ВУЗЬКІ ГЛОСИ, ПАЧКА 5 (сесія 52) — 172 картки, +203 варіанти.** Воркфлоу,
7 чанків × (шукач + скептик) = 14 агентів, 0 помилок. Робочий пул **2393 → 2221**.
Механічний аудит: **0 фатальних І 0 попереджень** — уперше за всі пачки.

**⚠️ ЩО ДАЛО ЧИСТИЙ АУДИТ — ПРАВИЛО H У ПРОМПТІ (урок пачки 4).** Скептикам сказано
розрізняти ДВА випадки, коли варіант виявляється глосом іншої картки:
- **та сама річ іншими словами** (`post office` / `post office branch`) → перехресні
  варіанти зробили б картки взаємозамінними: відкинути й записати в окреме поле
  `mergeCandidates`;
- **справжні синоніми-різні слова** (`carry-on` ↔ `hand luggage`) → лишити, спільне
  прийняття тут норма.
Через це колізії більше не доходили до мого аудиту — їх ловив сам воркфлоу.

**🔗 8 ПАР-ДУБЛІВ, ЗІБРАНИХ ВОРКФЛОУ (відкритий хвіст, НЕ чіпані):**
```
tape «скотч»              ↔ adhesive tape «клейка стрічка»   ⚠️ уже взаємозамінні через uaAlt
prepayment «попередня оплата» ↔ advance payment «передоплата» ⚠️ «передоплата» вже проходить обом
working hours «робочі години» ↔ opening hours «години роботи»  (переставлянка)
high-rise «висотний будинок»  ↔ high-rise building «висотна будівля»
adjoining rooms           ↔ connecting rooms
private bathroom          ↔ en suite bathroom
rough ground              ↔ uneven terrain
supporting document       ↔ supporting documents            (однина/множина)
```
⚠️ Дві перші — **не пропозиції, а наявна патологія**, знайдена скептиками під час
грепів: той самий український рядок уже зараховується на обидві картки. Це той клас,
проти якого існує правило H, і його варто розібрати окремою задачею.

**Типові осі пачки:** банківські й побутові дублети («кредитна картка»/«кредитна карта»,
«газована вода»/«вода з газом»), живі скорочення (пошуковик, техпідтримка, розмитнення,
легковик, фреш, МКПП, ДВЗ), медично-побутові пари («однояйцеві близнюки»/«близнята»).

**🔗 ЗЛИТТЯ ПАР-ДУБЛІВ І ЧИСТКА МИЛИЦЬ-ГЛОСІВ (кінець сесії 52).** Розібрано 11 пар,
які назбирали пачки 4-5. **Ключовий висновок: не всі «дублі» варто зливати** — з 11
справжніми виявились 4, ще 4 треба було просто РОЗВЕСТИ, а 3 взагалі не дефект.

**ЗЛИТО 4** (`remove-card.js` + `enAlt` до виживача), WORDS 13727 → **13723**:
```
post office branch  → post office        (та сама фраза навпаки)
high-rise building  → high-rise
advance payment     → prepayment         ⚠️ «передоплата» вже проходила ОБОМ
supporting document → supporting documents  (однина/множина однієї леми)
```
**ВИПРАВЛЕНО МИЛИЦІ-ГЛОСИ, БЕЗ злиття (4):** тут пари насправді різні, а схожими їх
робив лише криво поставлений глос.
- **`roller`** мав «валик малярний» — перевернутий порядок слів, щоб не зіткнутися з
  `paint roller`. Тепер `roller` = «ролик» (загальне слово), а `paint roller` дістав
  `enAlt:["roller"]`. Приклад roller переписано — він був про фарбування стіни.
- **`coach`** мав «автобус міжміський» (та сама милиця), хоча головний сенс слова —
  **«тренер»**. Тепер `ua:"тренер"`, автобус і карета в `uaAlt`, `intercity bus`
  дістав `enAlt:["coach"]`. Приклад переписано.
- **`rough ground`** «нерівна складна місцевість» → **«пересічена місцевість»**: тепер
  не сплутати з `uneven terrain` = «нерівна місцевість».
- **`opening hours`** «години роботи» → **«години роботи закладу»**: це НЕ те саме, що
  `working hours` = «робочі години» (коли працює людина). Стара форма пішла в `uaAlt`.

**ЛИШЕНО ЯК Є (3) — це НЕ дефект:** `tape` ↔ `adhesive tape`, `private bathroom` ↔
`en suite bathroom`, `adjoining rooms` ↔ `connecting rooms`. Перші — справжні синоніми
(спільне прийняття — норма, як `couch`/`sofa`), решта — реально різні речі.
⚠️ **Правило, яке з цього виведене: «дві картки приймають один рядок» саме по собі НЕ
є багом.** Дефект — лише коли глос ВИГАДАНО, щоб обійти колізію (перевернутий порядок
слів, канцелярит, звужений сенс). Перевіряй причину, а не симптом.

**⚠️ ЯК ЗНАЙТИ ЦЕЙ КЛАС (два скани, обидва дешеві):**
1. глоси-переставлянки — згрупувати картки за ВІДСОРТОВАНИМ набором слів глоса,
   лишити групи з >1 різним глосом (дало `roller`/`paint roller`, `coach`/`intercity bus`);
2. регекс по `ua` на `американ|британ|\(UK\)|\(US\)` — милиці-маркери (сесія 52, злиття).
Скан №1 БАЧИТЬ те, чого не бачить скан злиття за написанням ключа: там ключі різняться
цілим словом (`post office` / `post office branch`).


